---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.24
base_tag: v0.4.23   # clean merge-base e0d0b3815 (= v0.4.23^{commit}; 2ca4eedca is the annotated tag OBJECT, not the commit)

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done   # Phase 6 COMPLETE 2026-08-13: ALL irreversible gates fired. 6.1 pg17 backup 58M; 6.2 migrated 11 migrations (273-284 skip 280); 6.3 version stamped frontend+backend v0.4.24 (install.sh _version-stamp + ldflags, .env synced); 6.4 services restarted (install.sh plist reload); 6.5 health green (/healthz=200, /api/config server_version=v0.4.24); 6.6 daemon aligned: brew upgrade multica 0.4.23→0.4.24 + multica daemon restart (pid 43145→39135), 5 workspaces + 5 agents survived. Awaiting Phase 7 record (ledger + memory).

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true             # Phase 2 — CONFIRMED 2026-08-13: user chose (1) 取上游 MUL-6050 (drop fork slug-fallback, resolves step-workspace.tsx + handler.go getIssuePrefix + workspace.go generateIssuePrefix to upstream side); (2) 全推到 Phase 6 (full deploy: pg17 pg_dump → migrate 273-284 → install.sh rebuild + launchd restart → brew upgrade multica + daemon restart). Approved short downtime.
  audit_strategy_written: true     # Phase 1.4 — FLIPPED 2026-08-13: 4 fan-out agents (Go-backend / frontend-core / locale-slug / coverage-sweep) all returned; merge-tree preview run in main session; every critical negative adversarially re-verified in MAIN session with raw commands (migration×3, add/add-empty, schema.test.ts-exists, task.go-call-site-live, agenttmpl-safe). 6 real conflicts locked (merge-tree measured), per-file strategy table below.
  db_backed_up: true               # Phase 6.1 — DONE 2026-08-13T035243Z: pg17 pg_dump -F c, 58M, pg_restore -l verified (5112 TOC entries). Dump: ~/multica-backups/v0.4.24-pre-migrate-20260813T035243Z.dump
  migrated: true                  # Phase 6.2 — DONE 2026-08-13: applied 11 new migrations (273-279, 281-284; 280 SKIPPED — upstream itself skips 280, not a fork artifact). 273-283 = keyset/index (ran pre-migration hook for non-tx CONCURRENTLY), 284 = task_owner_row_fence DDL. Latest applied = 284_task_owner_row_fence. exit=0.
  version_stamped_frontend: true   # Phase 6.3 — DONE 2026-08-13: install.sh _version-stamp.sh exports NEXT_PUBLIC_APP_VERSION=v0.4.24 (from git describe, override .env) → build-frontend.sh inlines. Verified adversarially: 0.4.24 in 4 files (layout-8262ca8e..js + server chunks 1218/6601); 0.4.23 residue = ZERO real (2 hits are changelog history {version:"0.4.23",date:"2026-08-11",...}, correct). .env NEXT_PUBLIC_APP_VERSION bumped v0.4.23→v0.4.24 (line 27) for non-install.sh-path hygiene.
  version_stamped_backend: true    # Phase 6.3 — DONE 2026-08-13: install.sh `go build -ldflags "-s -w -X main.version=v0.4.24"`. Smoking gun: `strings ~/.multica/backend/server` → main.version=v0.4.24 (x2). Runtime proof: /api/config server_version='v0.4.24' (running process IS the new binary, not stale plist). commit-first satisfied (cf41b2434 at HEAD) so install.sh resolved v0.4.24 not v0.4.23.
  services_restarted: true         # Phase 6.4 — DONE 2026-08-13: install.sh bootout+bootstrap both plists (gui/UID domain, retry+verify — the 9c1a9e1bb fix). launchctl print: backend + frontend state=running, active count=1, last exit code=(never exited). Brief downtime approved by user Phase 2.
  health_checked: true             # Phase 6.5 — DONE 2026-08-13: backend /healthz=200, frontend /=200, /api/config server_version=v0.4.24. Backend accepted requests ~40s post-bootstrap (within host-facts 30-40s window — no 000 fault).

merge_commit: cf41b243416dbec96ffa0eaeede9970fdd4f2748   # TRUE 2-parent (parents 026e45e89 = fork HEAD, ad23d1da3 = v0.4.24 tag commit; 1029f42eb is the annotated tag OBJECT). git describe --tags HEAD = v0.4.24-294-gcf41b2434 → install.sh resolves v0.4.24 (commit-first satisfied).

# Negative claims — each confirmed/refuted by ONE raw command, never by reading.
# Claims below are ALREADY verified in the MAIN session (not delegated to agents).
# Agent-scouted claims get added in Phase 1.4 only after the main session re-runs the command.
negative_claims:
  # --- Baseline (verified in main session during Phase 0/1) ---
  - claim: "current base is v0.4.23, a TRUE 2-parent merge (76f9164a3 per v0.4.23 plan), not a fake single-parent merge"
    verified_by: "git describe --tags HEAD → v0.4.23-293-g026e45e89"
    result: "confirmed (293 commits ahead of v0.4.23)"
  - claim: "latest upstream release is v0.4.24, published 2026-08-12, not prerelease/draft"
    verified_by: "gh release view --repo multica-ai/multica --json tagName,isPrerelease,isDraft,publishedAt"
    result: "confirmed (tagName=v0.4.24, isDraft=false, isPrerelease=false, publishedAt=2026-08-12T09:55:57Z). v0.4.23 published 2026-08-11 — only 1 day apart, small delta."
  - claim: "HEAD is 31 commits behind the v0.4.24 TAG and 293 ahead"
    verified_by: "git rev-list --left-right --count v0.4.24...HEAD → '31 293'"
    result: "confirmed (31 behind / 293 ahead). vs origin/main HEAD is 36 behind (origin/main sits 5 past the tag). Merge the TAG v0.4.24, not origin/main."
  - claim: "merge-base(HEAD, v0.4.24) = e0d0b3815 = v0.4.23^{commit}, single clean hop from v0.4.23"
    verified_by: "git merge-base v0.4.24 HEAD → e0d0b3815...; git rev-parse 'v0.4.23^{commit}'"
    result: "confirmed (e0d0b3815342a80460f8a1c66c56ddfc662c7d46 = v0.4.23^{commit}). 2ca4eedca is the annotated tag OBJECT hash, not the commit."
  - claim: "TRUE upstream churn is 251 files +16394/-3234 (measured from merge-base, NOT from HEAD)"
    verified_by: "git diff --shortstat $(git merge-base v0.4.24 HEAD)..v0.4.24"
    result: "confirmed (251 files changed, 16394 insertions, 3234 deletions). NOT a small upgrade despite only 31 commits — large daemon GC/repocache + workspace-teardown rewrite + agent-template removal + wecom media. METHOD NOTE: always measure from merge-base."
  - claim: "NEW upstream migrations = 273-284 (12 files: 273-283 indexes/fence + 284_task_owner_row_fence DDL). ALL stems > fork max 272 → ZERO new collision"
    verified_by: "git ls-tree --name-only HEAD server/migrations/ | awk -F'/' '{print $NF}' | awk -F'_' '/^[0-9]+_/ {print $1}' | sort -n | uniq | tail -1 → 272; range check for 273-284 → NONE (clean)"
    result: "confirmed (fork max stem = 272; NO fork files in 273-284). The 6 known collisions (119/158/159/160/213/214) all present in fork stem list — stand unchanged, do NOT renumber. Verified via awk (the 'ls/grep/sed' family triggers a broken PostToolUse hook polluting stdout in this session; git+awk pipeline is clean). Agent 4 cross-checks independently."
  - claim: "the file intersection (both sides touched since merge-base) = exactly 24 files"
    verified_by: "comm -12 <(git diff --name-only e0d0b3815..v0.4.24 | sort -u) <(git diff --name-only e0d0b3815..HEAD | sort -u) | wc -l"
    result: "confirmed (24). The set: packages/core/api/{client.ts,schema.test.ts,schemas.ts}, packages/core/types/{agent.ts,index.ts}, packages/views/editor/{readonly-content.test.tsx,styles/prose.css}, packages/views/locales/{en,ja,ko,zh-Hans}/onboarding.json, packages/views/onboarding/steps/step-workspace.tsx, packages/views/{package.json,rich-content/rich-content.tsx}, server/cmd/multica/cmd_workspace.go, server/cmd/server/router.go, server/internal/daemon/gc.go, server/internal/handler/{github.go,handler.go,skill.go,workspace.go}, server/internal/service/task.go, server/pkg/db/generated/skill.sql.go, server/pkg/db/queries/skill.sql."
  - claim: "add/add intersection (fork-new ∩ upstream-new) AND (fork-new ∩ upstream-changed) are BOTH EMPTY — merge-tree can see every conflict; v0.4.20 thread-nav-panel blind spot does NOT recur"
    verified_by: "comm -12 <(git diff --name-only --diff-filter=A e0d0b3815..HEAD | sort -u) <(git diff --name-only --diff-filter=A e0d0b3815..v0.4.24 | sort -u); comm -12 <(git diff --name-only --diff-filter=A e0d0b3815..HEAD | sort -u) <(git diff --name-only e0d0b3815..v0.4.24 | sort -u)"
    result: "confirmed (BOTH empty). Verified in main session, not just by Agent 4. The 24-file name-intersection IS the complete conflict surface."
  - claim: "merge-tree preview: EXACTLY 6 files produce conflict markers (not 24) — client.ts, schema.test.ts, readonly-content.test.tsx, step-workspace.tsx, handler.go, workspace.go"
    verified_by: "git merge-tree --write-tree --merge-base=e0d0b3815 HEAD v0.4.24 (exit 1); awk on CONFLICT lines → 6 paths"
    result: "confirmed (6). The other 18 intersection files auto-merge clean. gc.go's import-block overlap (Agent 1 flagged 0-1) resolved to 0 markers — but post-merge must confirm BOTH issueguard + processtree imports survive."
  - claim: "schema.test.ts is NOT deleted by upstream (corrects the diff-stat premise) — it shrinks 749→647 lines by removing the agent-template test block"
    verified_by: "git cat-file -e v0.4.24:packages/core/api/schema.test.ts → EXISTS; wc -l base 749 / v0.4.24 647 / HEAD 794"
    result: "confirmed in main session. Agent 2 caught my 'file deleted' misread of the -102 diff stat. HEAD adds 45 lines (importSkillsBatch tests) on top of base. Normal content conflict, not a fork-content-loss catastrophe."
  - claim: "agent-template feature is DEAD CODE in the fork (both server + client) — accepting upstream's full removal loses no live fork behavior"
    verified_by: "Agent 2: git grep createAgentFromTemplate/agentTemplateListOptions consumers in packages/views, apps/web, apps/desktop → empty. Main session: /usr/bin/grep -rn agenttmpl server/ --include='*.go' → only agent_template.go (also deleted) + agenttmpl/* self-refs + 1 doc ref. Agent 1: git diff --stat e0d0b3815..HEAD on each deleted file → empty (fork never modified them)"
    result: "confirmed. The whole feature (server agenttmpl/ pkg + agent_template.go handler + client queries/schemas/types/tests) is removed atomically by upstream; fork carries it as dead base-inherited code. Clean accept-upstream."
  - claim: "agenttmpl deletion is build-safe — no surviving fork code imports the deleted package"
    verified_by: "/usr/bin/grep -rn 'internal/agenttmpl' server/ --include='*.go' | grep -v _test.go → only server/internal/handler/agent_template.go:18 (which upstream ALSO deletes)"
    result: "confirmed in main session. agent_template.go is the sole consumer and is itself deleted; modify/delete doesn't conflict because fork never modified agent_template.go."
  - claim: "task.go truncateFallbackCommentBody call site is LIVE at HEAD:3606 and upstream v0.4.24 does NOT touch the region — v0.4.22 latent hazard does NOT recur (4-axis verified)"
    verified_by: "(1) def in fork-only fallback_comment_truncate.go:16 (no redecl in task.go); (2) call site task.go:3606 live; (3) upstream task.go single hunk @@ -4275,7 +4275,11 ResumeUnsafeFailure (far from 3606); (4) task.go already imports taskfailure so upstream's AuthMethodUnresolved(@4282) resolves"
    result: "confirmed in main session AND Agent 1 independently. Fork cap=200 survives in fork-only file."
  - claim: "the generateIssuePrefix D-scenario: upstream DELETED generateIssuePrefix and split it into defaultIssuePrefixFromSlug(slug)+legacyIssuePrefixFromName(name); fork changed its signature (name)→(name,slug). Upstream is a strict superset of fork's slug-fallback intent."
    verified_by: "git show v0.4.24:server/internal/handler/workspace.go | grep '^func (generate|legacy|default)' → only legacyIssuePrefixFromName@65 + defaultIssuePrefixFromSlug@31 (generateIssuePrefix GONE); Agent 3 behavioral table (Latin/CJK/edited-slug/kana scenarios all covered better by upstream)"
    result: "confirmed. Both Agent 1 + Agent 3 recommend ACCEPT UPSTREAM (drop fork slug-fallback). handler.go getIssuePrefix must resolve to the SAME side. Loses only legacy-CJK-workspace slug fallback, which upstream intentionally freezes. PRODUCT DECISION → Phase 2."
  - claim: "slug.ts is upstream-only (zero fork footprint) — fork's slug customization is NOT in slug.ts, it's in workspace.go + step-workspace.tsx"
    verified_by: "Agent 4: git diff e0d0b3815..HEAD -- packages/views/workspace/slug.ts → empty; Agent 3 located fork commit 77732a92d touching workspace.go + step-workspace.tsx"
    result: "confirmed. slug.ts (+72, adds pinyin-pro) accepts cleanly. pnpm install required for pinyin-pro."
  - claim: "VCS routes are NOT in fork's router.go (corrects task brief) — only VCSIntegrationEnabled config flag + lark/slack/dingtalk secretbox live there"
    verified_by: "Agent 1: rg -n 'vcs|VCS|secretbox' server/cmd/server/router.go → only VCSIntegrationEnabled@211 + lark/slack/dingtalk secretbox; zero VCS route registration"
    result: "confirmed. router.go auto-merges clean (0 markers); fork's #5539 officialBaseline (provenance_baseline.go:24), #4118 SuperAdminEmails+/api/admin, #2669 import/batch all survive. VCS routes live in handler/vcs.go + a different registration, untouched this round."
  - claim: "skill.sql / skill.sql.go need NO sqlc regen — both sides committed self-consistent generated output (fork appended UpsertAgentSkillEnabled at end; upstream edited a comment)"
    verified_by: "Agent 1: fork skill.sql hunk @123 (append), upstream @24-27 (comment); skill.sql.go fork @575, upstream @131 — disjoint"
    result: "confirmed. Re-running ~/go/bin/sqlc generate post-merge is harmless verification but not required."
  - claim: "22 enumerated non-intersection upstream product files are clean accept-upstream (zero fork footprint)"
    verified_by: "Agent 4: git diff e0d0b3815..HEAD -- <each of env-tab/env-file/chat-thread-list/chat-window/inbox-{context-menu,item-actions,list-item,row-menu}/integrations-tab/{lark,slack}-mark/integration-channel-icon/row-actions-menu/use-download-attachment/bubble-menu/extensions-index/feedback×4/cjk-emphasis/slug.ts> → empty for all 22"
    result: "confirmed (all empty diffs). MUL-6002 env bulk-edit, MUL-5963 chat history, inbox refactor, integration marks, MUL-6045 touch controls, MUL-6023 forced download, feedback, CJK emphasis, MUL-6050 romanize all land clean."
  - claim: "agent_concurrency_test.go is NOT fully deleted — upstream removed only 64 lines (one test TestCreateAgentFromTemplate_MaxConcurrentTasksBoundsAndDefault); file remains"
    verified_by: "Agent 1: git ls-tree v0.4.24 shows file present; git log e0d0b3815..v0.4.24 --oneline -- agent_concurrency_test.go → 71bd84495 MUL-6055"
    result: "confirmed. Corrects the task brief's 'entire file deleted'."
  - claim: "creating-agents builtin-skill docs need NO manual update — upstream already removed the agent-template references in this same release"
    verified_by: "/usr/bin/grep -n 'agenttmpl|agent_template|CreateAgentFromTemplate|AgentTemplate|from-template' server/internal/service/builtin_skills/multica-creating-agents/references/creating-agents-source-map.md AND .../SKILL.md → no matches in either"
    result: "confirmed (both empty). Agent 1 flagged this as a stale-doc follow-up, but the merged docs are already clean — upstream's +9/- (SKILL.md) and +19/- (source-map.md) edits WERE the cleanup. Non-issue."

---

# v0.4.23 → v0.4.24 upgrade plan

## Phase 0 — Locate (DONE 2026-08-13)

- **Base**: v0.4.23 (commit e0d0b3815, fork HEAD `026e45e89` is 293 ahead).
- **Target**: v0.4.24 (published 2026-08-12T09:55:57Z, non-prerelease, non-draft).
- **Delta**: 31 upstream commits, 251 files, +16394/-3234. Drivers (from commit log):
  - **Workspace teardown rewrite** (MUL-5999 #6754): index-driven, bounded, fenced; `workspace.go` +371, `workspace_delete.sql` +634, new `workspace_delete_fence_test.go` (772) + `workspace_delete_task_discovery_test.go` (615); migrations 284 + 278-283 keyset indexes.
  - **agenttmpl removed** (MUL-6055 #6820): `server/internal/agenttmpl/` whole dir + `handler/agent_template.go` (-720) + tests deleted.
  - **Agent env bulk edit + paste env files** (MUL-6002 #6818/#6730): new env-tab/env-file views.
  - **Chat history readback** (MUL-5963 #6680): `multica chat history`, chat-thread-list/chat-window rewrite.
  - **WeCom agent-produced files** (MUL-5905 #6604): wecom media outbound.
  - **Workspace slug romanization** (MUL-6050 #6823) + **issue prefixes from slug** (#6815): step-workspace.tsx +128, slug.ts +72.
  - **Daemon GC/repocache + identity** (MUL-6038/6043/5974): gc.go +414, repocache/cache.go +588, health identity.
  - **Misc**: github close-intent disambiguation (MUL-6048), markdown H4-H6 (MUL-6060), CJK strong emphasis, cursor MCP, metrics label fix.

## Phase 1 — Deep pre-merge audit (IN PROGRESS)

### 1.1 Migration (verified, see negative_claims)

Fork max = **272**. Upstream adds **273-284**, all > 272 → **zero new collision**. 6 known collisions intact. Awaiting Agent 4 independent cross-check.

### 1.2 Conflict surface (verified)

24 intersection files (both sides touched). This is the only set that can produce real text conflicts. Full list in negative_claims. Agent 4 also sweeps for add/add collisions the name-intersection can't see (v0.4.20 thread-nav-panel lesson).

### 1.3 Fan-out audit agents (RUNNING)

| Agent | Scope | Status |
| --- | --- | --- |
| 1 — Go backend | router.go, handler.go, skill.go, skill.sql(.go), task.go, github.go, workspace.go, cmd_workspace.go, gc.go + **agenttmpl/agent_template deletion impact** | RUNNING |
| 2 — Frontend core/api | client.ts, schema.test.ts (DELETED), schemas.ts, types/agent.ts, types/index.ts, rich-content.tsx, readonly-content.test.tsx, prose.css, package.json | RUNNING |
| 3 — Locale + onboarding/slug | onboarding.json ×4, step-workspace.tsx + locate fork's slug/prefix-fallback vs upstream MUL-6050 romanization | RUNNING |
| 4 — Coverage + sweep + migration | ahead-commit clustering, secondary sweep (fork footprint zero on non-intersection product files), migration independent re-check | RUNNING |

### 1.4 Per-file strategy table (COMPLETE — 4 agents + main-session merge-tree)

**6 manual conflicts** (merge-tree measured) + **18 auto-merge-clean** (some need semantic verify) + **1 stale doc**.

#### Manual conflicts (6 files, ~8 hunks)

| File | Fork footprint | Upstream change | Strategy | Resolution |
| --- | --- | --- | --- | --- |
| `packages/core/api/client.ts` | #2669 importSkillsBatch + 9 super-admin methods + skillMentionAgents param + getConfig(AbortSignal) + archived fields | -45 (remove 3 agent-template methods + imports), +small (FeedbackContext, createWorkspace issue_prefix) | **B** | Conflict @203-211 schema-import block: **keep fork `BatchImportResponseSchema`, DROP `AgentTemplate*Schema` imports** (schemas.ts auto-merge removes their definitions — must sync or build breaks). |
| `packages/core/api/schema.test.ts` | +45 importSkillsBatch describe+tests | -102 (remove agent-template test block) | **B** | Conflict @479-589: **keep fork importSkillsBatch describe, DROP createAgentFromTemplate describe** (dead code). listAgentTemplates/getAgentTemplate tests auto-deleted (fork-unchanged region). |
| `packages/views/editor/readonly-content.test.tsx` | restructured: removed 3 autolink tests + their mock, kept Korean emphasis test, added actor-chip describe | +47 CJK emphasis describe (parametrized zh/ja/ko + negatives) | **D** | Conflict @269-363: **drop 3 autolink tests (fork intent — mock gone), adopt upstream CJK emphasis describe (supersedes fork's single Korean test), let fork actor-chip describe auto-merge.** Don't keep upstream autolink tests → reference deleted mock → compile fail. |
| `packages/views/onboarding/steps/step-workspace.tsx` | issuePrefix preview slice(0,4)→slice(0,3) | +128 editable prefix field, slice(0,4), romanize | **C→A** | Conflict @67-93 issuePrefix body: **TAKE UPSTREAM** (slice 0,4). Fork slice-3 subsumed; cross-layer contract with workspace.go requires same side. |
| `server/internal/handler/handler.go` | Config SuperAdminEmails, parseSkillMentionAgents@547, isSuperAdmin@847, getIssuePrefix slug-fallback@1032 | Handler.channelFileDelivery@285, DeclareChannelFileDelivery@560, getIssuePrefix→issuePrefixForWorkspace@986 | **C+D** | @537 additive: **keep both** functions. getIssuePrefix: **TAKE UPSTREAM** (resolve same side as workspace.go). Config/role additions auto-merge. |
| `server/internal/handler/workspace.go` | generateIssuePrefix(name)→(name,slug), CreateWorkspace call site, **CreateMemberRequest.InviteeName@444 (#4118)** | +371: DELETE generateIssuePrefix → defaultIssuePrefixFromSlug + legacyIssuePrefixFromName; MUL-5999 teardown rewrite | **D-A** | 2 hunks (def region + call site @186): **TAKE UPSTREAM MUL-6050** prefix functions (drop fork slug-fallback). **PRESERVE fork InviteeName@444** (between upstream hunks @403/@835, no conflict). |

#### Auto-merge clean (18 files) — semantic-verify watchlist

| File | Why clean | Semantic check |
| --- | --- | --- |
| `packages/core/api/schemas.ts` | fork schemas @1196, upstream refactor @top | Confirm SkillSchema/BatchImportResponseSchema/EMPTY_BATCH_IMPORT_RESPONSE survive (Agent 2 verified via temp-merge awk). |
| `packages/core/types/{agent.ts,index.ts}` | fork BatchImport types @900, upstream removes AgentTemplate* @676 | Confirm AgentPermissionMode survives (upstream removed zod *schema*, fork uses the *type*). |
| `packages/views/editor/styles/prose.css` | fork comment @392, upstream h4/h5/h6 @+32 | Confirm --text-label/--text-caption tokens resolve (they do). |
| `packages/views/locales/{en,ja,ko,zh-Hans}/onboarding.json` | fork mid-file @109/153/195/226/257, upstream +1 key @172 | All 4 clean (no CJK brace-trap this round — different boundaries). Valid JSON. |
| `packages/views/package.json` | fork ./admin export, upstream +3 deps (remark-cjk-friendly, unist-util-visit, @types/mdast) | pnpm install required. |
| `packages/views/rich-content/rich-content.tsx` | fork #5199/#5346 chip dispatch @62, upstream +4 (cjk-emphasis plugin) | Confirm actor/skill chips survive. |
| `server/cmd/server/router.go` | fork #5539/#4118/#2669 @145/216/1258/1579, upstream @725/1518/1554 | **Enumerate routes post-merge**: confirm /api/admin + import/batch + provenance survive; agent-template routes correctly removed. |
| `server/internal/handler/skill.go` | fork #2669 +391 @593+, upstream 1 comment @29 | — |
| `server/pkg/db/queries/skill.sql` + `generated/skill.sql.go` | fork append @123/@575, upstream comment @24/@131 | No sqlc regen needed (verify both sides' UpsertAgentSkillEnabled coexist). |
| `server/internal/service/task.go` | fork call-site comment @3598-3606, upstream @4275 | **Call site @3606 live** (verified). |
| `server/internal/handler/github.go` | fork IsClosedStatus @1517, upstream +211 (closePolicy signature change) | **go-build watch**: upstream changed mirrorPullRequestForWorkspace signature (+closePolicy). Fork added no caller → upstream caller-updates apply; go build catches any arity miss. |
| `server/cmd/multica/cmd_workspace.go` | fork #4118 @70/123/587, upstream @148 | — |
| `server/internal/daemon/gc.go` | fork issueguard import @12 + IsClosedStatus @440, upstream processtree import @11 + +414 | **Post-merge confirm BOTH imports survive** (issueguard + processtree). Agent 1 flagged possible import overlap; merge-tree showed 0 markers but verify. |

#### Stale doc (Phase 4/7)

- `server/internal/service/builtin_skills/multica-creating-agents/references/creating-agents-source-map.md` lines 49, 90 reference deleted `agenttmpl/`, `agent_template.go`, `CreateAgentFromTemplate` → update per CLAUDE.md Coding Rules (builtin-skill docs track in same PR).

## Phase 2 — Confirm irreversible gates (PENDING)

Awaiting user go-ahead on: (1) pg17 `pg_dump` before migrate 273-284; (2) `install.sh` full rebuild + launchd restart (brief downtime); (3) daemon drift check + `brew upgrade multica` + `multica daemon restart`.

## Phase 3 — Reversible preview (PENDING)

`git branch main-backup-v0.4.24 main` → `git merge v0.4.24 --no-commit --no-ff` → `git diff --name-only --diff-filter=U`. Trust `--diff-filter=U` over Phase 1 prediction (expect deviation either direction; locale hunk-level may exceed file-level estimate; fork-only new files invisible to prediction).

## Phase 4 — Resolve + re-apply (PENDING)

Per-file strategy from 1.4. Fork-only members (locale keys, client methods) → `git merge-file` 3-way, NEVER `--theirs`/`--ours`. Loss scan after every resolution: `flat(fork_main) - flat(now) == ∅`.

## Phase 5 — Verify (PASSED 2026-08-13, first try, zero fork-code × upstream-test collisions)

All code gates green, stop-on-first-failure never triggered:
- `pnpm install` → Done (4.5s, reconciled new deps pinyin-pro/remark-cjk-friendly/unist-util-visit/@types/mdast). exit 0.
- `pnpm typecheck` → **6/6 tasks successful** (core/ui/views/web/desktop/docs). exit 0.
- `go build -C server ./...` → exit 0.
- `go vet -C server ./...` → exit 0.
- `~/go/bin/sqlc generate` → exit 0, `git diff --stat` after = EMPTY (no drift; confirms Agent 1).
- `pnpm test` → **5/5 tasks successful**, 0 failures (views alone: 334 files / 3993 tests passed).
- `pnpm build` → **3/3 tasks successful** (web all routes / desktop / docs).

Phase 5 caught nothing — the 6 conflicts (3 dead-code drops + step-workspace take-upstream + handler.go keep-both + workspace.go D-A) were all resolved correctly in Phase 4, and the readonly-content mock/autolink-test drop was identified during audit so test ran green first try. Contrast v0.4.20 (14 test failures). → `phase: gates`.

## Phase 6 — Irreversible gates (DONE 2026-08-13)

GATE held (`phase==gates`, `user_confirmed`, `audit_strategy_written`, all 18 negative_claims verified). All six irreversible steps fired, each gate flag flipped in frontmatter immediately on success:

- **6.1 Backup:** pg17 `pg_dump -F c` → `~/multica-backups/v0.4.24-pre-migrate-20260813T035243Z.dump` (58M). `pg_restore -l` verified 5112 TOC entries — dump is complete/restorable. (pg17 binary from `/opt/homebrew/opt/postgresql@17/bin/` — the PATH pg_dump is 16.x and would abort on version mismatch.)
- **6.2 Migrate:** `go run -C server ./cmd/migrate up` exit=0. Applied **11 new migrations (273-279, 281-284)** — `280` is skipped because **upstream itself skips 280** (no file at v0.4.24 tag either), NOT a fork artifact. 273-283 = keyset/index (each ran a pre-migration hook for non-tx `CREATE INDEX CONCURRENTLY`); 284 = `task_owner_row_fence` DDL. Latest applied = `284_task_owner_row_fence`. Zero new prefix collision (all 11 stems > fork max 272); 6 known collisions (119/158/159/160/213/214) intact.
- **6.3 Version stamp:** one `bash scripts/selfhost/install.sh` call did both injection points. `_version-stamp.sh` resolved `VERSION=v0.4.24` via `git describe --tags --abbrev=0` (commit-first satisfied: cf41b2434 at HEAD → resolves v0.4.24, NOT the v0.4.23 fallback trap), then `export NEXT_PUBLIC_APP_VERSION=$VERSION` (overrides .env for the build). Backend: `go build -ldflags "-s -w -X main.version=v0.4.24"`. Smoking gun: `strings ~/.multica/backend/server` → `main.version=v0.4.24` (x2). Frontend: 0.4.24 appears in 4 built files (layout chunk + server chunks); 0.4.23 residue = ZERO real (the 2 hits are changelog history entries `{version:"0.4.23",date:"2026-08-11",...}`, which v0.4.24 correctly carries as the previous release). `.env` line 27 also bumped v0.4.23→v0.4.24 for non-install.sh-path hygiene (install.sh never writes .env back, only exports in-process).
- **6.4 Restart:** install.sh plist reload (bootout+bootstrap on `gui/UID` domain with retry+verify — the 9c1a9e1bb fix). Both jobs `state=running, active count=1, last exit code=(never exited)`.
- **6.5 Health:** backend `/healthz`=200, frontend `:3001`=`/`=200, `/api/config` `server_version=v0.4.24` (proves the running process IS the new binary, not stale plist). Backend accepted requests within the host-facts 30-40s post-bootstrap window — no `000` fault.
- **6.6 Daemon:** `brew upgrade multica` 0.4.23→0.4.24 (CLI commit `ad23d1da3` = the v0.4.24 tag commit itself), then `multica daemon restart` (pid 43145→39135). Daemon now v0.4.24, **5 workspaces + 5 agents (claude/codex/opencode/openclaw/hermes) survived**. (Daemon is the one production process outside install.sh/launchd — Homebrew CLI, self-managed PPID=1; must be upgraded separately every release.)

## Phase 7 — Record (DONE 2026-08-13)

**Actuals vs. plan (audit predictions checked against reality):**
- **Conflict count: predicted 6 == actual 6, EXACT.** merge-tree preview was fully reliable this round (client.ts / schema.test.ts / readonly-content.test.tsx / step-workspace.tsx / handler.go / workspace.go). No blind-spot surprise like v0.4.20's fork-only thread-nav-panel (the add/add-empty negative claim held: both intersections empty, so merge-tree saw everything).
- **Phase 5: zero fork-code × upstream-test collisions** (vs. v0.4.20's 14). All 6 conflicts were resolved correctly in Phase 4; the readonly-content mock/autolink-test drop and schema.test.ts dead-code shrink were identified during audit so tests ran green first try.
- **Migration count: predicted 12 (273-284) → actual 11.** The delta is `280`, which upstream itself skips (no file at the v0.4.24 tag) — NOT a fork collision. The "zero new collision" conclusion holds unchanged.
- **One product decision (Phase 2-approved):** dropped the fork's slug-fallback issue-prefix customization (`generateIssuePrefix(name, slug)` = 3-char + "ws" tail) in favor of upstream MUL-6050 (`defaultIssuePrefixFromSlug` = 4-char). This resolved step-workspace.tsx + handler.go `getIssuePrefix` + workspace.go to the upstream side. Carried forward as a *deliberately abandoned* fork invariant (no longer carried).
- **install.sh commit-first trap: did NOT bite** — merge commit cf41b2434 was created before install.sh, so `git describe --tags` resolved v0.4.24, not the v0.4.23 fallback.

**Record close-out:**
- Plan frontmatter: `phase: done`, all six `gates.*` true, `merge_commit: cf41b2434` (TRUE 2-parent: fork `026e45e89` + `ad23d1da3`=v0.4.24 tag commit). ✓
- Merge commit message (`cf41b2434`): created in Phase 5, documents each of the 6 conflicts' strategy per SOP Step 10. No amend needed.
- Ledger `docs/customizations.md`: added `## Last upgrade — v0.4.23 → v0.4.24` section (base now v0.4.24) + bumped frontmatter `last_updated`. The per-upgrade detail lives here in the plan, not the ledger.
- **Memory:** surfaced one non-obvious durable fact — Multica's `schema_migrations` uses a **text** `version` column (`<num>_<slug>`), so same-number collisions (119/158/159/160/213/214) coexist as distinct text values at the DB layer (the "do NOT renumber" rule now has its mechanism explained). Saved to memory.
