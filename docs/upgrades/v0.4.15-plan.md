---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.15
base_tag: v0.4.14
merge_commit: ""

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: preview

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true
  audit_strategy_written: true
  db_backed_up: false
  migrated: false
  version_stamped_frontend: false
  version_stamped_backend: false
  services_restarted: false
  health_checked: false

# Negative claims the audit made — each confirmed/refuted by one raw command, not by reading.
negative_claims:
  - claim: "v0.4.15 在 server/migrations/ 下零新增迁移文件 → 无新撞号；fork max=234；6 已知碰撞（119/158/159/160/213/214）不受影响；DB migrate 是 no-op"
    verified_by: "`git diff --name-only v0.4.14..v0.4.15 -- server/migrations/` → 空输出"
    result: confirmed
  - claim: "fork 未在 v0.4.14 之后新增定制化 PR 的 merge/close → 无 MERGED→drop local 策略翻转"
    verified_by: "`gh pr list --repo multica-ai/multica --author chouti --state all` → 与 ledger 一致（#5393 MERGED 2026-07-15 旧；#5346/#5309/#5199/#5160/#4118/#2669/#2460/#6106 OPEN；v0.4.14..v0.4.15 期间无新 merge/close）"
    result: confirmed
  - claim: "merge-tree 实测真实文本冲突 = 8 文件（mention-hover-card modify/delete、mention-view.test add/add、board-card/issue-detail/issues-page/list-row content、members-tab content、file-viewer content、runtime-local-skill-import-panel content、selfhost-config.test.sh add/add）"
    verified_by: "`git merge-tree --write-tree --merge-base=v0.4.14 HEAD v0.4.15` → tree `926a9acd3`，stderr 列出 8 个 CONFLICT（modify/delete×1 + add/add×2 + content×5 中 issues 4 + skills 2 + members-tab 1）"
    result: confirmed
  - claim: "mention-hover-card.tsx modify/delete：fork 是「搬迁」非「删除」（d5e21bdc1 把它从 ui/common 挪到 views/editor/mention-hover-card.tsx 并升级为 dispatch 到 Agent/Member/Squad/Skill profile card）；upstream 仅在旧文件改 token 类名（text-sm→text-body/text-xs→text-caption，无逻辑）"
    verified_by: "`git log --diff-filter=D --follow v0.4.14..HEAD -- <file>` → d5e21bdc1；`git show d5e21bdc1 --stat` → 删 ui/common 文件+建 views/editor 文件；`git diff v0.4.14..v0.4.15 -- <file>` → 仅 token rename；`grep text-sm packages/views/editor/mention-hover-card.tsx` → L102"
    result: confirmed
    action: "keep-deleted 接受 fork 删除 + 手动 port token 迁移到 packages/views/editor/mention-hover-card.tsx（text-sm→text-body/text-xs→text-caption，本集群唯一手动编辑）；验无他文件仍 import 旧 @multica/ui/components/common/mention-hover-card 路径"
  - claim: "mention-view.test.tsx add/add 是「假冲突」：两文件目的不相交（fork 测 skill-gesture 297 行 / upstream 测 MUL-5456 modifier-click 171 行），仅共享顶部 6 行 @tiptap/react mock"
    verified_by: "`git ls-tree v0.4.14 -- <file>` 空（无共同祖先纯 add/add）；`git show v0.4.15:<file>` = MUL-5456 modifier-click suite；`git show HEAD:<file>` = skill-gesture suite（520a0d984+e62cbb40f+b5fe261e2+633bab278）"
    result: confirmed
    action: "B keep-both 仔细 union：保留 fork skill-gesture 全套 + append upstream MUL-5456 describes；shared @tiptap/react mock header 去重一次（fork stub 加 data-testid=nvw 无害保留）"
  - claim: "mention-view.tsx / mention-suggestion.tsx auto-merge 语义安全：fork 纯增量（新 skill/actor-chip 分支），upstream 改的 ProjectMention.handleClick 和 keydown 块恰是 fork 未碰区域；pickerNavigationDirection 由 fork 未改的 suggestion-popup.tsx 提供（L63）"
    verified_by: "`git diff v0.4.14..HEAD -- mention-suggestion.tsx | grep ArrowUp/pickerNavigationDirection` → 空（fork 留 keydown 块 pristine 于 L296/302）；`git diff v0.4.14..HEAD --stat -- suggestion-popup.tsx` → 空"
    result: confirmed
  - claim: "upstream v0.4.15 未构建原生 mention-chip 系统 → fork #5199/#5346 仍是 carry 非 drop"
    verified_by: "`git show v0.4.15:mention-view.tsx | grep MentionType/skill/chip` → 仅 IssueChip/ProjectChip；`git ls-tree v0.4.15 -- skill-mention-chip.tsx actor-mention-chip.tsx skill-mention-context.tsx skill-agent-picker.tsx` → 空"
    result: confirmed
  - claim: "runtime-local-skill-import-panel.tsx 冲突是 type-scale ∩ adaptive-discovery 文本重叠、语义不冲突：upstream diff 无非字体新增（grep `^+` 排除 text-body/caption/title 后为空），fork 加 isSummaryMode/groupedSkills/data-branch/条件搜索"
    verified_by: "`git diff v0.4.14..v0.4.15 -- <panel> | grep -E '^\\+' | grep -vE 'text-body|text-caption|text-title|^\\+\\+\\+'` → 空；fork hunks @@ -1081/-1124 ∩ upstream hunks @@ -1089/-1117/-1173/-1184"
    result: confirmed
    action: "C 仔细 merge：采纳 upstream token 改名 + 重贴 fork isSummaryMode/groupedSkills/visibleGroups/data-branch/条件搜索 + 3 locale group-heading keys"
  - claim: "file-viewer.tsx 被 upstream MUL-5443 掏空（新 mode/readOnly API、剥离 frontmatter）；fork 真正价值在新文件 frontmatter-card.tsx（upstream 未碰，auto-merge 存活，被 SkillProfileCard 复用）"
    verified_by: "`git log v0.4.14..v0.4.15 -- file-viewer.tsx` → 37d66260e/7803a5b9e/a8c775e94；`git diff` → 51+/98-（去 editing 态、加 mode/readOnly、剥离 frontmatter）；`git ls-tree v0.4.15 -- frontmatter-card.tsx` → 空（pure-local）"
    result: confirmed
    action: "A 取 upstream file-viewer 整体 + 丢 fork 对其改动 + 保 frontmatter-card.tsx + 重接 skill-detail-page.tsx 的 FileViewer 调用点到新 mode/readOnly props"
  - claim: "members-tab.tsx 冲突是 type-scale ∩ #4118 invite display-name：upstream 纯 type-scale（8+/8- 全类名），fork 加 invitee_name 渲染/inviteName state/createMember payload/invite Input；super-admin rename 半边不在 packages/views/settings/"
    verified_by: "`git diff v0.4.14..v0.4.15 -- members-tab.tsx` → 纯 type-scale；`git diff --stat v0.4.14..HEAD -- packages/views/settings/` → 仅 members-tab +19（无 rename UI）"
    result: confirmed
    action: "C 仔细 merge：采纳 upstream token + 重贴 fork inviteName/invitee_name payload/invite-display-name Input/InvitationRow 渲染"
  - claim: "SkillProfileCard compact redesign 安全 auto-merge：在 packages/views/editor/skill-profile-card.tsx（pure-local +256），upstream v0.4.15 未碰"
    verified_by: "`git diff --stat v0.4.14..v0.4.15 -- packages/views/editor/skill-profile-card.tsx` → 空"
    result: confirmed
  - claim: "upstream MUL-5443 未兼并 fork #5160 adaptive-discovery 或 #2669 batch-import → 仍是 carry 非 drop"
    verified_by: "`git grep -n 'data-branch|batchImport|discover|adaptive|groupedSkills|isSummaryMode' v0.4.15 -- packages/views/skills/` → 空；upstream BulkImportSummary/Result 是 v0.4.14 已有基底非新增"
    result: confirmed
  - claim: "【台账纠偏】ledger 称『fork 未实现 data-branch 故 import-panel 测试 pre-existing 失败』—— 在当前 HEAD 证伪：L1115 有 data-branch，实现与测试一致（threeSkills 种 search 分支断言 [data-branch=search]）"
    verified_by: "`git grep -n 'data-branch' HEAD -- <panel>` → L1115；`git show HEAD:<panel>.test.tsx` → threeSkills/单双 skill 断言与实现一致"
    result: refuted
    action: "Phase 7 更新 ledger：删 stale 的 data-branch 注记；若测试仍失败改查 search-input-placeholder-in-summary-mode 路径"
  - claim: "迁移零新增：max=234 在 HEAD/v0.4.14/v0.4.15 三方一致；6 已知碰撞（119/158/159/160/213/214）完好；DB migrate 是 no-op"
    verified_by: "`git diff --name-only v0.4.14..v0.4.15 -- server/migrations/` → 空；`git ls-tree {v0.4.14,v0.4.15,HEAD} server/migrations/ | … sort -n | tail -3` → 均 232/233/234；`ls | grep -E '^(119|158|159|160|213|214)'` → 全对 intact"
    result: confirmed
  - claim: "task.go duplicate-symbol 不复发：upstream v0.4.15 未碰 task.go（byte-identical v0.4.14↔v0.4.15）→ 三方合并采纳 fork 无符号版；merged tree 926a9acd 全包扫描 func truncateFallbackCommentBody 定义=1（sole，fallback_comment_truncate.go cap=200）"
    verified_by: "`git diff v0.4.14..v0.4.15 -- server/internal/service/task.go` → 空；`git show 926a9acd:.../task.go | grep truncateFallbackCommentBody` → 空；`git cat-file -e 926a9acd:.../fallback_comment_truncate.go` → 存在；全包扫描 def=1"
    result: confirmed
    action: "【非阻塞 watch-item】合并后 task.go 调用点直接 redact.Text(body) 不截断 → fork 200-rune cap 变死代码（仅自测覆盖），与 upstream 8000-rune-cap 路径行为分歧。Phase 7 留给 fork owner 决定 keep/re-wire/drop（沿用 v0.4.13 悬而未决的 cap 8000 产品问题）"
  - claim: "comment.go/auth.go/admin.go 零 upstream 触碰；triggerTasksForComment/userToResponse/blockTarget 签名零漂移（blockTarget 是 comment.go 内 closure L2499 非 package func）"
    verified_by: "`git diff v0.4.14..v0.4.15 -- comment.go auth.go admin.go` → 空；`git diff v0.4.14..v0.4.15 -- 'server/**/*.go' | grep 'func (triggerTasksForComment|userToResponse|blockTarget)'` → 空"
    result: confirmed
  - claim: "upstream Go 后端变更不触 fork handler 定制：upstream 仅碰 chat/file/handler/issue（+4 测试），与 fork 定制 handler 交集=handler.go+issue.go，两者 auto-merge clean"
    verified_by: "`git diff --name-only v0.4.14..v0.4.15 -- server/internal/handler/` → chat/file/handler/issue；`comm -12 <fork handler set> <upstream set>` → handler.go+issue.go；merge-tree 对两者报 Auto-merging 无 CONFLICT"
    result: confirmed
  - claim: "selfhost-config.test.sh add/add 是共享脚手架+不相交尾部：前 88 行 byte-identical（同 require_config/require_env/docker-compose 断言），fork 尾测 provenance no-dev 不变量，upstream 尾测 MUL-5269 MULTICA_PUBLIC_URL worktree 派生"
    verified_by: "`diff <(git show v0.4.15:<file>) <(git show HEAD:<file>)` → 前 88 行同、尾部分叉；`git log v0.4.14..HEAD -- <file>` → 62f704fdc（fork 建）；`git log v0.4.14..v0.4.15 -- <file>` → ee7ba83f5（upstream 建）"
    result: confirmed
    action: "keep-ours（fork 版，保 launchd self-host 的 no-dev provenance 不变量）+ graft upstream 的 MULTICA_PUBLIC_URL/init-worktree-env.sh 尾部断言；Makefile/local-env.sh/init-worktree-env.sh auto-merge 无需手动"
  - claim: "board-card/list-row/issue-detail 的 4 个 content 冲突块全是同一琐碎模式：fork archived-aware formatProgressText(...)+字面 text-[11px] vs upstream 原始 {done}/{total}+text-micro（MUL-5451 改名）"
    verified_by: "`git ls-tree 926a9acd -- <path>` 提 blob + `git show <blob> | grep -nE '^(<<<<<<<|=======|>>>>>>>)'` 直读冲突块：board-card L296-302、list-row L121-127、issue-detail L743-749+L2534-2540"
    result: confirmed
    action: "Strategy B：全取 HEAD 的 formatProgressText(...)（保 archived 注释）+ 采纳 upstream text-micro 类名；每文件 1-2 行"
  - claim: "issue-detail.tsx 的 identifier badge + pin-comment-input 两大定制已 auto-merge 存活，不在冲突块内：badge 在 merged L2270-2272+import L54，pin-comment 在 L2768-2774，useCommentComposerStore selector 已正确移除"
    verified_by: "`git show 63bc12dc3 | grep -n 'IssueIdentifierBadge|onCopy={actions.copyIdentifier}'` → L2270+；merged blob grep pin-comment `shrink-0 border-t bg-background` → L2768-2774"
    result: confirmed
    action: "勿重贴 badge/pin（已在合并树）；只需改 2 行 formatProgressText 类名"
  - claim: "issues-page.tsx 是唯一结构性冲突：fork archived 空态重构（renderEmpty→IssuesEmptyState/archivedCountFromFacets）vs upstream 对旧内联空态的字体改名"
    verified_by: "merged blob L77-87 冲突块：HEAD 侧 renderEmpty={({controller})=>(<IssuesEmptyState…/>)} vs upstream 侧内联 JSX text-body/text-caption"
    result: confirmed
    action: "Strategy C 取 HEAD 整侧（fork 结构）+ 后续扫 IssuesEmptyState 内 3 处字面值 text-sm→text-body/text-xs→text-caption×2 对齐 type-scale（apps/web/app/type-scale.test.ts 守卫）"
  - claim: "identifier badge vs MUL-5354 不冗余：upstream 仍在面包屑内联 {issue.identifier}（L2264），未加 panel 内 copy-chip；MUL-5354 只加 IssueNotFound/IssueDetailSkeleton/路由解析，copyLink≠fork copyIdentifier"
    verified_by: "`git diff v0.4.14..v0.4.15 -- issue-identifier-badge.tsx` → 空；`git show v0.4.15:issue-detail.tsx | sed -n 2256,2275p` → 面包屑仍内联 identifier；`git show 6fc6858 | grep copyIdentifier|copyLink` → 两 action 并存"
    result: confirmed
  - claim: "MUL-5451 type-scale 重构纯机械无行为变更：对 packages/views/issues/** 全部 446 改动行做 token 抽象+multiset diff，residual MINUS 0/PLUS 0——每行抽掉字体 token 后完全相同"
    verified_by: "`git show 7803a5b9e -- 'packages/views/issues/**'` → python 把 text-(<px>|[scale-role]) 归一为 text-SCALE 后 multiset 对比，残差 0；token 在 v0.4.15 tokens.css 注册（--text-micro:11px 等），type-scale.test.ts 守卫"
    result: confirmed
---

# Upgrade v0.4.14 → v0.4.15

> Process artifact. Persistent customization list in `docs/customizations.md`.
> 27 upstream commits (v0.4.14..v0.4.15), published 2026-07-30 10:01 UTC.

## Phase 0 — Locate

- base: `v0.4.14` (HEAD `v0.4.14-256-g5dc6e225b`)
- target: `v0.4.15` (Latest, published 2026-07-30 10:01 UTC; non-prerelease, non-draft)
- to-merge: **27 commits** (v0.4.14..v0.4.15)
- vs origin/main: 45 behind / 256 ahead（按惯例 merge v0.4.15 tag，不 merge origin/main）
- working tree: **dirty**（M CONCEPTS.md + 3 untracked docs/solutions/*.md — 均为 docs，非代码；merge 前需 stash，见 Phase 3）

## Phase 1 — Deep audit（进行中）

### 主题（27 commits）
- **UI type-scale 角色化命名重构 MUL-5451 `7803a5b9e`** — 大面积 ad-hoc 字号迁移，横扫 packages/views(308) + packages/ui(52)，是本轮冲突面扩大的主因
- **issues**：人类可读 issue URL（MUL-5354 `577018649`）、"Open in new tab"（MUL-5455 `f3d88b1d4`）、comment rail resolved-thread badge（MUL-5543 `a9a4a3d63`）、Table query identity 稳定（MUL-5477 `b06c48941`）、URL-rewrite perf（`9c732c47c`）、sub-issues shimmer chip 修复（`d58dab075`）
- **skills**：detail 页重构 Overview/Files tabs（MUL-5443 `37d66260e`）、浮动 save pill（`a8c775e94`）、imported-origin marker 重构（`67744d413`）
- **chat/transcript**：键盘切换浮窗（MUL-5522 `6be7adcb6`）、tool events 渲染 diff/content/terminal（MUL-5494 `da7451843`）；follow-up queue（MUL-5493 `b13657be7` 加 + `9072cef12` revert，净零）
- **daemon/agent**：Hermes 会话恢复（MUL-5509 `d6bd4cf7d`）、runtime-update starvation（`423d86da0`）、qodercli 发现（`74d5fc41d`）
- **self-host**：`fix(self-host): apply setup config to daemon`（MUL-5269 `ee7ba83f5`）★ 直接关系本 fork
- **views**：mention/slash picker Ctrl+N/J/P/K（MUL-5495 `ab6da2e73`）、middle-click 后台 tab（MUL-5457 `9e3b661d4`）、modifier-click 真浏览器 tab（MUL-5456 `2889269c1`）
- **attachments**：bulk response opt-out pre-signed URL（MUL-5372 `2187b5829`）
- **docs**：四语言文档大修（MUL-5057 `e395fb874`，201 文件 apps/docs — fork 未定制，无冲突）

### merge-tree 实测冲突（8 文件）
| 文件 | 冲突类型 | 涉及定制 |
|---|---|---|
| `packages/ui/components/common/mention-hover-card.tsx` | **modify/delete** | mention 集群（#5199）—— fork 删它，upstream 改它 |
| `packages/views/editor/extensions/mention-view.test.tsx` | **add/add** | mention 集群（#5346） |
| `packages/views/issues/components/board-card.tsx` | content | archived status（#6106）∩ type-scale |
| `packages/views/issues/components/issue-detail.tsx` | content | pin-comment-input + identifier-badge ∩ MUL-5354/5543/5451 |
| `packages/views/issues/components/issues-page.tsx` | content | archived status（#6106）∩ type-scale |
| `packages/views/issues/components/list-row.tsx` | content | archived status（#6106）∩ type-scale |
| `packages/views/settings/components/members-tab.tsx` | content | #4118 member-name ∩ type-scale |
| `packages/views/skills/components/file-viewer.tsx` | content | skills（MUL-5443 重构） |
| `packages/views/skills/components/runtime-local-skill-import-panel.tsx` | content | #5160 adaptive discovery ∩ MUL-5443 |
| `scripts/selfhost-config.test.sh` | **add/add** | fork selfhost ∩ upstream 同名脚本 |

### Per-file strategy

| 文件 | strategy | 文本冲突 | 依据 + 动作 |
|---|---|---|---|
| `packages/ui/components/common/mention-hover-card.tsx` | **keep-deleted** | modify/delete | fork d5e21bdc1 把它搬到 `views/editor/mention-hover-card.tsx`（升级为 profile-card dispatch）；upstream 仅改旧文件 token。接受 fork 删除 + **手动 port token 迁移**到 `views/editor/mention-hover-card.tsx`（L102 text-sm→text-body 等），验无残留 import 旧路径 |
| `packages/views/editor/extensions/mention-view.test.tsx` | **B keep-both（仔细 union）** | add/add | fork skill-gesture suite（297）+ upstream MUL-5456 modifier-click suite（171），目的不相交，仅共享 6 行 @tiptap/react mock。union 两套 + mock header 去重一次 |
| `packages/views/issues/components/board-card.tsx` | **B** | 1 块 | 取 HEAD `formatProgressText(childProgress!)` + 采纳 `text-micro` |
| `packages/views/issues/components/list-row.tsx` | **B** | 1 块 | 取 HEAD `formatProgressText(childProgress)` + 采纳 `text-micro` |
| `packages/views/issues/components/issue-detail.tsx` | **B** | 2 块 | 取 HEAD `formatProgressText(...)` ×2 + 采纳 `text-micro`；badge/pin-comment 已 auto-merge，**勿重贴** |
| `packages/views/issues/components/issues-page.tsx` | **C（取 HEAD 整侧）** | 1 块 | fork archived 空态重构 vs upstream 旧内联空态字体改名；取 HEAD + 扫 IssuesEmptyState 内 3 处字面值对齐 type-scale |
| `packages/views/settings/components/members-tab.tsx` | **C** | 1 块 | upstream 纯 type-scale；重贴 fork inviteName state/invitee_name payload/invite-display-name Input/InvitationRow 渲染（#4118 invite 半边）|
| `packages/views/skills/components/runtime-local-skill-import-panel.tsx` | **C** | 1 块 | upstream 纯 type-scale；重贴 fork isSummaryMode/groupedSkills/visibleGroups/data-branch/条件搜索 + 3 locale keys（#5160）|
| `packages/views/skills/components/file-viewer.tsx` | **A（取 upstream）** | 1 块 | upstream MUL-5443 掏空（新 mode/readOnly API、剥 frontmatter）；取 upstream + 丢 fork 对其改动 + **保 frontmatter-card.tsx**（pure-local auto-merge）+ 重接 skill-detail-page.tsx 的 FileViewer 调用点 |
| `scripts/selfhost-config.test.sh` | **keep-ours + graft** | add/add | 前 88 行共享脚手架；保 fork provenance no-dev 尾部 + graft upstream MULTICA_PUBLIC_URL 尾部 |
| mention-view.tsx / mention-suggestion.tsx（auto-merge） | A（采纳 upstream） | 0 | fork 纯增量，upstream 改的 handleClick/keydown 是 fork 未碰区；pickerNavigationDirection 由未碰的 suggestion-popup.tsx 提供。typecheck 验 |
| SkillProfileCard / frontmatter-card.tsx / issue-identifier-badge.tsx / child-progress.ts（auto-merge） | A（保 fork pure-local） | 0 | upstream 未碰，auto-merge 存活 |
| Makefile / local-env.sh / init-worktree-env.sh（auto-merge） | A（upstream） | 0 | fork build-prod/provenance 与 upstream MULTICA_PUBLIC_URL disjoint |
| handler.go / issue.go（auto-merge） | A | 0 | 唯一 co-touch 后端文件，auto-merge clean；comment/auth/admin 零 upstream 触碰 |
| 迁移（无新增） | — | 0 | max=234 三方一致，migrate no-op |

### Audit prediction（Phase 3 对照）
- 预测真实文本冲突 **10 文件**（merge-tree 报 8 唯一文件，但 issue-detail 含 2 块故 9 块）：
  - **1 modify/delete**：mention-hover-card.tsx（keep-deleted + 手动 port token）
  - **2 add/add**：mention-view.test.tsx（union）、selfhost-config.test.sh（keep-ours+graft）
  - **7 content**：board-card/list-row/issue-detail(×2)/issues-page（type-scale ∩ archived）+ members-tab/import-panel（type-scale ∩ fork feature）+ file-viewer（A 取 upstream）
- **唯二需要「写代码」而非「选边」的**：mention-hover-card token port、file-viewer 调用点重接 skill-detail-page
- 风险等级：**低-中**。冲突数比 v0.4.14（3 手动）多，但 **9/10 是 type-scale 机械改名 ∩ fork 行的琐碎重叠**；无迁移、后端零冲突、无语义密集点（v0.4.14 的 comment.go malformed-id 那类）。真正要小心的是 file-viewer 的 A 策略重接 + hover-card 的手动 port。
- 对比 v0.4.14：主战场从「comment.go mention trigger 语义」转移到「type-scale 横扫 ∩ views 定制」——量大但浅。

## Phase 2 — Confirm irreversible gates ✅
- blocking question：本轮零新增迁移（migrate no-op）→ 不可逆面仅 launchd 重启 + pg_dump 备份 + 版本 re-stamp
- 用户批准「全程执行」→ `user_confirmed: true`
- 附带决策：file-viewer.tsx 采 Strategy A（取 upstream 掏空版，保 frontmatter-card.tsx）；task.go 死代码分歧留 Phase 7 决断，本轮不动

## Phase 3 — Reversible preview（进行中）
## Phase 4 — Resolve + re-apply（待）
## Phase 5 — Verify（待）
## Phase 6 — Irreversible gates（待）
## Phase 7 — Record（待）
