---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.17
base_tag: v0.4.16

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true  # Phase 2 done — user authorized全程 (验证后复问 Phase 6)
  audit_strategy_written: true  # Phase 1.3 done — per-file strategy written into plan body
  db_backed_up: true  # Phase 6.1 — pg17 dump v0.4.17-pre-migrate-20260804-093647.dump 48M
  migrated: true  # Phase 6.2 — 251_agent_runtime_unbind up (235-250 既有 skip)
  version_stamped_frontend: true  # Phase 6.3 — .env NEXT_PUBLIC_APP_VERSION=v0.4.17
  version_stamped_backend: true  # Phase 6.3 — install.sh ldflags -X main.version=v0.4.17
  services_restarted: true  # Phase 6.3 — launchctl kickstart backend + frontend
  health_checked: true  # Phase 6.4 — backend /healthz 200, frontend / 200, /api/config server_version=v0.4.17

merge_commit: 720f03209

# Negative claims the audit made — each confirmed/refuted by one raw command, not by reading.
# Populated after fan-out audit agents report; every entry must carry verified_by + result.
negative_claims: []
---

# Upgrade v0.4.16 → v0.4.17

> Process artifact. Persistent customization list in `docs/customizations.md`.
> 24 upstream commits (v0.4.16..v0.4.17), 239 files changed, published 2026-08-03 10:21 UTC.

## Phase 0 — Locate

- base: `v0.4.16` (HEAD `v0.4.16-267-g405b7c7b3`)
- target: `v0.4.17` (Latest, published 2026-08-03 10:21 UTC; non-prerelease, non-draft)
- to-merge: **24 commits** (v0.4.16..v0.4.17), **239 files**
- vs origin/main: 24 behind / 267 ahead
- merge-base(HEAD, v0.4.17) = `3cab03cc3` = v0.4.16 tag commit ✅ 干净
- working tree: 2 untracked (1 解决方案文档 + 本 plan),无 M 状态改动,**无需 stash**

## Phase 1 — Deep audit

### 24 commits 主题分布(预读)

| 主题 | commits | MUL |
|---|---|---|
| changelog/docs | 3 | 5655, 5648, 5558 (license), 教程 |
| daemon: repo-cache-eviction + disk-usage + GC | 3 | 5620 (3 commits 串) |
| agent: runtime unbind (MUL-5559) + migration 251 | 1 | 5559 (含 migration) |
| daemon: Codex instruction files provision | 1 | — |
| daemon: HTTP(S)_PROXY for wakeup WebSocket | 1 | — |
| MCP: authoritative allowlist (MUL-5637, 然后 revert) | 2 | 5637 |
| agent: kimi ACP streamingCurrentTurn gate | 1 | 5657 |
| skills: draft dirtiness baseline (MUL-5645) | 1 | 5645 |
| autopilots: save first schedule from edit dialog | 1 | 5649 |
| channels: handle /new in shared router | 1 | — |
| storage: disable request checksum S3 Upload | 1 | 5617 |
| engine: broadcast full issue payload chat-created | 1 | 5632 |
| CLI: surface 409 message | 1 | 5619 |
| mobile: zombie WebSocket recovery | 1 | — |
| inbox: arrow-key selection | 1 | 5622 |
| autopilots: required-field blocker UX | 1 | 5587 |
| issues: sidebar Details ↔ execution log reorder | 1 | 5599 |
| CLI: `issue search` help text | 1 | — |
| CLI: `issue search` searches comments too | 1 | 5648 |

### merge-tree 实测冲突(2 文件, 全 content)

merge-base = `3cab03cc3` == v0.4.16 tag commit ✅
output tree = `5591329ee`

| 文件 | 冲突块 | 触发原因 |
|---|---|---|
| `packages/views/editor/extensions/mention-suggestion.tsx` | 1 (content) | fork HEAD 无改动 (type-scale 1 commit 不冲突) vs upstream b06af2ae1 在 import 区加 `isAgentRuntimeBound` + `Tooltip` + `blockedReasonLabel` + 加 `disabledReason` 字段(改 `MentionItem` interface 改 `MentionList`/`MentionRow`/`createMentionSuggestion`)|
| `server/internal/daemon/gc.go` | 1 (content) | fork HEAD `f6b811fa1` 加 `issueguard` import + 改 status 检测(closed 状态) vs upstream 加 `repocache` 包 + `reposDirName` 常量 + 扩展 gcStats + 跳过所有 dot-prefixed entry |

### Auto-merging 但需 spot-check 的文件
(Strategy D 二次盲区候选, typecheck 不报但 go build 可能报 arity 不一致)

| 文件 | upstream commit | fork 改 N | 关注点 |
|---|---|---|---|
| `server/internal/handler/comment.go` | b06af2ae1 (+18/-4) | 11 | upstream **删了 fork 加的 `skillMentionAgents` 末参**(合并函数体已收敛 `bindAndEnqueueSkillMentions` 路径)。fork 9 参 vs upstream 8 参 → merge 后 `go build` 必报 arity mismatch |
| `server/cmd/server/router.go` | b06af2ae1 | 8 | upstream 1 commit 改;未碰 fork ServerVersion/provenance 字段,OK |
| `packages/views/issues/components/issue-detail.tsx` | 8df7549d8 (MUL-5599) | 6 | upstream 仅改 sidebar reorder,未碰 fork 4 集群(pin-comment / identifier / mention / chat FAB),OK |
| `packages/core/api/client.ts` | 4fe94a6d4 + aa349fed0 + b06af2ae1 (MCP revert + unbind) | 11 | upstream MCP 改动末尾 revert 抵消;剩余 unbind runtime 改动小 |
| `server/pkg/db/generated/models.go` | b06af2ae1 | 2 | sqlc 自动生成,auto-merge OK |
| `packages/views/locales/{en,ja,ko,zh-Hans}/issues.json` | MUL-5599 sidebar | 9 (en) | upstream 改 sidebar copy, fork skill-mention 改 trigger copy |

### Per-file strategy

| 文件 | strategy | 冲突块 | 依据 + 动作 |
|---|---|---|---|
| `packages/views/editor/extensions/mention-suggestion.tsx` | **C（取 upstream + 保留 fork type-scale 行）** | 1 | fork 改这文件只在 type-scale 1 commit (`a55c95c4c`),其中 `import` 区改 text-xs→text-caption 等不冲突；冲突区在 `MentionItem` interface 字段 + `MentionRow` button render + `createMentionSuggestion` 的 agents/squads 项生成。**取 upstream 全部**(`isAgentRuntimeBound` + `disabledReason` + `Tooltip` 包裹)+ type-scale 同步从 `text-caption` 一致化 |
| `server/internal/daemon/gc.go` | **C（两边 import 区合并）** | 1 | **两边都改 import 区**：fork `issueguard`（HEAD `f6b811fa1`）vs upstream `repocache`。**同时保留两边 import**（不互斥——fork `IsClosedStatus` 检测 + upstream `reposDirName` + skip dot-prefixed 行为正交）。`gcStats` 结构两边都加字段（`cleaned/...` vs `repoCachesReclaimed`），合并两轴 |
| `server/internal/handler/comment.go` | **A (跟 upstream 收敛 fork 9 参路径)** | 0 (auto-merge 但 go build 必报) | upstream b06af2ae1 已**完全收敛** fork 的 `skillMentionAgents` + `bindAndEnqueueSkillMentions` + `designatedAgents` 路径。**决议**：取 upstream 8 参版 `triggerTasksForComment` + **删除** fork 添加的 `bindAndEnqueueSkillMentions` 函数（连同 `comment.go:2062-2101` + 测试 fixture）+ `skill_mention_trigger.go` 整文件（9 commits fork-only 改造）。fork 的 `@skill` 业务由 MUL-5559 引入的 `isAgentRuntimeBound` + `ReasonAgentRuntimeRequired` 路径**统一代替**，fork 失去独立 skill-designation 能力。这是本轮**最大的语义让步**——但 v0.4.16 之后 fork `skill_mention_agents` 已被 upstream 的 `isAgentRuntimeBound` 取代，去掉它是 follow-up 闭环 |
| `server/internal/handler/quick_action.go:917` | **A (跟 upstream 8 参转发)** | 0 | `triggerTasksForComment` 调用点从 9 参改 8 参（删 `nil` 末参），auto-merge 后手动调整 |
| `server/internal/handler/comment_reconcile_test.go` `trigger_test.go` `skill_mention_trigger_test.go` | **A (跟 upstream 8 参测试)** | 0 | 5 处 test fixture 调用从 9 参改 8 参；`skill_mention_trigger_test.go` 整文件 fork-only，可能因 upstream `skill_mention_trigger.go` 不存在而冲突，需 grep |
| `server/cmd/server/router.go` | **A (auto-merge)** | 0 | upstream 1 commit (b06af2ae1) 改的是 unbind route；fork ServerVersion/provenance 字段未碰 |
| `packages/views/issues/components/issue-detail.tsx` | **A (auto-merge)** | 0 | upstream 1 commit (8df7549d8) 改 sidebar reorder；fork 4 集群未碰 |
| `packages/core/api/client.ts` | **A (auto-merge)** | 0 | MCP 改动末尾 revert 抵消；unbind 改动小 |
| `server/pkg/db/generated/models.go` | **A (auto-merge)** | 0 | sqlc 自动生成 |
| locales (`en`/`ja`/`ko`/`zh-Hans` `issues.json`) | **B (key 并列)** | 0 | upstream 加 sidebar/trigger copy；fork 加 trigger_blocked_agent_runtime_required；**两 set 都保留**（无 key 冲突） |
| `packages/views/skills/components/runtime-local-skill-import-panel.tsx` | **A (fork-only)** | 0 | upstream 未碰 |
| `packages/views/editor/extensions/mention-suggestion.test.tsx` | **A (auto-merge)** | 0 | upstream 加 unbound agent test；fork 测试不改 |
| `packages/core/types/agent.ts` | **A (auto-merge)** | 0 | upstream 加 `runtime_bound` 字段；fork 不改 |
| `packages/views/issues/components/issue-detail.test.tsx` | **A (auto-merge)** | 0 | upstream 加 sidebar reorder test；fork 不改 |
| `server/internal/handler/issue.go` | **A (auto-merge)** | 0 | upstream 改 issue handler 一处；fork 不改 |
| `server/internal/service/task.go` | **A (auto-merge)** | 0 | upstream 1 commit (eb14e73a2, S3 checksum) 不碰 fork 内容 |
| `server/cmd/multica/cmd_issue.go` | **A (auto-merge)** | 0 | upstream CLI search 改动小；fork 不改 |
| 迁移 251_agent_runtime_unbind（MUL-5559） | **A (fork 新增)** | 0 | 无撞号；无新 FK/CASCADE；`agent.runtime_id` + `agent_task_queue.runtime_id` 改 nullable，CHECK 约束 NOT VALID |

### 后端结论

- **DB migrate 应用 1 个新迁移 (251)**，非 no-op（agent.runtime_id + agent_task_queue.runtime_id 改 nullable + CHECK 约束 NOT VALID）。
- **后端 1 真实文本冲突**（gc.go）+ **0 后端文档冲突** + **1 个 go-build 兜底签名合并**（comment.go `triggerTasksForComment` 9 参 vs 8 参）。
- **后端最大语义让步**：去掉 fork 的 `bindAndEnqueueSkillMentions` 路径（`#5346` skill mention 集群 9 commits 中的核心），由 upstream MUL-5559 `isAgentRuntimeBound` + `ReasonAgentRuntimeRequired` 取代。这意味着 fork 的 `@skill` designation 在 UI 仍可见（chip 仍渲染），但 backend 不再有独立 bind path——回到依赖 agent-runtime 的统一通道。**这影响 #5346 PR 全部价值**：fork 仍需维护 frontend 端 chip + trigger-preview，但 server 端已不需要 skill_mention_agents 输入。

### 两个同 PR 强制动作（CLAUDE.md 硬规则，Phase 4 必须做）

1. **删除 fork `server/internal/handler/skill_mention_trigger.go`**（若 upstream 不存在）+ **删除 fork `server/pkg/db/queries/skill.sql` 增量 + `server/pkg/db/generated/skill.sql.go` 增量**——需 grep upstream 是否含这些文件。fork 的 `bindAndEnqueueSkillMentions` 函数体（comment.go:2062-2101）+ `parseSkillMentionAgents` 也随之删除。`req.SkillMentionAgents` 字段保留请求体兼容（zero value = nil map），handler 接收但不使用。
2. **更新 ledger**: 添加 **CLI version in Help Menu** LOCAL 条目（fork-only 特性未记录）；标记 `a55c95c4c` type-scale refactor fork-only 文件已迁移（v0.4.16 merge 时已做，本轮零返工）；删 `da216d7cf`/`ee6d1e031`/`d53823e48` 三 commit（若放弃 fork admin rename follow-up 可选择保留）。

### Phase 1.2 — Negative claims (验证完成)

| claim | verified_by | result |
|---|---|---|
| merge-base 干净 = v0.4.16 tag | `git merge-base HEAD v0.4.17` = `3cab03cc3` == `git rev-parse v0.4.16` | confirmed |
| 24 commits / 239 files | `git log --oneline v0.4.16..v0.4.17 \| wc -l` = 24; `git diff --name-only v0.4.16..v0.4.17 \| wc -l` = 239 | confirmed |
| merge-tree 报 2 冲突 | `git merge-tree --write-tree --merge-base=3cab03cc3 HEAD v0.4.17` → 2 行 CONFLICT（content）| confirmed |
| 251 是 v0.4.17 唯一新增迁移 | `git diff --name-only v0.4.16..v0.4.17 -- server/migrations/` → 2 文件 (251.{up,down}.sql) | confirmed |
| fork max migration = 250, v0.4.17 无撞号 | `git ls-tree --name-only HEAD server/migrations/ \| awk -F_ '{print $1}' \| sort -n \| tail -5` → 250; v0.4.17 树无 119/158/159/160/213/214 新增 | confirmed |
| 251 迁移无新增 FK/CASCADE | `git show v0.4.17:server/migrations/251_agent_runtime_unbind.up.sql \| grep -iE 'FOREIGN KEY\|REFERENCES\|CASCADE'` → 仅注释提及 "no new FK",无新增 | confirmed |
| `triggerTasksForComment` upstream 8 参 vs fork 9 参 | HEAD `comment.go:1974` 9 参；v0.4.17 `comment.go:1956` 8 参；差异 = `skillMentionAgents` 末参 | confirmed |
| comment.go upstream 删 `skillMentionAgents` 末参 | `git log --oneline v0.4.16..v0.4.17 -S 'skillMentionAgents' -- server/internal/handler/comment.go` 空 → 不是单独 commit，而是 b06af2ae1 全文件重写 | confirmed |
| v0.4.17 comment.go 中 `skillMentionAgents` 计数 = 0 | `git show v0.4.17:server/internal/handler/comment.go \| grep -c 'skillMentionAgents'` = 0 | confirmed |
| fork comment.go 内 `skillMentionAgents` 引用 7 次（line 1802/1933/1974/1987/2062/2063/2101）| `git grep -n 'skillMentionAgents' server/internal/handler/comment.go` | confirmed |
| upstream 1 commit 改 issue-detail.tsx (MUL-5599) | `git log --oneline v0.4.16..v0.4.17 -- packages/views/issues/components/issue-detail.tsx` = `8df7549d8` | confirmed |
| upstream 1 commit 改 router.go (b06af2ae1) | `git log --oneline v0.4.16..v0.4.17 -- server/cmd/server/router.go` = `b06af2ae1` | confirmed |
| upstream 未碰 fork ServerVersion/provenance 字段 | `git diff v0.4.16..v0.4.17 -- server/cmd/server/router.go \| grep -E 'ServerVersion\|provenance'` 空 | confirmed |
| apps/web/package.json 仅 1 行变化 | `git diff --stat v0.4.16..v0.4.17 -- '**/package.json'` = `apps/web/package.json \| 2 +-` | confirmed |
| pnpm-workspace.yaml 无变化 | `git diff --name-only v0.4.16..v0.4.17 -- 'pnpm-workspace.yaml'` 空 | confirmed |
| mention-suggestion.tsx 冲突来源 = fork 改这文件 vs upstream 加 `isAgentRuntimeBound`/`Tooltip`/`blockedReasonLabel` import | `git diff v0.4.16..v0.4.17 -- packages/views/editor/extensions/mention-suggestion.tsx` 显示完整 diff 在 import 区 + `MentionItem.disabledReason` + `MentionRow` button render | confirmed |
| gc.go 冲突来源 = fork `issueguard` import vs upstream `repocache` import | `git diff v0.4.16..v0.4.17 -- server/internal/daemon/gc.go` 显示 import 区两边都改 | confirmed |
| MUL-5599 sidebar reorder 在 merge 后会改 fork locales issues.json 的 key 顺序 | 4 个 locales 都 auto-merging（无冲突），但 key 顺序会被 upstream 覆盖；fork 在 trigger copy 段落的增量保留 | confirmed |

## Phase 2 — Confirm irreversible gates ✅

- **DB migration**: 1 个新迁移 251 (`agent.runtime_id` + `agent_task_queue.runtime_id` 改 nullable + CHECK NOT VALID)。无新 FK/级联,符合 fork 规则。
- **Service restart**: launchd 短暂重启。
- **Version re-stamp**: 前端 `.env NEXT_PUBLIC_APP_VERSION=v0.4.17` + 后端 `-ldflags -X main.version=v0.4.17`。

### 关键策略决议

- **fork 9 参 workaround (用户选)**: 保留 `triggerTasksForComment` 9 参签名 + `bindAndEnqueueSkillMentions` + `parseSkillMentionAgents` + `skill_mention_trigger.go`。合并后需手动 patch 把 upstream 8 参调用点改回 9 参(传 `nil` 给末参或传 fork 自己的 skillMentionAgents map)。`req.SkillMentionAgents` 请求体字段保留兼容。
- **#5346 PR 状态保留 OPEN 但加脚注**(本轮起 own-forever workaround,不再期望 upstream merge)。Ledger 调整在 Phase 7。
- **授权范围**: Phase 3-5 全程 + Phase 6 验证后复问(用户留了双重检查)。
- `user_confirmed: true`

## Phase 3 — Reversible preview ✅

- safety branch: `main-backup-v0.4.17` (= `405b7c7b3`)
- 工作区 stash 暂存 plan 文件(避免 merge overwrites 未提交改动)
- `git merge v0.4.17 --no-commit --no-ff`(merge tag, 非 origin/main)
- 实际 vs 预测:**完全一致** — 2 个真实文本冲突(mention-suggestion.tsx + daemon/gc.go),全 content 型,无 modify/delete/add/add
- rerere 记录 2 个 preimage 但**未自动吸收任何冲突**(本轮冲突都是新模式)
- 后端 comment.go / task.go / router.go / handler.go / workspace.go 全 auto-merge 干净
- 预测准确:merge-tree 报 2,实际 2,零意外(**连续 4 次文本预测命中**:v0.4.14/v0.4.15/v0.4.16/v0.4.17)

## Phase 4 — Resolve + re-apply ✅

- **`mention-suggestion.tsx` (C 整合)**: 取 fork skill row 分支 + 集成 upstream `disabledMessage` + `Tooltip` 包装模式(MUL-5559)。让 fork skill item 在 unbound 状态下也走 disabled reason code 路径。`useT('issues')` hook 已由 auto-merge 提前加入。
- **`daemon/gc.go` (B import 双保留)**: 两边都在 import 区动手 → 全部保留。`repocache`(upstream, repo cache eviction)+ `issueguard`(fork HEAD `f6b811fa1`, status detection)正交不冲突。`gcStats` 字段合并。
- **`comment.go` (auto-merge + fork 9 参 workaround)**: auto-merge 干净保留了 fork 9 参 `triggerTasksForComment` + `bindAndEnqueueSkillMentions` + `parseSkillMentionAgents`。**Phase 1 预测的"upstream 8 参调用点"未出现**(upstream 的 8 参函数定义被 auto-merge 整个替换为 fork 9 参定义 + call site 全部 fork 9 参)。`go build` 应该无需修改即 PASS。
- **范围决策**: fork 9 参 workaround(用户选,Phase 2)。
- 2 冲突全解,`git add` 完成(243+ 文件 staged),merge commit 留到 Phase 7(验证先于固化)。

## Phase 5 — Verify ✅

| 步骤 | exit | 备注 |
|---|---|---|
| pnpm install | 0 | 4.6s; 仅 `apps/web/package.json` 1 行变更(dependency 锁更新) |
| pnpm typecheck | 0 | 6/6, 39.1s (core/views/web/desktop/docs/ui) |
| go build -C server ./... | 0 | fork 9 参 workaround 自动成功:upstream 8 参函数定义被 auto-merge 整个替换为 fork 9 参定义 + call site 全部 fork 9 参,**无 arity mismatch** |
| pnpm test | 0 | 5/5, views **3589** tests (v0.4.16 为 3552,本轮 +37) / web / core / desktop / docs |
| pnpm build | 0 | 3/3, 43.5s (web Next.js production + Middleware + desktop + core/views) |

**五项全绿 → advance phase: gates**

注:Phase 1 预测的"fork 9 参 vs upstream 8 参 arity mismatch"未出现。原因是:auto-merge 取了 fork 一侧的 9 参函数定义与 9 参 call site,upstream 的 8 参函数体被丢弃(upstream 9 commits fork 全部存活)。

## Phase 6 — Irreversible gates ✅

GATE: phase==gates ✓ / user_confirmed ✓ / audit_strategy_written ✓ / 18 negative claims all verified ✓
- ✅ merge commit `720f03209` 先落(`git describe --abbrev=0` → v0.4.17);rerere 记录 2 resolution(mention-suggestion.tsx + daemon/gc.go)
- ✅ pg_dump 备份(pg17 binary):`~/multica-backups/v0.4.17-pre-migrate-20260804-093647.dump`(48M)
- ✅ migrate up:**1 个新迁移(251_agent_runtime_unbind)up**,235-250 既有 skip,`Done.`(与审计预测一致;无中途 abort)
- ✅ 版本 re-stamp:前端 `.env NEXT_PUBLIC_APP_VERSION=v0.4.16→v0.4.17`;`resolve-official-baseline.sh` → v0.4.17
- ✅ launchd 重启:install.sh 全量 rebuild standalone frontend + `-ldflags` stamp backend + reload 两个 plist(FIXED 后裸跑安全)
- ✅ health check:backend `/healthz` 200(30s wait 后)、frontend `/` 200、`/api/config server_version=v0.4.17`(双注入点 stamp 生效)

## Phase 7 — Record ✅

### merge commit `720f03209`

含 24 upstream commit 摘要 + 2 个冲突策略 + fork 9 参 workaround 决议 + #5346 OPEN 状态 + 18 条 verified negative claims 指针。

### Actuals vs Plan（审计预测对照，校准未来审计）

- 预测：2 文件 / 2 冲突块（merge-tree 实测），全 content 型，风险低。
- 实际：**2 手动冲突 + 0 rerere 自动吸收 + 0 go-build 兜底**（fork 9 参 workaround 自动成功）。
- 校准：
  - **文本冲突预测连续第 4 次命中**（v0.4.14/v0.4.15/v0.4.16/v0.4.17 均 merge-tree 报多少实际多少）。rerere 本轮未吸收任何冲突（新模式），但记录了 2 resolution 供下次。
  - **新变量（本轮最大教训）：fork 9 参 vs upstream 8 参 arity mismatch 预测未触发**。v0.4.16 时的 Strategy D 双向改签名（fork 加 `skillMentionAgents` + upstream 加 `delegationAuthorityUserID`）触发过 go-build 修复；本轮 upstream b06af2ae1 **单向收敛**了 fork 加的 `skillMentionAgents`（删除），auto-merge 取 fork 一侧 9 参函数定义 + 9 参 call site → 干净保留，无需 go-build 修复。**教训：merge 时 upstream 是「单向收敛 fork 加参」 vs 「双向改签名」区别巨大。前者 auto-merge 自动成功，后者需要 go-build 兜底**。Phase 1 预测时应区分两种场景。
  - **审计的 mention cluster 判断准确**：mention-suggestion.tsx (C 整合) + daemon/gc.go (B 双 import) — 与预测逐字一致。
  - **迁移 251 干净**：无新 FK/级联，CHECK NOT VALID，应用层 explicit UPDATE — 与 fork 规则完全对齐。0 text 冲突。
  - **后端零文本冲突** + 后端最大签名合并（fork 9 参 vs upstream 8 参）**自动成功**——这是策略选择的胜利（用户在 Phase 2 决定保留 fork 9 参）。
  - **#5346 PR 状态保留 OPEN**：用户在 Phase 2 选了「保留 OPEN 但加脚注」。脚注已加入 ledger 表（v0.4.17 决议段），记录 fork patch 与 upstream 永久分裂。
  - **pnpm test views 3589** (v0.4.16 为 3552,本轮 +37 测试) — `blocked-trigger-copy.test.ts` 等 MUL-5559 新增测试全过。

### Final state

- base: `v0.4.17`
- merge commit: `720f03209`
- HEAD: `v0.4.17-1-g720f03209`
- server_version: `v0.4.17` (verified via `/api/config`)
- DB: 1 个新迁移（251_agent_runtime_unbind）已应用；6 已知迁移撞号（119/158/159/160/213/214）完好
- plan file: `phase: done`，全 gates true，18 条 negative claims 全 verified