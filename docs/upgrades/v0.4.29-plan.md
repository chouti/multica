---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.29
base_tag: v0.4.28   # clean merge-base = v0.4.28^{commit} = 38c992ad0 (single hop; prior merge 08c61c3ab)

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true           # Phase 2 — CONFIRMED 2026-08-19: (1) archived frontend mapping = Option 1
                                 # "teach the frontend archived as 8th category" (mirror server compose-and-keep;
                                 # IssueStatusCategory union + "archived", status-category.ts 3 funcs, config/status
                                 # entries type-valid, queries.ts explicit archived fetch, "exactly the 7" test → 8);
                                 # (2) scope = full push to Phase 6 (pg17 backup → migrate 342/343 → re-stamp →
                                 # install.sh restart → health). Daemon NOT force-upgraded (protocol additive;
                                 # align via tap when it ships).
  audit_strategy_written: true    # Phase 1.4 — FLIPPED 2026-08-19: per-file strategy table in Phase 1 body
                                  # (4 lanes + main-session merge-tree). Epicenter (frontend archived↔category)
                                  # marked C pending Phase 2 decision (Option 1 recommended). 15 negative_claims
                                  # all carry verified_by commands + results.
  db_backed_up: true              # Phase 6.1 — DONE 2026-08-19T14:29:49Z: pg17 pg_dump -F c, 62M,
                                  # 762 TOC entries (pg_restore -l verified). Dump:
                                  # ~/multica-backups/v0.4.29-pre-migrate-20260819T142949Z.dump
  migrated: true                  # Phase 6.2 — DONE 2026-08-19T14:30: applied 342_runtime_profile_add_mcode +
                                  # 343_comment_delegated_failure_pending_index (concurrent-index pre-hook ran).
                                  # Latest = 343. Archived rows invariant intact (count verified post-migrate).
  version_stamped_frontend: true  # Phase 6.3 — DONE 2026-08-19: .env NEXT_PUBLIC_APP_VERSION v0.4.28→v0.4.29
                                   # (line 27) + install.sh standalone build; bundle grep hit v0.4.29 in layout chunk.
  version_stamped_backend: true   # Phase 6.3 — DONE 2026-08-19: install.sh ldflags build; strings
                                   # ~/.multica/backend/server → main.version=v0.4.29 (2 hits); runtime
                                   # /api/config server_version=v0.4.29. commit-first satisfied (895839b9a at
                                   # HEAD, git describe --tags = v0.4.29-325-g895839b9a).
  services_restarted: true        # Phase 6.4 — DONE 2026-08-19: install.sh rebuilt both + bootout/bootstrap
                                   # both plists (loaded com.fengzhao.multica-{frontend,backend}).
  health_checked: true            # Phase 6.5 — DONE 2026-08-19: backend /healthz=200 (FIRST attempt, no
                                   # startup-window retry needed), frontend :3001=200, /api/config
                                   # server_version=v0.4.29. Post-migrate invariants: archived rows 267 intact,
                                   # issue_status catalog 35 (5 ws × 7) intact.
                                   # Phase 6.6 daemon: NOT aligned (by plan) — tap latest is 0.4.28, so CLI=daemon
                                   # stay 0.4.28 vs backend 0.4.29 (protocol additive, not an outage; agents 5 +
                                   # workspaces 5 verified). Align via `brew upgrade multica && multica daemon
                                   # restart` when the tap ships 0.4.29 (same as v0.4.28 round).

merge_commit: 895839b9a  # TRUE 2-parent (e8364fb2b fork HEAD + c670e0549 v0.4.29 tag commit); git describe = v0.4.29-325-g895839b9a; rerere recorded both conflict resolutions

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done

# Negative claims — each confirmed/refuted by ONE raw command, never by reading.
# Verified in the MAIN session unless an entry notes it was delegated (then re-verify before gates).
negative_claims:
  - claim: "merge-base(HEAD, v0.4.29) = v0.4.28^{commit} — single clean hop, prior merge 08c61c3ab was true 2-parent"
    verified_by: "git merge-base HEAD v0.4.29 → 38c992ad0; git rev-parse v0.4.28^{commit} → 38c992ad0 (identical)"
    result: "confirmed. Single-hop v0.4.28 → v0.4.29; 18 upstream commits, 196 files (+9198/-814)."
  - claim: "NEW upstream migrations = 342/343 only, ALL > fork max 341 → ZERO new numeric collision; 6 known collisions (119/158/159/160/213/214) unchanged"
    verified_by: "diff <(git ls-tree v0.4.28 server/migrations/) <(git ls-tree v0.4.29 server/migrations/) → only 342_runtime_profile_add_mcode + 343_comment_delegated_failure_pending_index; ls server/migrations | grep -E '^(342|343)_' → none in fork; ls | sed s/_.*// | sort -n | tail → 341"
    result: "confirmed. 342/343 upstream-new; fork max 341; no fork 342/343 → no new collision."
  - claim: "upstream v0.4.29 does NOT touch fork's 9-param skill-mention workaround in comment.go (triggerTasksForComment / parseSkillMentionAgents / bindAndEnqueue)"
    verified_by: "git diff v0.4.28..v0.4.29 -- server/internal/handler/comment.go | grep -E 'triggerTasksForComment|skillMention|parseSkillMention|bindAndEnqueue' → only hunk @@3507 in retriggerCancelledTaskSurvivors (14 insertions, MUL-6063); diffstat = +14/1 file"
    result: "confirmed. comment.go surface is a single 14-line addition in retriggerCancelledTaskSurvivors; fork 9-param path untouched by upstream."
  - claim: "truncateFallbackCommentBody is NOT a collision: upstream did NOT modify the function body — the +8 hunk was a NEW const (RuntimeClaimFreshnessSeconds=150.0) added after it; merged tree defines the func exactly ONCE (fork file, cap=200); call site alive"
    verified_by: "git diff v0.4.28..v0.4.29 -- service/task.go | sed -n 1,40p → +8 = RuntimeClaimFreshnessSeconds const, NOT function body; git merge-tree --write-tree HEAD v0.4.29 → tree eb5a331af; git grep -c 'func truncateFallbackCommentBody' eb5a331af → exactly 1, in fallback_comment_truncate.go (NOT re-added to task.go); git grep truncateFallbackCommentBody( eb5a331af task.go → call site alive at :3748 with maxSynthesizedFallbackCommentRunes"
    result: "refuted-the-collision. NO duplicate-symbol break, NO silent loss (upstream never touched the truncate logic). Fork deletion from task.go honored by auto-merge; upstream's RuntimeClaimFreshnessSeconds const lands clean. Keep fork fallback_comment_truncate.go verbatim. (Corrects an earlier draft claim that mis-read the @@context line as a function-body change.)"
  - claim: "merge-tree predicts only 2 TEXT conflicts (queries.ts, hidden-columns-panel.tsx); the high-risk Go files (issue.go/issuestatus.go/task.go/comment.go) auto-merge clean at hunk level"
    verified_by: "git merge-tree --write-tree --name-only HEAD v0.4.29 → exit 1; CONFLICT(content) only in packages/core/issues/queries.ts + packages/views/issues/components/hidden-columns-panel.tsx; issuestatus.go/issue.go/task.go/comment.go/issue_status_test.go/config-status.ts/status-icon.tsx/4×settings.json all 'Auto-merging'"
    result: "confirmed as PREDICTION BASELINE (2 text conflicts). Caveat per skill: merge-tree CANNOT see fork-only files or semantic collisions (dup-symbol/different-region, signature drift, fork-code×upstream-test). Auto-merge-clean ≠ correct — Lane A/B/C agents verify semantics. Actual count expected to deviate (locale JSON diverges at more hunks than file-level estimate)."
  - claim: "SERVER archived compose-and-keep SURVIVES v0.4.29 with zero server code change — upstream category machinery keyed on IsBuiltIn/IsCategory, IsCategory==IsBuiltIn, fork's IsBuiltIn('archived') patch makes archived a valid category automatically (server sends status_category:'archived')"
    verified_by: "Lane A blob-read of merged tree: issuestatus.go merged blob has Archived const L105 + IsBuiltIn special-case L110 + IsCategory L120 + ActiveKeys tail L282-283 + ExpandCategories L340+ coexisting; git diff v0.4.28..v0.4.29 -- issue.go | grep '^@@' (18 hunks) does NOT cover fork's validIssueStatuses/buildSearchQuery/ChildIssueProgress regions"
    result: "confirmed. Fork's server carry surface needs NO re-application this round — the v0.4.28 compose pattern is transitive through upstream's IsCategory=IsBuiltIn. Server work = B keep-both + make sqlc only."
  - claim: "FRONTEND archived has NO home in upstream's category model — isIssueStatusCategory('archived')=false, issueStatusCategory(archived)=null (excluded from cache buckets), statusCategoryOfKey('archived')='todo' (mislabeled). This is a REAL collision needing a Phase 2 decision, NOT mechanical keep-both"
    verified_by: "Lane B: git show v0.4.29:packages/core/issues/status-category.ts (issueStatusCategory/statusCategoryOfKey/normalizeStatusPatch); git show v0.4.29:packages/core/types/issue.ts (union renamed IssueStatusCategory, IssueStatus=alias, custom keys deferred); isIssueStatusCategory test 'accepts exactly the 7' (queries.test.ts:59)"
    result: "confirmed COLLISION. Epicenter = types/issue.ts + config/status.ts + queries.ts. Phase 2 decision required (Option 1 = teach frontend archived-as-8th-category, mirrors server; Option 2 = map to cancelled — REJECTED per memory, inherits completed semantics)."
  - claim: "comment.go merge is CLEAN (fork 9-param skill-mention workaround safe); fork's retriggerCancelledTaskSurvivors delegation-removal is disjoint from upstream's +14 delegated-failure replay"
    verified_by: "Lane C: git merge-file -p (ours=HEAD base=v0.4.28 theirs=v0.4.29) → exit 0, 0 conflict markers; upstream diff = single @@3507 hunk +14, does not touch triggerTasksForComment"
    result: "confirmed. No manual resolution needed on comment.go."
  - claim: "6 TaskService method signatures ZERO drift v0.4.28→v0.4.29 (ClaimTask/StartTask/CompleteTask/FailTask/RerunIssue/HandleFailedTasks) → no fork-caller arity break"
    verified_by: "Lane C: git show v0.4.28:...task.go | grep -nE 'func (s *TaskService) (...)' vs v0.4.29 — byte-identical (only line offsets); fork callers (autopilot_listeners_test/quick_create_subscriber_test/retired_session_test/rerun_session_test/daemon.go) match"
    result: "confirmed. go vet sees no arity break. (Upstream ADDED CancelTaskOptions.UserInitiated field — additive, fork paths implicitly false; behavioral follow-up only.)"
  - claim: "make sqlc regen is a HARD compile dependency (not optional): issue_status.sql adds ListIssueStatusKeysByCategories (ExpandCategories needs it for the Querier interface); runtime.sql changes FailTasksForOfflineRuntimes signature"
    verified_by: "Lane A+C: git diff v0.4.28..v0.4.29 -- pkg/db/queries/issue_status.sql (+23, new query); runtime.sql (+32, FailTasksForOfflineRuntimes gains @reconnect_grace_secs/@max_per_tick); runtime_sweeper.go caller fork-untouched + upstream updated both"
    result: "confirmed. Phase 5 must run make sqlc (~/go/bin/sqlc generate on this host) then go build/vet."
  - claim: "desktop/landing/lark/daemon/attribution/docs/mobile areas have ZERO fork overlap with upstream v0.4.29 changes"
    verified_by: "Lane D: git diff v0.4.28..HEAD --name-only -- apps/desktop/ (only routes.tsx, not in upstream's 9); -- apps/web/features/landing/ (empty); -- server/internal/integrations/lark/ (empty); -- server/internal/integrations/ (empty); -- apps/docs/ (empty); -- apps/mobile/ (upstream empty)"
    result: "confirmed. All accept-upstream. skill.go/client.ts/SELF_HOSTING.md intersections merge-file exit 0."
  - claim: "archived child-progress chain is fully SAFE — upstream does not touch getChildIssueProgress / entry.archived / child-progress.ts"
    verified_by: "Lane B: git diff v0.4.28..v0.4.29 -- packages/core/issues/queries.ts | grep childIssueProgress/archived (no match); -- packages/core/api/client.ts | grep getChildIssueProgress (no match); child-progress.ts is fork-only (not in v0.4.29)"
    result: "confirmed. keep-fork, no action."
  - claim: "upstream v0.4.29 does NOT touch fork's SQL-guard files (pkg/db/queries/issue.sql, internal/issueguard/issue_status.go)"
    verified_by: "git diff --name-only v0.4.28..v0.4.29 -- server/pkg/db/queries/issue.sql server/internal/issueguard/issue_status.go → both empty (changed=0)"
    result: "confirmed. Fork's issue.sql guards + issueguard compose predicates untouched by upstream."
  - claim: "PR #6106 (fork archived status) still OPEN; ledger PR states current — fork archived customization remains live, upstream category-archival (archived_at) is a DIFFERENT concept"
    verified_by: "gh pr list --repo multica-ai/multica --author chouti --state all → '#6106 OPEN -', '#5346 OPEN', '#5006/#5393 MERGED', '#5539/#5199/#5466 CLOSED'"
    result: "confirmed. Ledger PR classification unchanged as of 2026-08-19."
---

# Upgrade plan — v0.4.28 → v0.4.29

Driver reading of the 18 upstream commits. Headline: **MUL-6243 follow-up (`14c2e4e83`) — board fetches by CATEGORY; catalog-status archive retires without migration.** This is the semantic + file-level collision with the fork's archived compose-and-keep carry.

> ⚠️ **Trap avoided:** "archive retires" in `14c2e4e83` refers to *archiving a custom status in the catalog* (`archived_at` column — retire from FUTURE assignment, existing issues keep it), NOT the fork's `archived` issue-status value. Verified by reading the full commit body. But the FILE overlap with the fork's archived carry surface is total.

## Phase 0 — Locate (DONE 2026-08-19)

- Current base: `v0.4.28-324-ge8364fb2b` (324 fork commits ahead of v0.4.28).
- Latest upstream release: **v0.4.29** (published 2026-08-18, not prerelease/draft). Tag commit `c670e0549`.
- merge-base(HEAD, v0.4.29) = `38c992ad0` = v0.4.28^{commit} → single clean hop.
- Upstream delta: 18 commits, 196 files (+9198/-814).

Upstream commit drivers:
- `14c2e4e83` MUL-6243 issue-status: board by category + catalog archive retire (THE collision).
- `afd05ca22` MUL-6063 recover terminal delegated task failures (task.go +738, comment.go +14).
- `22db0b0a2` MUL-5651 preserve tasks through runtime network partitions.
- `d6301091e`/`3dc87b669`/`4e10d6805` NUL-byte + execenv + failure-diagnostics fixes.
- `1b6d42864` desktop Cmd/Ctrl+, Settings reland (MUL-6233).
- `de902a4aa` MiniMax Code ACP runtime (MUL-6273) → migration 342.
- `4946f4685` lark standalone file/audio ingest (MUL-6198).
- `51ae68d8b` web /download newest fully-uploaded release (MUL-6313).
- `5620b7e22` bill cache reads in cost charts (MUL-6334); `c17553028` Hermes in-flight tool calls; `f320af4a5` transcript agent-once; `212ecfb8d` daemon reply-turn status arc; `27de4fb47` channel lease_gen log; `0b9dcc679` Pi RPC stdin Windows.

## Phase 1 — Deep pre-merge audit

Fan-out lanes (parallel agents, read-only):
- **Lane A — archived/issuestatus server collision** (the headline): `issuestatus.go` (+53 upstream, fork carry #1), `handler/issue.go` (+256, fork carry #2), `handler/issue_status.go` (+33), `pkg/db/queries/issue_status.sql` (+23, sqlc), `service/{issue,task}.go` category filler, `github.go`, `onboarding_shim.go`, `share_link.go`. Semantic core: how fork `archived` pseudo-builtin maps into upstream's `status_category` + `ExpandCategories` world.
- **Lane B — frontend issue-status surface + locales**: `core/issue-statuses/*` (new), `core/issues/{status-category,config/status,cache-helpers,cache-coordinator,queries,mutations,ws-updaters,index}`, `core/types/{issue,issue-status,api,agent}`, `views/issues/components/*`, `inbox-detail-label`, `mention-suggestion`, locale files. Fork frontend archived surface: `config/status.ts`, `status-icon.tsx`, `ChildIssueProgress` archived column (queries.ts reads `entry.archived`).
- **Lane C — comment.go + task.go + taskfailure + runtime.sql**: verify retriggerCancelledTaskSurvivors no fork overlap; truncateFallbackCommentBody modify/delete resolution + call-site liveness (v0.4.22 lesson); FailTask/CompleteTask/ClaimTask arity vs fork test files; runtime.sql → sqlc regen.
- **Lane D — everything else**: desktop Cmd+, landing/download, lark media, daemon/execenv, attribution, docs, server/cmd — verify ZERO fork overlap.

## Phase 1 — Deep pre-merge audit (DONE 2026-08-19 — 4 parallel lanes + main-session merge-tree)

Strategy legend: **A**=accept-upstream / **B**=keep-both (disjoint, auto-merge clean) / **C**=careful-merge (same region or semantic collision, manual) / **D**=orthogonal-signature.

### Headline findings

1. **SERVER: fork's archived compose-and-keep SURVIVES v0.4.29 automatically — zero server code change.** Upstream's category machinery is keyed on `IsBuiltIn`/`IsCategory`, and upstream's `IsCategory = IsBuiltIn`. The fork's patch (`IsBuiltIn("archived")==true`) makes `IsCategory("archived")==true` transitively → category builders emit `StatusCategory="archived"` (raw key, zero catalog query), `Resolver.Effective("archived")` returns "archived", write path accepts it as built-in. **Server SENDS `status_category:"archived"` for archived issues.** (Lane A, blob-verified on merged tree.)

2. **FRONTEND: the archived 8th pseudo-builtin has NO home in upstream's v0.4.29 category model — THE Strategy C epicenter.** Upstream renamed the closed-7 union `IssueStatus` → `IssueStatusCategory` and made `IssueStatus = IssueStatusCategory` (custom keys deferred to a later PR). So `isIssueStatusCategory("archived")=false`, `issueStatusCategory(archived)=null` (→ archived kicked OUT of board cache buckets via ws-updaters `bucketable` check + cache-coordinator), `statusCategoryOfKey("archived")="todo"` (→ every category-path component mislabels archived as todo). **Server sends "archived"; frontend recognizes only 7 → archived issues vanish from the board / get mislabeled.** Needs a Phase 2 architectural decision. Epicenter: `types/issue.ts`, `config/status.ts`, `queries.ts` (+ linked `status-category.ts`, `status-icon.tsx`, `hidden-columns-panel.tsx`).

3. **merge-tree predicts only 2 TEXT conflicts** (`queries.ts`, `hidden-columns-panel.tsx`) — both frontend, both epicenter. Go server files auto-merge clean at hunk level but several are SEMANTIC collisions (typecheck/test catches).

4. **`make sqlc` regen is a HARD compile dependency.** `issue_status.sql` adds `ListIssueStatusKeysByCategories` (ExpandCategories calls it; without regen `*db.Queries` won't satisfy `issuestatus.Querier`). `runtime.sql` rewrites `FailTasksForOfflineRuntimes` (+params). generated file-sets disjoint → git-merges clean, regen mandatory.

### Per-file strategy table

**SERVER (Lane A + Lane C) — all B or A; compose survives:**

| File | Fork | Upstream | Strategy |
|---|---|---|---|
| `issuestatus/issuestatus.go` | +20 (Archived/IsBuiltIn/ActiveKeys tail) | +53 (ExpandCategories + Querier method, disjoint) | **B** (blob-verified coexist) |
| `handler/issue.go` | +31 (validIssueStatuses append, search tail, rank CASE, include_archived, ChildIssueProgress.Archived) | +256 (StatusCategory, 3 fillers, status_categories param, ExpandCategories where-append — 18 hunks disjoint) | **B** (two independent where-conditions compose) |
| `handler/issue_status.go` | — | +33 (ArchiveIssueStatus drops in-use check) | **A** |
| `handler/issue_status_test.go` | TestUnseeded 7→8 keys | +357 (~10 new category/archive tests, none use "archived" as issue status) | **B** |
| `issuestatus/issuestatus_test.go` | — | +55 (ExpandCategories tests, no ActiveKeys/IsBuiltIn-archived assert) | **A** |
| `pkg/db/queries/issue_status.sql` (+gen) | — | +23 (ListIssueStatusKeysByCategories) | **A** + make sqlc |
| `pkg/db/queries/runtime.sql` (+gen) | — | +32 (FailTasksForOfflineRuntimes +params) | **A** + make sqlc (caller fork-untouched → self-resolves) |
| `service/task.go` | +94 (truncate helper moved out — SEPARATE custom) | +782 (RuntimeClaimFreshnessSeconds, CancelTaskOptions.UserInitiated, sweeper/recovery, IssueToMapWithCategory) | **B**; helper deletion honored, cap=200 kept (deliberate) |
| `handler/comment.go` | +357/-75 (9-param skill-mention, retrigger delegation-removal) | +14 (retrigger delegated-failure replay, disjoint) | **B** merge-file exit 0 (9-param SAFE) |
| `handler/github.go` | +6 (issueguard.IsClosedStatus) | +1 (fillStatusCategory) | **B** |
| `handler/issue_table_rows.go` | +2 (status CASE archived) | +3 (fillTableRow) | **B** |
| `service/{issue,autopilot}.go`, `handler/{share_link,onboarding_shim}.go`, `pkg/taskfailure/*`, `service/fallback_comment_truncate.go` (fork-only) | —/fork-only | +small / additive | **A** / keep-fork |

**FRONTEND (Lane B) — epicenter + clean majority:**

| File | Fork | Upstream | Strategy |
|---|---|---|---|
| `types/issue.ts` | +1 (`\|"archived"` in union) | renamed union→IssueStatusCategory + alias + status_category field | **C** — add "archived" to renamed union (Phase 2 decision) |
| `issues/config/status.ts` | STATUS_ORDER/ALL_STATUSES+archived, BOARD/CLOSED/COMPLETED_STATUSES, STATUS_CONFIG["archived"] | re-keyed IssueStatus→IssueStatusCategory + imports | **C** — entries type-valid once archived ∈ union (typecheck first catcher) |
| `issues/queries.ts` | fetchFirstPages per-status+include_archived; childIssueProgress select+archived | PAGINATED_STATUSES→PAGINATED_CATEGORIES, fetch per-category | **C** (fetchFirstPages TEXT conflict); childIssueProgress keep-fork |
| `issues/status-category.ts` (NEW upstream) | — | issueStatusCategory/statusCategoryOfKey/normalizeStatusPatch (archived→null/todo) | **A + re-apply fork archived** (teach 3 funcs + default branch) |
| `types/api.ts` | include_archived×3, invitee_name | +status_category/status_categories params | **B** |
| `types/agent.ts` | BatchImport interfaces | +"mcode" protocol, +TaskFailureReason | **B** |
| `issues/mutations.ts` | comment/skill-mention (not status) | normalizeStatusPatch wraps | **B** |
| `views/issues/components/status-icon.tsx` | ArchivedIcon + STATUS_RENDERERS["archived"] + svg aria | +statusCategoryOfKey import, cfg lookup re-keyed | **C** (bound to config/status.ts) |
| `views/issues/components/hidden-columns-panel.tsx` | useMemo+BOARD_STATUSES filter (parent) | +statusCategoryOfKey import, row lookup (child) | **C** (TEXT conflict — import block + BOARD_STATUSES dep) |
| `views/editor/extensions/mention-suggestion.test.tsx` | ReactRenderer mock, skills cache, new test | PAGINATED rename | **B** (TEXT conflict — adopt upstream rename + keep fork mocks) |
| `issues/{cache-helpers,cache-coordinator,ws-updaters,index}.ts`, `issue-statuses/*`, `types/issue-status.ts`, 6 views components, inbox-detail-label | — (fork footprint=0) | category re-key | **A** BUT linked to epicenter (archived→null/todo unless decision applied) |
| `issues/config/index.ts`, `child-progress.ts` (fork-only), `config/status.test.ts` (fork-only), `client.ts:getChildIssueProgress` | fork archived chain | — (upstream untouched) | **keep-fork** (archived child-progress chain SAFE, verified) |
| `locales/{en,ja,ko,zh-Hans}/settings.json` | +2 invite_name keys (#4118) | +1 open_settings key | **B** (600+ lines apart, zero risk) |

**EVERYTHING ELSE (Lane D) — clean:** desktop (Cmd/Ctrl+, reland vs fork routes.tsx /admin — orthogonal), landing/download, lark, daemon/execenv, attribution, docs (16 mdx), mobile → **A**, zero fork overlap. `handler/skill.go` + `api/client.ts` (#2669/#5160) both-changed but disjoint, merge-file exit 0 → **C-verified-clean** (note: fork skill-import inherits upstream `sanitizeNullBytes` U+FFFD semantic — benign). `SELF_HOSTING.md` merge-file clean → **A**. server/cmd: no new cobra flags, no overlap → clean (backstop `go test ./cmd/multica/`).

### fork-code × upstream-test collisions to expect in Phase 5 (3-way-invisible)
- upstream `issue-statuses/queries.test.ts` "isIssueStatusCategory accepts exactly the 7" → fork makes it 8 (if Option 1) → update to 8 (mirrors server TestUnseeded 7→8).
- `mention-suggestion.test.tsx` PAGINATED rename (resolved in merge); `cache-helpers.test.ts` PAGINATED re-key (accept upstream).
- typecheck flags config/status.ts + status-icon.tsx archived entries UNTIL the union decision lands.

### Behavioral (non-blocking) follow-ups
- `CancelTaskOptions.UserInitiated`: fork's existing cancel paths implicitly become `false` (upstream "automatic" semantics). Review whether fork's user-cancel paths should pass `true` (MUL-6063). Not a build error.
- `sanitizeNullBytes` → `util.SanitizeTextForPostgres`: fork's skill-import inherits U+FFFD-replacement semantics (upstream-intended, benign).

## Phase 2 — Confirm irreversible gates

(pending)

## Phase 3 — Reversible preview

(pending)

## Phase 4 — Resolve + re-apply (DONE 2026-08-19)

- Actual text conflicts: **2 == merge-tree prediction (10th consecutive audit hit)**. Both resolved:
  - `queries.ts` fetchFirstPages: upstream category fetch + **fork include_archived axis kept** (`include_archived: category === "archived"`). VERIFIED NECESSARY: server composes `status = ANY(<expanded>) AND status <> 'archived'` when include_archived unset → an archived-category fetch without the flag returns EMPTY.
  - `hidden-columns-panel.tsx`: import-block keep-both (useMemo + statusCategoryOfKey).
- **Option 1 (teach frontend archived as 8th category) was ABSORBED BY AUTO-MERGE**: the union (`types/issue.ts`) got "archived" via fork-addition × upstream-rename combination; `STATUS_ORDER`/`STATUS_CONFIG` fork entries survived; `BUILT_IN = new Set(STATUS_ORDER)` (issue-statuses/queries.ts:65) derives archived in → `isIssueStatusCategory("archived")=true` → `issueStatusCategory`→"archived" (not null), `statusCategoryOfKey`→"archived" (not todo), `normalizeStatusPatch` correct. **Semantic layer = zero code change.**
- Test re-pin: `issue-statuses/queries.test.ts` "accepts exactly the 7" → "exactly the 8 — 7 upstream + the fork's archived" + explicit `isIssueStatusCategory("archived")=true` assertion (mirrors server TestUnseeded 7→8).
- Loss scan ∅: invite_name + open_settings coexist in 4 locales; archived keys intact in 4 issues.json; no count-7 assertions anywhere. Server pins alive: TestValidateIssueStatusArchived, TestUnseeded (8 keys), Archived const L105/IsBuiltIn L110/ActiveKeys L282, validIssueStatuses append L102, 3 SQL guard tails + ChildIssueProgress archived column (issue.sql:404).
- status-icon.tsx composes: STATUS_RENDERERS["archived"]=ArchivedIcon typechecks (archived ∈ union), STATUS_CONFIG[statusCategoryOfKey("archived")] resolves archived cfg.

## Phase 5 — Verify (DONE 2026-08-19, all green)

| Gate | Result |
|---|---|
| pnpm install | exit 0 (3.6s) |
| pnpm typecheck | **6/6** after 1 fix: config/status.ts needed `IssueStatus` back in the type import (upstream narrowed it to IssueStatusCategory; fork body lines still name IssueStatus) |
| go build -C server ./... | exit 0 |
| go vet -C server ./... | exit 0 |
| sqlc generate (v1.31.1) | exit 0, **ZERO drift** (regen output identical to merged tree; `git diff` empty vs index) |
| pnpm test | **375 files / 4438 tests passed, 5/5 tasks** — ZERO fork-code × upstream-test collisions (v0.4.20 had 14) |
| pnpm build | **3/3** (web/desktop/docs) |
| go test ./internal/{issuestatus,issueguard} ./pkg/taskfailure | ok |
| go test ./cmd/multica | 1 fail = TestRunSkillImportJsonTreatsDuplicateAsConflictResult — **documented residual, fails identically on main-backup-v0.4.29** (#2669 batch routing). No cobra init panic. |
| go test ./internal/handler | 6 fails — ALL documented autopilot/skill-mention residuals (LineageBinding, LeaderMentionEnqueues, WorkerResultWakes, NoDesignationIgnoresAssignee, ReconcileRestoresAuthority, ReStampedToEditingTask) |
| go test ./internal/service | TestBuiltinSkillsConformToTemplate = documented residual; TestChannelMediaReconciler_SettlesThreeStates = **timing flake 1/9 full-suite runs, passes isolated, backup 0/5** (watch item) |
| go test ./cmd/server | 2 documented residuals (Synthesized/Failure StampsSourceTaskID); TestExpireStaleQueuedTasks = **timing flake 1/5, passes isolated, backup clean** (watch item) |

Dev test DB (:5432) migrated 341→343 BEFORE handler tests (memory: go-test-db-refresh method; 342/343 applied cleanly incl. concurrent-index pre-hook — a dress rehearsal for Phase 6 production migrate).

Residual set vs main baseline: **set-identical + 2 timing flakes** (both recorded with evidence; both pass deterministically in isolation on the merged tree).

## Phase 6 — Irreversible gates (DONE 2026-08-19)

GATE check passed before entry: phase==gates ✓, user_confirmed ✓, audit_strategy_written ✓, 15 negative_claims all verified ✓.

1. Backup: ~/multica-backups/v0.4.29-pre-migrate-20260819T142949Z.dump (62M, 762 TOC entries, pg17 -F c).
2. Migrate: applied 342_runtime_profile_add_mcode + 343_comment_delegated_failure_pending_index (pre-hook ran for 343's concurrent index). Latest = 343. Archived rows 267 ✓, catalog 35 ✓.
3. Version re-stamp: .env NEXT_PUBLIC_APP_VERSION=v0.4.29; install.sh rebuilt both (backend strings main.version=v0.4.29; frontend layout chunk contains v0.4.29).
4. Restart: install.sh bootout/bootstrap both plists.
5. Health: /healthz 200 (first attempt), :3001 200, /api/config server_version=v0.4.29.
6. Daemon: stays 0.4.28 (tap not yet shipping 0.4.29; CLI=daemon self-consistent; agents 5 + workspaces 5 intact). Align when tap ships.

## Phase 7 — Record (DONE 2026-08-19)

Actuals vs plan (calibration for future audits):
- merge-tree predicted 2 text conflicts → **actual 2** (10th consecutive hit). Both frontend, both epicenter.
- Lane A "server compose survives with zero code change" → **confirmed exactly**. The transitive IsCategory=IsBuiltIn mechanism worked as blob-read predicted; no server re-application needed.
- Lane B predicted config/status.ts would typecheck-fail with "archived ∉ IssueStatusCategory" → **the predicted failure mode did NOT materialize**: the auto-merge combined upstream's union rename with fork's archived member, making the union 8-valued BEFORE typecheck ran. The REAL failure was narrower (upstream narrowed the type import; fork body lines still named IssueStatus → TS2304 ×5, fixed by restoring the import). Lesson: when both sides touch the same TYPE declaration region, enumerate the import/narrowing axis too, not just the member axis.
- "exactly the 7" test predicted to need 7→8 → materialized only as a STALE NAME (assertions passed as-is); re-pinned with an explicit archived assertion anyway.
- Lane C comment.go/task.go/arity clean predictions → **confirmed exactly** (merge-file exit 0; 6 signatures zero-drift).
- NOT predicted: 2 timing flakes surfaced in go test full-suite runs (TestChannelMediaReconciler_SettlesThreeStates 1/9, TestExpireStaleQueuedTasks 1/5; both pass isolated, backup clean). Recorded as watch items — re-check if they recur.
- pnpm test: ZERO fork-code × upstream-test collisions (first fully-clean round since v0.4.23).
- Deploy: backup → migrate 342/343 → install.sh (commit-first correct; server_version=v0.4.29 first attempt health) → daemon left at 0.4.28 (tap not shipped; align when it does).

Durable refinements recorded: archived frontend carry surface is now mostly SELF-DERIVING (union member + STATUS_ORDER entry → BUILT_IN → all 3 category functions); the manual axes are (1) fetchFirstPages include_archived (server composes the guard as an AND), (2) the "exactly the N" test pin. Memory project_archived_status_compose_keep updated.
