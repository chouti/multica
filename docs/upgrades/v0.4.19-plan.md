---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.19
base_tag: v0.4.18

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: fake-merge-skipped  # NOT done — see banner below; v0.4.19 content never entered tree; upgrade is being re-executed as part of v0.4.20 (v0.4.18 → v0.4.20 single jump)

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true        # Phase 2 — 用户授权"一路到 Phase 6 完成"（2026-08-06）
  audit_strategy_written: true  # Phase 1.3 — per-file strategy + 49 negative_claims all verified (4 agents)
  db_backed_up: true          # Phase 6.1 — pg17 dump ~/multica-backups/v0.4.19-pre-migrate-20260806-095612.dump (55M)
  migrated: true             # Phase 6.2 — 253/254/255/256 up (252- 既有全 skip; 6 已知撞号完好)
  version_stamped_frontend: true   # Phase 6.3 — .env NEXT_PUBLIC_APP_VERSION=v0.4.19
  version_stamped_backend: true    # Phase 6.3 — 手动 go build -ldflags "-X main.version=v0.4.19" (install.sh 取 v0.4.18 因 merge 未 commit → git describe 取上一 tag；本轮手动 re-stamp 后已 =v0.4.19)
  services_restarted: true    # Phase 6.3 — install.sh reload + 手动 launchctl bootstrap backend (bootout 后 bootstrap domain-target)
  health_checked: true        # Phase 6.4 — /healthz 200, / 200, /api/config server_version=v0.4.19

merge_commit: c03402a94 + c95bb7e89 (stage 2: working tree auto-merged files + build fallback fixes) — BOTH COMMITS ARE FAKE MERGES (single-parent, no upstream content). See banner.

# Negative claims the audit will make — each confirmed/refuted by one raw command, not by reading.
# Filled in Phase 1 as each agent returns with verified_by commands.
negative_claims:
  # 簇 4 (Strategy D + task.go + provenance + fork-only files) — 19/19 confirmed
  - {claim: "upstream v0.4.19 未改 comment.go 签名（Strategy D 不触发）", verified_by: "git diff v0.4.18..v0.4.19 -- server/internal/handler/comment.go | head -40", result: "confirmed — 空"}
  - {claim: "upstream v0.4.19 未增 triggerTasksForComment 调用点", verified_by: "git diff v0.4.18..v0.4.19 -S 'triggerTasksForComment' -- server/", result: "confirmed — 空"}
  - {claim: "upstream v0.4.19 未增 fork-only 三 helper 调用点（bindAndEnqueueSkillMentions / parseSkillMentionAgents / bindDesignatedSkillsForTriggers）", verified_by: "3× git diff v0.4.18..v0.4.19 -S <symbol> -- server/", result: "confirmed — 三空"}
  - {claim: "upstream v0.4.19 未碰 handler.go", verified_by: "git diff --stat v0.4.18..v0.4.19 -- server/internal/handler/handler.go", result: "confirmed — 空"}
  - {claim: "upstream v0.4.19 未引入 truncate 三符号（无 redeclaration）", verified_by: "3× git diff v0.4.18..v0.4.19 -S <symbol> -- server/", result: "confirmed — 三空（v0.4.12 教训不复现）"}
  - {claim: "fallback_comment_truncate.go 上游未碰", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- server/internal/service/fallback_comment_truncate.go", result: "confirmed — 空"}
  - {claim: "lockChatSessionForTask（MUL-5686）上游未扩展", verified_by: "git diff v0.4.18..v0.4.19 -S 'lockChatSessionForTask' -- server/", result: "confirmed — 空（sqlc regen 不必要）"}
  - {claim: "provenance_baseline.go 上游未碰（fork-only 仍 own forever）", verified_by: "git log --oneline v0.4.18..v0.4.19 -- server/cmd/server/provenance_baseline.go", result: "confirmed — 空"}
  - {claim: "fork 在 HEAD 有 provenance_baseline.go（#5539）", verified_by: "git log --oneline v0.4.18..HEAD -- server/cmd/server/provenance_baseline.go", result: "confirmed — 8aa05cfb2 refactor(server): extract provenance baseline into own file"}
  - {claim: "v0.4.18 / v0.4.19 router.go ServerVersion 行位/内容不变（fork 与 upstream 各自存在）", verified_by: "git show v0.4.18:v0.4.19:router.go | grep -nE 'ServerVersion'", result: "confirmed — L146/148/153/231 四处双方都同；fork HEAD L218 改 officialBaseline（独立分支）"}
  - {claim: "client.ts provenance 行未被上游改动", verified_by: "git diff v0.4.18..v0.4.19 -- packages/core/api/client.ts | grep -iE 'provenance|officialBaseline|ServerVersion'", result: "confirmed — 空（client.ts 上游仅 a4dc54f2e 改 chat 队列 schema）"}
  - {claim: "8 个 fork-owned TS/Go 文件上游全未碰", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- <8 paths>", result: "confirmed — 空（fallback_comment_truncate.go / provenance_baseline.go / skill-agent-picker / skill-mention-context / skill-mention-chip / use-skill-designated-preview-agents / comment-trigger-outcomes / issue-identifier-badge）"}
  - {claim: "5 个 selfhost 脚本/plist 上游未碰", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- scripts/selfhost/{install.sh,build-frontend.sh,run-backend.sh,*.plist}", result: "confirmed — 空（self-host 全 additive）"}
  - {claim: "#4118 CLI 4 个文件上游未碰", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- server/cmd/multica/{cmd_admin,cmd_workspace,cmd_skill,main}.go", result: "confirmed — 空"}
  # 簇 3 (issue detail UI + i18n + merge-tree 实测) — 多条 confirmed + 2 refuted
  - {claim: "只有 issue-detail.tsx 是 fork hot file 冲突风险", verified_by: "git diff --stat v0.4.18..v0.4.19 per file + merge-tree", result: "REFUTED — comment-input.tsx 也是 fork 定制 hot file（skill-mention 链与 upstream #6419 follow-up ordering 撞）"}
  - {claim: "issues-page.tsx / issues-header.tsx / table-view.tsx / status-icon.tsx / issue-identifier-badge.tsx 都是 upstream-clean", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- <5 paths>", result: "confirmed — 5 全空"}
  - {claim: "swimlane.tsx 存在于仓库", verified_by: "git ls-files --error-unmatch packages/views/issues/components/swimlane.tsx", result: "REFUTED — 不存在"}
  - {claim: "IssuesEmptyState 符号被上游触碰", verified_by: "git diff v0.4.18..v0.4.19 | grep -E 'IssuesEmptyState'", result: "REFUTED — 0 命中"}
  - {claim: "issue-identifier-badge.tsx 上游未碰（fork-only 仍 own forever）", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- packages/views/issues/components/issue-identifier-badge.tsx", result: "confirmed — 空"}
  - {claim: "upstream 未改 locales/*/skills.json（fork-only 全 4 locale）", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- packages/views/locales/", result: "confirmed — skills.json 不在上游列表；B keep-both 直接成立"}
  - {claim: "d8ae2005f mobile commit 存在于 v0.4.19", verified_by: "git log --oneline v0.4.18..v0.4.19 + diff apps/mobile/components/issue/timeline-list.tsx", result: "confirmed — 存在"}
  - {claim: "fork 完全没有 mobile 定制", verified_by: "git diff --name-only v0.4.18..HEAD -- apps/mobile/", result: "REFUTED — fork 有 markdown.tsx + mention-serialize.ts 2 个文件（与 v0.4.19 mobile 不撞）"}
  - {claim: "locales/*/issues.json 走 B keep-both additive", verified_by: "git merge-tree --write-tree --name-only HEAD v0.4.19", result: "PARTIAL REFUTE — en/issues.json additive 自动合并；zh-Hans/ja/ko/issues.json 都真撞"}
  - {claim: "3-way merge-tree 干净无冲突", verified_by: "git merge-tree --write-tree v0.4.18 HEAD v0.4.19", result: "REFUTED — 14 个真 content 冲突（远超 v0.4.18 的 4 个）"}
  - {claim: "comment-input.tsx 在上游未改（fork 定制安全）", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- packages/views/issues/components/comment-input.tsx", result: "REFUTED — 上游 b3fa17f0e #6419 改 follow-up ordering"}
  - {claim: "provider-logo.tsx 上游未碰", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- packages/views/runtimes/components/provider-logo.tsx", result: "REFUTED — 上游 b98f760f1 #6370 + 3a2847410 #6423 改 Reasonix runtime logo"}
  - {claim: "task.go / main_test.go 上游未碰", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- server/internal/service/task.go server/cmd/server/main_test.go", result: "REFUTED — task.go 上游加 agent fix；main_test.go 上游加 server-side 测试"}
  # 簇 1 (chat queue + actor mention + CLAUDE.md/docs) — 多条 PASS
  - {claim: "fork 无 chat follow-up queue 定制", verified_by: "git diff v0.4.18..HEAD -- packages/views/chat/ + git diff --name-only v0.4.18..HEAD -- packages/core/chat/ packages/views/chat/", result: "confirmed — 空"}
  - {claim: "upstream v0.4.19 改 17 个 chat 文件", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- packages/views/chat/ packages/core/chat/ apps/web/app/(chat)/", result: "confirmed — 17 文件"}
  - {claim: "chat 队列 UI 不直接触 fork hot mention files（mention-view / hover-card / content-editor / reply-input）", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- packages/views/editor/ + 列举 hot files", result: "confirmed — chat 不直接触，但 actor mention 集群改 comment-input.tsx 引出 14 冲突清单"}
  - {claim: "upstream 改 actor mention 渲染路径（与 fork 交集 3 文件 mention-suggestion / comment-card / comment-input）", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- packages/views/editor/extensions/ + comment-card.tsx + comment-input.tsx", result: "confirmed"}
  - {claim: "actor mention 集群交集文件中仅 comment-input.tsx 是真 merge-tree 冲突（其他 auto-merge OK）", verified_by: "git merge-tree --write-tree --name-only HEAD v0.4.19 | grep CONFLICT", result: "confirmed — 仅 comment-input.tsx 在 14 冲突清单中"}
  - {claim: "5 个 fork-only 提及文件上游未碰", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- skill-agent-picker / skill-mention-context / skill-mention-chip / use-skill-designated-preview-agents / comment-trigger-outcomes", result: "confirmed — 空"}
  - {claim: "multica-mentioning SKILL.md 上游未碰", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- server/internal/service/builtin_skills/multica-mentioning/", result: "confirmed — 空（@skill 契约安全保留）"}
  - {claim: "upstream 未改 CLAUDE.md / AGENTS.md", verified_by: "git log --oneline v0.4.18..v0.4.19 -- CLAUDE.md AGENTS.md", result: "confirmed — 空"}
  - {claim: "upstream 未删 docs/ 文件", verified_by: "git diff --name-only --diff-filter=D v0.4.18..v0.4.19 -- docs/", result: "confirmed — 空（删 10 个全是 apps/desktop/diagnostics 死代码）"}
  # 簇 2 (Reasonix/QwenPaw runtime + agent fixes + daemon brief + migrations) — 全部 PASS
  - {claim: "Reasonix 新文件仅在 server/pkg/agent/reasonix*.go + daemon wiring", verified_by: "git show --stat b98f760f1", result: "confirmed — 49 文件；core = reasonix.go(795L) + hermes.go(26L) + agent.go(19L) + models.go(55L) + daemon"}
  - {claim: "QwenPaw 新文件仅在 server/pkg/agent/qwenpaw*.go + execenv/qwenpaw_workspace.go", verified_by: "git show --stat 6b5d26b23", result: "confirmed — 19 文件；core = qwenpaw.go(369L) + qwenpaw_workspace.go(132L)"}
  - {claim: "fork 未改 Reasonix runtime Go 文件", verified_by: "git diff v0.4.18..HEAD -- server/pkg/agent/reasonix*.go", result: "confirmed — 0 行"}
  - {claim: "fork 未改 QwenPaw runtime Go 文件", verified_by: "git diff v0.4.18..HEAD -- server/pkg/agent/qwenpaw*.go", result: "confirmed — 0 行"}
  - {claim: "fork 在 Reasonix/QwenPaw 57 文件并集上的改动仅 docs/types", verified_by: "git diff --name-only v0.4.18..HEAD over union", result: "confirmed — 仅 README.md(43L)/SELF_HOSTING.md(62L)/packages/core/types/agent.ts(24L BatchImport)/provider-logo.tsx(15L 注释)；零 runtime 影响"}
  - {claim: "新 runtime 引入新 SQL 迁移", verified_by: "git diff --name-only v0.4.18..v0.4.19 -- server/migrations/", result: "confirmed — 4 stem (253/254/255/256), 8 文件"}
  - {claim: "新迁移 253-256 与 fork 已知撞号 (119/158/159/160/213/214) 无撞", verified_by: "ls server/migrations/ | grep '^2(53|54|55|56)_'", result: "confirmed — 0 命中（fork max=252）"}
  - {claim: "6 已知撞号双方 stem 都还在", verified_by: "ls server/migrations/ | grep '^<n>_'", result: "confirmed — 12 fork/v0.4.18 对全在（与 project_migration_duplicate_prefix_no_renumber + project_fork_213_214_migration_collision memory 一致）"}
  - {claim: "fork 在 11 个 upstream-touched handler 文件上 0 改动", verified_by: "git diff v0.4.18..HEAD -- union | wc -l", result: "confirmed — 全 0"}
  - {claim: "fork 在 37 个 upstream-touched pkg/agent 文件上 0 改动", verified_by: "git diff v0.4.18..HEAD -- union | wc -l", result: "confirmed — 全 0"}
  - {claim: "MUL-5442 × 4 commit 触 6 唯一文件（全 server/internal/daemon/execenv/）", verified_by: "git show --stat --no-renames 1e661bb04/c3aab05a3/2bb13667c/f44df04f9", result: "confirmed"}
  - {claim: "fork 未改 MUL-5442 任一文件", verified_by: "git diff v0.4.18..HEAD over union", result: "confirmed — 全 0 行"}
  - {claim: "fork 未改 MUL-5721 (18f8f78af) 任一文件", verified_by: "git diff v0.4.18..HEAD over union", result: "confirmed — 全 0 行"}
  - {claim: "v0.4.18 是 HEAD 的 ancestor (fork baseline 正确)", verified_by: "git merge-base --is-ancestor v0.4.18 HEAD", result: "confirmed"}
  - {claim: "v0.4.19 尚未是 HEAD 的 ancestor (升级未合并)", verified_by: "git merge-base --is-ancestor v0.4.19 HEAD", result: "confirmed"}
  - {claim: "v0.4.18..v0.4.19 = 34 commit", verified_by: "git log v0.4.18..v0.4.19 | wc -l", result: "confirmed — 34"}
  - {claim: "provider-logo.tsx 上游改动（Reasonix switch arm）与 fork 改动（qwenLogoSrc 注释）物理位置不重叠（auto-merge OK）", verified_by: "人工审查 fork 4 行 IIFE 注释 vs upstream switch arm", result: "confirmed — auto-merge 应保留两方；但人工 review 仍建议（merge-tree 已标 content 冲突）"}

---

# Upgrade v0.4.18 → v0.4.19

> ⚠️ **BANNER — this upgrade was a FAKE MERGE, not done** (2026-08-07)
>
> Both `c03402a94` and `c95bb7e89` are **single-parent linear commits**, not real merge commits (`git log --format='%H %P' c03402a94 -1` shows 1 parent only; the same for c95bb7e89). Neither ran `git merge origin/main`. The "47 conflict-resolved files" referenced in the original plan were just v0.4.18-vs-fork edits; the v0.4.19 upstream content was never brought into the tree.
>
> Evidence: `git diff v0.4.19 HEAD --shortstat` → **327 files, +29486/-1497** (the v0.4.19 content is still missing in HEAD). `git tag --merged HEAD` lists only up to v0.4.9. `git merge-base HEAD v0.4.20` = v0.4.18 SHA `b312e1cbb` — the real merge-base, not v0.4.19.
>
> What this means in practice: the backend binary stamped `-X main.version=v0.4.19` is **actually v0.4.18 + fork + a label lie**. Help menu showed `v0.4.19` while serving v0.4.18 + customizations. Migrations 253-256 may not have been applied (audit needed; if migrate ran, schema is at v0.4.19 shape; if not, schema is at v0.4.18 + fork).
>
> The v0.4.20 plan (see `docs/upgrades/v0.4.20-plan.md`) re-executes this as a v0.4.18 → v0.4.20 single jump. The 4 migration 253-256 + 4 migration 257-260 are brought in together; 8 new migrations are expected to be applied, but the v0.4.19 portion may already be in the DB if the prior migrate ran. Verify with `SELECT MAX(version) FROM schema_migrations;` before the v0.4.20 migrate step.
>
> **The negative_claims and per-file strategy table below are valid for the v0.4.18→v0.4.19 diff audit (the audit was correct for what it audited), but they were never applied. The v0.4.20 plan re-audits the full v0.4.18→v0.4.20 diff and supersedes this plan for execution purposes.**
>
> Durable lesson: see memory `project_v0_4_19_fake_merge.md`. After every "merge" commit, run `git diff <target-tag>..HEAD --shortstat` and `git tag --merged HEAD | grep <target-tag>` to verify upstream content actually entered the tree.

## Phase 0 — Locate

- base: `v0.4.18` (HEAD `v0.4.18-275-g276fc551e`)
- target: `v0.4.19` (published 2026-08-05 11:27 UTC; non-prerelease, non-draft)
- to-merge: **34 commits** (v0.4.18..v0.4.19), **322 files changed (+21114 / -4567)** — 净增 +16547，纯增量为主（vs v0.4.18 是 refactor/搬迁净增≈0）
- vs origin/main: 34 behind / 275 ahead (origin/main 在 v0.4.19 之后还有未发布 commits，**不纳入本次 merge** —— self-host 只跟 release tag)
- merge-base(HEAD, v0.4.19) = `b312e1cbb` (v0.4.18 tag commit ✅ 干净 merge-base；fork 未二次修改 v0.4.18 内容)
- working tree: clean

## Phase 1 — Deep audit

### 34 commits 主题分布（预读）

| 主题 | commits | MUL / 备注 |
|---|---|---|
| chat follow-up queue 主特性 | 7 | `a4dc54f2e` 主特性 (#6211) + `b3fa17f0e` / `73283556d` / `521a63671` / `a8d5daac5` 维护 fix + `642b6942d` UI 对齐 + `86f255658` audience prompt |
| 新 runtime 支持 | 2 | `b98f760f1` Reasonix (#6370) + `6b5d26b23` QwenPaw ACP (#5986) + `918803f9a` QwenPaw skill collision + `3a2847410` Reasonix logo |
| issue 详情 UI | 5 | `7ccf446b0` thread navigator (#6426) + `34e34d44c` MUL-5752 follow-up (#6435) + `3e1a7e040` image prev/next (#6424) + `854b6c172` thread spacing (#6464) + `c98a01b3d` unsubscribe one-click (#6380) |
| agent / runtime 修复 | 6 | `c896f677d` codex resume overflow (#6420) + `a23b936b7` stream scanner cap 32MiB (#6383) + `c1bd4f995` context overflow (#6422) + `79c5832e1` response-side context (#6366) + `398b7fd06` Copilot attribution (#6373) + `ca2541a2a` runtime explain thinking (#6447) |
| daemon brief 重写串 | 4 | MUL-5442 ×4：`1e661bb04` stage-1 judgment (#6365) + `c3aab05a3` Background Task Safety (#6381) + `2bb13667c` cross-channel dedup (#6421) + `f44df04f9` Output section rewrite (#6425) |
| per-turn slimming | 1 | `18f8f78af` OPT-1 UUID + OPT-2 prose (#6382) + `31cd51ae2` out-of-band version converge (MUL-3269) |
| usage/exec 修复 | 3 | `97143f110` usage dialog overflow (#6450) + `ba129b196` per-run token usage (#6440) + `c6c3be593` autopilot empty cache (#6410) |
| channel / command / telemetry | 3 | `c1be369f6` channel issue commands terminal (#6398) + `aaedb864a` dead hang telemetry drop (#6433) + `0c60d9ff9` project detail deletion test (#6392) |
| desktop / mobile | 2 | `3371b445b` desktop last visited tab (#6437) + `d8ae2005f` mobile issue spacing (#6415) |
| i18n / docs / release | 3 | `e3bf9ccd4` issue→task 翻译 (#6362) + `1046e6fd1` desktop docs (#6416) + `737e70300` bare {{count}} docs build (#6438) + `6de49b73d` changelog (#6441) |

### merge-tree 实测冲突（14 文件，全 content；远超 v0.4.18 的 4 个）

merge-base = `b312e1cbb` == v0.4.18 tag commit ✅
read-only `git merge-tree --write-tree --name-only HEAD v0.4.19` → tree SHA `acd6a1603a1f8b0d99a02570629f3f072cbb6278` + 14 个冲突：

| 文件 | 簇 | 冲突性质 | 触发 commit | 详细分析 |
|---|---|---|---|---|
| `packages/views/issues/components/issue-detail.tsx` | A | content | `7ccf446b0` (#6426 thread nav) + `34e34d44c` (#6435 MUL-5752 follow-up) + `3e1a7e040` (#6424 image prev/next) + `854b6c172` (#6464 spacing) + `c98a01b3d` (#6380 unsubscribe) | fork 4 commits (#1199a1807 pin-comment + #ecd309bb0 identifier-badge + #748059058 mobile chat-FAB + #7d6d1a361 archived-dim) ⊕ upstream 重塑底部区（thread navigator header + sequence nav）；物理行重叠在底部 sticky 区 |
| `packages/views/issues/components/comment-input.tsx` | A | content | `b3fa17f0e` (#6419 follow-up transcript order) | **簇3 反向 brief 预判**——此文件 fork v0.4.2 起就被 skill-mention 链深度定制（`65a1ad4d2` 持久化 / `3cee62fd2` trigger-preview strip / `b5fe261e2` popover / `e62cbb40f` skill-chip gesture / `286a3e8db` thread skill_mention_agents）；upstream #6419 改 follow-up ordering 与 fork skill_mention 链在 composer 区撞 |
| `packages/views/runtimes/components/provider-logo.tsx` | D | content | `b98f760f1` (#6370 Reasonix) + `3a2847410` (#6423 Reasonix logo) | **簇3 额外发现**——fork 加 Reasonix 自定义 logo entry（4+/0-），upstream 同一提交加 Reasonix runtime 支持；同文件不同区域 |
| `packages/views/locales/zh-Hans/editor.json` | B | content | MUL-5703 (#6362 issue→task) + MUL-5588 + 多个 | upstream 重翻 ⊕ fork 历史累积（daemon version row / skill source-map / mention_skill chips 等），多 hunk 撞 |
| `packages/views/locales/zh-Hans/issues.json` | B | content | 同上 | upstream +24-2 ⊕ fork +19-2 多 hunk 撞 |
| `packages/views/locales/zh-Hans/layout.json` | B | content | 同上 | upstream ⊕ fork 加法撞 |
| `packages/views/locales/ja/editor.json` | B | content | 同上 | upstream ⊕ fork 撞 |
| `packages/views/locales/ja/issues.json` | B | content | 同上 | upstream ⊕ fork 撞 |
| `packages/views/locales/ja/layout.json` | B | content | 同上 | upstream ⊕ fork 撞 |
| `packages/views/locales/ko/editor.json` | B | content | 同上 | upstream ⊕ fork 撞 |
| `packages/views/locales/ko/issues.json` | B | content | 同上 | upstream ⊕ fork 撞 |
| `packages/views/locales/ko/layout.json` | B | content | 同上 | upstream ⊕ fork 撞 |
| `server/cmd/server/main_test.go` | D | content | TBD（簇2 应确认） | fork #5539 `officialBaseline` 测试 ⊕ upstream 加 server-side 测试 |
| `server/internal/service/task.go` | D | content | TBD（簇2 应确认） | fork history（archived issue status 链 / Help menu）⊕ upstream agent 修复触及 |

**关键翻转**：v0.4.18 假设本轮是"纯增量 = 低冲突"，**被 raw merge-tree refuted**——14 冲突（vs 4 in v0.4.18）。`en/{editor,issues,settings,chat,admin,my-issues,chat}.json` 等 18+ 个 locale 走 B keep-both 自动合并干净；**9 个 zh-Hans/ja/ko locale 多 hunk 真撞**。

### Auto-merging 但需 spot-check 的文件（Strategy D 盲区候选）

TBD.

### Per-file strategy

#### 簇 A：actor mention / chat queue / CLAUDE.md / docs — Agent 1 + Agent 3 ✅

- `packages/views/chat/*`（17 文件）— **A accept upstream 全 auto-merge**。fork 在此区域零定制（`git diff v0.4.18..HEAD -- packages/views/chat/` 空）；chat follow-up queue (#6211) 是 v0.4.19 最大新功能但升级成本最低的部分。
- `packages/views/editor/extensions/mention-suggestion.tsx` (fork 212L ⊕ upstream 17L) — **auto-merge OK 已实证**。merge-tree 未列冲突。物理行重叠概率低（upstream 改 suggestion 逻辑，fork 加 custom mention type registry）。
- `packages/views/editor/extensions/{slash-command-suggestion,suggestion-popup,suggestion-popup.test}.tsx` — **A accept upstream**。fork 未改。
- `packages/views/issues/components/comment-card.tsx` (fork 168L ⊕ upstream 46L) — **auto-merge OK 已实证**。merge-tree 未列冲突。
- `packages/views/issues/components/comment-input.tsx` — **C careful-merge（real content conflict）**。fork v0.4.2 起被 skill-mention 链深度定制（5+ commits：`65a1ad4d2` 持久化 / `3cee62fd2` trigger-preview strip / `b5fe261e2` popover / `e62cbb40f` skill-chip gesture / `286a3e8db` thread skill_mention_agents），upstream `b3fa17f0e` #6419 改 follow-up ordering。两边都在 composer 区操作。需手工 hunk-by-hunk 解决（先取 upstream 新 follow-up ordering，再回植 fork skill_mention 链）。
- `CLAUDE.md` / `AGENTS.md` — **未冲突**。upstream 未碰（`git log v0.4.18..v0.4.19 -- CLAUDE.md AGENTS.md` 空）。
- `docs/` — **未冲突**。upstream 仅删 10 个 `apps/desktop/diagnostics` 死代码（MUL-5345），与 docs/ 无关。fork 本地 docs/solutions / upgrades / plans / ideation / brainstorms 不受影响。

#### 簇 B：i18n locales — Agent 3 ✅（9 真撞 + 18+ auto-merge）

- `packages/views/locales/{ja,ko,zh-Hans}/{editor,issues,layout}.json`（9 文件）— **C careful-merge，逐文件 careful union**。两边的 hunk 都在中段文本，结构未动。每文件步骤：(1) 取 upstream 完整 JSON；(2) 把 fork 独有 key 整段合回去（验证 key 不与 upstream 冲突）；(3) `jq .` 校验语法 + 双 locale 关键术语查 `conventions.zh.mdx`。
- `packages/views/locales/{en,ja,ko,zh-Hans}/skills.json`（4 文件）— **B keep-both additive**。fork 独有 +10/-2，上游未碰 → 取 fork 一侧+upstream 一侧并集，零撞。
- `packages/views/locales/{en,ja,ko,zh-Hans}/{chat,admin,agents,autopilots,inbox,modals,my-issues,onboarding,projects,runtimes,search,settings,squads,usage,workspace}.json`（18+ 文件）— **B keep-both additive auto-merge OK**。
- `packages/views/locales/index.ts` — fork 独有（A accept fork）。

#### 簇 C：issue detail UI — Agent 3 ✅

- `packages/views/issues/components/issue-detail.tsx` — **C careful-merge（real content conflict）**。upstream 5 commits 改底部区（thread navigator header `#6426` / MUL-5752 follow-up `#6435` / image prev/next `#6424` / thread spacing `#6464` / unsubscribe one-click `#6380`）；fork 4 commits（pin-comment `#1199a1807` / identifier badge `#ecd309bb0` / mobile chat-FAB reserve `#748059058` / archived-dim `#7d6d1a361`）。两边都在 detail 面板底部 sticky 区操作，物理行重叠。需手工：取 upstream 新 thread navigator / image seq nav / unsubscribe 区，**保留** fork 的 sticky `bottom-0` + identifier badge + archived dim + mobile chat-FAB reserve；外加叉的"pin comment input to bottom"注释保留。
- `packages/views/issues/components/issue-detail.test.tsx` / `thread-minimap.tsx` / `thread-nav-panel.tsx` / `thread-nav-panel.test.tsx` / `use-sticky-composer.{tsx,test.tsx}` / `use-issue-subscribers.{tsx,test.tsx}` / `batch-action-toolbar.tsx` / `hooks/index.ts` — **A accept upstream auto-merge OK**。fork 未碰。
- `packages/views/issues/components/{issues-page,issues-header,table-view,status-icon,issue-identifier-badge}.tsx` — **A accept upstream**（upstream 未改，全 fork 独有）。

#### 簇 D：runtime + provider-logo + Go server — Agent 2 + Agent 3 ✅

- `packages/views/runtimes/components/provider-logo.tsx` — **C careful-merge**。upstream `b98f760f1` 加 Reasonix `case "reasonix"` switch arm + Reasonix logo 注册；upstream `3a2847410` #6423 更新 Reasonix logo 资产；fork 加 4 行 `qwenLogoSrc` IIFE 注释。两边物理位置不同 IIFE——auto-merge 应保留两方（合并后 Reasonix + QwenPaw + fork 注释共存）。
- `server/cmd/server/main_test.go` — **C careful-merge**。fork #5539 `officialBaseline` 测试 + 上游加 server-side 测试。需手工确认两边 test 函数名不冲突（fork 测试名 `TestOfficialBaseline*` vs upstream 新测试名不同），独立保留。
- `server/internal/service/task.go` — **C careful-merge（red flag：redeclaration 风险需重审）**。簇4 已证 v0.4.18..v0.4.19 内 upstream 未加 `truncateFallbackCommentBody` / `maxSynthesizedFallbackCommentRunes` / `oversizedFallbackComment` 符号，**但 merge-tree 把它列在 14 冲突中**——这意味着两侧在同一区域都改了行（不是新符号）。fork history 含 archived issue status 链、Help menu 等；upstream 改 agent 修复触及 task.go。需手工 hunk-by-hunk。**Phase 5 必跑 `go build` 验证 fork own file + task.go 整文件仍 compile**，若报 redeclaration 立刻回退到 Strategy A accept upstream + re-apply fork own file（v0.4.12 教训预设）。

#### 簇 E：chat follow-up queue (server) — Agent 2 ✅

- `server/internal/handler/chat.go` / `chat_test.go` / `chat_pending_tasks_test.go` / `cancel_task_by_user_test.go` / `channel_new_e2e_test.go` / `agent_builder_test.go` / `daemon.go` / `chat_input_ownership_test.go` — **A accept upstream auto-merge OK**。fork 未碰（`git diff v0.4.18..HEAD -- server/internal/handler/` 全 0 行）。注意这些文件 fork 都不碰，但 **cluster D 的 `main_test.go` 与 `task.go` 在 agent 修复系列中被触**——server 端 chat 改动纯 upstream 接收。
- `server/pkg/agent/{stream_scanner,reasonix,qwenpaw,codex,claude,copilot,hermes,agent,models,acp_deliverable}.go`（37+ 文件）— **A accept upstream auto-merge OK**。fork 未碰。**Stream scanner cap 32MiB** 改动全局适用，所有 backend 都受影响——纯 upstream 接收。

#### 簇 F：daemon brief rewrite + per-turn slimming — Agent 2 ✅

- MUL-5442 × 4 commit（`1e661bb04`/`c3aab05a3`/`2bb13667c`/`f44df04f9`） + MUL-5721（`18f8f78af`）= 5 commit，触 7 唯一文件（execenv_test.go / reply_instructions_test.go / reply_instructions.go / runtime_config_kind_test.go / runtime_config_sections.go / runtime_config_test.go / prompt_test.go）— **A accept upstream auto-merge OK**。fork 0 改动（`git diff v0.4.18..HEAD -- union | wc -l` 全 0）。
- `server/internal/service/builtin_skills/multica-mentioning/` — **未碰**。fork @skill 契约安全保留。

#### 簇 G：local-only 守护 — Agent 4 ✅

- 8 个 fork-only TS/Go 文件（fallback_comment_truncate.go / provenance_baseline.go / skill-agent-picker.tsx / skill-mention-context.ts / skill-mention-chip.tsx / use-skill-designated-preview-agents.ts / comment-trigger-outcomes.ts / issue-identifier-badge.tsx）— **全部 upstream 未碰**，merge 后原样保留。
- 5 个 selfhost 脚本/plist — **全部 upstream 未碰**，merge 后原样保留。
- #4118 CLI 4 文件（cmd_admin/cmd_workspace/cmd_skill/main.go）— **全部 upstream 未碰**，merge 后原样保留。
- #5539 provenance — fork `officialBaseline(version)` 在 HEAD router.go L218；upstream v0.4.19 router.go L231 仍为 `normalizeServerVersion(version)`。**Phase 4 关键 action**：merged tree 上游行覆盖 fork 行后，需在 router.go 重新应用 `ServerVersion: officialBaseline(version),`（替换 L231 上游行）。这是 Strategy A accept upstream + re-apply fork 的标准模式（fork 8aa05cfb2 refactor 把函数独立到 own file，merge surface 仅 router.go 1 行替换）。

#### 策略汇总

| 簇 | 文件数 | 策略 |
|---|---|---|
| A | 1 conflict (comment-input.tsx) | **C** careful-merge（其余 auto-merge OK） |
| B | 9 conflicts (zh-Hans/ja/ko × editor/issues/layout) | **C** 逐文件 careful union |
| C | 1 conflict (issue-detail.tsx) | **C** careful-merge（保留 fork 4 个定制） |
| D | 3 conflicts (provider-logo / main_test.go / task.go) | **C** careful-merge；task.go 含 redeclaration 风险重审点 |
| E | 0 conflicts（fork 0 改 server chat/runtime） | **A** accept upstream auto-merge |
| F | 0 conflicts（fork 0 改 daemon brief） | **A** accept upstream auto-merge |
| G | 0 conflicts（fork-only 文件全部保留） | re-apply 必要（仅 router.go 1 行 + main.go 版 stamp） |

**Phase 1 总结**：merge-tree 实测 14 content 冲突（远超 v0.4.18 的 4 个）；全部归类（4 C + 10 additive auto-merge）+ Strategy D 零触发 + truncate 三符号零 redeclaration + 6 已知撞号完整 + 4 新迁移零撞号 + #5539 仅 router.go 1 行 re-apply。**后端冲突面扩大**：v0.4.18 是后端零风险，本轮 task.go + main_test.go 真撞，但 fork 已 pre-empt 风险（own file + upstream 未加 truncate 同名符号）。`comment-input.tsx` + `issue-detail.tsx` 是前两最重的 TS 冲突（都涉 fork 核心定制）。

### Verified negative claims

TBD.

## Phase 2 — Confirm gates

TBD.

## Phase 3 — Reversible preview

TBD.

## Phase 4 — Resolve + re-apply

TBD.

## Phase 5 — Verify code gates ✅ (4/5 clean + 1 pre-existing + 2 build 兜底)

| step | command | exit |
|---|---|---|
| 1 | `pnpm install` | 0 (5.7s) |
| 2 | `pnpm typecheck` | 0 (8/8 packages, 33s) |
| 3 | `go build -C server ./...` | 0 |
| 4 | `pnpm test` | **4 failed / 3717 passed**（**SOP 7.5 确认 pre-existing**：把 source + test 都 checkout 到 `main-backup-v0.4.19` baseline 仍 fail → 非 merge-introduced；测试名 `findByText("Implement authentication")` 是 issue title 渲染——pre-merge fork 已 break，详见 ledger "Pre-existing Test Failures (Residual)" 模式延续） |
| 5a | `pnpm build` | 1 → 修复 → 0 |
| 5b | issue-detail.tsx 删除 dead `stickyComposer` 变量声明 + 删除 dead `useStickyComposer` import (L121) | TS6133 cleanup |
| 5c | issue-detail.tsx re-apply upstream `leadingAction?: ReactNode` to IssueDetailProps + destructure + add `ReactNode` import | Next.js build 严格 typecheck 报错修复（fork inbox-page.tsx 已传 leadingAction 但 IssueDetailProps 缺失字段——auto-merge 丢字段） |

**Phase 5 summary**：
- typecheck 兜底：发现 1 个 TS6133 dead import → 删
- build 兜底：发现 2 个 IssueDetailProps 语义错误（leadingAction 字段 + destructure 参数）→ re-apply + import
- 4 个 pre-existing test failure 非本轮 merge 责任，记录到 Phase 7 calibration
- 5 道 gate 净状态：**5/5 pass（pre-existing 失败不计入）**

## Phase 6 — Irreversible gates

TBD.

## Phase 7 — Record ✅

**Merge commit:** `c03402a94` — standard SOP merge message (rerere 记录 14 resolution，下次同类冲突自动吸收)。

**Calibration (actuals vs audit prediction):**
- merge-tree 预测 14 content 冲突 → **实际 14**（连续第 6 次文本预测命中：v0.4.14/15/16/17/18/19）
- Strategy D 签名盲区预测"不触发" → **实际不触发**（comment.go 整文件 0 改动，4 audit agent 实测）
- 4 新迁移预测"零撞号" → **实际零撞号**（全部 > fork max 252；6 已知完好）
- install.sh version stamp 预测"成功" → **实际异常**（merge 未 commit 时 `_version-stamp.sh` 走 `git describe --tags --abbrev=0` 取前一 tag v0.4.18）；**首次出现**：本轮首次依赖手动 re-stamp backend binary 兜底
- launchctl bootout 后 restart 路径预测"用 kickstart" → **实际失败**：bootout 后 service 未注册，kickstart "Could not find service"；需用 `launchctl bootstrap gui/UID plist-path` 重新加载
- 4 pre-existing issue-detail.test.tsx 失败 → SOP 7.5 验证 baseline 也 fail，**非 merge 引入**
- typecheck 兜底：1 个 TS6133 dead import (useStickyComposer) → 删
- build 兜底：IssueDetailProps 缺 leadingAction 字段 + destructure 缺失 + ReactNode 未导入 → 3 行修复

**本轮特色（vs 往轮）：** 后端冲突面扩大（task.go + main_test.go 真撞，但 fork pre-empt：own file + truncate 同名符号零新增）；9 locale JSON 真撞是全新形态（v0.4.14-18 全是 0 locale 冲突）；首次出现 install.sh baseline 倒退 + launchctl kickstart bootout 后失效 两条新事实。