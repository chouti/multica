---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.26
base_tag: v0.4.25   # clean merge-base ea1ace776 (= v0.4.25^{commit}; single hop from v0.4.25)

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done     # Phase 6 COMPLETE 2026-08-17: ALL irreversible gates fired. 6.1 pg17 backup 60M/728 TOC; 6.2 migrated 310-313 (latest=313_runtime_profile_add_dsh); 6.3 version stamped both sides v0.4.26; 6.4 install.sh plist reload; 6.5 health green (/healthz=200, :3001=200, /api/config server_version=v0.4.26); 6.6 daemon aligned: brew 0.4.25->0.4.26 + restart (pid 22569->6290), 5 workspaces + 5 agents survived. Awaiting Phase 7 record.

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true            # Phase 2 — CONFIRMED 2026-08-17: user chose 全推到 Phase 6 (merge+resolve+verify → pg17 pg_dump → migrate 310-313 → re-stamp → launchd restart → brew upgrade multica 0.4.26 + daemon restart). Approved short downtime. Also approved: panic fix as PRE-MERGE independent commit; delete packages/views/EOF inside merge; separately commit the auto-merge-collisions governance doc.
  audit_strategy_written: true    # Phase 1.4 — FLIPPED 2026-08-17: per-file strategy table below; 21 negative claims all carry verified_by raw commands (key ones re-verified in MAIN session: schemas symbol intersection, client.ts pure-additive, locale union, comment.go zero-diff, task.go zero-diff, issueTriggerWriteProbe call-line untouched, cmd_workspace.go panic reproduction, VCS content convergence)
  db_backed_up: true              # Phase 6.1 — DONE 2026-08-17T012028Z: pg17 pg_dump -F c, 60M, 728 TOC (pg_restore -l verified). Dump: ~/multica-backups/v0.4.26-pre-migrate-20260817T012028Z.dump
  migrated: true                  # Phase 6.2 — DONE 2026-08-17: applied 4 new migrations 310_workspace_private_plugin_identity / 311_plugin_identity_scoped_key_index (pre-hook) / 312_drop_global_plugin_identity_key_index / 313_runtime_profile_add_dsh. Latest applied = 313. exit 0 (Done.).
  version_stamped_frontend: true   # Phase 6.3 — DONE 2026-08-17: .env NEXT_PUBLIC_APP_VERSION v0.4.25->v0.4.26 (line 27) + install.sh standalone build
  version_stamped_backend: true    # Phase 6.3 — DONE 2026-08-17: install.sh go build -ldflags "-s -w -X main.version=v0.4.26". Smoking gun: strings ~/.multica/backend/server -> -X main.version=v0.4.26. Runtime proof: /api/config server_version=v0.4.26. commit-first satisfied (75ec751af at HEAD, git describe --tags = v0.4.26-301-g75ec751af).
  services_restarted: true         # Phase 6.3/6.4 — DONE 2026-08-17: install.sh bootout+bootstrap both plists; ports :3001 + :8081 listening
  health_checked: true             # Phase 6.4 — DONE 2026-08-17: backend /healthz=200 (retry-connrefused absorbed startup), frontend :3001 /=200, /api/config server_version=v0.4.26

merge_commit: 75ec751af   # TRUE 2-parent (parents d5428bc26 = fork HEAD, 19155e41f = v0.4.26^{commit}); git describe --tags HEAD = v0.4.26-301-g75ec751af -> install.sh resolves v0.4.26

# Negative claims — each confirmed/refuted by ONE raw command, never by reading.
# Verified in the MAIN session (not delegated to agents).
negative_claims:
  - claim: "current base is v0.4.25, a TRUE 2-parent merge (2ca50e006 per v0.4.25 plan), not a fake single-parent merge"
    verified_by: "git describe --tags HEAD → v0.4.25-298-g7fd0fcce8; git rev-list --count 2ca50e006..HEAD → 1 (docs-only commit since merge)"
    result: "confirmed (298 ahead of v0.4.25; the 1 post-merge commit is 7fd0fcce8 docs(upgrade) plan-close)"
  - claim: "latest upstream release is v0.4.26, published 2026-08-14, not prerelease/draft"
    verified_by: "gh release view --repo multica-ai/multica --json tagName,isPrerelease,isDraft,publishedAt"
    result: "confirmed (tagName=v0.4.26, isDraft=false, isPrerelease=false, publishedAt=2026-08-14T10:02:03Z)"
  - claim: "merge-base(HEAD, v0.4.26) = ea1ace776 = v0.4.25^{commit}, single clean hop from v0.4.25"
    verified_by: "git merge-base HEAD v0.4.26 → ea1ace776fa19413234880ee0bf89504b2f77d48; git rev-parse v0.4.25^{commit} → same"
    result: "confirmed. Single-hop upgrade v0.4.25 → v0.4.26. 25 upstream commits, 259 files (+12148/-630)."
  - claim: "NEW upstream migrations = 310-313 (4 stems: workspace_private_plugin_identity ×2, plugin_identity_scoped_key_index/drop_global_plugin_identity_key_index, runtime_profile_add_dsh). ALL stems > fork max 309 → ZERO new collision"
    verified_by: "diff <(git ls-tree --name-only ea1ace776:server/migrations/) <(git ls-tree --name-only v0.4.26:server/migrations/) | grep '^>' → exactly 310-313 up/down files; fork max stem 309, upstream max 313"
    result: "confirmed. The 6 known fork collisions (119/158/159/160/213/214) stand unchanged; legacy duplicate set (020-128 etc.) exists identically in both trees, not a fork risk."
  - claim: "the file intersection (both sides touched since merge-base) = exactly 23 files"
    verified_by: "comm -12 <(git diff --name-only ea1ace776..HEAD | sort) <(git diff --name-only v0.4.25..v0.4.26 | sort) | wc -l → 23"
    result: "confirmed. Full list in Phase 1 section."
  - claim: "merge-tree predicts exactly 1 text conflict: Makefile (both sides edited the .PHONY line; body hunks disjoint — fork selfhost-build/build-prod vs upstream db-drop/remove-worktree)"
    verified_by: "git merge-tree --write-tree --name-only HEAD v0.4.26 → 'CONFLICT (content): Merge conflict in Makefile' only"
    result: "confirmed at TEXT level. 22/23 intersection files auto-merge — semantic collisions (same-symbol ADD, fork-test × upstream-signature, locale key loss) are merge-tree blind spots audited in Phase 1."
  - claim: "uncommitted working-tree changes (graft tooling: .gitignore+3, AGENTS.md+42, untracked .ignore/.mcp.json/opencode.json/docs-solution doc) do NOT block the merge — upstream v0.4.26 touches none of those 5 paths"
    verified_by: "git diff --name-only v0.4.25..v0.4.26 -- .gitignore AGENTS.md .ignore .mcp.json opencode.json → empty"
    result: "confirmed. Leave the working tree as-is through the merge."
  - claim: "upstream did NOT touch the fork-carried mention cluster (mention-view.tsx, rich-content, comment.go, editor/, builtin_skills/multica-mentioning) in v0.4.26"
    verified_by: "git diff --name-only v0.4.25..v0.4.26 | grep -iE 'mention|rich-content|comment|editor/' → no match (exit 1)"
    result: "confirmed. Fork's hottest conflict cluster is immune this round."
  - claim: "upstream deleted ZERO files in v0.4.26 → no modify/delete conflicts, no fork dependency on removed files"
    verified_by: "git diff --diff-filter=D --name-only v0.4.25..v0.4.26 → empty"
    result: "confirmed. Pure-additive release (vs v0.4.20/24 which deleted/hollowed files)."
  - claim: "upstream churn centers on daemon/CLI/billing/docs — the fork's LOWEST-customization areas"
    verified_by: "git diff --name-only v0.4.25..v0.4.26 | awk -F/ … | sort | uniq -c | sort -rn → docs 34, locales 24, daemon 22, handler 17, cli 15, agent 13, billing NEW (packages/core/billing 4 + views/billing 3), settings 7, issues 7"
    result: "confirmed. Explains the 1-conflict prediction: fork surface concentrates in mention/issues/VCS which upstream barely touches."
  - claim: "schemas.ts has NO same-symbol ADD×ADD (the v0.4.25 SkillSchema trap does not recur)"
    verified_by: "comm -12 of both sides' added (export )?(const|function|type|interface) names from git diff ea1ce776..{HEAD,v0.4.26} → empty; merged-tree blob 6988d8e2 uniq -d duplicate-identifier scan → zero"
    result: "confirmed (main-session re-verified)"
  - claim: "upstream client.ts is PURE-ADDITIVE (0 removed lines) → fork's 4 signature tweaks (getChildIssueProgress/addIssueComment/updateComment/getConfig) have no opposing axis; no Strategy D"
    verified_by: "git diff ea1ce776..v0.4.26 -- packages/core/api/client.ts | grep -cE '^-[^-]' → 0 (main-session re-verified)"
    result: "confirmed. Upstream adds 7 billing methods + uninstallPlugin; method-name intersection with fork's additions = empty."
  - claim: "merged locales = exact union (no lost/dup keys)"
    verified_by: "merged-tree blob 6988d8e2 en/settings.json jq key-set == base∪fork∪up union (diff IDENTICAL, main-session re-verified); ja/ko/zh-Hans same (agent-verified); fork adds only members.invite_name_* (2 keys/locale), upstream adds 11 keys/locale (billing tab + plugins.* + shortcuts.archiveInboxItem)"
    result: "confirmed"
  - claim: "upstream did NOT touch comment.go at all in v0.4.26 → fork's 11-param triggerTasksForComment is fully immune"
    verified_by: "git diff ea1ce776..v0.4.26 -- server/internal/handler/comment.go server/internal/handler/comment_reconcile_test.go | wc -l → 0 (main-session re-verified)"
    result: "confirmed. The entire skill-mention backend keeps fork's version wholesale."
  - claim: "upstream task.go diff is COMPLETELY EMPTY → merge takes fork's task.go wholesale; truncateFallbackCommentBody redeclaration structurally impossible"
    verified_by: "git diff --stat ea1ce776..v0.4.26 -- server/internal/service/task.go → empty (STRONGER than agent claimed; main-session re-verified)"
    result: "confirmed. v0.4.12 import-gap lesson cannot recur."
  - claim: "all 5 upstream Go signature changes live in files fork never touched (issueTriggerWriteProbe 3→4 args is the only real one)"
    verified_by: "upstream -func/+func sweep → 5 changes (issueTriggerWriteProbe, compatibleWithV1Host→CompatibleWithV1Host, ensurePluginRelease del, codexFirstTurnNoProgressTimeout del, Queries.GetPluginIdentityByKey del); grep of fork-surface for those files → empty. issueTriggerWriteProbe: fork never touched its call lines (git diff ea1ce776..HEAD -- issue.go | grep -c issueTriggerWriteProbe → 0, main-session re-verified) → auto-merge correctly takes upstream 4-arg calls+def (Strategy D(b) clean)"
    result: "confirmed"
  - claim: "no upstream new callers of fork-changed signatures (findOrCreateUser/printSkillImportResult/triggerTasksForComment/truncateFallbackCommentBody)"
    verified_by: "upstream diff + lines grep for all 4 names → 0 hits"
    result: "confirmed"
  - claim: "router.go routes: zero lost, zero path collision; merged tree 375→392 routes"
    verified_by: "route-string enumeration on merged-tree blob vs base: only additions (fork 10: admin×9 + skills/import/batch; upstream 6 net + entitlements equivalent move); SuperAdminEmails L220 + ServerVersion: officialBaseline(version) L224 survive; upstream hunks (L59/L1160/L1183/L1347) disjoint from fork hunks (L145/L216/L1301/L1615)"
    result: "confirmed"
  - claim: "models.go: no same-struct-both-modified (fork touches WorkspaceInvitation+InviteeName, upstream touches PluginIdentity+OwnerWorkspaceID); sqlc regen on merged tree = ZERO drift"
    verified_by: "struct-name intersection of both diffs → empty; Lane C exported merged tree to /tmp/mergecheck, ran ~/go/bin/sqlc generate (v1.31.1, sqlc.yaml unchanged upstream): all 58 generated files byte-identical to auto-merge result"
    result: "confirmed. make sqlc NOT required post-merge (still safe to run)."
  - claim: "merged tree passes go build + go vet + lane unit tests BEFORE we even start Phase 4"
    verified_by: "Lane C on /tmp/mergecheck export: go build ./... exit 0; go vet ./... exit 0; TestGetConfig*/TestNormalizeRouterVersion PASS"
    result: "confirmed. Only red = pre-existing fork bug below."
  - claim: "PRE-EXISTING fork bug: cmd_workspace.go panics at package init 'invite flag redefined: role' (commit 215f833df double-registers role/output on workspaceMemberInviteCmd). NOT merge-introduced; breaks go test ./cmd/multica/ NOW at HEAD"
    verified_by: "go test ./cmd/multica/ -run TestNothingMatches → panic reproduced at HEAD (main-session); sed -n '123,129p' shows base L123-124 role/output + fork L126-129 email/role/name/output duplicate registrations"
    result: "confirmed. Production unaffected (daemon runs Homebrew CLI without fork flags). Must fix as independent fork commit for Phase 5 Go gate."
  - claim: "VCS cluster = upstream MERGED, fork copies are content-identical ghosts (ledger correction)"
    verified_by: "gh pr view 5006 → MERGED 2026-07-24 by Bohan-J (shipped v0.4.24); git diff ea1ce776..HEAD -- server/internal/handler/vcs*.go → EMPTY (byte-identical); ls-tree: v0.4.23 lacks vcs files, v0.4.24 has 4"
    result: "confirmed. Zero carry burden since v0.4.24; ledger reclassify OPEN→MERGED."
  - claim: "all other chouti PR states unchanged vs ledger"
    verified_by: "gh pr list --author chouti --state all → #6106/#5346/#5309/#5160/#4118/#2669/#2460 OPEN, #5199/#5539 CLOSED, #5466 withdrawn, #5393 MERGED (old)"
    result: "confirmed. No newly-merged PR to eliminate."
  - claim: "upstream zero e2e changes; new upstream TS tests don't probe fork-customized UI areas"
    verified_by: "upstream file list grep ^e2e/ → empty; chat-message-list.test.tsx (new) has 0 mention assertions; runtime-detail-visibility.test.tsx uses runtimeRowLabel (byte-identical both sides); settings-page.test.tsx mocks members-tab; fork gutter test asserts .max-w-4xl column (upstream changed leading bar only, PAGE_GUTTER='px-4' class-equivalent)"
    result: "confirmed. v0.4.20-style fork-code×upstream-test collision not predicted."
  - claim: "stray file packages/views/EOF (0 bytes) is heredoc residue from ecd309bb0, untracked by upstream"
    verified_by: "git log --oneline -1 -- packages/views/EOF → ecd309bb0; wc -c → 0"
    result: "confirmed. Safe to delete during merge (hygiene)."
---

# v0.4.25 → v0.4.26 upgrade plan

## Phase 0 — Locate (DONE 2026-08-17)

- Base: v0.4.25 (merge `2ca50e006`, TRUE 2-parent). HEAD `7fd0fcce8` = base + 1 docs commit.
- Target: **v0.4.26** (2026-08-14, stable). 25 upstream commits, 259 files, +12148/-630.
- Behind origin/main by 43 commits (tag-only merge per SOP; origin/main is past the tag).
- Release drivers: **DeepSeek Harness runtime** (4f10a944b, migration 313), **Private Skill Plugin dev loop** (31645cc51, migrations 310-312), **Workspace Billing UI + proxy** (cc8e020f8, 2d711afdd), **daemon workspaces root config** (698013ced MUL-5771), **agent-CLI diagnose** (f51ee62b5 MUL-6164), **left-align page titles/gutters** (0d2dc44e3 MUL-6169), worktree DB cleanup (e9528c722), tab-flare/desktop/inbox fixes.
- Working tree: graft tooling changes uncommitted — verified non-blocking (see negative_claims).

## Phase 1 — Deep pre-merge audit (DONE 2026-08-17)

3 audit agents (Lane B TS cluster / Lane C Go cluster / Lane D signatures+subsystems) + main-session adversarial re-verification of every load-bearing negative claim. **Verdict: cleanest upgrade on record — 1 text conflict (Makefile), zero predicted semantic collisions, and the merged tree already passes go build + go vet + sqlc-regen-zero-drift on an export.**

**Pre-existing fork bug found (NOT merge-introduced, must fix for Phase 5):** `server/cmd/multica/cmd_workspace.go` package-init panic `invite flag redefined: role` — fork commit `215f833df` registers `email`/`role`/`name`/`output` (L126-129) after base already registered `role`/`output` (L123-124). Reproduces at HEAD (`go test ./cmd/multica/` panics). Production unaffected (daemon = Homebrew CLI). Fix = delete base's L123-124 pair, keep fork's block + `MarkFlagRequired("email")`.

**Ledger corrections (verified):** VCS cluster = upstream MERGED (#5006 merged 2026-07-24 by Bohan-J, shipped v0.4.24; fork's vcs*.go byte-identical to v0.4.25 base — ghosts, zero carry burden). board-card fork delta = archived-aware progress text (not dimming). provenance = 4-file subsystem (router.go + provenance_baseline.go + main_test.go + config_test.go), all survive. `vcs` locale keys + client.ts VCS methods were already in base.

**Hygiene:** `packages/views/EOF` (0 bytes, heredoc residue from ecd309bb0) — delete during merge.

Intersection (23 files, both sides touched since ea1ace776):

```
CLAUDE.md
Makefile                                   ← the ONLY text conflict (.PHONY union + disjoint bodies)
SELF_HOSTING.md
packages/core/api/client.ts
packages/core/api/schema.test.ts
packages/core/api/schemas.ts               ← v0.4.25's same-symbol ADD trap lived here
packages/core/types/agent.ts
packages/core/types/index.ts
packages/views/issues/components/board-card.tsx
packages/views/issues/components/issue-detail.tsx
packages/views/issues/components/issues-header.tsx
packages/views/issues/components/issues-page.tsx
packages/views/locales/{en,ja,ko,zh-Hans}/settings.json
server/cmd/multica/cmd_issue.go
server/cmd/multica/cmd_issue_test.go
server/cmd/multica/main.go
server/cmd/server/router.go                ← VCS 5 routes + provenance ServerVersion must survive
server/internal/handler/config_test.go
server/internal/handler/issue.go           ← fork archived status in buildSearchQuery
server/pkg/db/generated/models.go          ← generated; upstream touched agent.sql/plugin.sql/workspace_delete.sql
```

Audit lanes (fan-out):
- **Lane B — TS cluster** (12 files): same-symbol ADD check (schemas.ts/client.ts), fork-only settings.json keys (invite_name/vcs) vs upstream billing keys, issues-header/page fork archived chip vs upstream gutter/title alignment, issue-detail fork pin/badge vs upstream changes, types.
- **Lane C — Go cluster** (9 files): router.go route enumeration post-merge requirement, issue.go fork 'archived' rank/CASE vs upstream search changes, models.go generated auto-merge sanity + sqlc regen need, CLI files (cmd_issue*.go, main.go) fork footprint vs upstream.
- **Lane D — signature sweep + subsystems**: upstream Go signature changes vs fork callers (incl. fork-only *_test.go files); DeepSeek Harness + Private Plugin + Billing footprints vs fork-carried areas; upstream deletions vs fork dependencies; Makefile/docs strategy confirmation.

### Per-file strategy table (Phase 1.4)

| File | Fork delta (vs ea1ce776) | Upstream delta | Strategy |
|---|---|---|---|
| `Makefile` | `build-prod` target + selfhost-build baseline block + `.PHONY`+build-prod | `db-drop` + `remove-worktree` targets + `.PHONY`+2 | **C** — `.PHONY` 行 union；正文双侧全收（body hunks 相距>=21行不相交） |
| `CLAUDE.md` | `## 用户偏好` 节 + docs/solutions/CONCEPTS bullets（纯插入） | Commands 节 +2 行（db-drop/remove-worktree） | **B** auto |
| `SELF_HOSTING.md` | 尾部 +43 行 self-host 指南 | +1 行 DeepSeek Harness CLI bullet | **B** auto |
| `core/api/client.ts` | 4 签名 tweak + importSkillsBatch + admin×9 + imports | +7 billing 方法 + uninstallPlugin（纯增量） | **B** auto（方法名交集空） |
| `core/api/schemas.ts` | skills 块搬迁 + BatchImport* 套件 | +9 WorkspaceSubscription schema + PluginInstallation 扩展 | **B** auto（符号交集空，TS2300 不复现） |
| `core/api/schema.test.ts` | +importSkillsBatch describe | +workspace-subscription contract 测试 | **B** auto |
| `core/types/agent.ts` | +BatchImportError/BatchImportResponse | RuntimeVisibility 注释 + RUNTIME_PROFILE_PROTOCOL_FAMILIES+dsh | **B** auto |
| `core/types/index.ts` | +agent/workspace 导出 | +billing 导出×9 | **B** auto |
| `views/issues/board-card.tsx` | child-progress 格式化 | +data-board-card 属性 | **B** auto |
| `views/issues/issue-detail.tsx` | isClosedStatus/badge/archived 进度/max-md:pr-14 | PAGE_GUTTER×2（IssueNotFound/Skeleton 区域） | **B** auto（fork hunks <=[2792] vs upstream [117]/[984]/[1014]） |
| `views/issues/issues-header.tsx` | archived chip + Archive icon + counts | PAGE_GUTTER import + 外层 div（IssuesHeader 组件，fork 改的是 IssueDisplayControls） | **B** auto |
| `views/issues/issues-page.tsx` | archivedCountFromFacets + IssuesEmptyState + renderEmpty 签名 | PageHeader className 单行删除 | **B** auto（插入点相隔 4 行不重叠，issue-surface.tsx 契约 upstream 零改动） |
| `locales/*/settings.json` ×4 | members.invite_name_* ×2 key/locale | 11 key/locale（billing tab + plugins.*×8 + shortcuts×2） | **B** auto（ADD×ADD 交集空；合并 blob == 精确并集已验） |
| `server/cmd/server/router.go` | admin×9 路由 + SuperAdminEmails + import/batch + officialBaseline 换血 | Idempotency-Key + plugins/private×3 + billing summary/prices | **B** auto（路由 375→392 零丢失零撞路径） |
| `server/internal/handler/issue.go` | archived 全套（搜索/排序/过滤/进度 <=L2073） | issueTriggerWriteProbe 4 参 ×2 调用点（L3159+/L3718+） | **B** auto（D(b)：upstream 单向收敛 + 自更新调用点） |
| `server/pkg/db/generated/models.go` | WorkspaceInvitation+InviteeName | PluginIdentity 重对齐+OwnerWorkspaceID | **B** auto（不同结构体；sqlc regen 零 drift 实测） |
| `server/cmd/multica/cmd_issue.go` | archived CLI + issueguard | --no-start ×3 命令 | **B** auto |
| `server/cmd/multica/cmd_issue_test.go` | TestValidateIssueStatusArchived | no-start helpers + 4 测试 | **B** auto（hunks [3513] vs [2817-2998]） |
| `server/cmd/multica/main.go` | adminCmd 注册（L86+） | pluginCmd 注册（<=L85） | **B** auto（相邻锚点不重叠） |
| `server/internal/handler/config_test.go` | v 前缀×3 + 溯源测试 | private_plugins_v1 flag 测试 | **B** auto |
| （合并时顺手）`packages/views/EOF` | 0 字节残留 | — | 删除 |

**无 A（accept-upstream）、无 Strategy D(a)；唯一手工动作 = Makefile `.PHONY` union + 独立修复 cmd_workspace.go panic + 删 EOF。**

Post-merge visual QA (non-blocking): upstream left-align/gutter × fork IssueIdentifierBadge 同屏排版过一眼。

## Phase 2 — Confirm irreversible gates (DONE 2026-08-17)

User confirmed: (1) full push to Phase 6 incl. DB migrate 310-313 + launchd restart + daemon brew upgrade, short downtime approved; (2) cmd_workspace.go panic fix as independent commit BEFORE the merge; (3) delete packages/views/EOF within the merge + separately commit the governance doc.

## Phase 3 — Reversible preview (DONE 2026-08-17)

`main-backup-v0.4.26` safety branch created; `git merge v0.4.26 --no-commit --no-ff` → **actual conflicts = exactly [Makefile], == merge-tree prediction (8th consecutive)**. All 22 other intersection files auto-merged. CLAUDE.md auto-merge carried upstream's db-drop/remove-worktree command lines as expected.

## Phase 4 — Resolve + re-apply (DONE 2026-08-17)

- **Makefile**: `.PHONY` union (fork `build-prod` + upstream `remove-worktree`/`db-drop`); all 4 target bodies verified present (selfhost-build/db-drop/remove-worktree/build-prod).
- **packages/views/EOF** deleted (staged).
- **Loss scans all empty**: 4 locale settings.json fork-only key sets survive (comm -23 empty ×4); client.ts fork methods ×5 present; router.go admin×9 routes enumerated + import/batch + officialBaseline + SuperAdminEmails survive; issue.go archived (WHEN 'archived' THEN 7 ×3, issue_status_is_closed); schemas.ts BatchImportResponseSchema; cmd_issue.go "archived".
- No other manual action required (all B auto-merge per strategy table).

## Phase 5 — Verify (DONE 2026-08-17, ALL GREEN)

| gate | result |
|---|---|
| pnpm install | no-op (deps unchanged, as predicted) |
| pnpm typecheck | 6/6 (TS2300 class absent — schemas symbol analysis confirmed) |
| go build ./... | exit 0 |
| go vet ./... | exit 0 (fork-test x upstream-signature class absent) |
| sqlc regen | ZERO drift (worktree == index after regen; Lane C's /tmp/mergecheck prediction confirmed on the real tree) |
| pnpm test | 5/5 workspaces; views 349 files / 4161 tests passed (fork-code x upstream-test class absent) |
| pnpm build | 3/3 |

Zero Phase-5 repairs needed — first upgrade on record with zero post-merge code fixes (v0.4.25 needed 2, v0.4.20 needed 14).

Merge commit created here per skill (commit-before-stamp): `75ec751af`.

## Phase 6 — Irreversible gates (DONE 2026-08-17)

GATE verified OPEN from plan frontmatter (phase=gates, user_confirmed=true, audit_strategy_written=true, 25 negative claims zero PENDING). Then:
1. **Backup** `~/multica-backups/v0.4.26-pre-migrate-20260817T012028Z.dump` (pg17, 60M, 728 TOC).
2. **Migrate up**: applied exactly 310-313; all existing skipped; latest = 313_runtime_profile_add_dsh.
3. **Re-stamp**: .env NEXT_PUBLIC_APP_VERSION=v0.4.26; install.sh full rebuild (frontend standalone + backend `-ldflags -X main.version=v0.4.26`).
4. **Restart**: install.sh reloaded both launchd plists.
5. **Health**: /healthz=200, :3001=200, /api/config server_version=v0.4.26 (running process IS the new binary).
6. **Daemon**: brew upgrade multica 0.4.25->0.4.26 + `multica daemon restart` (pid 22569->6290, version 0.4.26). 5 workspaces + 5 agents (hermes/claude/codex/opencode/openclaw) all survived.

## Phase 7 — Record (DONE 2026-08-17)

**Actuals vs plan (calibration):**
- merge-tree predicted 1 text conflict (Makefile) → **actual 1** (8th consecutive accurate text prediction).
- Audit predicted zero semantic collisions → **actual zero**: first upgrade on record needing ZERO Phase-5 code repairs (v0.4.25 needed 2, v0.4.20 needed 14).
- Migrations predicted 310-313 zero collision → **actual exactly that** (latest=313).
- pnpm install predicted no-op → **actual no-op**.
- Audit produced 2 durable ledger corrections: VCS cluster = upstream MERGED (#5006 merged 2026-07-24, shipped v0.4.24; fork copies byte-identical ghosts); board-card fork delta is archived-aware progress text, not dimming.
- Audit found 1 pre-existing fork bug (cmd_workspace.go init panic since 215f833df, invisible in production because daemon = Homebrew CLI) → fixed pre-merge as d5428bc26.
- **Process lesson (self-inflicted, cost ~3 commands):** a `cd server` residue made subsequent `git log`/`git ls-tree` pathspec lookups return false empties that read like a history contradiction; re-verified from repo root and it resolved. Always confirm cwd before treating an empty git pathspec result as evidence — an empty result from a wrong cwd is a false negative claim.

**Ledger updated** (docs/customizations.md): header audit line -> v0.4.26; v0.4.25 content preserved as its own section; VCS row moved Open->Adopted-upstream (MERGED); Drift notes appended v0.4.26 migration record.
