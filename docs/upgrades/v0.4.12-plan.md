---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.12
base_tag: v0.4.11
merge_commit: 7a2b7baba

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true
  audit_strategy_written: true
  db_backed_up: true
  migrated: true
  version_stamped_frontend: true
  version_stamped_backend: true
  services_restarted: true
  health_checked: true

# Negative claims the audit made — each confirmed/refuted by one raw command, not by reading.
negative_claims:
  - claim: "三方合并 0 内容文本冲突（auto-merge 成功）；本轮重点转为 auto-merge 后语义正确性"
    verified_by: "git 2.50.1 `git merge-tree --write-tree --merge-base=v0.4.11 HEAD v0.4.12` → exit 0, stdout 仅 tree oid `ac2608fe15c3633f15ea92fb81a27965de8a39dc`, stderr 空; 全局 server/**/*.go conflict-marker 扫描零命中"
    result: confirmed
  - claim: "merge v0.4.12 tag，不 merge origin/main（origin/main = v0.4.12 + 8 post-tag 未发布 commits）"
    verified_by: "`git merge-base --is-ancestor v0.4.12 origin/main` → YES; `git rev-list --count v0.4.12..origin/main` → 8"
    result: confirmed
  - claim: "v0.4.12 未 merge/close 任何 fork 定制 PR → 无 'MERGED→drop local' 策略翻转，所有 carry 定制继续 carry"
    verified_by: "`gh pr list --repo multica-ai/multica --author chouti --state all` → 与 ledger 一致：#5393 仍 MERGED(2026-07-15)；#5346/#5199/#5309/#4118/#2669/#5160/#2460 OPEN；#5539/#5466 CLOSED。v0.4.11..v0.4.12 无新变化"
    result: confirmed
  - claim: "v0.4.12 新增迁移 224-230（7 个），fork 无撞号（fork 最高 223），无 FOREIGN KEY，228/230 为 CONCURRENTLY 单语句"
    verified_by: "`comm -13 <(ls server/migrations/|sort) <(git ls-tree -r --name-only v0.4.12 -- server/migrations/|sed 's|server/migrations/||'|sort)` → 仅 224-230；grep up.sql 无 REFERENCES/FOREIGN KEY"
    result: confirmed
  - claim: "live DB schema_migrations 尾部 = 223，224-230 全未应用 → Phase 6 migrate up 迁 7 个新迁移"
    verified_by: "`psql ... 'SELECT version FROM schema_migrations ORDER BY version DESC LIMIT 1'` → 223_github_pr_check_run_pr_ordinal_index; 220-223 全在、224-230 全无"
    result: confirmed
  - claim: "truncateFallbackCommentBody / maxSynthesizedFallbackCommentRunes 无 duplicate-symbol（fork 早从 task.go 移出到 fallback_comment_truncate.go，cap 8000→200，3-way merge 保留移出）"
    verified_by: "`git cat-file -e ac2608fe:server/internal/service/fallback_comment_truncate.go` EXISTS; `git cat-file -p ac2608fe:server/internal/service/task.go | grep truncateFallbackCommentBody` → 空; `git cat-file -p ac2608fe:...fallback_comment_truncate.go | grep -nE 'func truncateFallbackCommentBody|maxSynthesized'` → line 9 const 200 + line 16 func; 调用点仅 fallback_comment_truncate_test.go"
    result: confirmed
  - claim: "cmd_agent_copy.go (#5279) fork 与 upstream v0.4.12 blob sha 完全一致 → add/add 同内容 zero conflict（fork 先行合入）"
    verified_by: "`git rev-parse HEAD:server/cmd/multica/cmd_agent_copy.go` = `git rev-parse v0.4.12:server/cmd/multica/cmd_agent_copy.go` = 289d00cf7492710a3628f7a669eeb268d0b70e44"
    result: confirmed
  - claim: "editor zoom-canvas 两处重命名（use-diagram-canvas→use-zoom-canvas, diagram-transform→zoom-transform）不产生断 import（upstream 原子重命名+改 consumer，fork 无 consumer）"
    verified_by: "merged tree `git cat-file -e ac2608fe:packages/views/editor/hooks/use-zoom-canvas.ts` EXISTS 且旧路径 use-diagram-canvas.ts `fatal: Not in tree`; `git grep -n 'use-diagram-canvas\|diagram-transform' ac2608fe` 仅命中 upstream 自改 consumer（mermaid-viewer/mermaid-diagram），无 fork 定制文件"
    result: confirmed
  - claim: "comment.go orthogonal-D 签名共存：fork triggerTasksForComment(skillMentionAgents map[string][]pgtype.UUID) + upstream mergeCommentIntoPendingTask(headSha) 同时正确，MUL-5285 hunk 不触及 triggerTasksForComment"
    verified_by: "`git cat-file -p ac2608fe:server/internal/handler/comment.go | grep -n 'triggerTasksForComment\|mergeCommentIntoPendingTask'` → 两定义各一份；`git show 49fd6cd08 -- comment.go` hunk 头全在 enqueueCommentAgentTriggers/mergeCommentIntoPendingTask 区(line 1534+/1750+/1826+/1841+/1864+)，无一处触 line 1491 的 triggerTasksForComment"
    result: confirmed
  - claim: "readonly-content.tsx fork == upstream v0.4.11 逐字节（fork 早把 mention chip 迁到 mention-view.tsx 并还原本文件）；v0.4.12 不改 .tsx，只改 .test.tsx 的 2 处选择器名（.mermaid-viewer-canvas→.zoom-canvas），不同 describe block"
    verified_by: "`diff <(git show v0.4.11:.../readonly-content.tsx) <(git show HEAD:.../readonly-content.tsx)` → IDENTICAL; `git show a964c5229 --name-status` 对 readonly-content.tsx 无输出"
    result: confirmed
  - claim: "唯一 post-merge 关注点：issue-detail.tsx thread rail（c7fe10e54, MUL-4522）left-2→right-3 与 fork pin-comment 容器的视觉交互（非功能破坏，预先存在，右侧更明显）"
    verified_by: "`git show c7fe10e54 -- packages/views/issues/components/issue-detail.tsx` → 仅改 ThreadMinimap className + 注释，不碰 fork pin-comment 容器(issue-detail.tsx:2690-2710); 0 文本冲突(merge-tree); Phase 5 后视觉验证 rail 是否遮评论输入框右下角"
    result: confirmed
    action: "Phase 5 后打开多-thread issue + pin-comment 开启做视觉验证；若 rail 遮输入框，给 pin-comment 容器加 relative z-10 或收窄 rail 定位基线到 scroll 容器"
---

# Upgrade v0.4.11 → v0.4.12

> Process artifact. Persistent customization list in `docs/customizations.md`.
> 20 upstream commits (v0.4.11..v0.4.12), published 2026-07-27 09:55 UTC. Themes: **desktop hang diagnostics (MUL-5345)**, **editor zoom-canvas abstraction (mermaid→generic, MUL-5316)**, attachment preview pan/zoom, `multica agent copy` CLI (MUL-5279), mention-search allow spaces (#5980), project picker search (MUL-5344), lark image/video ingest (MUL-4934, mig 225-230), daemon codex gate (MUL-5305, mig 224), duplicate-key 409 (MUL-5285), quick-create error surface (MUL-5268).

## Phase 0 — Locate
- base: `v0.4.11` (HEAD `v0.4.11-230-g571b38122`)
- target: `v0.4.12` (Latest, published 2026-07-27 09:55 UTC; non-prerelease)
- merge-base(HEAD, v0.4.12) = `v0.4.11` (clean)
- to-merge: **20 commits** (HEAD..v0.4.12)
- v0.4.12 is ancestor of origin/main; origin/main = v0.4.12 + 8 post-tag commits → **merge v0.4.12 tag, not origin/main**
- vs origin/main: 28 behind / 230 ahead
- working tree: clean (only untracked `reply.md` + this plan file); old stashes unrelated

## Phase 1 — Deep audit

### 已确认（独立验证 + fan-out，见 frontmatter 11 条 negative_claims）
- merge-tree 0 文本冲突(git 2.50.1 exit 0, tree `ac2608fe`, 全局 server/**/*.go 无 conflict-marker)
- merge tag 不 merge main（origin/main 多 8 个 post-tag 未发布 commits）
- v0.4.12 未 merge/close 任何 fork PR（无策略翻转）
- 新迁移 224-230 无撞号、无 FK、228/230 CONCURRENTLY 单语句；live DB 尾部 223 → 迁 7 个
- fallback_comment_truncate 唯一定义（task.go 无符号，复核确认）→ 无 duplicate-symbol
- cmd_agent_copy.go sha 一致（289d00cf7）→ add/add zero conflict
- editor 重命名无断 import（merged tree 旧路径不存在）
- comment.go orthogonal-D 签名共存（MUL-5285 不触及 fork 签名）
- readonly-content.tsx fork==upstream，v0.4.12 不改 .tsx
- Phase 6 重戳起点：`.env NEXT_PUBLIC_APP_VERSION=v0.4.11`→`v0.4.12`；`:5433` pg17；备份目录 `~/multica-backups`；host-level（install.sh 不碰）：`/opt/homebrew/etc/Caddyfile` + 3 个 plist

### Per-file strategy（fan-out 4 agents + 复核）
| 集群 / 文件 | strategy | 文本冲突 | 依据 |
|---|---|---|---|
| mention 集群(16 文件) | **A** | 0 | 仅 mention-suggestion.tsx+.test 被 #5980 触(`allowSpaces: true` 落 fork 未改区)；与 fork skill/actor mention 数据流正交；共享 API clean；locale `mention.group_skills` 存活 |
| `issue-detail.tsx` (thread rail, c7fe10e54) | **C** | 0/**1 视觉** | rail `left-2`→`right-3`;fork pin-comment 容器使 `absolute bottom-0` rail 贯穿到评论输入框区。预先存在、非功能破坏。Phase 5 后视觉验证(见 negative_claims #11 action) |
| `issue-identifier-badge.tsx` | A(fork-only) | 0 | upstream 不碰 |
| `queries.ts`(archived) | C | 0 | fork 改 fetchFirstPages/childIssueProgressOptions，upstream 改 issueTableRowPageOptions，不同函数 |
| property-picker/project-picker/thread-minimap/inbox 4 文件 | B | 0 | fork 无定制 |
| `comment.go` | C(orthogonal D 存活) | 0 | fork triggerTasksForComment 签名 + upstream mergeCommentIntoPendingTask 签名共存 |
| router.go/main.go/handler.go | C | 0 | fork provenance/version + upstream channel media 不同区 |
| `fallback_comment_truncate.go` | A(fork-only) | 0 | 唯一定义，无 duplicate(复核) |
| `provenance_baseline.go` | A(fork-only) | 0 | upstream 不碰 |
| skill*.go / cmd_admin / cmd_skill / cmd_workspace | A | 0 | upstream 不碰 |
| `cmd_agent_copy.go`(#5279) | A(sha 一致) | 0 | fork==upstream blob sha 289d00cf7(复核) |
| editor zoom-canvas 重命名(2 文件) + zoom-canvas.tsx/css(新) | A(adopt) | 0 | upstream 原子重命名+改 consumer，fork 无 consumer，无断 import |
| mermaid-viewer.tsx(-109)/attachment-preview-modal.tsx(+273) | A | 0 | fork 自 v0.4.11 未触 |
| readonly-content.tsx | N/A | 0 | fork==upstream v0.4.11 逐字节；v0.4.12 不改 .tsx |
| readonly-content.test.tsx | C | 0 | upstream 2 处选择器改名，fork 删改在不同 describe block |
| locale editor.json(en/ja/ko/zh-Hans) | B | 0 | fork mention.skill_* + upstream mermaid/canvas 不同区块 |
| 迁移 224-230 | A(adopt) | 0 | 新号无撞号，低风险增量 |
| desktop diagnostics 新包 / daemon/agent/kiro/lark Go | A | 0 | fork 无定制（fork 不碰 desktop/daemon runtime） |

### Audit prediction（Phase 3 对照）
- **预期真实手动冲突 0 个**（merge-tree 已钉死 0 文本冲突；rerere 本次无 recorded resolution 可套用，但本就无需）。
- 唯一 post-merge 动作：issue-detail thread rail 视觉验证（可选轻修）。
- Phase 5 语义关注：VCS 无（本轮无 VCS commit）；editor 重命名 + comment.go 签名 + fallback_symbol 已由 merged-tree cat-file 逐项验证；pnpm typecheck/go build 预期一次过。

## Phase 2 — Irreversible-gate confirmation（两段式）
- 第一阶段（2026-07-28）：用户授权 **Phase 3-5 可逆流程**（建 backup→merge 预览→resolve→代码验证），DB 备份预选 = pg17 pg_dump。
- 第二阶段：Phase 5 全绿后，Phase 6（迁 7 个迁移 + launchd 重启 + 版本重戳 v0.4.12）前再次单独确认 → `gates.user_confirmed: true`。

## Phase 3 — Preview
- safety branch `main-backup-v0.4.12` → 571b38122（merge 前 HEAD）。
- `git merge v0.4.12 --no-commit --no-ff`：**0 内容冲突**（`git diff --diff-filter=U` 空），"Automatic merge went well"，172 文件 staged，MERGE_HEAD = e52b50658（v0.4.12 head）。
- **vs 预测**：audit 预测 0 冲突 → 实际 0。Auto-merging 命中点（mention-suggestion / readonly-content.test / issue-detail / locales editor.json / router.go / comment.go / handler.go / task.go）与 4 agent 预测 100% 吻合。

## Phase 4 — Resolve
- **0 手动文本冲突**。一处 auto-merge 语义修正（Phase 5 go build 抓到）：
  - `server/internal/service/task.go`：fork 早前移走 `truncateFallbackCommentBody` 时删了 `import "unicode/utf8"`（以为无人用）；upstream MUL-5268（4581e9ee7 "surface real reason for failed quick-create"）在 task.go:4435 新增了结构相似的 `utf8.RuneCountInString` rune 截断（注释明说参考 comment-fallback path）。merge 后 task.go 有 utf8 用法、无 import → `go build` 报 `undefined: utf8`。**修复**：加回 `"unicode/utf8"`（标准库区，time 后）。0 文本冲突但有语义交互——auto-merge ≠ 语义正确的典型案例；merge-tree/agent 都没抓到，go build 抓到。
- lint map 无需改：224-230 各一 stem，无新 duplicate-prefix（fork 最高 223；真 duplicate 仅 119/158/159/160/213/214，已在 map）。
- issue-detail thread rail（right-3）↔ pin-comment 视觉副作用：留 Phase 6 部署后视觉验证，非功能破坏。

## Phase 5 — Verify (code gates, no DB/service)
全绿，stop-on-first-failure 全过：
- `pnpm install` ✓（exit 0，5.5s，无新依赖冲突）
- `pnpm typecheck` ✓（6/6，38s）— 无 auto-merge 语义错/unused import/narrowing
- `go build -C server ./...` ✓（修 task.go utf8 import 后；`go vet ./internal/service/` ✓）
- `pnpm test` ✓（5/5 tasks；@multica/views **3115 passed** / 269 files，61s；较 v0.4.11 的 3075 +40，含 upstream zoom-canvas/diagnostics/transcript/queries/attachment 新测试）
- `pnpm build` ✓（3/3，42s）
- **Skipped**：`make test`/`make start`（需 Docker，本 host 无）；Go integration tests 需独立 DB（不对 live DB 跑）；7 个 residual handler 测试失败（autopilot authority，Go 侧，v0.4.8/9 即存在，pnpm test 不覆盖 Go，与本次无关）。
- **→ 推进 `phase: gates`**（解锁 Phase 6 的唯一动作）。

## Phase 6 — Irreversible ops (EXECUTED 2026-07-28)
- **Backup**: `~/multica-backups/v0.4.12-pre-migrate-20260728-141159.dump`（37M，pg17 pg_dump，pg_restore --list 624 对象）✓
- **Migrate**: `migrate up` 应用 **7 个新迁移**（224_agent_task_session_rollout_missing … 230_channel_media_pending_object_claim_index），223 及以下全 `skip`。撞号 213×2/214×2 按版本串正确 skip。`Done.` ✓
- **Version stamp v0.4.12**（手动方式，沿用 v0.4.11；build-frontend.sh 不 source .env，故先 export）：
  - frontend: `.env NEXT_PUBLIC_APP_VERSION=v0.4.12` → `export NEXT_PUBLIC_APP_VERSION=v0.4.12; bash scripts/selfhost/build-frontend.sh`（layout chunk 含 v0.4.12 ×4，0 处 v0.4.11 残留）
  - backend: `go build -C server -ldflags "-X main.version=v0.4.12" -o ~/.multica/backend/server ./cmd/server`（strings 含 v0.4.12）
- **Restart**: `launchctl kickstart -k gui/$UID/com.fengzhao.multica-{backend,frontend}`。backend **~10s** 后 /healthz 200（本轮 daemon runtime 注册快于预期的 30-40s）。
- **Health**: backend `/healthz` 200，frontend `/` 200，`/api/config` → `"server_version":"v0.4.12"`（双注入生效，Help 菜单正确）✓

## Phase 7 — Record
- **Merge commit**: `7a2b7baba`（parents 571b38122 + e52b50658）。Base advanced v0.4.11 → v0.4.12（merge-base 干净 v0.4.11，20 upstream commits）。
- **Predicted vs actual**: merge-tree 预演 0 冲突 → 实际 **0 内容文本冲突**。**但 1 处 auto-merge 语义错**（go build 抓到，merge-tree/4 agent/merged-tree cat-file 全漏）：`task.go` 的 `import "unicode/utf8"` 被 fork 删除（移走 truncateFallbackCommentBody 时）+ upstream MUL-5268 新增 `utf8.RuneCountInString`（task.go:4435）→ 加回 import。**核心教训再确认：auto-merge ≠ 语义正确；静态审计再充分也替代不了 Phase 5 的真实 go build。**
- **本轮 vs v0.4.11 差异**：v0.4.11 迁 0 个迁移，本次**迁 7 个**（224-230，真实 schema 变更：agent_task_queue/chat_message 加列 + channel_media_pending_object 新表 + 2 个 CONCURRENTLY 索引）。DB 备份实打实必要（非走过场）。
- **fork 热文件零文本冲突**：mention cluster / issue-detail / comment.go / skill*.go / cmd_* / provenance / fallback_truncate 全部 auto-merge 干净（merged-tree cat-file 逐项验证）。
- **issue-detail thread rail（MUL-4522 right-3）↔ pin-comment 视觉副作用**：非功能破坏、预先存在、右侧更明显；留用后视觉验证（若 rail 遮评论输入框右下角，给 pin-comment 容器加 `relative z-10` 或收窄 rail 定位基线）。
- **Ledger**: base 推进 v0.4.11 → v0.4.12，Last upgrade 段替换，fallback_comment_truncate 条目加 import 删除注意。
- **Memory**: 无新 self-host quirk（无新撞号 / 无新 service gotcha；utf8 import 是 fork 定制与 upstream 新增的通用交互，记入 ledger 而非 memory）。
