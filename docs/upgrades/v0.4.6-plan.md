---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.6
base_tag: v0.4.3
merge_commit: 36dbc22ba

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done

# Irreversible gates — every one must be true before Phase 6 fired. Recorded after the fact.
gates:
  user_confirmed: true
  audit_strategy_written: true
  db_backed_up: true
  migrated: true
  version_stamped_frontend: true
  version_stamped_backend: true
  services_restarted: true
  health_checked: true

# Negative claims the audit made — each had to be killed/confirmed by one raw command, not by reading.
negative_claims:
  - claim: "upstream v0.4.6 adds no new migration collision"
    verified_by: "ls server/migrations/ | sort -n  AND  git ls-tree <merge-base> server/migrations/  (diff the number sets)"
    result: confirmed          # upstream adds only 202_runtime_profile_add_qwen; local head 201
  - claim: "upstream is NOT building a native mention-chip system (5a11232c4)"
    verified_by: "opened packages/views/rich-content/ + project-mention-a11y.test.tsx"
    result: confirmed          # renderer move + an a11y fix, not a chip system → re-apply, don't drop
  - claim: "no other preprocessMarkdown call sites beyond the 3 known"
    verified_by: "grep -rn \"preprocessMarkdown(\" packages/"
    result: confirmed
  - claim: "migration lint map covers 158/159/160"
    verified_by: "go test ./server/internal/migrations -run TestMigrationNumericPrefixesStayUniqueAfterLegacySet"
    result: refuted            # map stopped at 119/128; test was failing red → fixed (see Phase 7)
---

# Upgrade v0.4.3 → v0.4.6

> Process artifact for one upgrade. The persistent "what we carry" list lives in
> `docs/customizations.md`; this file freezes **this** upgrade's strategy, gates, and outcome.
> Migrated out of the ledger on 2026-07-22 (it was the "v0.4.3 → v0.4.6 action list" section).

## Phase 0 — Locate

- current base: `v0.4.3` (`git describe --tags`)
- latest upstream: `v0.4.6`
- behind / ahead of `origin/main`: **59 behind / 134 ahead**
- size: far below the v0.3.42 ActorAvatar refactor (35 commits / 50+ files) — one high-conflict commit, three big Go customizations untouched.

## Phase 1 — Deep audit

Sourced from a 3-agent fan-out audit plus independent negative-claim verification. Strategies A/B/C/D per SOP Step 4.

### Per-file strategy (drove Phase 4)

| file / commit | what it does | local hot files hit | strategy | difficulty |
| --- | --- | --- | --- | --- |
| **`41315989b`** fix(editor): prevent incorrect comment autolinks | Refactors `ContentEditor` `defaultValue` → `defaultValue\|value` union; renames `lastDefaultValueRef`→`lastSyncedValueRef` | `content-editor.tsx`, `issue-detail.tsx`, `comment-card.tsx`, `comment-input.tsx`, `reply-input.tsx` | **C + D** | **High** |
| `content-editor.tsx` (three-way) | — | hit by `5a11232c4` (preprocess call) **+** `41315989b` (signature) **+** local #5346 (+58 gesture) | three-way manual merge | High |
| mention cluster / `5a11232c4` "unify on one RichContent renderer" | hollows `readonly-content.tsx` (556 → ~40-line wrapper), moves read-only render to new `rich-content/rich-content.tsx` | readonly face only; editable face (`mention-view.tsx` etc.) zero-touched | **C** (re-apply) | High |
| `comment.go`, `skill.go`/`cmd_skill.go`/`skill.sql`, admin/invitation handlers | three heaviest local Go customizations | upstream's 59 commits do not touch them | **B** keep-as-is | zero-conflict |
| `router.go` | local 3 edits vs upstream 3 new routes in different route groups | provenance `officialBaseline`, `/api/admin`, `/import/batch` vs `/query`, `/cron-preview`, `/gc-check` | **D** orthogonal | low |
| migrations | upstream adds only `202_runtime_profile_add_qwen`; local head `201` | — | DO NOT renumber | none |
| `002ea0d87` configurable issue table view (6000+ lines) | ~90% files zero local footprint | `router.go` one route (D), `client.ts` different methods (D), `mutations.ts` per-line (C) | C/D | medium |
| `f8bf6cd8b` Qwen runtime | brings the `202` migration | `packages/core/types/agent.ts` (local #5309 type sync) | C per-line | medium |
| `ce1530049` agent working-chip colour tiers | — | only `issues.json` overlaps | B take-both | low |
| locales (`issues.json`/`editor.json` × several) | keys don't overlap | — | B take-both | trivial |

### Mention-cluster detail (the decision that mattered)

- **Renderer move, NOT a native mention system** — so re-apply, don't drop. Re-apply both readonly chips in ONE edit to `rich-content.tsx`'s `RichLink`: restore the actor-chip dispatch (deleted `readonly-content.tsx:297-320` → `MentionHoverCard` + `ActorMentionChip`) and the skill-chip branch (deleted `:321-338` → `MentionHoverCard` + `SkillMentionChip`). Resolve #5199 and #5346 together — same code site.
- The chip components themselves survive upstream untouched.
- **Caveat:** upstream's static `COMPONENTS` map is deliberately anti-fork ("no second render branch"); re-introducing a branch is architecturally tense and will recur every RichContent refactor. Decision: re-apply anyway to keep full readonly UX.
- **Mechanical must-do:** `preprocessMarkdown` signature became required `{ cdnDomain: string; ... }`. Patch the 3 call sites — `readonly-content.tsx:511` (moves with the renderer), `content-editor.tsx:442`, `content-editor.tsx:617`.

### Audit predictions (to be checked in Phase 3)

- Predicted `41315989b` to be high-conflict across content-editor / issue-detail / comment-card / comment-input / reply-input.
- Watch `002ea0d87`'s `issues.json` +68-line nested object — merge keys at the same level, don't drop local skill keys into a new upstream object.

## Phase 2 — Irreversible-gate confirmation

- Confirmed 2026-07-21 before merge. Defaults taken: pg17 `pg_dump` before migrate; confirm before restart.

## Phase 3 — Preview (actual vs predicted)

- **Actual conflicts: only 7.** The predicted high-conflict `41315989b` cost just comment-input / reply-input at 1 hunk each — content-editor / issue-detail / comment-card all auto-merged clean.
- Lesson: file-level audit over-predicts; git auto-merges at hunk level, so expect fewer real conflicts than predicted (but auto-merge ≠ semantically correct — Phase 5 typecheck catches that).

## Phase 4 — Resolve record

- Readonly actor + skill chips re-applied to `rich-content.tsx` in one edit (per the Phase 1 strategy).
- Two post-merge type fixes:
  - `file-viewer` — dropped unused `SkillFrontmatter` import (auto-merge fallout).
  - `provider-logo` — `qwenLogo` via `as unknown`, matching the in-file `antigravityLogoSrc` pattern (type-narrowing-to-`never` from merged ambient declarations).

## Phase 5 — Verify results

- `pnpm typecheck` → green (6 packages)
- `go build ./...` → green
- `pnpm test` → green (2887 tests)
- `pnpm build` → green (3 apps)
- **Skipped:** `make test` / `make start` — both call `ensure-postgres.sh`, which needs Docker (unavailable on this Homebrew-pg self-host).

## Phase 6 — Irreversible ops record

- **Backup:** `~/multica-backups/v0.4.6-pre-migrate-*.dump` (pg17 `pg_dump`).
- **Migrate:** `migrate up` applied only `202`; existing skipped.
- **Restart:** `go run` backend + `pnpm dev:web` frontend. *(Note: launchd supervision landed the next day, 2026-07-22 — later upgrades restart via `launchctl kickstart` / `scripts/selfhost/install.sh`, not `go run`.)*
- **Health:** frontend `/` → 200, backend `/healthz` → 200.

## Phase 7 — Record

- **Merge commit:** `36dbc22ba`; base advanced to `v0.4.6-145-g36dbc22ba`.
- **Actuals-vs-plan (sharpening future audits):** the fan-out audit correctly identified the cost centers but over-predicted conflict count 7×; the mention renderer-move classification (re-apply, not drop) was the load-bearing correct call.
- **Post-execution fix (eval-found):** `TestMigrationNumericPrefixesStayUniqueAfterLegacySet` was failing on 158/159/160 (the lint map missed the three); added their stems to `legacyDuplicateMigrationStems`, test green (`384e5de0a`). This is the `refuted` negative claim in the frontmatter.
- **Ledger corrections applied during this audit:**
  - Drift-note migration section rewritten (was stale "#5309 carries 158/159/160/166-168, renumber").
  - Drift-note mention section: added "renderer move, not native mention" clarification.
  - Registered previously-untracked `identifier-badge click-to-copy` (Pure-local table).
  - Added `'project'` token anti-misread note (Drift notes).

## Related

- `docs/customizations.md` — the persistent ledger (pre-merge audit table, Drift notes).
- `docs/solutions/workflow-issues/safe-upstream-upgrade-with-local-customizations.md` — the 10-step SOP this plan orchestrates around.
