---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.21
base_tag: v0.4.20   # clean merge-base 93342d04a (v0.4.20 changelog commit = tag point)

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done  # All 7 phases complete 2026-08-07. Merge commit 55bf3432f (true 2-parent). DB at 264. CLI/daemon/backend/frontend all v0.4.21 (daemon brew-upgraded 0.4.20→0.4.21 + restarted, 5 workspaces intact).

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true       # Phase 2 — CONFIRMED 2026-08-07: (1) pg17 pg_dump before migrate 261-264; (2) install.sh full rebuild+restart accepted (30-40s backend warmup OK)
  audit_strategy_written: true  # Phase 1.4 — DONE 2026-08-07: per-file strategy table complete (C1-C4 integrated); all negative_claims carry verified_by + result
  db_backed_up: true         # Phase 6.1 — DONE 2026-08-07: /Users/fengzhao/multica-backups/v0.4.21-pre-migrate-20260807-192339.dump (649 TOC entries, valid custom format); DB baseline at 260
  migrated: true             # Phase 6.2 — DONE 2026-08-07: 261/262/263/264 up (DB was at 260); final=264. No new collision vs known 119/158/159/160/213/214.
  version_stamped_frontend: true   # Phase 6.3 — DONE: .env NEXT_PUBLIC_APP_VERSION=v0.4.20→v0.4.21; verified in ~/.multica/frontend .next chunks
  version_stamped_backend: true    # Phase 6.3 — DONE: strings ~/.multica/backend/server shows -X main.version=v0.4.21 (git describe resolved v0.4.21, merge commit existed)
  services_restarted: true         # Phase 6.4 — DONE: install.sh rebuilt standalone+binary, bootstrapped both launchd plists; both state=running active=1, ports 8081+3001 listening
  health_checked: true             # Phase 6.5 — DONE 2026-08-07: backend /healthz=200 (immediate, fast warmup), frontend /=200, /api/config server_version=v0.4.21 (backend+frontend)

merge_commit: 55bf3432f  # TRUE merge commit (2 parents: ec3dea072 fork + 0dfaac266=v0.4.21 tag)

# Negative claims the audit will make — each confirmed/refuted by one raw command, not by reading.
negative_claims:
  - claim: "v0.4.20..v0.4.21 = 29 commits"
    verified_by: "git rev-list --count v0.4.20..v0.4.21"
    result: "confirmed (29)"
  - claim: "v0.4.20..v0.4.21 = 315 files, +19892/-2761"
    verified_by: "git diff --shortstat v0.4.20 v0.4.21"
    result: "confirmed (315 files)"
  - claim: "merge-base(HEAD, v0.4.21) = 93342d04a (v0.4.20 changelog/tag point)"
    verified_by: "git merge-base HEAD v0.4.21"
    result: "confirmed (93342d04a7a9f788fec921e5aa736f86c7f22d8f)"
  - claim: "HEAD is 29 behind origin/main, 283 ahead"
    verified_by: "git rev-list --left-right --count origin/main...HEAD"
    result: "confirmed (29 behind / 283 ahead)"
  - claim: "NEW upstream migrations are 261/262/263/264, all > fork max 260 → zero collision"
    verified_by: "git diff --name-only v0.4.20 v0.4.21 -- server/migrations/ | grep -oE '[0-9]+' | sort -n | uniq; git ls-tree HEAD server/migrations/ | grep -oE '0*N_'"
    result: "confirmed (261-264 new, fork_already_has=0 for each; 6 known collisions 119/158/159/160/213/214 untouched)"
  - claim: "No added-in-both file collisions (upstream new files vs fork new files)"
    verified_by: "for each 'A' file in git diff --name-status v0.4.20 v0.4.21, test git cat-file -e HEAD:<f>"
    result: "confirmed (empty output — zero collisions)"
  - claim: "3-way merge-tree v0.4.20→HEAD→v0.4.21 produces 38 'changed in both' candidates"
    verified_by: "git merge-tree 93342d04a HEAD v0.4.21 | grep -c '^changed in both'"
    result: "confirmed (38)"
  - claim: "Of those 38, 6 files / 8 hunks carry real conflict markers"
    verified_by: "python3 bytes-safe parse of git merge-tree 93342d04a HEAD v0.4.21, count '<<<<<<<'"
    result: "confirmed (6 files: mention-suggestion.tsx=3, README.md=1, en/ja/ko/zh-Hans editor.json=1 each; 8 hunks total)"
  - claim: "sqlc regen IS required — upstream touched 6 query files (agent/channel/chat/issue/squad/task_usage)"
    verified_by: "git diff --name-only v0.4.20 v0.4.21 -- 'server/**/*.sql' | grep -v migrations"
    result: "confirmed (6 non-migration query files changed → make sqlc in Phase 5)"
  - claim: "No DB dep changes (go.mod untouched); JS deps in 3 package.json (web/core/views)"
    verified_by: "git diff --name-only v0.4.20 v0.4.21 -- '**/go.mod' '**/package.json'"
    result: "confirmed (no go.mod; apps/web + packages/core + packages/views package.json changed → pnpm install)"
  # --- Phase 1 deep-audit (C1-C4 agents, all decisive claims re-verified in main session) ---
  - claim: "typed-mention NOT rolled back — 7-type system (member/agent/squad/issue/project/all/skill) live in HEAD; memory feat-typed-project-mention is STALE"
    verified_by: "git show HEAD:packages/core/mention/types.ts | grep -oE '\"(member|agent|squad|issue|project|all|skill)\"'"
    result: "confirmed (all 7 types present, via fork cbeddef49)"
  - claim: "packages/core/mention/ absent from upstream v0.4.21 (fork-only)"
    verified_by: "git ls-tree -r v0.4.21 --name-only | grep -c packages/core/mention/"
    result: "confirmed (0)"
  - claim: "group_cancelled is upstream-new (absent in fork HEAD all 4 locales) → editor.json is keep-both not semantic-conflict"
    verified_by: "for l in en ja ko zh-Hans: git show HEAD:.../$l/editor.json | grep -c group_cancelled"
    result: "confirmed (0/0/0/0)"
  - claim: "editor.json comma-trap: fork skill_agent_row_aria has NO trailing comma (line 84) → naive union = invalid JSON, must insert ','"
    verified_by: "git show HEAD:packages/views/locales/en/editor.json | grep -A1 skill_agent_row_aria"
    result: "confirmed (no comma, followed by '},')"
  - claim: "enqueueMedia (engine/router.go) = Strategy D scenario B clean — fork made ZERO edits, upstream 9→11 param"
    verified_by: "git diff --stat 93342d04a HEAD -- engine/router.go (empty); git show v0.4.21:... | grep enqueueMedia"
    result: "confirmed (fork zero edits; upstream 11-param merges wholesale)"
  - claim: "generateIssuePrefix = scenario B — fork 2-param (name,slug), upstream untouched 1-param"
    verified_by: "git show HEAD vs v0.4.21 :server/internal/handler/workspace.go | grep 'func generateIssuePrefix'"
    result: "confirmed (HEAD: name,slug; v0.4.21: name only)"
  - claim: "truncateFallbackCommentBody duplicate-symbol hazard — fork moved it to fork-only fallback_comment_truncate.go (0 defs in HEAD task.go), upstream v0.4.21 still defines inline (1)"
    verified_by: "git show HEAD:task.go | grep -c 'func truncateFallbackCommentBody' (0); git cat-file -e HEAD:.../fallback_comment_truncate.go (YES); git cat-file -e v0.4.21:.../fallback_comment_truncate.go (ABSENT); git show v0.4.21:task.go | grep -c (1)"
    result: "confirmed — post-merge task.go must have 0 defs or redeclared build error"
  - claim: "dreaded backend funcs (enqueueIssueTask/CompleteTask/FailTask/StartTask/MarkTaskWaitingLocalDirectory/HandleFailedTasks/ReconcileAgentStatus) byte-identical sigs across all 3 refs — no Strategy D scenario A"
    verified_by: "git grep -h 'func.*NAME(' 93342d04a/HEAD/v0.4.21 (C2 agent)"
    result: "confirmed identical"
  - claim: "issue.go buildSearchQuery IS a real conflict (merge-tree MISSED it) — fork ELSE 7→WHEN archived vs upstream cancelledRank insert abutting same anchor"
    verified_by: "git diff 93342d04a HEAD vs git diff v0.4.20 v0.4.21 -- issue.go (C4+C2 agents)"
    result: "confirmed — manual keep-both: fork archived rank + upstream cancelledRank + 3-arg ORDER BY"
  - claim: "Analytics rename MUL-5759 zero fork impact — 0 file renames, fork never touched dashboard/usage.json, deleted usage keys not fork-referenced"
    verified_by: "git diff --name-status v0.4.20 v0.4.21 | grep -cE '^R' (0); git diff --name-only 93342d04a HEAD -- packages/views/dashboard (empty) (C4 agent)"
    result: "confirmed"
  - claim: "v0.4.20 title-uniqueness test collision does NOT recur — fork IssueIdentifierBadge renders only {identifier} not title; upstream added 0 new getByText probes"
    verified_by: "issue-identifier-badge.tsx renders {issue.identifier}; getByText/getAllByText count reconciliation base/fork/upstream/merged (C4 agent)"
    result: "confirmed (merged matches fork's intentional 4+2 split)"
  - claim: "broadcastTaskEvent variadic change is backward-compatible — fork's 24 three-arg call sites remain valid"
    verified_by: "git grep broadcastTaskEvent( HEAD = 24 calls, all 3-arg (C2 agent)"
    result: "confirmed"
  - claim: "generated issue.sql.go stale on BOTH sides — fork lacks upstream's 2 queries, upstream lacks fork's Archived field → make sqlc mandatory"
    verified_by: "git grep LockIssueForDescriptionUpdate HEAD (ABSENT); git grep Archived v0.4.21 (ABSENT) (C2 agent)"
    result: "confirmed → ~/go/bin/sqlc generate, do NOT hand-merge"
  - claim: "locale loss scan flat(fork)-flat(merged)==∅ achievable for all 12 locale files; locale parity EN-covers clean (no missing-EN hazard this cycle)"
    verified_by: "C3 agent ran git merge-file dry-run + deep-flatten key-set comm -23 on all 12 → all ∅"
    result: "confirmed (all 12 clean, only editor.json×4 need manual union)"
  - claim: "sqlc binary at ~/go/bin/sqlc, NOT in PATH"
    verified_by: "ls ~/go/bin/sqlc (exists 79M); command -v sqlc (empty) (C2 agent)"
    result: "confirmed → prepend PATH or invoke directly"
---

# v0.4.20 → v0.4.21 Upgrade Plan

**Status: phase = audit.** Self-host fork upgrade. Merge-base is clean at `93342d04a` (the v0.4.20 changelog commit = tag point). Small delta: 29 commits, 315 files. WeCom channel (MUL-5226) + Analytics rename (MUL-5759) are the two feature drivers.

## Phase 0 — Locate

- Current base: `v0.4.20-283-gec3dea072` (283 fork commits on top of v0.4.20).
- Latest tag: `v0.4.21` (0dfaac266, published 2026-08-07T10:09:12Z, not prerelease/draft).
- Upstream delta: **29 commits, 315 files (+19892/-2761)**.
- Behind/ahead vs origin/main: **29 behind / 283 ahead**.
- Merge-base HEAD↔v0.4.21: **93342d04a** (= v0.4.20 tag point). Clean, single-parent-hop base.

## Phase 0.5 — merge-tree preview (raw, read-only)

- `git merge-tree 93342d04a HEAD v0.4.21`: **38 "changed in both"** candidates.
- Of those: **6 files / 8 hunks** carry real `<<<<<<<` conflict markers:
  - `packages/views/editor/extensions/mention-suggestion.tsx` — **3 hunks** (hot file)
  - `README.md` — 1 hunk (docs)
  - `packages/views/locales/{en,ja,ko,zh-Hans}/editor.json` — 1 hunk each (locale, same logical edit × 4 locales)
- **Zero added-in-both** file collisions.
- ⚠️ merge-tree cannot see fork-only files (absent from merge-base). 6 is a floor, not a ceiling — Phase 3 real merge is the authority.

## Phase 1 — Deep pre-merge audit

### Upstream drivers (29 commits)

Two feature clusters + a fix tail:
- **WeCom (企业微信) channel** — MUL-5226 (#5833, #6529, #6549, #6532, #6534): new channel engine integration, inline media, docs. New server internals + migrations 263/264.
- **Analytics rename** — MUL-5759 (#6508, #6579, #6564, #6518): Usage → Analytics with Usage/Errors tabs, rework filters/freshness. Heavy `packages/views` churn.
- **Agent/runtime fixes** — CodeBuddy MCP (MUL-5846), OpenCode stdin (MUL-5841), ACP MCP drop (MUL-5843), empty-stream sentinel (MUL-5785/5831), Kimi mtimes (MUL-5844).
- **Chat/daemon** — settled-message cache door (MUL-5711), fan-out cookbook (MUL-5825), channel issue commands hidden (MUL-5870), directory waits (MUL-5832), git metadata isolation (MUL-5774).
- **Misc** — squad leader role (MUL-5811), search demote cancelled (MUL-5824), CLI --compact (MUL-5442), issue media preserve (MUL-5839), README rebuild (MUL-5812), QwenPaw docs (MUL-5828), inbox/chat layout fold (MUL-5842), `taskfailure` pkg.

### Candidate conflict files (fork ∩ upstream overlap, 40 files)

`comm -12` of (fork-touched since merge-base) ∩ (upstream-touched v0.4.20→v0.4.21). High-churn ones (fork:upstream +/-):

| File | fork | upstream | Note |
|---|---|---|---|
| packages/views/editor/extensions/mention-suggestion.tsx | 98+13 | 103+4 | **HOT** — 3 preview hunks |
| packages/core/api/client.ts | 99+7 | 72+0 | fork adds methods; upstream 72 add/0 del |
| packages/core/api/schemas.ts | 58+0 | 56+0 | both add-only (zod) |
| packages/views/editor/content-editor.tsx | 45+13 | 22+3 | editor |
| packages/views/issues/components/issues-header.tsx | 30+23 | 4+1 | fork churn > upstream |
| packages/views/issues/components/issue-detail.tsx | 26+6 | 6+2 | issue detail |
| packages/core/types/api.ts | 16+0 | 3+0 | types |
| packages/core/types/agent.ts | 13+0 | 8+0 | types |
| packages/core/issues/mutations.ts | 6+2 | 24+7 | upstream churn > fork |
| server/cmd/server/router.go | (fork) | (upstream) | **Strategy D watch** — v0.4.20 hit here |
| server/internal/service/task.go | (fork) | (upstream) | **Strategy D watch** — CompleteTask/FailTask |
| server/internal/handler/issue.go, handler.go | (fork) | (upstream) | backend |
| server/cmd/multica/cmd_issue.go (+_test) | (fork) | (upstream) | CLI |
| server/pkg/db/{queries,generated}/issue.sql | — | (upstream) | sqlc regen |
| locales {en,ja,ko,zh-Hans}/{editor,layout,settings}.json | (fork) | (upstream) | 12 locale files; editor.json×4 conflict |

### Cluster assignments for deep agents

- **C1 (hot file)**: `mention-suggestion.tsx` (+ `content-editor.tsx`, `mention-suggestion.test.tsx`, editor locales). Why 3 hunks; fork custom = typed-mention/@project; upstream churn 103+4. Strategy likely C.
- **C2 (backend Strategy D)**: `router.go`, `service/task.go`, `handler/issue.go`, `handler/handler.go`, `cmd_issue.go`. Verify whether upstream changed signatures fork also changed (double-signature blindspot) or single-sided. WeCom/media enqueue may touch enqueue paths.
- **C3 (core/api/types + locale)**: `client.ts`, `schemas.ts`, `types/{api,agent,index}.ts`, `mutations.ts`, all 12 locale JSON. Fork adds members (client methods, schema fields, locale keys); upstream adds too → merge-file 3-way, loss-scan after.
- **C4 (issues views + sweep)**: `issue-detail.tsx`, `board-card.tsx`, `issues-header.tsx`, Analytics/Usage rename blast radius vs fork issues views; sweep remaining upstream commits for secondary overlap with fork clusters.

### Per-file strategy table

Integrated from C1–C4 deep agents (all decisive claims independently re-verified by raw command in the main session).

**KEY CORRECTION (C1):** memory `feat-typed-project-mention` (rolled back 2026-07-16) is **STALE** — a complete 7-type mention system (`member/agent/squad/issue/project/all/skill`) is live via fork `cbeddef49`; `packages/core/mention/` is fork-only (absent upstream v0.4.21). Update that memory in Phase 7.

| File | fork_only_adds | upstream_only_adds | collision | strategy | merge_tool / notes |
|---|---|---|---|---|---|
| `packages/views/editor/extensions/mention-suggestion.tsx` | Skills group, `typeGroups` Map, skill render branch, `ensureCaches`, `isClosedStatus` dim (22 skill refs) | cancelled-demotion: `isDemotedCancelled`, `demoteCancelledItems`, `cancelled` bucket; **sig change `groupItems(items)`→`(items, query)`** | **REAL (3 hunks)** — both rewrote `groupItems` body with incompatible var schemes + upstream sig change | **C+D** | manual: adopt upstream `query` param + cancelled tier, integrate fork `typeGroups`. **Dangling-ref trap**: auto-merged loop body refs BOTH `cancelled` and `typeGroups`; rewrite whole function coherently. NEVER --theirs (22 fork skill refs, 0 upstream). |
| `packages/views/editor/content-editor.tsx` | `SkillMentionContext` provider + `getSkillMentionIds` | `onUpdate` sig `(md)`→`(md, base)` + base-tracking refs | none (disjoint) | A (verify D) | auto-merge; typecheck catches `onUpdate` caller ripple |
| `packages/views/editor/extensions/mention-suggestion.test.tsx` | 7 skill/archived test blocks | ~15 cancelled-demotion tests | none textual | A + MANDATORY test run | RTL tests exercise merged `groupItems` end-to-end — tripwire for bad C1 resolution |
| `packages/views/locales/{en,ja,ko,zh-Hans}/editor.json` | `group_skills` + 8 `skill_*` keys | `group_cancelled` | **TEXTUAL (1 hunk ×4)** — both rewrite `group_search` anchor, append disjoint keys | **D (manual union + comma fix)** | merge-file + hand-resolve: keep fork block + **insert `,` after `skill_agent_row_aria`** + keep `group_cancelled`; validate `python3 -m json.tool`. NEVER --theirs (loses 9 keys ×4). |
| `server/internal/handler/issue.go` | archived-status SQL: `WHEN 'archived' THEN 7 ELSE 8` in statusRank, `include_archived`, `NOT issue_status_is_closed()`, `Archived` field | `cancelledRank` expr + ORDER BY arity 2→3 (MUL-5824); `DescriptionBase` + `updateIssueWithDescriptionMerge` (MUL-5442) | **REAL in `buildSearchQuery`** (merge-tree MISSED this) | **manual keep-both** | keep fork's `WHEN 'archived' THEN 7 ELSE 8` AND upstream's `cancelledRank` + 3-arg ORDER BY. Do NOT reconcile fork's DB-fn filter with upstream's literal `'cancelled'` — different clauses. Update-path disjoint. |
| `server/internal/handler/search_test.go` | 2 assertions → `issue_status_is_closed` SQL | +107 lines MUL-5824 cancelled tests (EOF) | none (disjoint regions) | A + tripwire | upstream's new `..._CancelledDemotedAheadOfRelevance` only passes if `buildSearchQuery` merged right — it's the correctness tripwire |
| `server/internal/service/task.go` | MOVED `truncateFallbackCommentBody`→`fallback_comment_truncate.go` (fork-only), removed inline call | `broadcastTaskEvent`→variadic (backward-compat), `publishTaskFailedEvent` etc. | none sig; **duplicate-symbol hazard** | A + verify | **post-merge: `grep "func truncateFallbackCommentBody" task.go` MUST be 0** (def only in fork-only file, else `redeclared`). 24 fork 3-arg calls valid vs variadic. |
| `server/cmd/server/router.go` | `officialBaseline` (fork-only), `SuperAdminEmails`, `/api/admin/*` (7 routes), `/import/batch` | WeCom init block + routes (MUL-5226) | none (disjoint) | A | auto-merge |
| `server/internal/handler/handler.go` | `generateIssuePrefix` 1→2 param (call site), `SuperAdminEmails`, super-admin methods | 3 WeCom `Handler` fields | none (scenario B) | A | `generateIssuePrefix` def is fork 2-param in workspace.go (upstream untouched 1-param) → keep fork 2-arg call; cross-file dep, upstream didn't touch workspace.go |
| `server/internal/integrations/channel/engine/router.go` | — (fork zero edits) | `enqueueMedia` 9→11 param | **scenario B clean** | A | fork made ZERO edits; upstream 11-param def+call sites merge wholesale. NOT a collision. |
| `server/cmd/multica/cmd_issue.go` | archived-status: `issueguard.IsCompletedStatus`, `validIssueStatuses`+"archived" | `--compact` flag + `compactComments` (MUL-5442) | none (disjoint) | A | auto-merge |
| `server/cmd/multica/cmd_issue_test.go` | `TestValidateIssueStatusArchived` (inserted before help test) | rewrites help test +159 lines | **possible textual** at boundary | keep-both | keep fork's new test AND upstream's rewritten/added tests |
| `server/pkg/db/queries/issue.sql` | 4 queries → `issue_status_is_closed/completed` (fork DB fns, migration 214), `archived` COUNT | 2 new queries `LockIssueForDescriptionUpdate`, `MaterializeIssueChannelMediaMarkdown` | none (disjoint) | A → **`make sqlc`** | merge .sql auto; **regen .sql.go via `~/go/bin/sqlc`, do NOT hand-merge generated** |
| `packages/core/api/client.ts` | 10 admin/import methods + 5 sig edits | 4 WeCom methods | none (disjoint) | A | merge-file, NEVER --theirs (10 fork methods). Loss scan ∅. |
| `packages/core/api/schemas.ts` | Skill/BatchImport schemas + EMPTY_* | WeCom schemas + `cancelled_count` | none | A | merge-file. Loss scan ∅. |
| `packages/core/api/schema.test.ts` | importSkillsBatch tests | **NONE** | none | accept-ours | only file where --ours safe |
| `packages/core/types/{api,agent,index}.ts` | include_archived×3, invitee_name, BatchImport×2, Admin re-exports | description_base, qwenpaw, cancelled_count, wecom export | none | A | merge-file. Loss scan ∅. |
| `packages/core/issues/mutations.ts` | `skillMentionAgents` threading | `description_base` optimistic-cache door | none | A | merge-file. Loss scan ∅. |
| `packages/core/package.json` | 5 export keys | 4 export keys | none | A | merge-file. Loss scan ∅. |
| `packages/views/locales/{4}/layout.json` | cli_*/backend_*/admin_user_management | `nav.usage` value →"Analytics" | none | A | merge-file (fork didn't touch nav.usage). Loss scan ∅. |
| `packages/views/locales/{4}/settings.json` | invite_name_* (+prettier reformat) | `wecom.*` block (~38 keys) | none | A | merge-file. Loss scan ∅. |
| `README.md` | (fork edits) | rebuild (MUL-5812) | 1 hunk | B keep-both | docs, low-stakes |

**C4 PART B (Analytics rename MUL-5759): zero fork impact.** 0 file renames; fork never touched dashboard/usage.json; deleted usage keys (subtitle/metric_errors) not referenced by any fork file.

**C4 predicted Phase 5 test collisions: NONE new.** The v0.4.20 title-uniqueness pattern does NOT recur — fork's `IssueIdentifierBadge` renders only `{identifier}` not title; upstream added 0 new `getByText(title)` probes; merged file matches fork's intentional 4 getByText + 2 getAllByText split.

**Backend headline (C2): NO Strategy D double-signature disasters.** All dreaded funcs (`enqueueIssueTask`/`CompleteTask`/`FailTask`/`StartTask`/`MarkTaskWaitingLocalDirectory`/`HandleFailedTasks`/`ReconcileAgentStatus`) byte-identical across all 3 refs.

### Real manual conflicts (the merge authority will confirm in Phase 3)

1. `mention-suggestion.tsx` — 3 hunks (C+D)
2. `editor.json` × 4 locales — 1 hunk each (D manual union + comma fix)
3. `issue.go` `buildSearchQuery` — keep-both (merge-tree missed this)
4. `cmd_issue_test.go` — possible boundary keep-both
5. `README.md` — 1 hunk (docs keep-both)

Everything else: auto-merge via `git merge-file` (NEVER `--theirs` on member-adding files), then `make sqlc` + typecheck + targeted test runs as tripwires.

### Phase 1.4 — gate

`audit_strategy_written` flips true only when: per-file strategy table above is complete AND every entry in `negative_claims` carries a non-empty `verified_by` + `result`.

## Phase 2 — Confirm irreversible gates

CONFIRMED 2026-08-07 via blocking question:
- **DB backup**: pg17 `pg_dump` (`/opt/homebrew/opt/postgresql@17/bin/pg_dump`) before migrate 261-264. → `~/multica-backups/v0.4.21-pre-migrate-<ts>.dump`.
- **Service restart**: `bash scripts/selfhost/install.sh` full rebuild + launchd reload (commit-first order verified safe last cycle). Backend ~30-40s warmup accepted.

## Phase 3 — Reversible preview

DONE 2026-08-07. Safety branch `main-backup-v0.4.21` created. `git merge v0.4.21 --no-commit --no-ff` in working tree.

**Actual conflicts (`--diff-filter=U`): 6** — exactly matching merge-tree prediction (no more, no less):
- `README.md` (1 hunk)
- `packages/views/editor/extensions/mention-suggestion.tsx` (3 hunks)
- `packages/views/locales/{en,ja,ko,zh-Hans}/editor.json` (1 hunk each)

**Auto-merged but flagged for SEMANTIC verify (text-merge success ≠ correctness):**
- `server/internal/handler/issue.go` — auto-merged, but C4/C2 confirmed fork's `WHEN 'archived' THEN 7 ELSE 8` and upstream's `cancelledRank` sit at the same anchor. MUST verify both survived (keep-both).
- `server/cmd/multica/cmd_issue_test.go` — auto-merged; verify fork's `TestValidateIssueStatusArchived` + upstream's rewritten help test coexist.
- `server/internal/service/task.go` — auto-merged; verify `truncateFallbackCommentBody` has 0 inline defs (lives only in fork-only `fallback_comment_truncate.go`).

**Actuals-vs-plan: prediction (6) == actual (6).** The two "extra" candidates the audit worried about (issue.go, cmd_issue_test.go) auto-merged textually — git's hunk-level merge was smarter than the file-level/human estimate, but they remain semantic-verify gates, not free passes.

## Phase 4 — Resolve + re-apply

DONE 2026-08-07. All 6 conflicts resolved, 3 semantic traps verified, all loss scans clean.

**Conflict resolutions:**
1. `mention-suggestion.tsx` (C+D, 3 hunks): kept fork `typeGroups` Map (covers Users/Issues/Skills via `getMentionGroupLabel`) + added upstream `cancelled` array (status-axis, always-last group). Deleted upstream's redundant `users`/`issues` arrays (subsumed by typeGroups). Hunk3 keep-both labels (Skills + Cancelled). Signature `groupItems(items, query)` adopted. 0 residual markers, no dangling refs.
2. `editor.json` ×4 (D manual union + comma fix): kept fork block + inserted `,` after `skill_agent_row_aria` + appended upstream `group_cancelled` (per-locale translation). All 4 valid JSON; loss scan ∅; 94 fork keys + 1 upstream = 95 keys each.
3. `README.md` (A accept-upstream): took upstream's MUL-5812 rewrite. Fork's old self-host `<details>` block superseded — upstream relocated equivalent content to "Get started → Self-hosting the whole thing" (covers `--with-server`, `MULTICA_MODE`, GHCR/Docker, `make selfhost-build`). Fork's detailed selfhost doc lives in `SELF_HOSTING.md` (unconflicted, preserved).

**Semantic traps verified (auto-merge succeeded, confirmed keep-both by raw grep):**
- `issue.go buildSearchQuery`: fork `WHEN 'archived' THEN 7` (×3) + upstream `cancelledRank` (×2) + 3-arg ORDER BY (`cancelledRank, rankExpr, statusRank`, lines 645-647) + fork `issue_status_is_closed` all survive.
- `task.go`: `truncateFallbackCommentBody` inline defs = 0 (lives only in fork-only `fallback_comment_truncate.go`, ×1 there). No duplicate symbol.
- `cmd_issue_test.go`: fork `TestValidateIssueStatusArchived` (×1) + upstream compact tests (×4) coexist.

**Loss scans:**
- Code symbols (client/schemas/types×3/mutations): all ∅. `skillMentionAgents` (6), `include_archived` (5), admin methods (3) all survive.
- Full locale scan (every locale file): only diff = `usage.json` `daily.metric_errors` + `subtitle` across 4 locales — confirmed UPSTREAM-deleted dead keys (Analytics rename), absent in v0.4.21 AND not referenced by any code in the `usage` namespace. Correct action = accept deletion, not restore. **ZERO fork-only members lost.**

## Phase 5 — Verify (code gates)

DONE 2026-08-07 — ALL GREEN, in order:

| Command | Result |
|---|---|
| `pnpm install` | ✅ (4.5s, no new native build issues) |
| `pnpm typecheck` | ✅ 6/6 (mention C+D, onUpdate sig, content-editor all pass) |
| `go build -C server ./...` | ✅ exit 0 (no dup symbol, no sig mismatch) |
| `go vet ./...` | ✅ exit 0 |
| `make sqlc` (PATH=~/go/bin) | ✅ exit 0 — issue.sql.go unified: upstream LockIssueForDescriptionUpdate(3)+MaterializeIssueChannelMediaMarkdown(3) AND fork Archived(2) |
| `go build` (post-sqlc) | ✅ exit 0 |
| `pnpm test` | ✅ **3824 passed / 0 failed** (325 files) — mention-suggestion.test.tsx 50/50 (fork skill/typeGroups + upstream cancelled-demotion compose correctly); NO v0.4.20-style fork-code×upstream-test collision (matches C4 prediction) |
| `pnpm build` | ✅ 3/3 (new /wecom/bind route built) |

**Merge commit created (before Phase 6 re-stamp, per commit-first rule):** `55bf3432f` — TRUE 2-parent (ec3dea072 fork + 0dfaac266=v0.4.21 tag). `git describe --tags --abbrev=0 HEAD` = `v0.4.21` ✓; tag merged into HEAD ✓; working tree clean ✓.

## Phase 6 — Irreversible gates (DB + service)

DONE 2026-08-07 — all steps green:

1. **Backup** ✅ — pg17 pg_dump → `~/multica-backups/v0.4.21-pre-migrate-20260807-192339.dump` (649 TOC entries, valid). DB baseline 260.
2. **Migrate** ✅ — only 261/262/263/264 applied (260 prior skipped). Final DB = 264. No new collision.
3. **Version re-stamp** ✅ — `.env NEXT_PUBLIC_APP_VERSION=v0.4.20→v0.4.21`; backend `-X main.version=v0.4.21` (install.sh `git describe` resolved v0.4.21 because merge commit existed). Verified in both binaries.
4. **Restart** ✅ — `install.sh` full rebuild + both launchd plists bootstrapped; state=running active=1; ports 8081+3001 listening.
5. **Health check** ✅ — backend `/healthz`=200 (immediate, fast warmup), frontend `/`=200, `/api/config server_version=v0.4.21` on both.
6. **Daemon drift** ✅ — CLI/daemon were 0.4.20 (aligned with each other, behind backend). User confirmed brew upgrade. `brew upgrade multica` 0.4.20→0.4.21 + `multica daemon restart`. Post-restart: CLI 0.4.21 / daemon 0.4.21, 5 workspaces intact, all agents (claude/codex/opencode/openclaw/hermes) present. **Full alignment: CLI=daemon=backend=frontend=v0.4.21.**

## Phase 7 — Record

**Merge commit:** `55bf3432f` (TRUE 2-parent: `ec3dea072` fork + `0dfaac266` = v0.4.21 tag). Commit message documents each conflict's strategy (SOP Step 10 format). DB final = 264. CLI/daemon/backend/frontend all v0.4.21.

**Actuals-vs-plan comparison:**

| Metric | Predicted (Phase 1) | Actual | Delta |
|---|---|---|---|
| merge-tree conflict files | 6 | 6 | **exact** |
| merge-tree conflict hunks | 8 | 8 | exact |
| `--diff-filter=U` real conflicts | — | 6 (README, mention-suggestion, editor.json×4) | matches preview exactly |
| Phase 5 test failures | 0 new (C4) | 0 (3824 pass) | exact |
| New migrations | 261-264, no collision | 261-264 applied, DB 260→264 | exact |
| Strategy D scenario A collisions | 0 | 0 | exact |

**Where the audit was conservative (predicted conflict, actual auto-merge):**
- `issue.go buildSearchQuery` — C4/C2 flagged as a real both-edit-same-anchor conflict; git's hunk-level merge actually kept both cleanly (verified by grep). Text-merge succeeded; semantic keep-both confirmed manually.
- `cmd_issue_test.go` — flagged possible boundary conflict; auto-merged clean.

**Lesson reinforced:** merge-tree's `changed-in-both` file count (38) vs real-marker count (6) shows git's hunk-level merge resolves most file-level overlaps. The audit's job is not to predict the exact conflict count but to (a) enumerate the candidate set so nothing is a surprise, and (b) flag the *semantic* traps (issue.go, task.go, cmd_issue_test.go) that auto-merge can't validate. Both held: zero files surprised us, and all 3 semantic traps were verified keep-both.

**Stale memory corrected (C1 finding):** `project_feat_typed_project_mention_plan` recorded the typed-mention work as "ROLLED BACK 2026-07-16, do not resume." FALSE — a complete 7-type mention system (member/agent/squad/issue/project/all/skill) is live via fork `cbeddef49`; `packages/core/mention/` is fork-only. Memory updated in Phase 7.
