---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.8
base_tag: v0.4.6
merge_commit: ""
phase: gates
gates:
  user_confirmed: false
  audit_strategy_written: true
  db_backed_up: false
  migrated: false
  version_stamped_frontend: false
  version_stamped_backend: false
  services_restarted: false
  health_checked: false
negative_claims:
  - claim: "真实 merge-base 是 58162ed2e (v0.4.6-9)，待合 25 commits 非 34"
    verified_by: "git merge-base HEAD v0.4.8 → 58162ed2e; git rev-list --count HEAD..v0.4.8 → 25"
    result: confirmed
  - claim: "mention/rich-content/editor cluster 三点 merge delta 全空 (MUL-4922 5a11232c4 已在 base)"
    verified_by: "git diff --shortstat HEAD...v0.4.8 -- packages/views/rich-content/ packages/views/editor/ packages/ui/markdown/ → 空"
    result: confirmed
  - claim: "qwen runtime 是 100% upstream (非 local 添加)"
    verified_by: "git merge-base --is-ancestor f8bf6cd8b v0.4.8; 202_runtime_profile_add_qwen.up.sql + qwen-logo.svg blob hash HEAD==v0.4.8"
    result: confirmed
  - claim: "无新 migration 碰撞"
    verified_by: "ls server/migrations/ → 200/201/202 各单一 stem (up/down 成对非碰撞); upstream 新增 206-211, local 203-211 空, 不撞 head 202; 119/158/159/160 不受影响"
    result: confirmed
  - claim: "router.go 仍 D orthogonal, upstream 新路由不抢 local route group"
    verified_by: "git merge-tree --merge-base=$(git merge-base HEAD v0.4.8) HEAD v0.4.8 → router.go auto-merge clean (无 CONFLICT)"
    result: confirmed
  - claim: "web-providers version 注入点 (NEXT_PUBLIC_APP_VERSION/frontendBaseline) 未被 upstream 动"
    verified_by: "git diff MB..v0.4.8 -- web-providers.tsx 仅触及 detectWebOS; git grep officialBaseline v0.4.8 → 空"
    result: confirmed
  - claim: "types/agent.ts 双方改动在不同区域 (BatchImport ~741 ↔ disabled_runtime_skills ~445)"
    verified_by: "git diff HEAD v0.4.8 -- packages/core/types/agent.ts 区域分布"
    result: confirmed
---

# Upgrade v0.4.6 → v0.4.8

> Process artifact. Persistent customization list in `docs/customizations.md`.
> Scope confirmed with user 2026-07-23: run through **Phase 5 (verify)**; **stop before Phase 6 irreversible gate** (migrate/restart) for separate confirmation. This is a real-data test of the upgrade-upstream skill + this artifact.

## Phase 0 — Locate
- release base: `v0.4.6` (HEAD `v0.4.6-159-g7026d21ad`)
- latest upstream: `v0.4.8` (published 2026-07-22 10:24 UTC, not prerelease/draft)
- **真实 git merge-base(HEAD, v0.4.8) = `58162ed2e` (v0.4.6-9)** — 上次"v0.4.6 升级"实际 merge 的是当时的 `origin/main`，多拉了 v0.4.6 之后的 9 个 commit（含 MUL-4922 mention cluster `5a11232c4`、Qwen runtime `f8bf6cd8b`）。release 语义 base 仍是 v0.4.6。（三方确认：audit agent ×2 + `git merge-base`。）
- **真实待合 = 25 commits** (`HEAD..v0.4.8`)，不是 `v0.4.6..v0.4.8` 的 34（9 个已在 base）。
- v0.4.8 is on `origin/main`; main is 12 commits ahead of v0.4.8 → **merge the `v0.4.8` tag, not `origin/main`**.
- vs origin/main: 37 behind / 150 ahead.

## Phase 1 — Deep audit
Fan-out: 3 audit agents (mention / runtime / infra) over `HEAD..v0.4.8` (25 commits), each using `git merge-tree` / blob-hash / `cherry` to verify. All negative claims in frontmatter confirmed by command.

### Per-file strategy (by cluster)
| cluster | strategy | 冲突? | 依据 |
|---|---|---|---|
| mention/rich-content/editor (~17 files) | **A** (auto-preserve) | **0** | MUL-4922 (`5a11232c4`) 已在 base `58162ed2e`；三点 merge delta 全空。我们 re-apply 的 chips / @skill gesture / squad mention 正则自动保留 |
| runtime/daemon/qwen (~30 files) | **A** (adopt upstream) | **0** | **qwen 是 100% upstream**（`f8bf6cd8b` 是 v0.4.8 祖先；202 migration + qwen-logo.svg blob HEAD==v0.4.8）。7 文件已字节相同；`daemon.go` +125 行纯 upstream |
| `packages/core/types/agent.ts` | **C** (careful-merge) | **可能 1** | local `BatchImport*` (~741) ↔ upstream `disabled_runtime_skills` (~445)，不同区域，应 clean auto-merge |
| `packages/core/api/client.ts` | **C** (import add/add) | **1** | import 块同位置双插 (`SetAgentRuntimeSkillEnabledRequest` ↔ `BatchImportResponse`)，保留两行 |
| `server/cmd/server/router.go` | **D** (orthogonal) | **0** | upstream 新路由 (`/api/client-usage`, `/runtime-skills/enabled`) 与 local (`/api/admin`, `/import/batch`, `officialBaseline`) 不同 group；merge-tree clean |
| `apps/web/components/web-providers.tsx` | **B** | **0** | upstream 加 `detectWebOS`；local 加 `frontendBaseline` prop；version 注入点未动 |
| `packages/views/runtimes/components/provider-logo.tsx` | **B** (trivial) | **~0** | 仅差 4 行注释（`as unknown` cast 本身 upstream） |
| db generated (`models.go`/`workspace.sql.go`) | regen `make sqlc` | 0 | generated |
| migrations 206-211 (upstream 新增) | apply | 0 | local 203-211 空，不撞 head 202 |
| locales (issues/layout/modals × 4 lang) | **B** take-both | trivial | keys 不重叠 |

### Audit prediction (Phase 3 verifies against reality)
- 预期真实冲突 **~5 个，全低难度**：`client.ts`（import add/add，保留两行）、`types/agent.ts`（C，不同区域）、`app-sidebar.tsx`、`provider-logo.tsx`（4 行注释）、`skill.go`+`skill_test.go`（#2669 batch import）。
- 大头（mention cluster + qwen runtime）已在 base，零工作。

## Phase 2 — Irreversible-gate confirmation
- User confirmed through Phase 5 only. Phase 6 (DB migrate + service restart) pending separate confirmation.

## Phase 3 — Preview
`git merge v0.4.8 --no-commit --no-ff`（safety branch `main-backup-v0.4.8` 已建）。
- **实际冲突: 5** — `client.ts`、`app-sidebar.tsx`、`provider-logo.tsx`、`skill.go`、`skill_test.go`。
- **vs 预测: 命中** — audit fan-out 预测的 ~5（同集合）完全命中；`types/agent.ts` 预测"可能"→ 实际 clean auto-merge（agent 判断"不同区域应 clean"正确）。merge-tree 预演的精度经住了真实 merge 检验。

## Phase 4 — Resolve
- `client.ts`: import add/add → 保留两行（`BatchImportResponse` + `SetAgentRuntimeSkillEnabledRequest`）。
- `provider-logo.tsx`: 保留 local 4 行解释性注释（`as unknown` cast 本身 upstream）。
- `app-sidebar.tsx`: 保留 local `Shield` icon import（admin 用）；`BookOpenText` 经 typecheck 发现 unused → 删。
- `skill.go` (2 conflict, 最复杂): adopt upstream `fetchFromSkillsSh` 新签名（加 `ctx`、`parseSkillsShParts` 返回 `owner/repo/skillName`），**保留 #2669 batch import**（`importAllSkillsFromRepo`/`ImportSkillsBatch`，upstream 无等价物），适配所有调用点，删 `skillsShSpec` 死结构 + `resolveGitHubSkillDirByName` 死代码。subagent 解 + `go build`/`go vet` 自验。
- `skill_test.go`: import 保留 `encoding/json`+`errors`+`fmt`；#2669 test 适配新签名。

## Phase 5 — Verify (code gates, no DB/service)
全绿，stop-on-first-failure 全过：
- `pnpm install` ✓
- `pnpm typecheck` ✓ (6/6) — **修了 2 个 auto-merge / upstream fallout**:
  1. `BookOpenText` unused（app-sidebar）— auto-merge 后 local icon 不再用，删。
  2. `table-view-editing.test.tsx` `serverIssues` 未声明 + Harness 缺 `issues` — **v0.4.8 release 自身 typecheck 红**（#5767 加了两个 case 但声明/适配不完整；修复在 main 的 `dd45f3055` MUL-5164，release tag 漏）。手改两 case 复刻 main 修法（删 `serverIssues`、Harness 内联 `issues`），未带入 MUL-5164 的 toolbar 改动。
- `go build -C server ./...` ✓
- `pnpm test` ✓ — **2927 passed**（含修好的 table-view test）。
- `pnpm build` ✓ (3/3)。
- **Skipped**: `make test`/`make start`（需 Docker，本 host 无）。

## Phase 6 — Irreversible ops
**STOP — pending user confirmation; not part of this test run.**

## Phase 7 — Record
(tbd)
