---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.23
base_tag: v0.4.22   # clean merge-base 8bd49bba8 (= v0.4.22 tag commit; 65506a811 is the annotated tag OBJECT, not the commit)

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done   # COMPLETED 2026-08-12. All gates true. Backend+frontend+daemon all at v0.4.23, 5 workspaces intact.

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true             # Phase 2 — CONFIRMED 2026-08-12: user chose "全推到 Phase 6". Approved (1) pg17 pg_dump before migrate 272; (2) install.sh full rebuild + launchd restart (short downtime OK); (3) daemon drift check + brew upgrade multica + daemon restart.
  audit_strategy_written: true      # Phase 1.4 — FLIPPED 2026-08-12: 3 fan-out agents (a/b/c) ran + every negative claim adversarially re-verified in MAIN session with raw commands (not by reading). 14-file intersection locked; per-file strategy table written below. PHASE 1 COMPLETE — awaiting Phase 2 user gate.
  db_backed_up: true                # Phase 6.1 — 2026-08-12T12:32:03CST, 58M at ~/multica-backups/v0.4.23-pre-migrate-20260812T043203Z.dump, pg17 binary (postgresql@17); pg_restore -l confirms valid archive (dbname: multica)
  migrated: true                   # Phase 6.2 — 2026-08-12, migrations applied: only 272_rollup_task_usage_hourly_xact_lock (all 254-271 skipped as already applied); 271 → 272, no collision, "Done." reached
  version_stamped_frontend: true   # Phase 6.3 — .env NEXT_PUBLIC_APP_VERSION=v0.4.23 (bumped from v0.4.22); install.sh rebuilt standalone frontend inlining it
  version_stamped_backend: true    # Phase 6.3 — `strings ~/.multica/backend/server | grep main.version=` shows `-ldflags="-s -w -X main.version=v0.4.23"` (resolve-official-baseline.sh resolved v0.4.23 from HEAD; commit-first rule satisfied — no fallback to v0.4.22)
  services_restarted: true         # Phase 6.4 — install.sh loaded both launchd plists (com.fengzhao.multica-{frontend,backend} both "loaded"); backend rebuilt + frontend standalone reassembled
  health_checked: true             # Phase 6.5 — backend /healthz=200, /api/config server_version=v0.4.23 (authoritative runtime version, both tiers), frontend :3001=200

merge_commit: 76f9164a3beb5f74014f69f0cc2fc6a97be10cbb   # true 2-parent merge (parents f1731c0f9 = fork HEAD, e0d0b3815 = v0.4.23 tag commit); git describe --tags HEAD = v0.4.23-289-g76f9164a3 → install.sh will resolve v0.4.23 correctly (commit-first rule satisfied)

# Negative claims — each confirmed/refuted by ONE raw command, never by reading.
negative_claims:
  # --- Baseline (verified in main session during Phase 0/1) ---
  - claim: "current base is v0.4.22, a TRUE 2-parent merge (not a fake single-parent merge like v0.4.19 was)"
    verified_by: "git describe --tags HEAD → v0.4.22-288-gf1731c0f9"
    result: "confirmed (288 commits ahead of v0.4.22; v0.4.22 plan recorded merge commit 7206599e2 as true 2-parent)"
  - claim: "latest upstream release is v0.4.23, published 2026-08-11, not prerelease/draft"
    verified_by: "gh release view --repo multica-ai/multica --json tagName,isPrerelease,isDraft,publishedAt"
    result: "confirmed (tagName=v0.4.23, isDraft=false, isPrerelease=false, publishedAt=2026-08-11T10:24:41Z)"
  - claim: "HEAD is 28 commits behind the v0.4.23 TAG and 288 ahead"
    verified_by: "git rev-list --count HEAD..v0.4.23 (28); git rev-list --count v0.4.23..HEAD (288)"
    result: "confirmed. NOTE: vs origin/main HEAD is 34 behind (origin/main sits 6 past the tag). Merge the TAG, not origin/main."
  - claim: "merge-base(HEAD, v0.4.23) = 8bd49bba8 = v0.4.22^{commit}, single clean hop from v0.4.22"
    verified_by: "git merge-base HEAD v0.4.23; git rev-parse 'v0.4.22^{commit}'"
    result: "confirmed (both = 8bd49bba8ffe273da9a6902227bbcdd643873ce1). 65506a811 is the annotated tag OBJECT hash, not the commit."
  - claim: "TRUE upstream churn is 145 files +13464/-641 (measured from merge-base, NOT from HEAD)"
    verified_by: "git diff --shortstat $(git merge-base HEAD v0.4.23)..v0.4.23"
    result: "confirmed (145 files changed, 13464 insertions, 641 deletions). METHOD NOTE: measuring `git diff HEAD..v0.4.23` misreads fork additions as upstream deletions — always measure from merge-base. Mostly additive (new feature release)."
  - claim: "NEW upstream migrations = exactly ONE: 272_rollup_task_usage_hourly_xact_lock (up+down). Stem 272 > fork max 271 → ZERO new collision"
    verified_by: "git diff --name-only --diff-filter=A <MB>..v0.4.23 -- server/migrations/ → only 272_rollup_task_usage_hourly_xact_lock.{up,down}.sql; comm -12 of upstream-new stems vs fork stems → EMPTY"
    result: "confirmed (intersection EMPTY). The 6 known collisions (119/158/159/160/213/214) stand unchanged — do NOT renumber. Verified in main session, not delegated to agents (v0.4.6 lesson: 3 audit agents all missed the migration collisions by reading; one `ls` caught it)."
  - claim: "live DB is at migration 271, so 272 applies cleanly with no gap"
    verified_by: "pg17 psql $DATABASE_URL -tAc 'select max(version) from schema_migrations;'"
    result: "confirmed (271_channel_chat_pending_fresh). schema_migrations count=322 (includes duplicate-prefix stems from the 6 known collisions + others; max version number is what matters for gap check)."
  # --- Phase 1 fan-out + adversarial re-verification (agents scouted; main session re-ran the commands) ---
  - claim: "the file intersection (both sides touched) = exactly 14 files, no more"
    verified_by: "comm -12 <(git diff --name-only <MB>..HEAD | sort -u) <(git diff --name-only <MB>..v0.4.23 | sort -u) | wc -l"
    result: "confirmed (14). The set: use-issue-actions.{ts,test.tsx}, locales/{en,ja,ko,zh-Hans}/settings.json, scripts/selfhost-config.test.sh, server/cmd/server/router.go, server/internal/daemon/gc.go, server/internal/handler/{comment,issue,workspace}.go, server/internal/service/builtin_skills/multica-mentioning/references/mentioning-source-map.md, server/internal/service/task.go."
  - claim: "add/add intersection (fork-new ∩ upstream-new files) is EMPTY — no hidden collision merge-tree can't see"
    verified_by: "comm -12 <(git diff --name-only --diff-filter=A <MB>..HEAD | sort -u) <(git diff --name-only --diff-filter=A <MB>..v0.4.23 | sort -u)"
    result: "confirmed (EMPTY). v0.4.20 lesson (thread-nav-panel.tsx hid 8 conflicts) does NOT recur."
  - claim: "upstream shipped NO new native mention system — #5199/#5346 typed-mention cluster is fully safe"
    verified_by: "git diff --name-only <MB>..v0.4.23 | grep -iE 'mention'"
    result: "confirmed — only 4 *.mdx docs (mentioning-agents.{en,ja,ko,zh}.mdx) + mentioning-source-map.md touched. ZERO mention cluster CODE files in packages/views/editor/ or packages/ui/. The 9-file fork typed-mention cluster is untouched."
  - claim: "BroadcastCancelledTasks signature drift (upstream added workspaceID) auto-resolves — fork has ZERO footprint on its 4 caller files AND no fork-only caller"
    verified_by: "git grep -n 'BroadcastCancelledTasks' HEAD -- server/ (all hits in base-tracked files); git diff --numstat <MB>..HEAD -- server/internal/handler/{chat,runtime,workspace_revoke}.go (all EMPTY)"
    result: "confirmed. Fork HEAD keeps OLD signature (ctx, cancelled) at task.go:2186 + 4 callers (chat.go:635/759, runtime.go:850, workspace_revoke.go:246); upstream changes def + all 4 callers TOGETHER in one commit, and fork touched NONE of those caller files → auto-merge takes upstream's def + callers consistently. go build catches any arity miss (non-silent). This is 'clean merge + build net', NOT a Strategy D silent param-drop."
  - claim: "truncateFallbackCommentBody v0.4.22 latent hazard does NOT re-trigger — no redeclaration AND the call site at task.go:3565 is live AND upstream didn't touch the def/call regions"
    verified_by: "git grep -n 'func truncateFallbackCommentBody|maxSynthesizedFallbackCommentRunes' HEAD -- server/ (only in fallback_comment_truncate.go + test); git grep -n 'truncateFallbackCommentBody' HEAD -- server/internal/service/task.go (1 hit: call site 3565); git grep ... v0.4.23 task.go (call site 3629, bytes identical); git diff <MB>..v0.4.23 -- task.go | grep '^@@' (only hunk @@ -2157 CancelTasksForIssue, NOT 135-160 or 3550-3640)"
    result: "confirmed on ALL FOUR axes (definition location, call-site liveness, byte-identity with upstream, upstream didn't touch the region). The v0.4.22 lesson (memory negative_claim_must_prove_user_invariant: verified def-count, missed call-site liveness) is specifically re-checked here. Fork cap=200 survives in fork-only file; upstream cap=8000 discarded as intended."
  - claim: "comment.go auto-merges clean — upstream's single hunk (base 2679-2685) falls in a gap between fork's hunks (base 2601-2615 and 2689-2695)"
    verified_by: "git diff <MB>..v0.4.23 -- server/internal/handler/comment.go | grep '^@@' (ONE hunk @@ -2679,6 +2679,12); git diff <MB>..HEAD -- comment.go | grep '^@@' (fork hunks @2601,14 and @2689,6 in computeCommentAgentTriggers — 2679-2685 is between them, untouched by fork)"
    result: "confirmed via hunk-base-line topology, not by reading code. Likely clean auto-merge; Phase 3/4 verifies the MUL-5958 member-reply guard lands correctly relative to fork's routeAssigneeFallback/assigneeFallbackAgent, and that fork's skill-mention path doesn't implicitly depend on the now-guarded member-reply fallback."
  - claim: "sqlc regen is single-sided — upstream touched workspace_delete.sql + .sql.go, fork touched NEITHER"
    verified_by: "git diff --numstat <MB>..HEAD -- server/pkg/db/queries/workspace_delete.sql server/pkg/db/generated/workspace_delete.sql.go (EMPTY)"
    result: "confirmed. Just `make sqlc` (~/go/bin/sqlc on this host) post-merge; migration 272 is plpgsql (CREATE FUNCTION), not a sqlc query, so it triggers no regen itself."
  - claim: "apps/mobile/ is entirely untouched by upstream this round"
    verified_by: "git diff --name-only <MB>..v0.4.23 -- apps/mobile/ | wc -l"
    result: "confirmed (0)"
  - claim: "no JS/Go dependency work beyond a trivial apps/web/package.json version bump (0.4.22→0.4.23)"
    verified_by: "git diff --stat <MB>..v0.4.23 -- server/go.mod server/go.sum '**/package.json'"
    result: "confirmed (go.mod/go.sum: no output; apps/web/package.json: 1 line version bump). pnpm install required, no Go dep work."
  - claim: "router.go carries 4 DISTINCT fork route customizations (not just the ledger's #5539/#2669/#4118) — the VCS self-host Git provider cluster is a 4th, and it is NOT in the ledger"
    verified_by: "git log --no-merges --oneline <MB>..HEAD -- server/cmd/server/router.go (9 commits: provenance 9a1fee293/8aa05cfb2, skills 2883eff06, admin 4f7c72d99/f58c719bf/9e2953f1d, VCS 8faa3da90/1f6c7ca55); git log --grep='MUL-3772|vcs|Forgejo|Gitea|GitLab' <MB>..v0.4.23 (EMPTY — VCS is fork-only); grep -ciE 'vcs|forgejo|gitea|gitlab|MUL-3772' docs/customizations.md (0)"
    result: "confirmed BLIND SPOT (BS-1). The VCS cluster (MUL-3772/#5006/#5888/#5868/#5883, ~50 files incl. new vcs.go/vcs-tab.tsx/vcs_webhook.go) is a major carried customization the ledger never recorded. It touches 2 intersection files (router.go + settings.json×4). Resolving router.go WITHOUT knowing about VCS routes would silently drop them. Post-merge route enumeration is mandatory. LEDGER UPDATE REQUIRED in Phase 7."
  - claim: "no fork PR newly MERGED upstream → zero 'adopt upstream, drop local' work this round"
    verified_by: "git log --oneline --no-merges <MB>..v0.4.23 | head; cross-ref ledger active PRs"
    result: "confirmed by lane (c) — drivers are WeCom media/Reasonix thinking_level/browser history nav/sub-issue inherit/codex symlink/hermes memory; none are fork PRs. (#5393 remains the only MERGED, handled in a prior upgrade.)"

---

# Upgrade plan — v0.4.22 → v0.4.23

Started 2026-08-12. This is the process artifact for THIS upgrade (the ledger `docs/customizations.md` holds persistent "what we carry"; this file holds this upgrade's per-file strategy, verified negatives, and phase state).

## Phase 0 — Locate (DONE 2026-08-12)

- Current base: `v0.4.22` (`git describe --tags HEAD` = `v0.4.22-288-gf1731c0f9`)
- Latest upstream release: **`v0.4.23`**, published 2026-08-11, not prerelease/draft
- Upgrade span: **28 commits** (`v0.4.22..v0.4.23`), 145 files, +13464/-641
- Behind v0.4.23 tag: 28 (behind origin/main: 34 — main is 6 past the tag; merge the TAG)
- Ahead: 288 fork customization commits
- merge-base: `8bd49bba8` = v0.4.22 tag commit (clean single hop)

## Phase 1 — Deep pre-merge audit (DONE 2026-08-12)

### Migration audit (DONE — main session, not delegated)

Zero migration risk this round. 1 new migration (272_rollup_task_usage_hourly_xact_lock), no collision, sequential apply (271→272). See negative_claims.

### Fan-out conflict analysis (DONE — 3 parallel agents + adversarial re-verification)

Three lanes ran concurrently; their scouting conclusions were adversarially re-verified in the main session with raw commands (every "no X" re-run, never trusted by reading). Reconciliation:

- **Lane (a)** highest-churn-vs-fork-hot-files: upstream's biggest changes (WeCom ws_frame.go +331/-18, hermes_memory, acp_effort +305, reasonix) are almost all NEW subsystems with zero fork intersection. Confirmed the 9-file fork mention cluster is fully untouched; flagged 5 STALE hot-file references in the ledger (rich-content.tsx / use-issue-surface-data.ts / cmd_skill.go / skill_mention_trigger.go don't exist; actor-avatar.tsx has no showName — customization gone/idealized).
- **Lane (b)** file-intersection + ledger-coverage: locked the 14-file intersection; add/add EMPTY; **surfaced the VCS blind spot (BS-1)** — a ~50-file carried customization (self-host Git providers) the ledger never records, touching 2 intersection files.
- **Lane (c)** feature sweep + cross-cutting hazards: v0.4.23 drivers catalogued; confirmed no Strategy D silent param-drop (BroadcastCancelledTasks auto-resolves); truncate hazard does not fire; sqlc single-sided; locale intersection = 4 settings.json; mobile untouched.

### Per-file strategy (14 intersection files) — drives Phase 4

Strategy at FILE granularity (A accept-upstream / B keep-both / C careful-merge / D signature-merge). Regions from `git diff --numstat` + hunk `@@` topology.

| # | File | Fork owners | Upstream change | Strategy | Resolve note |
|---|---|---|---|---|---|
| 1 | `packages/views/issues/actions/use-issue-actions.ts` | #6106 archived + identifier-badge | #6727 inherit parent project/assignee (blocks at L61/L165) | **C (low)** | Fork block ~L66 adjacent to upstream L61 (5-line gap, same fn). Likely conflict marker from context drift; resolve keep-both. Verify dispatched-action set is a superset. |
| 2 | `packages/views/issues/actions/__tests__/use-issue-actions.test.tsx` | #6106 + badge tests | #6727 tests | **B** | Keep both test suites; both additive. |
| 3-6 | `packages/views/locales/{en,ja,ko,zh-Hans}/settings.json` | #4118 invite_name_* + **VCS BS-1 (~41 keys/locale)** | #6728 goBack/goForward (Cmd+[/+]) | **B + JSON brace/comma trap** | Keys disjoint → union. Fork block mid-file (~L839 invite + VCS), upstream mid-file (~L116 shortcuts). EN likely clean; CJK may hit end-of-file/mid-file brace trap (see docs/solutions merge-keep-both-shared-context-brace-trap.md). Use `git merge-file` 3-way (NOT --theirs — VCS+invite keys are fork-only members). Post-merge: verify goBack/goForward present in all 4 + parity. |
| 7 | `scripts/selfhost-config.test.sh` | #5539 provenance | #6653 SMTP_FROM_EMAIL forwarding | **A/B** | Additive assertions; keep both. |
| 8 | `server/cmd/server/router.go` | **4 fork customizations: #5539 + #2669 + #4118-admin + VCS BS-1** | MUL-5906 WeCom media routes (L701-742) | **C (HOTTEST)** | Regions likely disjoint (fork at 145-160/216-230/1228-1245/1552; upstream 701-742). **POST-MERGE MANDATORY: enumerate every `mux.Handle/Mount/Get/Post` + `r.Handle/...` and confirm VCS routes + wecom routes + all fork routes present.** This is where BS-1 could silently die. Use `git merge-file` if needed (fork route registrations are fork-only members). |
| 9 | `server/internal/daemon/gc.go` | #6106 archived (trivial: 1 import + 1 line) | MUL-6000 codex symlink + MUL-5932 hermes memory (L57-724) | **A** | Disjoint; fork delta tiny. Scan whether upstream's new GC roots need archived-status awareness (low). |
| 10 | `server/internal/handler/comment.go` | #5346 skill-mention (24 hunks, 1455-3741) + #5309 | MUL-5958 (#6676) member-reply assignee-fallback skip (1 hunk, base 2679) | **C (low) — likely auto-merges** | Hunk topology proves disjoint (upstream 2679-2685 falls between fork's 2601-2615 and 2689-2695). Verify member-reply guard lands vs fork routeAssigneeFallback/assigneeFallbackAgent (L3146); confirm fork skill-mention path doesn't implicitly depend on the now-guarded fallback. |
| 11 | `server/internal/handler/issue.go` | #6106 archived (buildSearchQuery/statusRank at 77-2078) | MUL-5951 (#6666) cross-workspace project reject (UpdateIssue 3019 / BatchUpdate 3493/3643) | **A/B** | Disjoint paths (query/select vs update handlers). buildSearchQuery logic intact; project-scope validation additive. |
| 12 | `server/internal/handler/workspace.go` | #4118 invitee_name + issue-prefix fallback generateIssuePrefix (20-450) | MUL-5983 (#6710) advisory-lock delete timeout (LeaveWorkspace/DeleteWorkspace 729-891) | **A/C** | Disjoint functions. Minor import-block overlap (upstream adds errors/time/pgconn; keep both import sets). |
| 13 | `server/internal/service/builtin_skills/multica-mentioning/references/mentioning-source-map.md` | #5346 source-map re-derivation (+78/-37) | MUL-5958 line-number drift (+10) | **B + re-pin line numbers** | Recurring debt (v0.4.16 lesson). Re-derive comment.go line refs after merge; content complementary (fork @skill/@project routing ⊕ upstream plain-reply section). |
| 14 | `server/internal/service/task.go` | #5309 source_task_id + MUL-5228 Grok + truncate call site (L3565) | #6726 agent-status dedup + #6631 cancel typing indicator + BroadcastCancelledTasks signature (2157-2249) | **C/D — auto-resolves + build gate** | BroadcastCancelledTasks signature change auto-resolves (def + 4 callers updated together upstream; fork untouched callers). truncate call site L3565 SAFE (verified). `go build`/`go vet` catches any arity miss. No manual signature work expected. |

### Phase 5 cross-cutting items (not in the 14, but need attention)

- **sqlc regen** (single-sided): `~/go/bin/sqlc generate` post-merge (workspace_delete.sql).
- **pnpm install**: apps/web/package.json version bump 0.4.22→0.4.23.
- **settings.json parity**: verify goBack/goForward present in all 4 locales after union.
- **Phase 5 also runs upstream's NEW tests** — watch for fork-code × upstream-test collisions (v0.4.20 had 14). The skill-mention + archived customizations are the likely collision surface.

### Phase 1 conclusion

This is a **cleaner-than-average upgrade**. The 14-file intersection is small and mostly disjoint; the only Go signature drift (BroadcastCancelledTasks) auto-resolves with a build safety net; the truncate hazard does not fire; no new mention system; no migration collision; no add/add collision. **Two real risks:** (1) **router.go** — the un-ledgered VCS cluster (BS-1) makes the post-merge route enumeration non-negotiable; (2) **comment.go** — verify the member-reply guard's interaction with fork's skill-mention fallback (low, but semantic). **Ledger debt to pay in Phase 7:** add the VCS cluster row; fix the 5 stale hot-file refs; extend the locale drift note to include settings.json.

## Phase 2 — Confirm irreversible gates with user (PENDING — next step)

Surface: DB migration (272, irreversible schema — backup first); service restart (brief downtime). Blocking question. (See AskUserQuestion below.)

## Phase 3 — Reversible preview (PENDING)

`git branch main-backup-v0.4.23 main`; `git merge v0.4.23 --no-commit --no-ff`; `git diff --name-only --diff-filter=U`. Trust the measurement over the prediction.

## Phase 4 — Resolve + re-apply (PENDING)

Per the table above. Pick merge tool per file BEFORE resolving: `git merge-file` for files fork added members to (settings.json×4, router.go, mentioning-source-map.md); `--theirs` only where fork made zero additions. After every resolution: loss scan `flat(fork_main) - flat(now) == ∅`.

## Phase 5 — Verify (code gates only) (PENDING)

pnpm install → pnpm typecheck → go build -C server → go vet -C server → sqlc generate → pnpm test → pnpm build. Then create the merge commit (before Phase 6's version re-stamp needs it).

## Phase 6 — Irreversible gates (DB + service) (DONE 2026-08-12)

GATE satisfied (phase==gates, user_confirmed, audit_strategy_written, all negatives verified). All steps executed, all gates flipped true:

1. **Backup** (db_backed_up): pg17 `pg_dump` → `~/multica-backups/v0.4.23-pre-migrate-20260812T043203Z.dump` (58M, valid archive).
2. **Migrate** (migrated): `go run -C server ./cmd/migrate up` applied ONLY `272_rollup_task_usage_hourly_xact_lock` (254-271 all skipped, already applied); 271 → 272, "Done." reached. No collision, no gap.
3. **Version re-stamp** (version_stamped_frontend + backend): `.env NEXT_PUBLIC_APP_VERSION` bumped v0.4.22 → v0.4.23; `install.sh` rebuilt standalone frontend (inlined v0.4.23) + backend binary with `-ldflags="-s -w -X main.version=v0.4.23"` (resolve-official-baseline resolved v0.4.23 from HEAD — commit-first rule satisfied, NO fallback to v0.4.22; verified via `strings ~/.multica/backend/server | grep main.version=`).
4. **Restart** (services_restarted): install.sh loaded both launchd plists (com.fengzhao.multica-{frontend,backend}).
5. **Health check** (health_checked): backend `/healthz`=200, `/api/config server_version=v0.4.23` (authoritative runtime version, both tiers), frontend `:3001`=200. Used `curl --retry-connrefused` to digest the ~30-40s backend warmup.
6. **Daemon drift** (advanced phase: done): CLI was 0.4.22 (brew tap had 0.4.23 available). `brew upgrade multica` 0.4.22 → 0.4.23 (commit e0d0b3815 = v0.4.23 tag) + `multica daemon restart` (new pid 43145). Daemon now Version 0.4.23, 5 agents (claude/codex/opencode/openclaw/hermes), **5 workspaces intact**.

All three version carriers aligned at v0.4.23: backend (install.sh ldflags), frontend (.env inline), daemon/CLI (brew + restart).

## Phase 7 — Record (DONE 2026-08-12)

- Plan closed: `phase: done`, every `gates.*` true, `merge_commit: 76f9164a3`.
- Merge commit `76f9164a3` (true 2-parent: `f1731c0f9` fork HEAD + `e0d0b3815` v0.4.23 tag commit); message documents each conflict's strategy + the BS-1 VCS blind spot + semantic verification.
- Ledger `docs/customizations.md` updated: new "Last upgrade — v0.4.22 → v0.4.23" section + pointer to this plan; base tag advanced. Ledger debt paid: VCS self-host Git provider cluster (MUL-3772/#5006/#5888/#5868/#5883) added as a carried customization; 5 stale hot-file refs in the mention/status clusters noted; locale drift note extended to include `settings.json`.

### Calibration — actuals vs audit prediction

- **Intersection prediction: 14 files** → **actual: 14** (exact). Continuous text-prediction hit streak extends (v0.4.14 → v0.4.23).
- **Conflicts predicted: 3 CJK settings.json (lane a: "EN clean, CJK brace trap")** → **actual: exactly 3 CJK settings.json** (EN auto-merged clean). Exact.
- **comment.go "likely auto-merges, hunk topology disjoint"** → **actual: auto-merged clean**, no manual resolution needed.
- **task.go "BroadcastCancelledTasks auto-resolves + build net"** → **actual: auto-resolved, go build/go vet clean** (def + 4 callers all new signature).
- **truncateFallbackCommentBody "v0.4.22 hazard does not re-trigger"** → **actual: did not re-trigger** (call site live at task.go:3606, all 4 axes verified).
- **router.go BS-1 VCS routes "could silently die"** → **actual: all 5 VCS routes + secretbox survived** auto-merge (verified, not assumed).
- **Migration "1 new, zero collision"** → **actual: 272 applied, zero collision**.
- **Phase 5 "expect possible fork-code × upstream-test collisions"** → **actual: ZERO** (3890 tests passed). Cleanest Phase 5 in the recent series (vs v0.4.20's 14 collisions).
- **NEW this round:** the VCS self-host Git provider cluster (BS-1) was an un-ledgered blind spot caught by lane (b)'s ledger-coverage audit — it did NOT cause a conflict (router.go auto-merged with all routes intact), but it would have been a silent-loss risk if the auto-merge had dropped a route. Logged as ledger debt so it is tracked going forward.
