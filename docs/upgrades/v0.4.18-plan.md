---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.18
base_tag: v0.4.17

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done  # Phase 6 all irreversible gates fired (db/migrate/stamp/restart/health all green)

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true        # Phase 2 done — user authorized 一路执行到 Phase 6（不再阻塞复问）
  audit_strategy_written: true   # Phase 1.3 done — per-file strategy + all negative_claims verified (4 agents, ~35 claims)
  db_backed_up: true          # Phase 6.1 — pg17 dump ~/multica-backups/v0.4.18-pre-migrate-20260805-093744.dump (51M)
  migrated: true             # Phase 6.2 — 252_agent_builder_draft up (251- 既有全 skip)
  version_stamped_frontend: true   # Phase 6.3 — .env NEXT_PUBLIC_APP_VERSION=v0.4.18
  version_stamped_backend: true    # Phase 6.3 — install.sh ldflags -X main.version=v0.4.18 (resolve-official-baseline.sh: git describe --tags --abbrev=0)
  services_restarted: true    # Phase 6.3 — install.sh reloaded com.fengzhao.multica-{frontend,backend} plists
  health_checked: true        # Phase 6.4 — /healthz 200, / 200, /api/config server_version=v0.4.18

merge_commit: 0d1994332

# Negative claims the audit made — each confirmed/refuted by one raw command, not by reading.
# Core 10 below; full ~35-claim table (4 clusters) in body "Phase 1 — Verified negative claims".
negative_claims:
  - {claim: "upstream 252_agent_builder_draft 与 fork migration 零撞号", verified_by: "ls migrations + git diff --diff-filter=A v0.4.17..v0.4.18 -- server/migrations/", result: "confirmed — 252>fork max 251；6 已知撞号(119/158/159/160/213/214)完好"}
  - {claim: "migration 100/101/103/104 无 SQL 语义改动", verified_by: "git show c1ec1f464 -- <4 files> | grep ^[-+@]", result: "confirmed — 全 -- 注释删 RFC 指针"}
  - {claim: "triggerTasksForComment upstream 签名 v0.4.17==v0.4.18 未变（Strategy D 不触发）", verified_by: "git diff --stat v0.4.17..v0.4.18 -- server/internal/handler/comment.go", result: "confirmed — 整文件 0 lines"}
  - {claim: "task.go truncate 三符号无 redeclaration（v0.4.12 教训不复现）", verified_by: "git cat-file -p <merged>:server/internal/service/task.go | grep truncate", result: "confirmed — fork deletion 干净赢，fork own file 唯一定义；utf8 import 保留"}
  - {claim: "multica-mentioning SKILL.md upstream 未碰（daemon brief A∩B=∅）", verified_by: "git diff --name-only v0.4.17..v0.4.18 -- .../builtin_skills/multica-mentioning/", result: "confirmed — 空"}
  - {claim: "CLAUDE.md ## 用户偏好节不在冲突区", verified_by: "git merge-tree + git diff v0.4.17..HEAD -- CLAUDE.md", result: "confirmed — 顶部 upstream 零触碰，唯一冲突在 Project Shape"}
  - {claim: "fork 本地 docs 100% 不受 upstream 17 删影响", verified_by: "upstream 17 删项 vs git ls-tree HEAD docs/ 逐一比对", result: "confirmed — 0 命中；14 fork 副本 blob-equal baseline"}
  - {claim: "fork #4118 CLI 文件未被 upstream 碰", verified_by: "git diff --name-only v0.4.17..v0.4.18 -- server/cmd/multica/", result: "confirmed — 仅 cmd_attachment/cmd_issue 系列"}
  - {claim: "#5539 provenance 零回退零碰撞", verified_by: "git cat-file -p <merged>:router.go | grep ServerVersion + ls-tree provenance_baseline.go", result: "confirmed — ServerVersion L218 保留，own file blob 不变"}
  - {claim: "bindAndEnqueueSkillMentions/parseSkillMentionAgents fork-only，upstream 不存在", verified_by: "git diff v0.4.17..v0.4.18 -S ... -- server/", result: "confirmed — 双空"}
---

# Upgrade v0.4.17 → v0.4.18

> Process artifact. Persistent customization list in `docs/customizations.md`.
> 36 upstream commits (v0.4.17..v0.4.18), 222 files changed (+10907/-11042), published 2026-08-04 10:03 UTC.
> Deletions (11042) ≈ additions → 本轮含**重构/搬迁**类改动（纯 feature 通常净增），是 fork 定制高危区。

## Phase 0 — Locate

- base: `v0.4.17` (HEAD `v0.4.17-273-gbec6e2042`)
- target: `v0.4.18` (Latest, published 2026-08-04 10:03 UTC; non-prerelease, non-draft)
- to-merge: **36 commits** (v0.4.17..v0.4.18), **222 files**
- vs origin/main: 40 behind / 273 ahead (origin/main 在 v0.4.18 之后还有 4 个未发布 commit，**不纳入本次 merge** —— self-host 只跟 release tag)
- merge-base(HEAD, v0.4.18) = `6b56bc05a` = v0.4.17 tag commit ✅ 干净（fork 未二次修改 v0.4.17 内容）
- working tree: clean

## Phase 1 — Deep audit

### 36 commits 主题分布（预读）

| 主题 | commits | MUL / 备注 |
|---|---|---|
| docs/CLAUDE.md 结构重构 | 3 | 6f14c3004 (fold design.md→CLAUDE.md MUL-5698), c1ec1f464 (cut docs/ English default MUL-5698, 还扫到 migration 100/101/103/104), e9ea05e38 (@reui registry) |
| changelog/release | 1 | b312e1cbb (MUL-5716) |
| daemon brief/metadata refactor | 5 | dc6042936 / 766c7a7fa / e92d828f0 / 3c6176cbc / 459dd90f8 (MUL-5442 串) |
| daemon injected-instruction 修复 | 1 | 84fe84cd9 (MUL-5696 conflict audit) |
| agent-builder draft/session | 4 | a5c1d4470 / 736fbc8a5 (MUL-5685) / 9fb46adc4 (MUL-5642) + migration 252 / eea687d46 (MUL-5686 chat resume) |
| issues quick-actions / unsubscribe | 2 | 646101ba5 (MUL-5588 reply composer /menu), 556661207 (MUL-5710 unsubscribe) |
| server prompt language | 2 | 757b09c80 (remove lang seeds MUL-5689), 2f431cd74 (follow user lang in chat quick actions MUL-5689) |
| Revert pair (net zero) | 2 | d60bc63f9 (revert MUL-5516 re-trigger), 11e2906fe (revert mobile chat refresh) |
| channel / runtime / squad | 4 | fe62be266 / 671a1a0c2 (channel), 0940a3d0d (runtime names), f5275597c (MUL-5677 squad) |
| misc UI/CLI/test fixes | 12 | e4537bf57 / 210482476 / 18cd750f8 / 81030d46a / 55dc302d0 / 1251cbca0 / e422b169a / e6a0d6f1a / 547fcca06 / 04c48cac6 / 738217c27(stale) / 2f431cd74(已计) 等 |

### merge-tree 实测冲突（4 文件，全 content）

merge-base = `6b56bc05a` == v0.4.17 tag commit ✅
read-only `git merge-tree --write-tree --name-only HEAD v0.4.18`：

| 文件 | 冲突块 | 触发 commit | 详细分析 |
|---|---|---|---|
| `CLAUDE.md` | content | 6f14c3004 + c1ec1f464 | 见 Agent 1 报告（fold design.md rules + English default 命中 fork `## 用户偏好` 节） |
| `packages/core/issues/config/index.ts` | content | 待 Agent 2 | fork #6106 archived vs upstream config 改动 |
| `packages/views/issues/components/reply-input.tsx` | content | 646101ba5 (MUL-5588) | fork quickActionMenu/skillMentionContext vs upstream reply composer /menu |
| `server/cmd/multica/cmd_issue_test.go` | content | 待 Agent 3 | fork CLI 测试 vs upstream |

### Auto-merging 但需 spot-check 的文件（Strategy D 盲区候选）

merge-tree 报告 auto-merge（无文本冲突，但 auto-merge ≠ 语义正确；typecheck/go build 才抓得到）：

| 文件 | upstream commit | fork 定制 | 关注点 |
|---|---|---|---|
| `server/internal/service/task.go` | ? | truncateFallbackCommentBody 移出（v0.4.12 教训） | upstream 是否实现同名符号？fork own file 保留/drop？（Agent 4） |
| `server/internal/handler/comment.go` | 2f431cd74 (MUL-5689) | triggerTasksForComment 9 参（Strategy D） | upstream 是否再改签名？（Agent 2/4） |
| `server/internal/handler/quick_action.go` | 2f431cd74 | fork v0.4.16 签名合并区 | 同上 |
| `server/cmd/server/router.go` | ? | #5539 ServerVersion provenance | upstream 是否碰 provenance 行？（Agent 4） |
| `packages/core/api/client.ts` | ? | #5539 2-line + EMPTY_BATCH_IMPORT_RESPONSE | （Agent 4） |
| `server/pkg/db/generated/models.go` | migration 252 | sqlc 生成 | auto-merge OK（Agent 4 确认） |
| `packages/views/issues/components/comment-input.tsx` | 646101ba5 | quickActionMenu + skillMentionContext | upstream MUL-5588 prop 冲突？（Agent 2） |
| `apps/desktop/src/renderer/src/routes.tsx` | ? | desktop 路由 | auto-merge spot-check |
| 多个 `*.test.tsx` (comment-composers / issue-detail / issues-page / swimlane) | ? | fork 测试 | auto-merge，断言语义查 |
| locales ×8 (en/ja/ko/zh-Hans issues+skills) | MUL-5588/5710/5442 | fork #5346/#6106 trigger copy | B take-both key sets |

### Per-file strategy

#### 簇：CLI / cmd_issue — Agent 3 ✅

- `server/cmd/multica/cmd_issue_test.go` — **B KEEP-BOTH**。两侧 EOF 各追加独立 test 函数：fork `TestValidateIssueStatusArchived`（#6106 archived 守护，commit `074d4c755`）/ upstream `TestIssueCommentListHelpCarriesReadContract`（MUL-5442 `--recent`/`--before` read-contract 守护，commit `766c7a7fa`）。函数名/被测对象/断言零重叠，均不引入新 import（只用 base 已导入的 `strings`+`testing`）。两函数都留，顺序无所谓。
- `server/cmd/multica/cmd_issue.go` — auto-merge 干净且**语义正确**（merge-tree 实证 `Auto-merging` 无 CONFLICT）。fork 改 `issueStatusCmd.Long`+`validIssueStatuses`+`runIssueChildren`(→`issueguard.IsCompletedStatus`)+import；upstream 改 `issueCommentListCmd` 的 `--recent`/`--before` flag help —— 不同 cobra command、零行邻近。无需手工干预。
- fork #4118 CLI 定制（`cmd_admin.go`/`cmd_workspace.go`/`main.go`/`cmd_skill.go`）**全部未被 upstream 碰**，零返工。`cmd_attachment*.go` upstream-only（MUL-5696），fork 零改动，直接吃 upstream。

#### 簇：quick-action / config / reply — Agent 2 ✅

- `packages/core/issues/config/index.ts` — **B 两侧并列保留**。整个文件仅 2 行（status export + priority export），任一方改动整段即撞。fork 把 status 单行扩成多行含 `BOARD_STATUSES`/`CLOSED_STATUSES`/`COMPLETED_STATUSES`/`isClosedStatus`/`isCompletedStatus`（#6106 archived 配套）；upstream 加 `PRIORITY_DISPLAY_ORDER` 到 priority 行（MUL-5666 `e4537bf57`，拆 sort rank vs display order）。两侧正交，合并两行各取并集。`priority.ts` 配套 auto-merge 干净带上（fork 未改）。
- `packages/views/issues/components/reply-input.tsx` — **B 两侧并列保留**。3 处冲突 hunk（imports 区 / `uploadGate` 后插点 / `<ContentEditor>` prop 区），特性正交：fork #5346 skill-mention+草稿持久化（imports `useWorkspaceId`+`useSkillDesignatedPreviewAgents`，state 加 skillMention 草稿块，`onSubmit` 加第 4 参 `skillMentionAgents`，ContentEditor 挂 `skillMentionContext`）；upstream MUL-5588 `646101ba5`（imports `useQuickActionMenu`，加 `const quickActionMenu`，ContentEditor 挂 `quickActionMenu`）。合并：import 三行全留、两块插点都留、ContentEditor 同时挂 `quickActionMenu={...}` + `skillMentionContext={{...}}`。
- **残留测试风险（Phase 5 必跑）**：upstream 同步扩 `comment-composers.test.tsx`(+107, MUL-5588 参数化测试覆盖两 composer)。fork 已给 reply-input `onSubmit` 加第 4 参 `skillMentionAgents` → 该测试可能撞 fork 改过的接口。合并后 `pnpm test packages/views/issues/components/comment-composers.test.tsx` 验证。

#### Strategy D 签名盲区 — Agent 2 ✅ **本轮不触发**

- `triggerTasksForComment`（fork `comment.go:1984`，method 10 参含 ctx / 9 业务参）— upstream **整个 comment.go 一行没改**，`-S` log 空、diff `+` 行空、调用点零增删。**go build 兜底针对此签名不必要**。
- `bindAndEnqueueSkillMentions`(`comment.go:2072`) / `parseSkillMentionAgents`(`handler.go:538`) / `bindDesignatedSkillsForTriggers`(`comment.go:2217`) — upstream 全 silent（`-S` + `grep ^+` 双空）。
- fork 三处 `triggerTasksForComment` 调用点（`quick_action.go:917` / `comment.go:1943` CreateComment / `comment.go:3560` UpdateComment）全传满 10 参，无 arity 风险。
- **MUL-5689（`2f431cd74`）落点澄清**：改 `chat_quick_actions_generate.go`（chat 会话内 LLM follow-up pill 语言跟随），与 issue-sidebar `handler/quick_action.go`（MUL-5465 preset）是两个独立机制，fork 未定制前者 → 干净 follows upstream。
- `comment-input.tsx` / `handler/quick_action.go` — upstream **完全未改**，fork 双 prop 结构安全。

#### 簇：CLAUDE.md / docs / AGENTS.md — Agent 1 ✅

- `CLAUDE.md` — **C careful-merge，仅 1 处 3 行加法并集**。upstream 3 commit 碰（`e9ea05e38` @reui + `c1ec1f464` cut + `6f14c3004` design.md 折进；注意 task 背景少算了 `e9ea05e38`）。**唯一冲突块**在 Project Shape bullet list `packages/tsconfig/` 之后：fork 加 `docs/solutions/` + `CONCEPTS.md` 两条，upstream 加 `packages/eslint-config/` 一条 → **三条都留**，建议 `packages/eslint-config/` 紧跟 `packages/tsconfig/`（保 packages/* 分组）再接 fork 两条。
  - **`## 用户偏好` 整节不在冲突区**（文件顶部，upstream 零触碰）→ 自动干净合并。**fork 最关键治理节安全无虞**（adversarial verify 推翻了预判）。
  - upstream design.md 折进内容（UI Rules 3 条）+ ReUI registry（2 条）+ `apps/docs/` bullet 全部落 fork 未碰区域 → 自动到达。
- `AGENTS.md` — auto-merge **VERIFIED CLEAN**（merge-tree `Auto-merging` 无 CONFLICT；merged blob `ed90640208:AGENTS.md` 实测两侧改动全保留）。fork 加 `### Knowledge Store` 子节，upstream 补架构 bullet，非重叠。无需手工。
- **docs/ 裁剪**：upstream 删 17 个 docs 文件，fork 影响 **0**。fork 现有 14 个被删副本全 blob-equal v0.4.17 baseline → modify/delete 不触发，干净删除生效（含 `docs/design.md`，内容已折进 CLAUDE.md）；3 个 both-deleted no-op。**fork 本地目录**（docs/solutions, docs/upgrades, docs/customizations.md, docs/plans, docs/ideation, docs/brainstorms）**100% 不受影响**。
- **migration 100/101/103/104**：纯 `--` 注释清理（删指向已删 `docs/timezone-architecture-rfc.md` 的指针），**零 SQL 语义改动**（commit msg 明言 runner 按 filename 而非 checksum 跟踪）；fork 零改动 → auto-merge 取 upstream，零风险。

#### 簇：task.go / daemon / provenance / 全局签名 sweep — Agent 4 ✅

- `server/internal/service/task.go` + `fallback_comment_truncate.go` — **auto-merge 干净，无 redeclaration**。merged task.go 中 truncate 三符号缺位（fork deletion 干净赢），fork own file（cap=200）原样在 → 全包唯一定义。`unicode/utf8` import 保留（L4685 仍用 `utf8.RuneCountInString`）→ **v0.4.12 教训不复现**。upstream 一直就自己实现 truncate（cap=8000，文案更友好），是 fork-only 决定用更保守的 cap=200 — 产品决策非合并问题，本轮无 action。
- **post-merge action（Phase 5）**：task.go 引入 `lockChatSessionForTaskWrite`（MUL-5686 `eea687d46`），调 sqlc 生成方法 `qtx.LockChatSessionForTask`/`AdvanceCancelledChatSessionPointer`。merged tree 已带 upstream 生成代码，Phase 5 先 `go build ./...`，若报 undefined 再 `make sqlc` 重生成（fork 有 `skill.sql` 定制，需确保生成代码一致）。
- **全局 Strategy D 签名 sweep — 零命中**：`triggerTasksForComment`（upstream 签名 v0.4.17==v0.4.18，comment.go 整文件 0 改动）/ `bindAndEnqueueSkillMentions`+`parseSkillMentionAgents`（fork-only，upstream 不存在）/ 5 个 Go 双向修改文件（cmd_issue{,_test}/router/task/models.go）逐个 signature diff 零交叠。
- `router.go` + `provenance_baseline.go` + `client.ts`（#5539）— **零回退零碰撞**。router.go `ServerVersion: officialBaseline(version)` L218 保留（upstream 只加 agent-builder 路由）；provenance_baseline.go fork own file（blob `0b39fb38`）upstream 从未有、merged 保留；client.ts fork diff 全是 admin/skill-mention，无 provenance 行（#5539 brief 的"client.ts 2 行"在本轮不存在，早期已 in base），upstream +44 纯加法不重叠。
- **daemon brief / multica-mentioning SKILL.md** — **"upstream 改 A，fork 定制 B，A∩B=∅" 最干净场景**。upstream MUL-5442 ×5 + MUL-5696 碰 `prompt.go`/execenv/`multica-squads` SKILL.md/`multica-working-on-issues` SKILL.md，fork 逐个查全 "FORK MODIFIED: no"；fork 的 multica-mentioning/SKILL.md upstream **未碰该目录** → fork @skill 契约（Step 3 + undesignated silent no-op）原样保留。

**Phase 1 总结**：merge-tree 实测 4 content 冲突，全部归类（cmd_issue_test.go=B / issues-config/index.ts=B / reply-input.tsx=B / CLAUDE.md=C 3行并集）；auto-merge 文件语义全部 spot-check 通过；**无 Strategy D 签名盲区、无 redeclaration、无 #5539 回退、无 fork 本地文件受损**。唯一 post-merge action：Phase 5 的 `go build`（含可能的 `make sqlc`）。

### Verified negative claims

<!-- frontmatter negative_claims[] 在全部 agent 返回后统一回填；body 按簇记录 -->

#### 簇：CLI / cmd_issue — Agent 3 ✅

| claim | verified_by | result |
|---|---|---|
| upstream v0.4.18 未碰 fork #4118 CLI 定制文件 | `git diff --name-only v0.4.17..v0.4.18 -- server/cmd/multica/` | 仅 `cmd_attachment{,_test}.go` + `cmd_issue{,_test}.go`；4 个 fork 定制文件全缺席 → **confirmed** |
| `cmd_issue.go` auto-merge 无 conflict | `git merge-tree -z --write-tree --merge-base v0.4.17 HEAD v0.4.18` | `Auto-merging server/cmd/multica/cmd_issue.go`，无 CONFLICT 行 → **confirmed** |
| `cmd_issue_test.go` 是真实 content conflict | 同上 merge-tree | `CONFLICT (content): …cmd_issue_test.go` + 3 stage blob `f39889cfc`/`7184f3b5f`/`64da594b9` → **confirmed** |
| 两侧 appended test 独立、不共享 helper/新 import | `git cat-file -p 7184f3b5f` + `git cat-file -p 64da594b9` + base import 块 | fork `TestValidateIssueStatusArchived` vs upstream `TestIssueCommentListHelpCarriesReadContract`；均只引 base `strings`/`testing` → **confirmed（支撑 KEEP-BOTH）** |
| upstream 改 cmd_issue*.go 的 commit 唯一 = `766c7a7fa` | `git show --stat 766c7a7fa -- server/cmd/multica/` + 同 `84fe84cd9` | 766c7a7fa=cmd_issue{,_test}；84fe84cd9=cmd_attachment{,_test} → **confirmed** |
| fork 未碰 cmd_attachment*.go | `git diff --name-only v0.4.17..HEAD -- server/cmd/multica/cmd_attachment*.go` | 空输出 → **confirmed** |
| `validateIssueStatus` 仍存在于 cmd_issue.go:402，未被 upstream 破坏 | `grep -rn "func validateIssueStatus" server/cmd/multica/` | `cmd_issue.go:402`，upstream diff 未触及 → **confirmed** |

#### 簇：quick-action / Strategy D — Agent 2 ✅

| claim | verified_by | result |
|---|---|---|
| upstream v0.4.18 未碰 `triggerTasksForComment` 签名 | `git log --oneline v0.4.17..v0.4.18 -S "triggerTasksForComment" -- server/` | 空 → **confirmed（Strategy D 本轮不触发）** |
| upstream 无任何 `triggerTasksForComment` 调用点增删改 | `git diff v0.4.17..v0.4.18 -- server/ \| grep -E "^[+-].*triggerTasksForComment"` | 空 → **confirmed** |
| upstream 整个 `comment.go` 零改动 | `git diff v0.4.17..v0.4.18 -- server/internal/handler/comment.go` | 空 → **confirmed（后端重头戏文件本轮免疫）** |
| upstream 整个 `handler/quick_action.go` 零改动 | `git diff v0.4.17..v0.4.18 -- server/internal/handler/quick_action.go` | 空 → **confirmed** |
| 三签名 `bindAndEnqueueSkillMentions`/`parseSkillMentionAgents`/`bindDesignatedSkillsForTriggers` 全 silent | `git diff v0.4.17..v0.4.18 -S ... -- server/` ×3 + `grep ^+` | 双空 → **confirmed** |
| upstream 未改 `comment-input.tsx` | `git diff v0.4.17..v0.4.18 -- packages/views/issues/components/comment-input.tsx` | 空 → **confirmed（fork 双 prop 结构安全）** |
| fork 未定制 `chat_quick_actions_generate.go`（MUL-5689 真实落点） | `git diff v0.4.17..HEAD -- server/internal/service/chat_quick_actions_generate.go` | 空 → **confirmed** |
| fork 未定制 `priority.ts`（auto-merge 干净带 upstream `PRIORITY_DISPLAY_ORDER`） | `git diff v0.4.17..HEAD -- packages/core/issues/config/priority.ts` | 空 → **confirmed** |
| index.ts + reply-input.tsx 确为 content conflict | `git merge-tree v0.4.18 HEAD 2>&1 \| grep ^CONFLICT` | 命中两文件（+ CLAUDE.md / cmd_issue_test.go） → **confirmed** |
| fork 三处 `triggerTasksForComment` 调用点全传满 10 参 | `grep -cE "h\.triggerTasksForComment\(" comment.go quick_action.go` | `2 / 1`，与签名一致 → **confirmed** |

#### 簇：CLAUDE.md / docs / AGENTS.md — Agent 1 ✅（含 5 条 refuted 预判）

| claim | verified_by | result |
|---|---|---|
| 仅 2 个 upstream commit 碰 CLAUDE.md（task 背景断言） | `git log --oneline v0.4.17..v0.4.18 -- CLAUDE.md AGENTS.md` | **refuted** — 实际 3 个（多 `e9ea05e38` @reui） |
| `## 用户偏好` 节在冲突区 | `git diff v0.4.17..HEAD -- CLAUDE.md` + merge-tree hunk 比对 | **refuted** — 顶部 upstream 零触碰，自动干净合并；唯一冲突在 Project Shape 中段 |
| design.md 折进内容会与 fork 冲突 | `git show 6f14c3004 -- CLAUDE.md` + merge-tree | **refuted** — 落 UI Rules 区，fork 未碰，自动到达 |
| AGENTS.md 会冲突 | `git merge-tree --write-tree HEAD v0.4.18 2>&1 \| grep AGENTS` | **refuted** — `Auto-merging AGENTS.md` 无 CONFLICT；merged blob 两侧全保留 |
| migration 100/101/103/104 有 SQL 语义改动 | `git show c1ec1f464 -- <4 files> \| grep -E "^[-+@]"` | **refuted** — 4 处全 `--` 注释删 RFC 指针，零 SQL |
| fork 碰过这 4 个 migration | `git log --oneline v0.4.17..HEAD -- <4 files>` | empty → **confirmed 未碰** |
| fork 14 个被删 docs 副本有内容分歧 | 14 文件 `git rev-parse v0.4.17:X` vs `HEAD:X` 全量比对 | 14/14 SAME blob → **confirmed 无分歧**（modify/delete 不触发） |
| fork 本地 docs 会被 upstream 删 | upstream 17 删项 vs `git ls-tree HEAD docs/` 逐一比对 | 0 命中 → **confirmed fork 本地文件全安全** |
| CLAUDE.md 冲突有多处 hunk | merge-tree `grep -nE "<<<<<<<\|=======\|>>>>>>>"` | 仅 1 块 → **confirmed（仅 Project Shape 一处）** |

#### 簇：task.go / daemon / provenance / 签名 sweep — Agent 4 ✅

| claim | verified_by | result |
|---|---|---|
| merged task.go 不含 truncate 三符号（fork deletion 干净赢，无 redeclaration） | `git cat-file -p "ed9064020…:server/internal/service/task.go" \| grep -nE "maxSynthesized\|truncateFallback\|oversizedFallback\|lockChatSession\|unicode/utf8"` | 只命中 `unicode/utf8`(L14) + `lockChatSessionForTaskWrite`(×6)；truncate 三符号缺位 → **confirmed** |
| fork fallback_comment_truncate.go 是唯一符号定义 | `git cat-file -p "ed9064020…:…/fallback_comment_truncate.go" \| grep truncate` | L5/L9/L11/L16 单一来源 → **confirmed** |
| utf8 import 非 dead（v0.4.12 教训不复现） | `git show HEAD:…/task.go \| grep "utf8\."` | L4685 `utf8.RuneCountInString` → **confirmed** |
| upstream comment.go v0.4.17→v0.4.18 零改动 | `git diff --stat v0.4.17..v0.4.18 -- server/internal/handler/comment.go` | 空（0 lines） → **confirmed** |
| triggerTasksForComment upstream 两版同签名 | `git grep -n "func.*triggerTasksForComment" v0.4.17 v0.4.18 -- server/` | 两版均 L1956 同签名（无 skillMentionAgents） → **confirmed** |
| router.go ServerVersion 行 merged 保留 | `git cat-file -p "ed9064020…:server/cmd/server/router.go" \| grep -niE "ServerVersion\|provenance"` | L218 `ServerVersion: officialBaseline(version),` → **confirmed** |
| provenance_baseline.go fork own file，upstream 从未有 + merged 保留 | `git ls-tree HEAD/v0.4.17/v0.4.18/ed9064020 -- …/provenance_baseline.go` | 仅 HEAD + merged 命中（blob `0b39fb38`） → **confirmed** |
| upstream 未碰 multica-mentioning 目录 | `git diff --name-only v0.4.17..v0.4.18 -- …/builtin_skills/multica-mentioning/` | 空 → **confirmed（@skill 契约原样保留）** |
| fork 未改 upstream 本轮触碰的 daemon/SKILL 文件 | `git diff --name-only v0.4.17..HEAD -- <each>` ×4 | 全 "FORK MODIFIED: no" → **confirmed（A∩B=∅）** |
| merged cmd_issue.go 双方改动共存 | `git cat-file -p "ed9064020…:cmd_issue.go" \| grep -nE "issueguard\|archived\|IsCompletedStatus\|N caps THREADS"` | fork(L22/224/374/955) + upstream(L526) 全在 → **confirmed** |
| merged models.go 双方改动共存 | `git cat-file -p "ed9064020…:models.go" \| grep -nE "AgentBuilderDraft\|InviteeName"` | L57 upstream + L1216 fork → **confirmed** |
| client.ts fork 无 provenance 改动 | `git diff v0.4.17..HEAD -- packages/core/api/client.ts \| grep -iE "provenance\|officialBaseline\|ServerVersion"` | 空 → **confirmed（#5539 "2 行" 本轮不存在）** |

## Phase 2 — Confirm gates ✅

`user_confirmed: true` — 用户授权"一路执行到完成"，Phase 6 前不再阻塞复问。

## Phase 3 — Reversible preview ✅

- backup branch `main-backup-v0.4.18` = `bec6e2042`（merge 前 main）
- `git merge v0.4.18 --no-ff` pause at 4 conflicts（`MERGE_HEAD` in-progress）
- 实测冲突 = merge-tree 预测的 4 个（连续第 5 次文本预测命中：v0.4.14/15/16/17/18）

## Phase 4 — Resolve + re-apply ✅

4 conflicts resolved（见 Phase 1 per-file strategy，全部 additive/orthogonal，无语义矛盾）:
- `CLAUDE.md` — C union（3 行：`packages/eslint-config/` + `docs/solutions/` + `CONCEPTS.md`）
- `issues/config/index.ts` — B（fork status 多行 + upstream `PRIORITY_DISPLAY_ORDER`）
- `reply-input.tsx` — B（`skillMentionContext` + `quickActionMenu` 双 prop）
- `cmd_issue_test.go` — B keep-both（python slice + gofmt；git 把尾部 `}` 当共同 context 导致结构错位，手工重建两完整函数）

## Phase 5 — Verify code gates ✅ (all five green)

| step | command | exit |
|---|---|---|
| 1 | `pnpm install` | 0 (4.5s) |
| 2 | `pnpm typecheck` | 0 (clean) |
| 3 | `go build -C server ./...` | 0 (task.go 新 sqlc 依赖已 resolve，**无需 make sqlc**) |
| 4 | `pnpm test` | 0 (5/5 packages, 1m11s；comment-composers.test.tsx 未撞 fork onSubmit) |
| 5 | `pnpm build` | 0 (3/3 packages, 46s) |

注：`gofmt -l` 报 `comment.go` 未格式化，确认 **pre-existing fork debt**（HEAD 版本已不 clean，upstream 本轮 0-line diff），不阻塞 go build，不在本次 scope。

## Phase 6 — Irreversible gates ✅ (all fired)

| step | command | exit / result |
|---|---|---|
| 6.1 backup | `/opt/homebrew/opt/postgresql@17/bin/pg_dump "$DATABASE_URL" -F c -f ~/multica-backups/v0.4.18-pre-migrate-20260805-093744.dump` | 0 (51M) |
| 6.2 migrate | `set -a; source .env; set +a; go run -C server ./cmd/migrate up` | 0 — only `252_agent_builder_draft` up, 251 priors skip |
| 6.3 stamp+restart | `sed .env NEXT_PUBLIC_APP_VERSION=v0.4.18 && bash scripts/selfhost/install.sh` | 0 — rebuild frontend (`~/.multica/frontend`) + backend (`~/.multica/backend/server`), reload both plists |
| 6.4 health | `curl /healthz, /, /api/config` | `/healthz` 200, `/` 200, `server_version=v0.4.18` |

version stamp 双注入点验证通过：前端 `.env` inline + 后端 ldflags（`resolve-official-baseline.sh` → `git describe --tags --abbrev=0` = v0.4.18）。**轻量 tag 兼容性确认**：v0.4.18 是 unannotated tag，`git describe --abbrev=0` 默认失败，需 `--tags`（install.sh 内部已兼容）。`curl --retry-connrefused` 消化 backend ~40s daemon-runtime 启动窗口，首次即 200（install.sh 内部 bootout-then-wait + retry + verify 已兜底）。

## Phase 7 — Record ✅

**Merge commit:** `0d1994332`（rerere 记录 4 resolution，下次同类冲突自动吸收）。

**Calibration (actuals vs audit prediction):**
- merge-tree 预测 4 content 冲突 → **实际 4**（连续第 5 次文本预测命中：v0.4.14/15/16/17/18）
- Strategy D 签名盲区预测"不触发" → **实际不触发**（comment.go 0-line upstream diff，4 audit agent 实测）
- task.go sqlc 依赖预警"可能需 make sqlc" → **实际不需要**（merged tree 已带 upstream 生成代码，go build 直接过）
- `comment-composers.test.tsx` 预警"可能撞 fork onSubmit 第 4 参" → **实际未撞**（pnpm test 5/5 绿）
- migration 252 预测"零撞号" → **实际零撞号**（stem 252 > fork max 251；6 已知完好）
- CLAUDE.md `## 用户偏好` 节预判"在冲突区" → **实际不在**（adversarial verify 推翻预判，顶部 upstream 零触碰）

**本轮特色（vs 往轮）：** 后端零风险（comment.go/task.go/router.go/client.ts/SKILL.md 全 clean），全部冲突在前端 TS + CLAUDE.md/docs。唯一手工重建是 `cmd_issue_test.go`（git 把 fork/upstream 两函数尾部 `}` 当"共同 context"放冲突块外，简单删标记会破坏结构 → python slice + gofmt 重建两完整函数）。这是近几轮里后端风险最低、冲突最少的一次。

**Gates:** 8/8 true。Backup path: `~/multica-backups/v0.4.18-pre-migrate-20260805-093744.dump` (51M)。
