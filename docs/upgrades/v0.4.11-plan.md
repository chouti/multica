---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.11
base_tag: v0.4.9
merge_commit: 185d446d8

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true
  audit_strategy_written: true
  db_backed_up: true
  migrated: true
  version_stamped_frontend: true
  version_stamped_backend: true
  services_restarted: true
  health_checked: true

# Negative claims the audit made — each confirmed/refuted by one raw command, not by reading.
negative_claims:
  - claim: "merge-base 干净落在 v0.4.9 (ecbdbda09)，待合 35 commits（= v0.4.9..v0.4.11，无需修正）"
    verified_by: "git merge-base HEAD v0.4.11 → ecbdbda09 (= git describe v0.4.9); git rev-list --count HEAD..v0.4.11 → 35 = MB..v0.4.11 → 35"
    result: confirmed
  - claim: "v0.4.10/v0.4.11 都在 origin/main 上；main 领先 v0.4.11 6 commits → merge v0.4.11 tag 而非 origin/main"
    verified_by: "git merge-base --is-ancestor v0.4.11 origin/main → YES; git rev-list --count v0.4.11..origin/main → 6"
    result: confirmed
  - claim: "fork 213/214 (issue_status_archived / classifier_functions) 文件不被 upstream 触碰 → merge 时自动保留，唯一净影响是 lint 报 duplicate"
    verified_by: "git show --name-status 3f2e1c68d+be7c3521a (VCS renumber 只 rename 213_vcs*→216, 214_vcs*→217…, 213_chat*→214_chat*) — 均不碰 fork 的 213_issue_status*/214_issue_status*; merge-tree 无 migration CONFLICT"
    result: confirmed
  - claim: "fork 的 213_task_usage/214_chat_session/215-223 与 v0.4.11 字节相同（fork ahead 时已 cherry-pick 同一 upstream commits）→ merge 不产生内容冲突，DB 记录已匹配最终编号"
    verified_by: "for f in 213_task_usage/214_chat_session/215_chat_session_project_index: git rev-parse HEAD:.. == v0.4.11:.. → IDENTICAL ×3; fork 含同名 MUL-5228/MUL-3772/#5868/#5883 commits (parents=1, 非 merge = cherry-pick)"
    result: confirmed
  - claim: "merge-tree 预演仅 1 个内容冲突：docker-compose.selfhost.build.yml；全部 36 个 migration 文件 0 冲突；mention cluster 文件 0 个在交集"
    verified_by: "git merge-tree --write-tree --merge-base=v0.4.9 HEAD v0.4.11 → grep 'CONFLICT (' = 1 (docker-compose.selfhost.build.yml); comm -12 fork×upstream migration 文件 = 36 但全 identical/无冲突; mention/rich-content/editor/issue-detail/comment.go 等 hot 文件 grep intersection 全 miss"
    result: confirmed
    caveat: "auto-merge ≠ semantic correct；Phase 5 typecheck/test 验证 VCS 大特性与 fork backport 的语义无缝合并"
  - claim: "live DB schema_migrations 已含 upstream 最终形态 213_task_usage/214_chat_session/215-223 + fork 213_issue_status_archived/214_issue_status_classifier_functions → Phase 6 migrate up 无 pending，仅需迁 0 个新迁移"
    verified_by: "psql schema_migrations WHERE version ~ '^21[3-9]|^22[0-3]' → 13 行 (213×2, 214×2, 215, 216…223)"
    result: confirmed
  - claim: "fork 213/214 需加入 legacyDuplicateMigrationStems，否则 TestMigrationNumericPrefixesStayUniqueAfterLegacySet 在 merge 后转红（fork 213+upstream 213 同号异 stem）"
    verified_by: "grep legacyDuplicateMigrationStems migrations_lint_test.go → 现表含 119/158/159/160 但无 213/214; cut -d_ -f1 local_stems | uniq -d 含 213 214 (upstream dupes 不含)"
    result: confirmed   # action: Phase 4 加 213/214 两 entry 进 map
---

# Upgrade v0.4.9 → v0.4.11

> Process artifact. Persistent customization list in `docs/customizations.md`.
> 35 upstream commits (v0.4.9..v0.4.11), published 2026-07-24 (v0.4.11 = Latest). Themes: **VCS self-hosted Git providers (Forgejo/Gitea/GitLab, MUL-3772)**, chat project-context (MUL-5150), GitHub PR API-snapshot CI/mergeability (MUL-5265/5180), runtime alias display (MUL-5248), transcript revamp (#5890), Claude Opus 5 catalog (MUL-5282), Grok cost billing (MUL-5228).

## Phase 0 — Locate
- base: `v0.4.9` (HEAD `v0.4.9-220-g15cf7f60e`)
- target: `v0.4.11` (Latest, published 2026-07-24 20:04 UTC; v0.4.10 published同日 10:02 → 合并升级两步为一步)
- merge-base(HEAD, v0.4.11) = `ecbdbda09` = v0.4.9 (clean)
- to-merge: **35 commits** (HEAD..v0.4.11 = MB..v0.4.11，无修正)
- v0.4.10/v0.4.11 均在 origin/main；main 领先 v0.4.11 6 commits → **merge v0.4.11 tag，不 merge origin/main**
- vs origin/main: 41 behind / 220 ahead

## Phase 1 — Deep audit
Fan-out via direct commands (clean merge-base + `git merge-tree` 预演)。交集 255 文件，但 **fork 的 hot 定制文件（mention cluster / issue-detail / comment.go / cmd_admin / provenance / fallback_truncate / skill.go）全部不在交集** → 零冲突。

### 头条：fork 213/214 migration 撞号（本轮核心）
fork 在 v0.4.9 之后（ahead 220 期间）为 archived issue status 功能（FZG-340/341/342/343）新增了 `213_issue_status_archived` + `214_issue_status_classifier_functions` 两个本地迁移。**同时 upstream 在 v0.4.10/11 也推进到 213-223 区间**（VCS/chat_session/Grok 等），并已自我 renumber（`3f2e1c68d` 把 213_vcs*→216 等、`be7c3521a` 把 213_chat_session→214_chat_session）。结果：fork 的 213/214 与 upstream 的最终 213/214 **同号不同 stem**，成为继 119/158/159/160 之后的**第 5、6 个 duplicate-prefix 碰撞**。

**为何 merge 无冲突 + DB 无需重编号（关键正路径）：**
- fork 的 `213_task_usage_authoritative_cost`/`214_chat_session_project`/`215`-`223` 是 fork **already cherry-pick 过的同一批 upstream commits**（`7b91e425f`=MUL-5228、`834b18b2e`=MUL-3772 renumber、`4d6824504`=#5868、`8faa3da90`=MUL-3772，均 parents=1 非 merge），与 v0.4.11 **字节相同** → merge 同改同内容 = clean。
- fork 自己的 `213_issue_status_archived`/`214_issue_status_classifier_functions` 不被任何 upstream commit 触碰 → auto-merge 自动保留。
- live DB `schema_migrations` 已记录 upstream 最终形态（213_task_usage…223）+ fork 两个 213/214 → **无 pending 迁移，Phase 6 迁 0 个**。

**唯一残留动作：** lint 表 `legacyDuplicateMigrationStems` 现缺 213/214 → merge 后 `TestMigrationNumericPrefixesStayUniqueAfterLegacySet` 转红，需 Phase 4 加两 entry（同 v0.4.6 补 158/159/160 的既定模式）。

### Per-file strategy（intersection = both sides changed）
| cluster / file | strategy | 冲突? | 依据 |
|---|---|---|---|
| mention cluster / rich-content / editor / content-editor / skill-agent-picker | — | **0** | 不在交集（upstream 35 commits 无 mention 重构） |
| `packages/views/issues/components/issue-detail.tsx` (pin-comment + identifier-badge + archived) | — | **0** | 不在交集（upstream issue 改动在 table/board/list/search，非 detail） |
| `server/internal/handler/comment.go` / `skill.go` / `skill_mention_trigger*.go` / `cmd_admin.go` | — | **0** | 不在交集 |
| `server/cmd/server/provenance_baseline.go` / `fallback_comment_truncate.go` | — | **0** | 不在交集 |
| `docker-compose.selfhost.build.yml` | **C (careful-merge)** | **1** | merge-tree 唯一冲突。fork #5539 把 VERSION/NEXT_PUBLIC_APP_VERSION 改必填 `:?`；upstream 删了 web 的 REMOTE_API_URL+NEXT_PUBLIC_WS_URL 两行。解法见 Phase 4 |
| `server/migrations/213_issue_status_archived*` + `214_issue_status_classifier_functions*` (fork-only) | **A (auto-preserve)** | 0 | upstream 不碰；merge 保留 |
| `server/migrations/213_task_usage…223` (fork backport = upstream 正式) | **A (identical)** | 0 | 字节相同，clean |
| `server/internal/migrations/migrations_lint_test.go` | **edit** | 0 | 加 213/214 两 entry 进 `legacyDuplicateMigrationStems` |
| `server/cmd/server/router.go` | D (orthogonal) | 0 | merge-tree clean；fork officialBaseline/admin 与 upstream VCS 路由不同 group |
| `server/cmd/server/main.go` | C | 0 | merge-tree clean；fork version stamp 注入点 vs upstream 改动不同区 |
| VCS 新代码 (server/internal/integrations/vcs/*, handler/vcs*.go, ghsnapshot/*, packages/core/vcs/*, packages/core/github/*) | A (adopt upstream) | 0 | fork 已 backport 同 commits；fork 无 VCS 自研 |
| `packages/core/api/client.ts` | C | 0 | merge-tree clean；fork #2669/#5539 与 upstream VCS/chat 方法不同区 |
| `packages/core/types/{agent,chat,github,vcs,index}.ts` | C | 0 | merge-tree clean |
| locales (en/ja/ko/zh-Hans × agents/chat/issues/settings) | B (take-both keys) | trivial | keys 不重叠（fork archived/identifier keys ↔ upstream VCS keys） |
| db generated (`*.sql.go`, `models.go`) | regen `make sqlc` | 0 | generated |
| web-providers / runtime-urls / proxy / next.config / apps/web | C | 0 | merge-tree clean；fork version 注入点未动 |
| docs (apps/docs/*, README*, SELF_HOSTING, deploy/helm, docker-compose.selfhost.yml, Dockerfile.web) | A/B | 0 | 文档/部署，低风险 |

### Audit prediction（Phase 3 对照）
- 预期真实手动冲突 **1 个**：`docker-compose.selfhost.build.yml`（低难度，fork 必填注释 + upstream 删两行）。
- 大头（mention cluster、VCS、provenance、admin、skill-mention）均零冲突（不在交集或字节相同）。
- **Phase 4 必做非冲突动作**：lint map 加 213/214；resolve docker-compose 冲突。
- **Phase 5 语义关注**：VCS 大特性（fork backport 版 ↔ upstream 正式版）merge 后 typecheck/test 是否无缝（字节相同应无缝，但 VCS 涉及的 db generated / types / client 需过 typecheck）。

## Phase 2 — Irreversible-gate confirmation
- 第一阶段授权 **Phase 3-5**（可逆）；DB 备份预选 = 是。
- 第二阶段（2026-07-27）用户明确指令 **"执行 Phase 6"** → `gates.user_confirmed: true`。
- **本轮特殊：DB 无 pending 迁移（migrate up 预期迁 0 个），故 irreversible 风险主要集中在 service restart + version re-stamp，不在 DB schema 变更。**

## Phase 3 — Preview
- safety branch `main-backup-v0.4.11` 已建（@15cf7f60e）。stash 未动（`stash@{0}` 是 07-09 旧 `pre-upgrade-v0.3.41`，与本轮无关，保留）。
- `git merge v0.4.11 --no-commit --no-ff`：**实际内容冲突 0** —— merge-tree 预演的唯一候选 `docker-compose.selfhost.build.yml` 被 **rerere** 自动套用旧 resolution（"Staged ... using previous resolution"），`git diff --diff-filter=U` 为空。
- **vs 预测**：audit 预测 1 冲突 → 实际 0（rerere 拦截）。比 v0.4.6(7)、v0.4.8(5) 更干净。
- fork 213/214 archived migrations 经 merge **保留**（`ls` 确认 4 文件在）。

## Phase 4 — Resolve
无手动 conflict（rerere 已解 compose）。两处修正：
1. **rerere resolution 语义过期（重要）**：旧 resolution 保留了 `REMOTE_API_URL`/`NEXT_PUBLIC_WS_URL` 两行 build args —— 但 upstream `a90aa92d0` (runtime URL resolution, #4840) 已把 API/WS URL 改为**运行时注入**（`Dockerfile.web:58 ENV REMOTE_API_URL=http://backend:8080` + `runtime-urls.ts` runtime 解析），v0.4.11 `Dockerfile.web` 已无这两个 ARG。旧 resolution 会错误回加死 build-args。已重解：删 REMOTE_API_URL/NEXT_PUBLIC_WS_URL，**保留** fork 的 `VERSION`/`NEXT_PUBLIC_APP_VERSION` 必填 `:?` guard（防 prod 误 stamp dev）。教训：**rerere 命中 ≠ 语义正确，须复核**。
2. **lint map 加 213/214**（v0.4.6 补 158/159/160 同模式）：`legacyDuplicateMigrationStems` 现缺 fork 213/214，merge 后 `TestMigrationNumericPrefixesStayUniqueAfterLegacySet` 红（`213 reused by [issue_status_archived, task_usage_authoritative_cost]`、`214 reused by [chat_session_project, issue_status_classifier_functions]`）。已加两 entry → 测试转绿。
3. **fork 213/214 mtime re-checkpoint**：fork 两迁移原本提交于 2026-07-27（mtime 1785115655），与 upstream 213/214 同号。migrate runner (`migrations.go Files()`) 按 `sort.Strings` 字母序应用 → 同号时 `213_issue_status_archived` < `213_task_usage`（字母序）。已 `touch` re-checkpoint（内容未变，DB 已记录版本串，重放是 no-op）防御 fresh-clone 排序歧义。fork 213(issue_status_check) 与 214(classifier fns) 均独立于 upstream 213(task_usage_cost)/214(chat_session)，无跨依赖，字母序安全。

## Phase 5 — Verify (code gates, no DB/service)
全绿，stop-on-first-failure 全过：
- `pnpm install` ✓ (exit 0; 无新增依赖冲突，仅 postinstall 提示)
- `pnpm typecheck` ✓ (6/6) — 无 auto-merge 语义错/unused import/narrowing（mention cluster 零冲突 → 无此类 fallout；VCS 大特性 fork backport 与 upstream 正式版字节相同 → 无缝）
- `go build -C server ./...` ✓
- `pnpm test` ✓ — **8 tasks 成功**，`@multica/views` **3075 passed** (267 files)（较 v0.4.9 的 2982 增加，含 upstream VCS/chat-project-context/transcript 测试）
- `pnpm build` ✓ (3/3)
- **补充**：`go test ./internal/migrations ./internal/issueguard/...` ✓（lint map 加 213/214 后 migrations 包绿；issueguard = fork archived status helper 包，绿）。
- **Skipped**：`make test`/`make start`（需 Docker，本 host 无）；Go integration tests 需独立 DB（不对 live DB 跑）。
- 已知 7 个 residual handler 测试失败（autopilot authority，v0.4.8/9 即存在，与本次无关，见 ledger）——未在本次范围内，不视为阻塞。

## Phase 6 — Irreversible ops (EXECUTED 2026-07-27)
- **Backup**: `~/multica-backups/v0.4.11-pre-migrate-20260727-113157.dump` (38M, pg17 `pg_dump`; `pg_restore --list` 列出 572 对象，完整) ✓
- **Migrate**: `migrate up` 应用 **0 个新迁移**（与审计预测一致）—— fork 213/214 archived 与 upstream 213-223 全部 `already applied`。撞号在 runner 层无影响（按版本串逐条判重，非数字）。
- **Version stamp v0.4.11**（避开 install.sh 两缺陷，未裸跑 install.sh）：
  - frontend: `.env NEXT_PUBLIC_APP_VERSION=v0.4.11` → `export NEXT_PUBLIC_APP_VERSION=v0.4.11; bash scripts/selfhost/build-frontend.sh`（bundle layout chunk 确认含 v0.4.11）
  - backend: `go build -C server -ldflags "-X main.version=v0.4.11" -o ~/.multica/backend/server ./cmd/server`（二进制 `strings` 含 v0.4.11）
- **Restart**: `launchctl kickstart -k gui/$UID/com.fengzhao.multica-{backend,frontend}`（避开 bootstrap 不重启缺陷）。新 PID backend=58466 frontend=58485。
- **Health**: backend `/healthz` 200, frontend `/` 200（首次探测即 200，未遇 ~40s init 窗口）；`/api/config` 返回 `server_version=v0.4.11`（重戳生效，Help 菜单正确）。

## Phase 7 — Record
- **Merge commit**: `185d446d8`。Base advanced v0.4.9 → v0.4.11（merge-base 干净 `ecbdbda09`=v0.4.9，35 commits）。
- **Predicted vs actual**: merge-tree 预演 1 候选冲突（compose）→ 实际 **0 内容冲突**（rerere 拦截）。**但 rerere resolution 语义过期** —— 它回加了 upstream 已删的 `REMOTE_API_URL`/`NEXT_PUBLIC_WS_URL` build args（`a90aa92d0` 改 runtime URL resolution，`Dockerfile.web` 已无这些 ARG），手动重解保留 fork 必填 guard。**核心教训：rerere 命中 ≠ 语义正确，凡是 rerere 自动解的冲突必须复核其 resolution 是否仍匹配 upstream 最新意图。**
- **fork 213/214 archived 迁移撞号 upstream 213/214**（第 5、6 个 duplicate-prefix 碰撞）—— 不 renumber，加进 `legacyDuplicateMigrationStems`（v0.4.6 补 158/159/160 同模式）。fork 的 VCS/Grok backport（MUL-3772/5228, #5868/#5883）与官方 v0.4.11 字节相同 → 干净合并、DB 0 待迁移。
- **fork 热文件零冲突**：mention cluster / issue-detail / comment.go / skill.go / skill_mention_trigger* / cmd_admin.go / provenance_baseline.go / fallback_comment_truncate.go 全部不在交集。
- **install.sh 两缺陷本轮第三次成功避开**（手动 export + `-ldflags` + `build-frontend.sh` + `launchctl kickstart -k`，无 prod down）。
- **Memory**: 新增 `project_fork_213_214_migration_collision`（撞号细节 + 不 renumber + lint map 维护）。
- **Ledger**: `docs/customizations.md` migration 条目更新为 6 个碰撞（加 213/214），Last upgrade 段推进到 v0.4.11。
- **Daemon 顺带升级（2026-07-27）**: daemon 是 Homebrew `multica` CLI（`multica-ai/tap`），非 repo-built、不在 backend/frontend launchd 范围。`brew upgrade multica` 0.4.9→0.4.11 → `multica daemon restart`（CLI 自管理生命周期，旧 pid 38375 停、新 pid 63833 起、仍 PPID=1）→ `daemon status` Version 0.4.11 + 5 workspaces，daemon.log 显示 5 provider runtimes registered + wakeup websocket connected。backend v0.4.11 始终健康。Memory `project_daemon_upgrade` 记录 daemon 独立升级路径。
