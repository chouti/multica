---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.22
base_tag: v0.4.21   # clean merge-base 0dfaac266 (= v0.4.21 tag point)

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true             # Phase 2 — CONFIRMED 2026-08-11: (1) pg17 pg_dump before migrate 265-271; (2) install.sh full rebuild + launchd restart accepted (short downtime OK)
  audit_strategy_written: true     # Phase 1.4 — FLIPPED 2026-08-11: C1/C2/C3/C4 all in; every negative claim carries verified_by + result; 4 conflict files (mention-view, issues-header, swimlane-view, use-issue-surface-data) + 3 locale JSONs all have explicit per-hunk strategy; CRITICAL must-fix for task.go:3565 documented; strategy D(a)/D(b) decision captured as fork-feature preservation question (resolve in Phase 4)
  db_backed_up: true               # Phase 6.1 — 2026-08-11T10:54:00Z, 58M at ~/multica-backups/v0.4.22-pre-migrate-20260811T025400Z.dump, pg17 binary (postgresql@17)
  migrated: true                   # Phase 6.2 — 2026-08-11T10:54:16Z, migrations 265-271 applied (issue_view, issue_view_owner_index, issue_view_shared_index, issue_view_preference, issue_view_workspace_variant, pinned_item enum widened to 'view', channel_chat_session_binding.pending_fresh column added); 264 → 271
  version_stamped_frontend: true   # Phase 6.3 — NEXT_PUBLIC_APP_VERSION=v0.4.22 in .env, install.sh rebuilt standalone frontend
  version_stamped_backend: true    # Phase 6.3 — strings ~/.multica/backend/server shows `main.version=v0.4.22` (ldflags -s -w -X main.version=v0.4.22 stamped at install.sh time)
  services_restarted: true         # Phase 6.4 — install.sh loaded both launchd plists (com.fengzhao.multica-{backend,frontend}), both at exit 0
  health_checked: true             # Phase 6.5 — /api/config returns server_version=v0.4.22 (sandbox proxy intercepts /healthz curl returning 503 HTML, but the underlying backend is healthy — confirmed via launchctl exit 0 + lsof LISTEN on :8081 + /api/config response)

merge_commit: 7206599e2065a6280ba9063d48790123da21efd2   # true 2-parent merge (parents c78d65637 = fork HEAD, 8bd49bba8 = v0.4.22 tag); git describe HEAD = v0.4.22-287-g7206599e2 — version stamp will read this in Phase 6

# Negative claims — each confirmed/refuted by ONE raw command, never by reading.
negative_claims:
  - claim: "current base is v0.4.21, a TRUE merge (not a fake single-parent merge like v0.4.19)"
    verified_by: "git describe --tags HEAD; git tag --merged HEAD | sort -V | tail -6"
    result: "confirmed (v0.4.21-286-gc78d65637; v0.4.16..v0.4.21 all merged; merge commit 55bf3432f)"
  - claim: "latest upstream release is v0.4.22, published 2026-08-10, not prerelease/draft"
    verified_by: "gh release view v0.4.22 --repo multica-ai/multica --json tagName,isPrerelease,isDraft,publishedAt"
    result: "confirmed (isDraft=false, isPrerelease=false, publishedAt=2026-08-10T10:04:04Z)"
  - claim: "HEAD is 40 commits behind the v0.4.22 tag and 286 ahead"
    verified_by: "git rev-list --left-right --count v0.4.22...HEAD"
    result: "confirmed (40 behind / 286 ahead); vs origin/main = 46/286 (main is 6 past the tag)"
  - claim: "merge-base(HEAD, v0.4.22) = 0dfaac266 (= v0.4.21 tag point), single clean hop"
    verified_by: "git merge-base HEAD v0.4.22"
    result: "confirmed (0dfaac266eed3b7ac710de33d8207e4f71cfb20b, git describe = v0.4.21)"
  - claim: "TRUE upstream churn is 405 files +26222/-4302 (NOT the misleading HEAD..v0.4.22 direction, whose deletions are the fork's OWN code)"
    verified_by: "git diff --shortstat $(git merge-base HEAD v0.4.22)..v0.4.22"
    result: "confirmed (405 files changed, 26222 insertions, 4302 deletions). METHOD NOTE: measuring with `git diff HEAD..v0.4.22` misreads fork additions as upstream deletions (e.g. comment.go -432, skill.go -396 are fork code, not upstream churn). Always measure from merge-base."
  - claim: "NEW upstream migrations are 265-271, ALL greater than fork max 264 → zero new collision"
    verified_by: "comm -12 <(git diff --name-only --diff-filter=A $MB..v0.4.22 -- server/migrations/ | sed 's|.*/||; s/_.*//' | sort -u) <(ls -1 server/migrations/ | sed 's/_.*//' | sort -u)"
    result: "confirmed (intersection EMPTY). New: 265-269 issue_view*, 270 pinned_item_view, 271 channel_chat_pending_fresh (upstream itself renumbered this one to 271 in 080f088dd). 6 known collisions 119/158/159/160/213/214 untouched — do NOT renumber."
  - claim: "live DB is at migration 264, so 265-271 apply cleanly with no gap"
    verified_by: "psql17 $DATABASE_URL -tAc 'select max(version) from schema_migrations;'"
    result: "confirmed (264_issue_origin_wecom_chat_validate)"
  - claim: "merge-tree preview yields 7 conflicted files / 11 hunks / ~572 conflict lines"
    verified_by: "git merge-tree --write-tree HEAD v0.4.22 (tree f7f4da1c60bb0700315f7778d75ad02866d9ac51); then git merge-file -p per stage-1/2/3 blob, grep -c '^<<<<<<<'"
    result: "confirmed — mention-view.tsx=4 hunks/199 lines, issues-header.tsx=2/95, swimlane-view.tsx=1/17, use-issue-surface-data.ts=1/15, locales ja|ko|zh-Hans issues.json=1/82 each. CAVEAT: merge-tree cannot see fork-only files, so Phase 3 actuals may exceed this (v0.4.20 lesson: thread-nav-panel.tsx hid 8)."
  - claim: "no hidden add/add collision between fork-new and upstream-new files"
    verified_by: "comm -12 <(git diff --name-only --diff-filter=A $MB..HEAD | sort) <(git diff --name-only --diff-filter=A $MB..v0.4.22 | sort)"
    result: "confirmed (EMPTY; fork added 164 files, upstream added 76, zero path overlap)"
  - claim: "backend fork hot files are UNTOUCHED by upstream: handler/comment.go, handler/issue.go"
    verified_by: "git diff --numstat $MB..v0.4.22 -- server/internal/handler/comment.go server/internal/handler/issue.go"
    result: "confirmed (both produce no output = untouched). Fork's 9-param triggerTasksForComment workaround and archived-status backend carry zero risk this round. handler.go only +14/-1."
  - claim: "fork mention hot files mention-suggestion.tsx and rich-content/rich-content.tsx are UNTOUCHED by upstream"
    verified_by: "git diff --numstat $MB..v0.4.22 -- packages/views/editor/extensions/mention-suggestion.tsx packages/views/editor/rich-content/rich-content.tsx"
    result: "confirmed (no output). Only mention-view.tsx (+24/-102) is hit, via MUL-5860 ad6b8f7113 link-click unification."
  - claim: "core status config is untouched: core/issues/config/status.ts, core/types/issue.ts, status-icon.tsx, ui/ActorAvatar.tsx, README.md"
    verified_by: "git diff --numstat $MB..v0.4.22 -- <each path>"
    result: "confirmed (all produce no output)"
  - claim: "sqlc regen required — upstream touched 5 non-migration query files, fork touched NONE of them (single-sided, unambiguous regen)"
    verified_by: "git diff --name-only $MB..v0.4.22 -- server/pkg/db/queries/ ; then git diff --numstat $MB..HEAD -- <each>"
    result: "confirmed (upstream: channel.sql, chat.sql, issue_view.sql, issue_view_preference.sql, workspace_delete.sql; fork delta on each = EMPTY)"
  - claim: "zero go.mod/go.sum change; JS deps change in 2 package.json → pnpm install required, no Go dep work"
    verified_by: "git diff --stat $MB..v0.4.22 -- server/go.mod server/go.sum '**/package.json'"
    result: "confirmed (go.mod/go.sum: no output; apps/web/package.json 2 lines, packages/core/package.json +7)"
  - claim: "apps/mobile/ is entirely untouched by upstream this round"
    verified_by: "git diff --numstat $MB..v0.4.22 -- apps/mobile/ | wc -l"
    result: "confirmed (0)"
  - claim: "truncateFallbackCommentBody duplicate-symbol hazard does NOT fire this round — the merged task.go carries zero redeclaration"
    verified_by: "git cat-file -p f7f4da1c60bb0700315f7778d75ad02866d9ac51:server/internal/service/task.go | grep -n 'func truncateFallbackCommentBody\\|maxSynthesizedFallbackCommentRunes *='"
    result: "confirmed (EMPTY — fork's deletion of task.go:143-160 survived the auto-merge). Fork-only fallback_comment_truncate.go present in merged tree. LATENT DIVERGENCE RECORDED: upstream task.go:143 defines cap=8000, fork file defines cap=200 (40x). Upstream's fallback_comment_truncate_test.go asserts relative to the constant (not hardcoded 8000) so it passes at 200. This becomes a build break the moment upstream edits task.go:143-154."
  - claim: "no fork PR newly MERGED upstream → zero 'adopt upstream, drop local' work this round"
    verified_by: "gh pr list --repo multica-ai/multica --author chouti --state all --limit 60 --json number,title,state,mergedAt"
    result: "confirmed (#5393 remains the only MERGED, already handled in a prior upgrade; #6106 archived-status still OPEN; #5346/#5309/#5160/#4118/#2669/#2460 still OPEN; #5199/#5466/#5539/#5157/#5156/#4117 CLOSED)"
  - claim: "16 of the 40 upstream commits are WeCom, and they are value-zero AND cost-zero for this host"
    verified_by: "git log --oneline --no-merges HEAD..v0.4.22 | grep -ci wecom (16); psql 'select count(*) from channel_installation' (0); 'select count(*) from lark_installation' (0); git log HEAD --not v0.4.22 -- server/internal/integrations/wecom/ | wc -l (1)"
    result: "confirmed — zero channel installations of any provider on this host, and the fork never meaningfully edits integrations/wecom/, so those 16 commits merge wholesale with no conflict and no behavior change here."
  - claim: "services are healthy pre-upgrade (the 503 seen on /healthz was the sandbox proxy, not an outage)"
    verified_by: "launchctl list | grep multica; ps aux | grep -E 'bin/server|next-server|multica daemon'"
    result: "confirmed (com.fengzhao.multica-backend pid 11638 exit 0, -frontend pid 11627 exit 0; ~/.multica/backend/server, next-server v16.2.6, /opt/homebrew/bin/multica daemon start all running)"
  # --- Phase 1 deep audit, agent C1 (mention-view.tsx), all decisive claims re-verified in main session ---
  - claim: "MUL-5860 needs ZERO propagation into fork-only chip files — none of them render an anchor"
    verified_by: "grep -n 'AppLink\\|resolveClickIntent\\|useIntentNavigate' packages/ui/components/common/actor-mention-chip.tsx packages/ui/components/common/skill-mention-chip.tsx packages/views/editor/mention-hover-card.tsx"
    result: "confirmed (no output — hover-only by design, cannot drift on click semantics)"
  - claim: "upstream touched NO fork-only chip/picker/context file this round"
    verified_by: "git log --oneline $MB..v0.4.22 -- packages/ui/components/common/{actor,skill}-mention-chip.tsx packages/views/editor/{mention-hover-card.tsx,skill-agent-picker.tsx,skill-mention-context.ts}"
    result: "confirmed (no output)"
  - claim: "the 'open issue links in new tab' preference is NOT a fork customization — fork == merge-base on every file involved, so the merge silently accepts upstream's deletion"
    verified_by: "per-file symbol-hit counts across $MB / HEAD / v0.4.22 / merged-tree f7f4da1c6 for issue-link-store.ts, issue-mention-card.tsx, preferences-tab.tsx, locales/*/settings.json"
    result: "confirmed (base==fork on all; upstream==merged==removed everywhere). USER DECISION 2026-08-11: follow upstream's removal (Strategy A)."
  - claim: "after the merge, the deleted issue-link-store has NO dangling reference anywhere except the hand-resolved mention-view.tsx"
    verified_by: "git ls-tree -r --name-only f7f4da1c6 | grep -E '\\.(ts|tsx)$' | while read f; do git cat-file -p f7f4da1c6:$f | grep -qE 'useIssueLinkStore|issue-link-store' && echo $f; done"
    result: "confirmed (exactly 1 file: packages/views/editor/extensions/mention-view.tsx — closed by hunk-2 import drop). stores/index.ts carries no dangling export."
  - claim: "issue-mention-card.test.tsx needs NO fork work; mention-view.test.tsx WILL go red in Phase 5"
    verified_by: "git diff --numstat $MB..HEAD -- <each test file>"
    result: "confirmed (issue-mention-card.test.tsx fork delta EMPTY → takes upstream's rewritten 80-line version; mention-view.test.tsx fork delta +300/-22, 449 lines vs base 171, exercises the inline anchors hunk 4 deletes → expect red, rewrite against the cards)"
  # --- Phase 1 deep audit, agent C3 (locale JSON), all decisive claims re-verified in main session ---
  - claim: "the 3 locale conflicts are pure keep-both adjacency collisions — zero key intersection, zero value disagreement"
    verified_by: "python3 deep-flatten key-set intersection of the ours-block (6 archived.* keys) vs the theirs-block (75 save_view/view_selector/view_bar/filters.chip_* keys)"
    result: "confirmed (intersection EMPTY; 0 of the 75 upstream keys exist in any fork-HEAD issues.json)"
  - claim: "EN not conflicting is a structural insert-position accident, NOT the v0.4.20 fork-only-key parity asymmetry"
    verified_by: "grep -nE '^  \"archived\": \\{$|^  \"save_view\": \\{$' on ours/theirs per locale; plus git merge-file exit codes"
    result: "confirmed (fork put archived at en:213 mid-file but ja/ko/zh-Hans:679 end-of-file; upstream inserts at end-of-file in all 4 → only CJK overlap. Exit codes en=0, ja/ko/zh-Hans=1.)"
  - claim: "keep-both resolution loses ZERO fork keys across all 20 upstream-touched locale files"
    verified_by: "python3 flat(fork) - flat(merged) per file, dry-run over all 20"
    result: "confirmed — issues.json ×4 lost=0 (+75 each), common/inbox/projects ×12 lost=0 (+1 each), settings.json ×4 lost=2 each (INTENTIONAL, the Strategy A preference retirement)"
  - claim: "locale parity passes after correct keep-both merge — no missing-EN hazard this cycle"
    verified_by: "python3 normalized (_one/_other → _count) key-set comparison both directions for all 4 issues.json + all 4 settings.json"
    result: "confirmed (issues.json all 4 = 660 normalized keys, en_missing=0 loc_missing=0 both directions; settings.json 769 each, 0/0; dead-plural guard 0 offenders in ja/ko/zh-Hans)"
  - claim: "the JSON brace/comma trap FIRES on all 3 conflicted locales — naive marker-stripping produces invalid JSON"
    verified_by: "python3 strip-markers-then-json.tool on the raw merge-file output"
    result: "trap CONFIRMED PRESENT (error: Expecting ',' delimiter: line 767). git merge-file collapses the shared tail '  }\\n}\\n' into post-marker context, so the ours-block lacks its closing '  }' and comma. Fix per docs/solutions/workflow-issues/merge-keep-both-shared-context-brace-trap.md: fork_block.rstrip() + '\\n  },' then upstream block. Post-fix: all 3 VALID, 660 keys, lost 0."
  - claim: "fork-only keys members.invite_name_{label,placeholder} survive the settings.json auto-merge"
    verified_by: "grep -c on ours/base/theirs/merged settings.json"
    result: "confirmed (ours=3 incl. issue_link_new_tab, base=1, theirs=0, merged=2 = both invite_name_* preserved)"
  - claim: "preferences-tab.tsx does NOT conflict, and the issue-link-store deletion is a clean delete (not a delete/modify conflict)"
    verified_by: "git merge-tree --write-tree HEAD v0.4.22 | grep -iE 'preferences-tab|issue-link-store|CONFLICT'"
    result: "confirmed (neither path appears; only the known 7 CONFLICT lines). Closes both of C3's UNVERIFIED items."
  - claim: "the referenced trap solution docs and the parity test actually exist (not hallucinated paths)"
    verified_by: "ls docs/solutions/workflow-issues/ | grep -iE 'comma|keep-both'; ls packages/views/locales/parity.test.ts"
    result: "confirmed (merge-keep-both-json-trailing-comma-trap.md 9381B, merge-keep-both-shared-context-brace-trap.md 7444B, both written 2026-08-07 during v0.4.21; parity.test.ts 4329B)"
  # --- Phase 1 deep audit, agent C2 (issues-header + swimlane + use-issue-surface-data) ---
  - claim: "saved-views V1 feature correctly handles fork's 6th 'archived' status — no hardcoded 5-status list anywhere in upstream view files"
    verified_by: "grep -rn 'ALL_STATUSES\\|BOARD_STATUSES\\|archived' baseline.ts filter-chips-bar.tsx view-bar.tsx save-view-dialog.tsx view-bar-popover.tsx manage-views-dialog.tsx view-baseline-context.tsx use-issue-view-url-sync.ts"
    result: "confirmed — only baseline.ts:3,50 references ALL_STATUSES, and uses (ALL_STATUSES as readonly string[]).includes(s) — generic, not 5-element hardcoded"
  - claim: "server-side filter storage has NO enum/CHECK constraint rejecting 'archived' in issue_view.query JSONB"
    verified_by: "cat migrations/265_issue_view.up.sql (only CHECK (jsonb_typeof(query)='object')); grep -n 'status\\|archived' server/internal/handler/issue_view.go (zero hits on status field validation); git show v0.4.22:packages/core/api/schemas.ts (only query: z.record(z.string(), z.unknown()).default({}))"
    result: "confirmed — server stores 'archived' in query JSONB without complaint; only the JSON object shape is validated"
  - claim: "fork's ALL_STATUSES already includes 'archived' (8 statuses total)"
    verified_by: "git show HEAD:packages/core/issues/config/status.ts | grep -A 10 'ALL_STATUSES ='"
    result: "confirmed — backlog, todo, in_progress, in_review, done, blocked, cancelled, archived"
  - claim: "archived chip (Archive + X icons + setStatusFilters + archivedCount) survives in auto-merged issues-header.tsx outside the conflict hunks"
    verified_by: "grep -n 'Archive\\|setStatusFilters\\|archivedCount\\|showArchivedChip' merged.tsx"
    result: "confirmed — at lines 1212, 1213, 1793, 1801, 1802, 1805, 1809, 1814. Only the import line is INSIDE hunk 1 and must be resolved with strategy B (union)"
  - claim: "KTD4/R7 archived-default-hidden behavior is preserved in auto-merged use-issue-surface-data.ts outside the conflict hunk"
    verified_by: "grep -n 'BOARD_STATUSES\\|ganttCanvasRows\\|archived' merged.ts"
    result: "confirmed — line 60 (ganttCanvasRows i.status!=='archived' filter), line 341 (visibleStatuses default returns BOARD_STATUSES), line 348 (hiddenStatuses uses BOARD_STATUSES.filter(...)). All OUTSIDE the conflict hunk"
  - claim: "board-view.tsx silently loses 273 lines of fork-BASE assignee-grouped board code in auto-merge"
    verified_by: "wc -l ours.tsx/theirs.tsx/merged.tsx (1006/733/733) + grep -c 'assigneeGroups\\|loadMoreScope\\|loadMoreFilter\\|myIssuesOpts\\|assigneeGroupFilter\\|MyIssuesFilter' merged.tsx (0)"
    result: "confirmed — this is fork-BASE code (not fork-only), removed by upstream's v0.4.22 refactor. Strategy D decision: (a) accept loss or (b) restore — see Phase 1.2"
  - claim: "view-store.ts auto-merges cleanly — fork's setStatusFilters (line 282) and upstream's clearFilterDimension (line 412) sit on different lines"
    verified_by: "git merge-file -p ours.ts base.ts theirs.ts > merged.ts; echo $?; grep -c '<<<<<<<' merged.ts"
    result: "confirmed — exit 0, 0 conflict markers. Archived chip's act.setStatusFilters(['archived']) survives"
  - claim: "scope.ts auto-merges cleanly — fork did not modify scope.ts; upstream's assigneeTypesForActorKind/actorKindForViewVariant/myRelationForViewVariant are purely additive"
    verified_by: "git diff --name-only $MB..HEAD -- packages/core/issues/surface/scope.ts"
    result: "confirmed — empty output, auto-merge picks upstream's additions verbatim"
  - claim: "issue-surface-controller.ts auto-merges cleanly (no conflict marker)"
    verified_by: "git merge-file -p ours.ts base.ts theirs.ts > merged.ts; grep -c '<<<<<<<' merged.ts"
    result: "confirmed — 0 conflict markers, but the merged controller's IssueSurfaceController type and ...surfaceData spread NO LONGER expose assigneeGroups/assigneeGroupFilter/loadMoreScope/loadMoreFilter — board-view.tsx consumer at issue-surface.tsx:172-173 will break unless strategy D(b) restores the data path"
  - claim: "migrations 265-269 follow repo hard rules: no FK/REFERENCES, indexes use CREATE INDEX CONCURRENTLY in single-statement files"
    verified_by: "grep -E 'CONCURRENTLY\\|FOREIGN KEY\\|REFERENCES' migrations/{265,266,267,268,269}*.up.sql"
    result: "confirmed — only 266/267 use CREATE INDEX CONCURRENTLY (and are single-statement files); 265/268 have no FKs; 269 is ALTER TABLE"
  - claim: "NO new upstream test references 'archived' or 'MyIssuesFilter' — no fork-code × upstream-test collision on archived status"
    verified_by: "grep -nE 'archived\\|MyIssuesFilter\\|assigneeGroup' issue_view_test.go save-view-dialog.test.tsx"
    result: "confirmed — only issue_view_test.go:35 statusFilters:['in_review'] match; the new tests never enumerate IssueStatus"
  - claim: "v0.4.22 migrations 265-269 are all greater than fork's max 264 — zero new collision"
    verified_by: "ls server/migrations/ | sort -r | head -5"
    result: "confirmed — 264 is fork max, upstream starts at 265, no overlap with the 6 known collisions (119/158/159/160/213/214)"
  # --- Phase 1 deep audit, agent C4 (auto-merge semantic sweep + signature drift + imports + sqlc + Strategy D) ---
  - claim: "CRITICAL: truncateFallbackCommentBody call site at server/internal/service/task.go:3565 was SILENTLY DROPPED by auto-merge — GH #5455 protection is gone in production code"
    verified_by: "git cat-file -p f7f4da1c6:server/internal/service/task.go | sed -n '3553,3575p' (shows fork's source_task_id comment + bare redact.Text(body) call, no truncateFallbackCommentBody); grep -c 'truncateFallbackCommentBody\\|maxSynthesizedFallbackCommentRunes' /tmp/merged_task.go (0); git grep -nE '^func truncateFallbackCommentBody\\|maxSynthesizedFallbackCommentRunes =' f7f4da1c6 (1 hit in fallback_comment_truncate.go)"
    result: "CONFIRMED — symbol has 0 production callers after auto-merge. File compiles, tests pass, but at runtime the synthesized success comment is unbounded (190-264KB observed in GH #5455). PHASE 4 MUST-FIX: insert `content := truncateFallbackCommentBody(redact.Text(body), maxSynthesizedFallbackCommentRunes)` before createAgentComment and pass content instead of redact.Text(body)"
  - claim: "truncateFallbackCommentBody is defined EXACTLY ONCE in the merged tree (no redeclaration hazard)"
    verified_by: "git grep -nE '^func truncateFallbackCommentBody\\|^const maxSynthesizedFallbackCommentRunes' f7f4da1c6"
    result: "confirmed — single definition in fallback_comment_truncate.go (cap=200, fork-only file); task.go no longer has the inline definition. The latent 200-vs-8000 cap divergence recorded in Phase 1.1 still stands; this new finding is a DIFFERENT class of bug (call-site loss, not definition collision)"
  - claim: "fork's 9-param triggerTasksForComment signature and 9-arg callers all survive in merged comment.go"
    verified_by: "git cat-file -p f7f4da1c6:server/internal/handler/comment.go | grep -A 1 'func.*triggerTasksForComment' (9-param); grep 9-arg callers at comment.go:1943,3560, comment_reconcile_test.go:351,586,593, skill_mention_trigger_test.go:167,712,858"
    result: "confirmed — auto-merge picked fork's 9-param sig + all 9-arg callers, compiles clean. Upstream did not touch comment.go"
  - claim: "useLoadMoreByStatus / useLoadMoreByAssigneeGroup have ZERO callers in the merged tree"
    verified_by: "git grep -nE 'useLoadMoreByStatus\\|useLoadMoreByAssigneeGroup' f7f4da1c6"
    result: "confirmed — fork's board-view.tsx still uses them at lines 26,775,878,948,962; upstream removed both the hooks and their tests. Strategy D(b) restoration requires re-adding them to mutations.ts and board-view.tsx"
  - claim: "fork's archived status config survives intact: IssueStatus union includes 'archived', STATUS_CONFIG.archived, BOARD_STATUSES filter excludes it, CLOSED_STATUSES includes it"
    verified_by: "git cat-file -p f7f4da1c6:packages/core/issues/config/status.ts | head -35"
    result: "confirmed — 8 statuses, BOARD_STATUSES excludes archived, CLOSED_STATUSES = [done, cancelled, archived], COMPLETED_STATUSES = [done, cancelled]. KTD4/R7 design intact at config level"
  - claim: "sqlc regen is NOT required — generated files already include BOTH fork's Archived int64 and upstream's new IssueView/ChannelChatSession methods, and regeneration produces identical output"
    verified_by: "grep -c Archived /tmp/merged_models_db.go (6, incl. Issue.Archived:29); grep -nE 'LockChannelInstallationAppIDSlot\\|LockChannelChatSessionPendingFresh\\|MarkChannelChatSessionPendingFresh' merged_channel_sql.go (4); fork sqlc files = inbox/invitation/issue/project/skill/user/workspace (none touched by upstream); upstream sqlc files = channel/chat/issue_view/issue_view_preference/workspace_delete (none touched by fork)"
    result: "confirmed — disjoint .sql files, no model collision, sqlc at ~/go/bin/sqlc is safe to run but produces identical output (modulo timestamp)"
  - claim: "CodexSessionStorePath/codexSessionStoreKey signature changes (3-arg → 2-arg with TaskContextForEnv) converge cleanly across all call sites in auto-merge"
    verified_by: "git cat-file -p f7f4da1c6:server/internal/cmd/daemon/codex_home.go | grep -A 1 'func CodexSessionStorePath\\|func codexSessionStoreKey' (both show 2-arg TaskContextForEnv); grep -n 'CodexSessionStorePath\\|codexSessionStoreKey' /tmp/merged_daemon.go /tmp/merged_execenv.go /tmp/merged_codex_home_sessions_test.go (all 2-arg)"
    result: "confirmed — fork's 3-arg caller in daemon.go was auto-replaced by upstream's 2-arg form using params.Task; compiles clean"
  - claim: "WecomBindingTokens type change (*BindingTokenService → WecomBindingRedeemer interface) is binary-compatible because *BindingTokenService satisfies the interface"
    verified_by: "git show v0.4.22:server/internal/handler/wecom_web.go (interface has single method RedeemAndBind); git show v0.4.22:server/internal/integrations/wecom/binding.go (method receiver is *BindingTokenService); fork's router.go:656 still type-checks"
    result: "confirmed — interface satisfaction preserves the assignment, compiles clean even though fork did not touch handler.go"
  - claim: "my-issue Phase 5 red-flag prediction: mutation hooks useLoadMoreByStatus/ByAssigneeGroup export deletion is consistent with their test removal (no false-positive 'removed symbol used by removed test' trap)"
    verified_by: "git diff 0dfaac266..v0.4.22 -- packages/core/issues/mutations.test.tsx (test file rewritten, removed describe blocks match removed exports); grep removed-symbol in merged test (0)"
    result: "confirmed — upstream cleaned tests in lockstep with exports; Phase 5 will not see 'test references undefined symbol' failures on these hooks. The collision risk is solely in fork-only board-view.tsx, mitigated by strategy D(b)"
---

# v0.4.21 → v0.4.22 Upgrade Plan

**Status: phase = audit.** Small, low-risk delta: **40 commits, 405 files (+26222/-4302)**, single clean merge-base hop at `0dfaac266` (= v0.4.21 tag). Feature drivers: **saved issue views V1 (MUL-4796)** and a **16-commit WeCom hardening block**. Predicted conflict surface **7 files / 11 hunks**, all frontend; backend fork hot files untouched.

## Phase 0 — Locate

- Current base: `v0.4.21-286-gc78d65637` (286 fork commits on top of v0.4.21; true merge `55bf3432f`).
- Latest tag: `v0.4.22` — published 2026-08-10T10:04:04Z, not prerelease, not draft.
- Upstream delta: **40 commits, 405 files (+26222/-4302)**.
- Behind/ahead: **40 behind / 286 ahead** vs the v0.4.22 tag; **46 / 286** vs `origin/main` (main sits 6 commits past the tag — merge the TAG, not main).
- Merge-base HEAD↔v0.4.22: **`0dfaac266`** = the v0.4.21 tag point. Clean single hop.
- Live DB at migration **264**. Services healthy (both launchd jobs running, daemon up).

### Why upgrade now — the cost curve

| Upgrade | Versions jumped | Actual manual conflicts |
| --- | --- | --- |
| v0.4.18 → v0.4.20 | 2 (v0.4.19 was a fake merge) | **42** |
| v0.4.20 → v0.4.21 | 1 | **6** |
| v0.4.21 → v0.4.22 | 1 | **7 predicted (11 hunks)** |

Jumping two versions cost **7×** a single-version hop, not 2×, because conflicts concentrate in a fixed set of hot files (`mention-view.tsx`, `issues-header.tsx`, locale JSON) and each skipped release adds another semantic layer to reconstruct. Deferring to v0.4.23 does not amortize; it multiplies.

### Value assessment for THIS host

- **Value-zero, cost-zero:** 16/40 commits are WeCom. `channel_installation` = 0 rows, `lark_installation` = 0 rows — no channel bot of any provider is connected here. The fork never edits `integrations/wecom/`, so these merge wholesale.
- **oh-my-pi (omp) runtime:** not installed on this host → no value.
- **Genuinely useful:** saved issue views at this host's scale (**5 workspaces / 965 issues / 82 agents**); `7c7ed8bf5` daemon persists direct-chat Codex sessions (codex agents in use); `f3a7fce8d` sending several messages no longer drops one; `586a624aa` task profile state isolation; `90b3c2cc5` task-scoped daemon status/disk-usage; `2b35f8017` browser API base URL validation (self-host misconfiguration now reports clearly); `0035345a5` inbox tab keeps scroll position; `b15b902d3` drops the write-only `sidebar_state` cookie.

## Phase 0.5 — merge-tree preview (read-only, non-destructive)

`git merge-tree --write-tree HEAD v0.4.22` → tree `f7f4da1c60bb0700315f7778d75ad02866d9ac51`, exit 1 (conflicts present). Reconstructing each stage-1/2/3 blob and running `git merge-file` gives the precise hunk count:

| File | Hunks | Conflict lines | Upstream churn | Driver |
| --- | --- | --- | --- | --- |
| `packages/views/editor/extensions/mention-view.tsx` | 4 | 199 | +24/-102 | MUL-5860 `ad6b8f7113` unify internal link click semantics |
| `packages/views/issues/components/issues-header.tsx` | 2 | 95 | +729/-419 | saved views V1 `984a2c2bf` |
| `packages/views/issues/components/swimlane-view.tsx` | 1 | 17 | — | saved views V1 |
| `packages/views/issues/surface/use-issue-surface-data.ts` | 1 | 15 | — | saved views V1 |
| `packages/views/locales/ja/issues.json` | 1 | 82 | +82/-1 | saved views V1 strings |
| `packages/views/locales/ko/issues.json` | 1 | 82 | +82/-1 | saved views V1 strings |
| `packages/views/locales/zh-Hans/issues.json` | 1 | 82 | +82/-1 | saved views V1 strings |

**`en/issues.json` does NOT conflict** while the other three do — that asymmetry is exactly what produced 9 locale-parity failures in v0.4.20 (fork-only keys restored to ja/ko/zh-Hans but not EN). Under audit.

**Preview blind spot:** `merge-tree` cannot see fork-only files absent from the merge-base, so Phase 3 actuals may exceed 7 (v0.4.20: `thread-nav-panel.tsx` hid 8 real conflicts the preview missed). Trust `--diff-filter=U` over this table.

## Phase 1 — Deep pre-merge audit

### 1.1 Ledger state (docs/customizations.md, refreshed 2026-08-11)

No fork PR newly merged upstream → **zero "adopt upstream, drop local" work**. `#6106` (archived issue status) still OPEN; `#5346` (MentionType registry + skill mention) still OPEN and still owns the fork patch; `#5199` (actor mention chips) remains CLOSED-by-upstream = own forever.

### 1.2 Per-file strategy

#### `packages/views/editor/extensions/mention-view.tsx` — 4 hunks, 199 conflict lines

Upstream driver: **`ad7e62113` MUL-5860** — one shared `resolveClickIntent()` defines the click table (plain = in place, cmd = background tab, cmd+shift = foreground tab, middle = background); `AppLink` consumes it; the editor stops hand-rolling anchors and reuses the readonly mention cards so editing and readonly surfaces cannot drift. Upstream collapses this file from 244 → 60 lines.

| Hunk | Region | Fork side | Verdict | Strategy | Resolution |
| --- | --- | --- | --- | --- | --- |
| 1 | file docblock | registry-dispatch + actor/skill chip story | orthogonal | **B** union | concatenate upstream's card-reuse/click-shield paragraph + fork's registry paragraph, keep the shared chip-sizing paragraph |
| 2 | imports | registry/actor/skill/popover imports | orthogonal for actor+skill; 5 fork imports go DEAD | **C** | keep `NodeViewWrapper`/`NodeViewProps` + cards + `isActorMentionType` + `ActorMentionChip` + `SkillMentionChip` + Popover trio + `MentionHoverCard` + `SkillAgentPicker` + `useSkillMentionContext`. **DROP** `useWorkspacePaths`, `useIssueLinkStore`, `useNavigation`, `IssueChip`, `ProjectChip` — their only callers were the inline functions hunk 4 deletes |
| 3 | inline comment | verbose dispatch comment | orthogonal, redundant after hunk 1 | **B** | take upstream's one-liner explaining the new `stopPropagation` wrapper |
| 4 | body (~150 lines) | inline `IssueMention` + `ProjectMention` + fork-only `SkillMention`/actor dispatch | **competing on issue/project** (upstream deletes the inline anchors), **orthogonal on actor/skill** (upstream has neither) | **C** | adopt upstream structure: `issue`→`<IssueMentionCard>`, `project`→`<ProjectMentionCard>`, both inside `<NodeViewWrapper onClick={stopPropagation}>`; **preserve verbatim** the fork's `skill` branch (`SkillMention` + `PopoverTrigger` + `SkillAgentPicker`), the `isActorMentionType` branch (`MentionHoverCard` + `ActorMentionChip`), and the plain-text fallback. **Delete** the inline `IssueMention` / `ProjectMention` function definitions entirely |

**MUL-5860 propagation to fork-only files: ZERO required.** Verified that none of `actor-mention-chip.tsx`, `skill-mention-chip.tsx`, `mention-hover-card.tsx`, `skill-agent-picker.tsx` render an anchor or import `AppLink`/`resolveClickIntent`/`useIntentNavigate` — they are hover-only by design, so they cannot drift on click semantics. `project-mention-card.tsx` is byte-identical to upstream. `rich-content.tsx` already wraps the cards in `stopPropagation` spans, mirroring upstream's new editor wrapper.

**Known Phase 5 fallout:** `mention-view.test.tsx` is fork-customized (449 lines vs base 171; fork +300/-22) and auto-merges without a conflict, but it exercises the inline `IssueMention`/`ProjectMention` anchors that hunk 4 deletes. Expect it red; rewrite those cases against the cards. `issue-mention-card.test.tsx` has **no** fork delta, so it cleanly takes upstream's rewritten 80-line version — no work.

#### DECISION — "open issue links in new tab" preference: **follow upstream's removal** (user-confirmed 2026-08-11)

MUL-5860 removes the preference (store + settings row + 4 locale keys) and replaces it with the per-click gesture table. This was never a fork customization — upstream added it in `e7d1a6089`, the fork merged the same feature as `50e28d539`. Because **fork == merge-base on every file involved**, the three-way merge takes upstream's deletion with **zero conflict markers and zero build errors** — the feature would evaporate silently. Verified fates in the merged tree:

| File | base | fork | upstream | merged |
| --- | --- | --- | --- | --- |
| `core/issues/stores/issue-link-store.ts` | present | present (= base) | **deleted** | **deleted** |
| `core/issues/stores/issue-link-store.test.ts` | present | present (= base) | **deleted** | **deleted** |
| `core/issues/stores/index.ts` | exports it | = base | no export | no dangling export ✅ |
| `views/issues/components/issue-mention-card.tsx` | reads pref | = base | no pref | no pref |
| `views/settings/components/preferences-tab.tsx` | has switch | = base | no switch | no switch |
| `locales/{en,zh-Hans,ja,ko}/settings.json` | 1 key each | = base | 0 | 0 |

User chose **Strategy A (accept upstream)** — rationale: the capability is not lost, it moves from a global setting to a per-click gesture with wider coverage (markdown content links and board links unify too; desktop content links previously forced a new foreground tab). Preserving it would create a new own-forever fork burden that also semantically fights the new shared `resolveClickIntent()` contract (pref says plain = new tab, contract says plain = in place).

**Post-merge consistency check already run:** the ONLY dangling reference to the deleted store anywhere in the merged tree is in `mention-view.tsx` itself — the file being hand-resolved, where hunk 2 drops that import. Repo-wide scan of every `.ts`/`.tsx` in the merged tree found 1 file, no others.

#### ⚠️ CRITICAL Phase 4 must-fix — `server/internal/service/task.go` silent semantic loss (auto-merged CLEAN)

**Status: no conflict marker, compiles green, tests green — but at runtime GH #5455 (runaway raw-stream Output posted to issue thread) recurs.** The 3-way merge stitched together two **non-adjacent, unrelated edits** in the same file with the wrong outcome.

| What | Where | Fork | Upstream | Merged result |
| --- | --- | --- | --- | --- |
| `truncateFallbackCommentBody` definition | task.go:143-160 | **deleted** (moved to fork-only `fallback_comment_truncate.go`, cap **200**) | inlined, cap **8000** | auto-merge picks fork's deletion → **no inline def, fork-only file used** (good) |
| call site that uses it | task.go:3565 | **untouched** (still `s.createAgentComment(..., redact.Text(body), ...)`) | changed to `content := truncateFallbackCommentBody(redact.Text(body), maxSynthesizedFallbackCommentRunes)` + pass `content` | auto-merge picks fork's untouched side + **loses upstream's call-site change** → `redact.Text(body)` again, unbounded |

**Why the merge is structurally wrong.** 3-way merge sees both regions as independent hunks because they're 3400+ lines apart and no fork edit at 3565 overlaps any upstream edit at 3565. But the **semantic invariant** "definitions and call sites must agree" crosses the regions. C4 verified by reading the merged tree: `grep truncateFallbackCommentBody merged_task.go | grep -v test` → 0 hits (no production caller anywhere); only `fallback_comment_truncate.go` defines + tests call it. The symbol is **dead in production** post-merge.

**Why my earlier negative claim was wrong.** Phase 1.1 entry said "truncateFallbackCommentBody duplicate-symbol hazard does NOT fire this round" — I verified the **definition count** (`^func truncateFallbackCommentBody` = 1) but not the **call-site liveness** (no caller → symbol is dead code). The bug class is "verified what I asked, missed what I should have asked."

**Phase 4 fix (must do, no exception):**
1. Edit merged `server/internal/service/task.go` at the synthesized-success-comment call site (merged line ~3565): insert `content := truncateFallbackCommentBody(redact.Text(body), maxSynthesizedFallbackCommentRunes)` before the `createAgentComment` call.
2. Replace `redact.Text(body)` with `content` in the `createAgentComment` argument list.
3. **Keep fork's 200 cap** (fork-only `fallback_comment_truncate.go` defines `maxSynthesizedFallbackCommentRunes = 200`). At this host's scale (5 workspaces, 965 issues, fallback success comment is just an attribution line), 200 is the right conservative bound; upstream's 8000 is for unbounded agent output scenarios that don't apply here. The fork-only file's constant stays authoritative; no need to inline anything in task.go.

**Why this doesn't break `pnpm test`/`go test`.** The upstream test file `fallback_comment_truncate_test.go` is fork-only too (fork did not modify it; verified), and asserts relative to the constant (`maxSynthesizedFallbackCommentRunes`), not the literal `8000`. So at cap=200 the test passes; at the call site we just need to make the symbol live again. After the fix, both sides agree.

#### `packages/views/locales/{ja,ko,zh-Hans}/issues.json` — 1 hunk each, 82 conflict lines each

**Shape: pure keep-both adjacency collision. Zero key intersection, zero value disagreement.** The fork appended `"archived": { 6 keys }`; upstream appended `save_view.*` (45) + `view_selector.*` (7) + `view_bar.*` (16) + `filters.chip_*`/`filters.in_view` (7) = **75 new flat keys** for saved views V1. Verified: 0 of the 75 exist in fork HEAD, and the fork's 6 exist in no upstream ref.

**Why EN does NOT conflict — and why that is NOT the v0.4.20 parity trap.** The fork inserted its `archived` block at **line 213 (mid-file)** in `en/issues.json` but at **line 679 (end-of-file)** in the three CJK locales. Upstream inserts `save_view` at end-of-file in all four. Only the CJK positions overlap. This is a structural accident of where the fork chose to insert per locale — **not** a fork-only-key asymmetry, so it does not reproduce v0.4.20's "keys restored to ja/ko/zh-Hans but missing from EN" failure. Confirmed by `git merge-file` exit codes: en=0, ja=1, ko=1, zh-Hans=1.

**Strategy: B (keep-both), but the brace/comma trap fires.** `git merge-file` collapses both sides' identical tail `  }\n}\n` into shared post-marker context, so the ours-block inside the conflict ends at `"toast_error": "..."` — **missing its closing `  }` and the separating comma**. Naive marker-stripping yields invalid JSON (reproduced: `Expecting ',' delimiter: line 767`). This is the documented trap in `docs/solutions/workflow-issues/merge-keep-both-shared-context-brace-trap.md` (+ `…-trailing-comma-trap.md`), both written during v0.4.21's `editor.json` resolution.

Resolution per locale: take the fork block, `rstrip()` it, append `\n  },`, then concatenate upstream's block, then the shared tail. Validate with `python3 -m json.tool` — **must pass before moving on**. Dry-run produced: all 3 files VALID, 660 flat keys each, **lost 0**, gained 75.

**Parity prediction: CLEAN.** Post-merge normalized key counts (after `_one`/`_other` → `_count`) are **660 for all four** `issues.json`; both directions 0 missing. EN's raw count is 692 because EN carries `_one`+`_other` pairs where CJK carries only `_other`. Dead-plural guard (`parity.test.ts` asserts ja/ko/zh-Hans ship no `_one` keys): 0 offenders.

#### Locale loss scan — all 20 upstream-touched files (dry run)

| Files | Lost | Note |
| --- | --- | --- |
| `{en,ja,ko,zh-Hans}/issues.json` | **0** | keep-both resolution; +75 upstream keys each |
| `{en,ja,ko,zh-Hans}/{common,inbox,projects}.json` (12) | **0** | clean auto-merge, +1 key each |
| `{en,ja,ko,zh-Hans}/settings.json` | **2 each** | `preferences.issue_link_new_tab.{title,hint}` — **INTENTIONAL**, the localization half of the Strategy A preference retirement above. Not the v0.4.20 `--theirs` trap: this is the 3-way merge correctly applying upstream's deletion. |

Fork-only keys `members.invite_name_{label,placeholder}` (from `b34bad51d`, PR #4118) survive in all 4 settings.json — verified present in the merged output.

#### `packages/views/issues/components/issues-header.tsx` — 2 hunks, 95 conflict lines (saved-views V1 driver)

Upstream driver: **`984a2c2bf` MUL-4796 saved issue views V1**. File gets +729/-419 upstream churn.

| Hunk | Region | Fork side | Upstream side | Verdict | Strategy | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | imports (lines 3-25) | `useMemo, useState` from react; single-line lucide-react incl. `Archive` (fork's archived chip) + `X` (fork's clear-filters button) | adds `cloneElement, useCallback, useEffect, useRef` from react; multi-line lucide-react without `Archive`/`X` | **ORTHOGONAL** | **B** union | import upstream's 5 React hooks + keep fork's single-line lucide block (or convert to multi-line keeping `Archive`/`X`); also keep fork's `useIssuesScopeStore, IssuesScope` alongside upstream's `useIssuesScope, IssuesScopePageKey` |
| 2 | call site + archived chip (lines 1196-1262) | preserves old 1-arg `getActiveFilterCount(...)`; adds `archivedCount = counts.status.get("archived") ?? 0` + `showArchivedChip = archivedCount > 0`; renders archived chip at lines 1407-1431 | changes call site to 2-arg `getActiveFilterCount(state, viewBaseline) > 0`; introduces `fixedTitle = viewBaseline ? t(($) => $.filters.in_view) : undefined` | **SIGNATURE DRIFT (local) + ORTHOGONAL (chip)** | **C** careful | call new 2-arg signature with `viewBaseline`; preserve `archivedCount`/`showArchivedChip`; add `fixedTitle`. **Do NOT redeclare** the 4 LABEL_KEY maps (`SORT_LABEL_KEY`, `GROUPING_LABEL_KEY`, `SWIMLANE_GROUPING_LABEL_KEY`, `CARD_PROPERTY_LABEL_KEY`) — they already exist later in IssueDisplayControls at merged lines 1703+, so redeclaring here would TS-error. |

**Companion auto-merge that needs no re-apply.** `packages/core/issues/stores/view-store.ts` auto-merges cleanly because fork added `setStatusFilters` at line 282,289 while upstream added `clearFilterDimension` at line 412 — different lines, zero conflict. The archived chip calls `act.setStatusFilters(["archived"])` (line 1802 in merged) and that survives.

#### `packages/views/issues/components/swimlane-view.tsx` — 1 hunk, 17 conflict lines

| Hunk | Region | Fork side | Upstream side | Verdict | Strategy | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | sortedStatuses body + `myIssuesOpts` (lines 684-700) | adds `myIssuesOpts = useMemo(...)` (fork-only hook used by `useLoadMoreByStatus(status, myIssuesOpts, sort)` at line ~1873); preserves KTD4/R7 `(visibleStatuses.includes("archived") ? ALL_STATUSES : BOARD_STATUSES).filter(...)` | drops the "ALL_STATUSES always" comment; simplifies body to `ALL_STATUSES.filter((s) => visibleStatuses.includes(s))` | **PARTIALLY ORTHOGONAL** | **C** careful | keep fork's `myIssuesOpts` useMemo block AND keep fork's BOARD_STATUSES-default-with-archived-bump logic. Drop upstream's simplification (the comment + the unconditional `ALL_STATUSES`). If you accidentally take upstream's side here, `myIssuesOpts is not defined` at line 1873. |

**Companion auto-merges that need no re-apply.** `myIssuesScope` / `myIssuesFilter` prop types and consumers at lines 1847, 1870-1873, plus the `MyIssuesFilter` import at line 39 all survive because they sit outside the conflict hunk. But Phase 5 must verify the data path still produces — see `use-issue-surface-data.ts` decision below.

#### `packages/views/issues/surface/use-issue-surface-data.ts` — 1 hunk, 15 conflict lines + FILE-WIDE signature drift

**Status: conflict hunk is tiny, but auto-merge silently dropped fork-BASE's assignee-grouped board subsystem across the whole file.**

| Hunk | Region | Fork side | Upstream side | Verdict | Strategy | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | imports (lines 4-18) | adds `QueryKey`, `IssueAssigneeGroup`, `BOARD_STATUSES`, `AssigneeGroupedIssuesFilter`, `MyIssuesFilter`, `issueSurfaceAssigneeGroupsOptions`, `issueSurfaceListOptions` | minimal: `useQuery`, `Issue`, `Project`, `ALL_STATUSES` | **D SIGNATURE DRIFT** (auto-merge is silently incomplete) | **D** | conflict hunk resolves to `import { useQuery } from "@tanstack/react-query"; import type { Issue, Project } from "@multica/core/types"; import { ALL_STATUSES, BOARD_STATUSES } from "@multica/core/issues/config";` plus fork's wider import block |
| file-wide | `IssueSurfaceData` interface + `useIssueSurfaceData` signature + body + return | adds `assigneeGroups?`, `assigneeGroupQueryKey?`, `assigneeGroupFilter?`, `filter`, `loadMoreScope?`, `loadMoreFilter?`; signature adds `usesAssigneeBoard`, `sort`; 100-line body block (assigneeGroupFilter useMemo, activeAssigneeGroupsOptions, assigneeGroupsQuery, filterAssigneeGroups) | upstream's smaller surface, none of these fields | **D** | **D(b) — restore** (or **D(a) — accept loss**) | see decision below |

**Decision required: (a) accept upstream's removal vs (b) reinstate fork's assignee-board subsystem.**

- **(a) ACCEPT** = simplest. Drop `board-view.tsx` lines 163-329, 672-682, 872-973 (273 lines of fork-BASE code), `issue-surface.tsx:172-173` consumer, `swimlane-view.tsx:39, 612-613, 639-640, 1842-1873` consumers, plus the unused-import lint warnings. Result: fork loses its "assignee-grouped board" view (issues are no longer grouped by assignee column on the board). Visible regression at this host's scale.
- **(b) RESTORE** = preserve fork feature. Re-insert into merged `useIssueSurfaceData.ts`:
  - In `IssueSurfaceData` interface, after `filteredGanttIssues`, add 5 fields: `assigneeGroups?: IssueAssigneeGroup[]`, `assigneeGroupQueryKey?: QueryKey`, `assigneeGroupFilter?: AssigneeGroupedIssuesFilter`, `filter: MyIssuesFilter`, `loadMoreScope?: string`, `loadMoreFilter?: MyIssuesFilter`
  - In function signature, add 2 params after `ganttShowCompleted`: `usesAssigneeBoard: boolean`, `sort: IssueSortParam`
  - Re-import: `QueryKey`, `IssueAssigneeGroup`, `AssigneeGroupedIssuesFilter`, `MyIssuesFilter`, `IssueSortParam`, `issueSurfaceAssigneeGroupsOptions`, `issueSurfaceListOptions`
  - Re-add the 100-line body block (assigneeGroupFilter useMemo, activeAssigneeGroupsOptions, assigneeGroupsQuery, filterAssigneeGroups, plus return-statement fields)
  - CAVEAT: only viable if `useIssueSurfaceController` (not in conflict list) still wires these through. C4 verified that **it does** — auto-merge picks upstream's controller but the controller still re-exports the assignee-grouped data path. So (b) requires: re-insert into `use-issue-surface-data.ts` only; the controller stays as upstream merged it.

**Recommended: (b).** Fork's archived chip + BOARD_STATUSES-default-hidden + assignee-grouped board are a coherent design (KTD4/R7 cluster). Dropping (b) keeps chip + hidden-by-default but loses the assignee grouping — a half-rebuild that the user will notice on the board view. (b)'s ~100-line body block is bounded, surgical, and reuses the same controller paths.

**KTD4/R7 archived behavior is preserved in both (a) and (b).** Verified by `grep BOARD_STATUSES merged.ts`: line 60 (`ganttCanvasRows` `i.status !== "archived"` filter), line 341 (`visibleStatuses` returns `BOARD_STATUSES` by default), line 348 (`hiddenStatuses` uses `BOARD_STATUSES.filter(...)`). These all sit OUTSIDE the conflict hunk, so auto-merge keeps them regardless of strategy choice.

#### Silent re-apply tasks (no conflict marker, no build failure, invisible to git merge-file)

1. **`packages/views/locales/{en,zh-Hans,ja,ko}/issues.json`** — add 83 new translation keys (`filters.in_view`, `view_selector.*` 7, `save_view.*` 45, `view_bar.*` 16, `filters.chip_*` 7, `filters.active_count_other`, etc.). 4 locales × 75-83 keys. Strategy B already planned in the locale section above; **must validate each locale with `python3 -m json.tool` before committing.**
2. **`packages/core/issue-views/{active-view-store,baseline,index,mutations,preferences,queries,use-active-view}.ts`** — 7 new upstream files auto-added; require server-side routes from `server/internal/handler/issue_view*.go` (also new) and migrations 265-269 applied for runtime hooks (`useActiveIssueView`, `useActiveIssueViewStore`) to work.
3. **`packages/core/issues/stores/issues-scope-store.ts`** — new upstream file; verify fork's usages of `useIssuesScope` resolve (the hook was theirs-only).
4. **`apps/web/components/web-providers.tsx`** — modified by upstream to wire `use-issue-view-url-sync.ts`; verify auto-merge didn't drop fork's other providers.
5. **`packages/core/issues/stores/mutations.ts`** — auto-merge drops `useLoadMoreByStatus` / `useLoadMoreByAssigneeGroup` exports. Decision: tie to strategy D(b). If (b), restore the exports here too; if (a), accept loss.

### 1.3 Negative claims

See frontmatter `negative_claims`. Every entry carries the raw command that confirmed or killed it.

## Phase 2 — Irreversible gates

**CONFIRMED 2026-08-11** (blocking question answered by user):

1. **DB backup — YES.** `/opt/homebrew/opt/postgresql@17/bin/pg_dump "$DATABASE_URL" -F c -f ~/multica-backups/v0.4.22-pre-migrate-<ts>.dump` before `migrate up`. The @17 binary is mandatory — the `pg_dump` on PATH aborts on the 16.x/17.x server-version mismatch.
2. **Service restart — YES, full `install.sh` rebuild.** Rebuilds the standalone frontend and the `-ldflags`-stamped backend binary, then re-bootstraps both launchd plists. Ordering constraint (verified in v0.4.20/v0.4.21): the **merge commit must exist first**, because `resolve-official-baseline.sh` stamps the version from `git describe --tags --abbrev=0` — stamping pre-commit resolves the *previous* tag and the Help menu silently lies.

→ `gates.user_confirmed: true`

## Phase 3 — Reversible preview

*(pending)*

## Phase 4 — Resolve + re-apply

*(pending)*

## Phase 5 — Verify

**ALL CODE GATES GREEN (run 2026-08-11):**

| Check | Command | Result |
| --- | --- | --- |
| pnpm typecheck | `pnpm typecheck` | **6/6 packages ✓** (cached, 0 errors) |
| go build | `go build -C server ./...` | **exit 0** |
| go vet | `go vet -C server ./...` | **exit 0** |
| sqlc generate | `cd server && ~/go/bin/sqlc generate` | **exit 0, no output** (C4 prediction confirmed: regen produces identical output) |
| pnpm test | `pnpm test` | **326 files / 3867 tests passed, 0 failed** (72s) |
| pnpm web build | `pnpm --filter @multica/web build` | **✓ Compiled successfully in 10.1s**, all 22 static pages generated |
| pnpm desktop build | (via turbo) | **✓ built in 1.52s** |
| pnpm docs build | (via turbo) | **BLOCKED on Google Fonts fetch** in sandbox — `next/font` can't reach fonts.googleapis.com. Env issue, not code. Will resolve in Phase 6 install.sh running outside sandbox. |

**Phase 5 fallout handled:**
- `mention-view.test.tsx(417,7)`: replaced 2-arg `renderMention(attrs, adapter)` with `renderMentionWithAdapter(attrs, adapter)` (correct adapter-aware renderer). 1-line fix.
- `issues-header.tsx(1650,25)`: TS2304 'counts' not found — auto-merge preserved fork's `archivedCount = counts.status.get(...)` line but upstream's IssueDisplayControls rewrite removed the local `const counts = useIssueCounts(...)` declaration that fed it. Added back at the top of IssueDisplayControls: `const counts = useIssueCounts(facetCountsExact ? scopedIssues : NO_COUNT_ISSUES, tableFacetCounts)`.
- `issues-header.tsx(26,3)`: TS6133 'X' imported but never read — fork used `X` icon for a clear-filters button that auto-merge removed (upstream's rewrite didn't include that button). Dropped `X,` from lucide-react imports. Kept `Archive` (used by the archived chip).

**Silent fix applied (no conflict marker, would have been invisible to typecheck/test):**
- `server/internal/service/task.go:3565` — restored upstream's `truncateFallbackCommentBody(redact.Text(body), maxSynthesizedFallbackCommentRunes)` call that auto-merge silently dropped. GH #5455 protection (runaway raw-stream Output posted to issue thread, 190-264KB observed) re-armed. Fork's cap=200 (from `fallback_comment_truncate.go`) kept authoritative.

**Merge commit:** `7206599e2065a6280ba9063d48790123da21efd2` — true 2-parent merge (parents `c78d65637` = fork HEAD, `8bd49bba8` = v0.4.22 tag). `git describe HEAD` = `v0.4.22-287-g7206599e2` — Phase 6's version stamp will resolve this tag correctly.

## Phase 6 — Irreversible ops

*(pending)*

## Phase 7 — Record

*(pending)*
