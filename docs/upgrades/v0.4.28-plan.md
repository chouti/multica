---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.28
base_tag: v0.4.26   # clean merge-base = v0.4.26^{commit} (single hop; prior merge 75ec751af)

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true           # Phase 2 — CONFIRMED 2026-08-18: (1) archived = compose-and-keep (teach
                                 # issuestatus resolver + compose SQL predicates; 267 rows keep semantics);
                                 # (2) housekeeping = restore apps/desktop/build/ + commit graft tooling docs;
                                 # (3) scope = full push to Phase 6 (pg17 backup → migrate 314-341 → re-stamp →
                                 # install.sh restart → health). Daemon NOT force-upgraded (protocol additive).
  audit_strategy_written: true    # Phase 1.4 — FLIPPED 2026-08-18: per-file strategy in Phase 1 section;
                                  # 5 main-session negative claims + agent command-verified negatives recorded
  db_backed_up: true              # Phase 6.1 — DONE 2026-08-18T13:13:31Z (CST): pg17 pg_dump -F c,
                                  # 728 TOC entries (pg_restore -l verified). Dump:
                                  # ~/multica-backups/v0.4.28-pre-migrate-20260818T131331Z.dump
  migrated: true                  # Phase 6.2 — DONE 2026-08-18: applied 314-341 (28 migrations,
                                  # concurrent-index pre-hooks ran e.g. 336). Latest = 341. Verified:
                                  # archived rows intact (267), issue_status seeded (35 = 5 ws × 7),
                                  # 0 format-violating rows (337/338 constraint swap safe on real data).
  version_stamped_frontend: true   # Phase 6.3 — DONE 2026-08-18: .env NEXT_PUBLIC_APP_VERSION v0.4.26→v0.4.28
                                    # (line 27) + install.sh standalone build; bundle grep hit v0.4.28 in layout chunk.
  version_stamped_backend: true    # Phase 6.3 — DONE 2026-08-18: strings ~/.multica/backend/server →
                                    # -ldflags="-s -w -X main.version=v0.4.28"; runtime /api/config
                                    # server_version=v0.4.28. commit-first satisfied (08c61c3ab at HEAD,
                                    # git describe = v0.4.28-307-g08c61c3ab).
  services_restarted: true         # Phase 6.4 — DONE 2026-08-18: install.sh rebuilt both + bootout/bootstrap
                                    # both plists (loaded com.fengzhao.multica-{frontend,backend}).
  health_checked: true             # Phase 6.5 — DONE 2026-08-18: backend /healthz=200 (startup retry loop),
                                    # frontend :3001=200, /api/config server_version=v0.4.28.
                                    # Phase 6.6 daemon: ALIGNED 2026-08-18 (later same day) — user manually
                                    # upgraded via tap (brew shipped 0.4.28, commit 38c992ad0 = tag) and
                                    # restarted: daemon 0.4.28 pid 6818, 5 agents + 5 workspaces verified.
                                    # Remote-MCP daemon support now available. Full stack aligned: CLI/daemon/
                                    # backend/frontend all 0.4.28.

merge_commit: 08c61c3ab   # TRUE 2-parent (p1 dcb8d70d8 fork HEAD incl. graft chore, p2 38c992ad0
                          # = v0.4.28^{commit}); git describe = v0.4.28-307-g08c61c3ab

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done    # ALL PHASES COMPLETE 2026-08-18. Upgrade v0.4.26 → v0.4.28 fully executed.

# Phase 7 — Record (DONE 2026-08-18)
# - Ledger updated: rolling audit line + "Last upgrade — v0.4.26 → v0.4.28" section + drift-notes
#   migration chain (314-341) + last_updated. Plan committed with ledger in docs commit.
# - Actuals vs plan: 23 predicted conflicts == 23 actual (9th consecutive audit hit; zero surprise
#   files). Semantic traps the audit flagged ALL materialized and were preempted in Phase 4:
#   SkillSchema dup (surgery), setServerVersion rename (auto-merge had already reconciled — audit's
#   warning did NOT materialize), entry.archived regen trap (kept column + sqlc regen), cmd_issue
#   contradictory contracts (test rewritten to format-only world). One prediction MISSED by audit:
#   issue_child_done_stage_test stageBarrierClosed gained a 3rd arg (predicate) — go vet caught it,
#   fixed by passing literalTerminalChild (semantically identical for fork statuses).
# - Handler-test environment revived as a side product: dev DB :5432/multica was stale at 212 with
#   0 rows; drop/recreate + migrate 1-341 turned 365 failures into exactly the 6 ledger residuals.
#   Reusable for future upgrades before Phase 6 (validate on dev DB first, never :5433).
# - New pre-existing residuals surfaced (all verified identical on backup, NOT regressions):
#   TestBuiltinSkillsConformToTemplate (892-char multica-mentioning description vs 300 cap),
#   TestRunSkillImportJsonTreatsDuplicateAsConflictResult (#2669 batch routing),
#   TestSynthesized/FailureSystemCommentStampsSourceTaskID (190-era constraint in seed).
# - Deferred: CLOSED 2026-08-18 (same day) — user aligned daemon to 0.4.28 via tap (brew shipped it);
#   daemon pid 6818, agents/workspaces verified. Long-term archived-vs-custom-status-system migration
#   question stays open (compose-and-keep is the carry; revisit if upstream's system adds an
#   archived-like closed-not-completed category or #6106 merges).

# Negative claims — each confirmed/refuted by ONE raw command, never by reading.
# Verified in the MAIN session (not delegated to agents).
negative_claims:
  - claim: "merge-base(HEAD, v0.4.28) = v0.4.26^{commit} — single clean hop, prior merge was true 2-parent"
    verified_by: "git merge-base HEAD v0.4.28 → 19155e41f; git rev-parse v0.4.26^{commit} → 19155e41f (identical)"
    result: "confirmed. Single-hop v0.4.26 → v0.4.28; 49 upstream commits, 612 files (+39629/-3423)."
  - claim: "NEW upstream migrations = 314-341 (28 stems), ALL > fork max 313 → ZERO new numeric collision; 6 known collisions (119/158/159/160/213/214) unchanged"
    verified_by: "comm -13 <(git ls-tree v0.4.26 server/migrations/ --name-only|sort) <(git ls-tree v0.4.28 server/migrations/ --name-only|sort) → min 314 max 341; git ls-tree HEAD server/migrations/ | awk top = 313"
    result: "confirmed. MUL-6243 custom-status migrations live inside 314-341; semantic (not numeric) collision is the real question — under audit."
  - claim: "upstream PR #6106 (archived status) is still OPEN — fork customization remains live, upstream MUL-6243 is a parallel system not an adoption"
    verified_by: "gh pr list --repo multica-ai/multica --author chouti --state all → '#6106 OPEN -'; also #5346/#5309/#4118/#2669/#5160/#2460 OPEN, #5539/#5199 CLOSED, #5006/#5393 MERGED"
    result: "confirmed. Ledger PR states current as of 2026-08-18."
  - claim: "desktop build assets deletion is an uncommitted working-tree accident, and v0.4.28 does not touch apps/desktop/build/ — restoring is merge-safe"
    verified_by: "git ls-tree HEAD apps/desktop/build/ → files still tracked; git log -- apps/desktop/build/ → last touch upstream ebd0248be, no fork removal commit; git diff --stat v0.4.26..v0.4.28 -- apps/desktop/build/ → empty; ls apps/desktop/build/ → No such file"
    result: "confirmed. Recommend git checkout -- apps/desktop/build/ before merge (Phase 2 item)."
  - claim: "pre-upgrade baseline is healthy and consistent with v0.4.26 (for Phase 6 comparison)"
    verified_by: "curl /healthz=200, :3001=200; multica daemon status → running v0.4.26, 5 agents, 5 workspaces; psql schema_migrations → 313_runtime_profile_add_dsh"
    result: "confirmed 2026-08-18. Daemon = Homebrew CLI 0.4.26, uptime 17h."

# Phase 0 — Locate (DONE 2026-08-18)
# - git describe --tags HEAD → v0.4.26-305-g75ec2d481 (fork 305 ahead of v0.4.26)
# - gh release view → v0.4.28, published 2026-08-17T14:21:34Z, not prerelease/draft
# - upstream delta v0.4.26..v0.4.28 = 49 commits, 612 files (+39629/-3423)
# - merge-base(HEAD, v0.4.28) = v0.4.26^{commit} (single hop; prior merge 75ec751af true 2-parent)
# - WORKING TREE DIRTY (pre-existing, NOT from this upgrade): .gitignore + AGENTS.md graft docs,
#   apps/desktop/build/* DELETED (uncommitted — breaks desktop packaging; needs user decision),
#   untracked .mcp.json/.ignore/opencode.json (local tooling). v0.4.26..v0.4.28 does NOT touch
#   any of these files → merge itself unblocked.
#
# Phase 1 — Deep pre-merge audit (IN PROGRESS 2026-08-18)
#
# === Agent A: issue-status semantic collision (RETURNED; key claims re-verified in main session) ===
# MUL-6243 = migrations 332-340 + new pkg server/internal/issuestatus/ + handler issue_status.go.
# Model: per-workspace issue_status CATALOG table; issue.status stays TEXT holding keys; 7 hardcoded
# categories (backlog/todo/in_progress/in_review/done/blocked/cancelled) = behavior equivalence classes.
# - 337 DROPS issue_status_check (the exact constraint fork 213 re-created) → format-only check;
#   existing status='archived' rows pass format regex. VERIFIED: git show v0.4.28:...337_issue_status_open_check.up.sql
# - Resolve("archived") → ErrUnknownStatus → 400 on ALL write paths (CLI included). VERIFIED: read Resolve body.
# - CustomIssueStatuses flag gates only CREATING custom statuses (default false); reader/Resolve ungated. VERIFIED keys.go:83.
# - Pure-upstream code on fork data: archived issues REAPPEAR in default list (ListIssues open filter),
#   count as active duplicates, keep parent stage open, child-progress archived count column lost (undefined).
# - Fork 213/214 migrations mechanically coexist; 337 drops fork's constraint by design (name collision intended).
# - cmd_issue_test.go AUTO-MERGES CLEAN but yields contradictory contracts (fork expects "bogus" rejected;
#   upstream pins "active"/"human_review" accepted locally) — go test ./cmd/multica/ is the only gate.
# - queries.ts auto-merges clean but fork child-progress reads entry.archived which regen won't emit.
# - 'archived' NOT subsumed: no archived-like category exists; cancelled category ≠ fork semantics
#   (fork archived = closed-NOT-completed, hidden from default lists; cancelled = counts-as-done + auto-archive).
# USER DECISION (Phase 2 blocker): (a) compose-and-keep fork archived [audit recommends; requires teaching
#   issuestatus.Resolve/Effective 'archived' as fork pseudo-builtin + composing SQL predicates], or
#   (b) drop fork archived + migrate rows to a cancelled-category status (semantic drift: counts as done;
#   267 live archived rows would need migrating). Follow-up work regardless: fork-local resolver extension
#   (archived stays out of catalog, handled as pseudo-builtin in Go) — design in Phase 4.
#
# === Agent B: per-file conflict prediction + strategy (RETURNED; anchors re-verified in main session) ===
# merge-tree (git 2.50.1, tree 1b9db110f) → 23 REAL conflict files (output 103 lines = 23 paths + 80
#   "Auto-merging" info lines; unique-path count cross-checked in main session). All 23 inside fork-delta set.
# CONFLICTS (strategy): core/api/client.ts (C: union methods; KEEP fork's getConfig(signal) optional param),
#   core/api/schemas.ts (C: trap — merged tree has SkillSchema/SkillFileSchema/EMPTY_SKILL ×2 @1457+2713,
#     verified main session; fix = delete fork's moved copy, keep file-tail block, keep BatchImport*, dedupe imports),
#   core/platform/auth-initializer.tsx (A: take upstream MUL-6254 rewrite as skeleton, re-plant fork #5539
#     3 things; TRAP: upstream calls configStore.setServerVersion which fork renamed setBackendBaseline →
#     must reconcile or typecheck breaks),
#   core/types/index.ts + types/workspace.ts (C: member union both sides),
#   core/workspace/queries.test.ts (B: add/add — concatenate both suites),
#   views/issues/issue-detail.tsx (C: import-region union only; body hunks disjoint),
#   locales ja/ko/zh-Hans settings.json ×3 (B: surgical conflict-region merge, NEVER --theirs; brace/comma trap),
#   cmd/multica/cmd_issue.go (C: take upstream isTerminalChildIssue @956 — semantically equal to fork's
#     IsCompletedStatus(archived)=false AND custom-status compatible; keep fork archived in help/list),
#   handler/auth.go (C: fork hadInviteName threads through upstream rate-limit flow),
#   handler/comment.go (C HEAVIEST: fork +357/-75 skill-mention cluster vs upstream noteBlockedRuntimeTargets
#     (MUL-6164) + commentMentionTarget.unusable (MUL-6136); keep fork bodies, suture in upstream calls+field;
#     upstream triggerTasksForComment signature UNCHANGED → no Strategy-D trap this round),
#   handler/github.go + issue_child_done.go (+stage_test) (C: same-line opposite; template =
#     issueguard.IsClosedStatus(issuestatus.Effective(ctx, q, wsID, status)) composition),
#   handler/issue.go (C: 2 conflict regions; fork archived filters @883-1715 survive clean — upstream zero hunks there),
#   db/queries/{issue,inbox,project}.sql + generated ×3 (C: hand-merge then make sqlc; upstream generated diff
#     has 0 new const/func → no sqlc version reshuffle; verify zero drift after regen).
# AUTO-MERGE (33 rest of 56-intersection): all D-orthogonal, verified hunk-disjoint. Notables: CLAUDE.md,
#   cmd_workspace.go (invite flags + MCP subcommands coexist; NO same-command-same-flag → no pflag double-reg,
#   verified in merged tree), router.go (fork /api/admin + upstream new routes coexist; upstream has NO
#   /api/admin → no chi duplicate route), task.go (fork deletion of truncate symbols applies clean; utf8 alive),
#   mention UI cluster (mention-view/rich-content/comment-trigger-chips/mention-suggestion/content-editor/
#   comment-input): upstream ZERO changes — this round backend-only for #5346/#5199.
# LOCALE LOSS-SCAN SET (en fork-only keys to verify post-resolution): editor.json 9 keys (skill gesture),
#   issues.json archived labels+archived.* block+identifier_*/view_run*/trigger_source_* (~22 keys),
#   settings.json invite_name_{label,placeholder}, skills.json 6 batch-import keys. Zero name overlap with
#   upstream's new keys (mcp.*/share-link/current_issue/trigger_blocked_*).
# Verified negatives (main session or agent w/ command): no pflag double-registration; no chi route dup;
#   no truncateFallbackCommentBody dup symbol (merged-tree grep); no utf8 import gap; CompleteTask/FailTask
#   + triggerTasksForComment upstream signatures unchanged; cmd_issue_test.go no duplicate test fns (but
#   NOTE contradictory contracts — fork TestValidIssueStatuses expects 8 incl archived while upstream pins 7;
#   resolve intentionally, not by auto-merge); sqlc no version reshuffle.
#
# === Agent C: ops-level sweep (RETURNED; sizing claims from live DB) ===
# Redis: NO-ACTION. CHANNEL_WS_LEASE_BACKEND default=postgres (router.go buildChannelSupervisor);
#   even invalid redis config only disables channel supervisor (nil), never crashes server. Host .env has
#   zero REDIS/CHANNEL_WS vars → stays postgres+in-memory hub. Do NOT install Redis; do NOT set the var.
# Deps: no new runtime Go modules (chi 5.2.5→5.3.0 etc.); govulncheck build-time only; **go directive
#   1.26.1→1.26.6** — local go1.26.4, GOTOOLCHAIN=auto will fetch 1.26.6 on first build (or brew upgrade go).
#   No new NEXT_PUBLIC_*; node >=22 ok (local v24). New optional env RATE_LIMIT_INVITATION_* (in-proc defaults).
# Migrations 314-341 (full inventory in agent output; live-DB sizing: issues=1055 archived=267 done=688,
#   workspaces=5, issue_property=0, plugin_contribution=0): all sub-second; biggest scan = 338 VALIDATE on
#   1055 rows (SHARE UPDATE EXCLUSIVE, non-blocking). DROPs verified safe (318 col never used; 337 constraint
#   — 267 archived rows pass format regex; 319/325/341 constraints on 0-row tables). 339 backfill = 35 rows.
# cmd/server: no new listeners/ports/hard-fails — launchd run-backend.sh unchanged. Migration runner changes
#   (1e1321a9c/7c2b8f012) = robustness only (concurrent-index cleanup map generalized; 341 rollback fail-closed).
# truncateFallbackCommentBody: upstream STILL defines symbols in task.go:146/157 (since v0.4.26); fork compiles
#   because v0.4.26 merge kept fork's deletion from task.go + fork file (cap 200 vs upstream 8000). v0.4.26→28
#   does NOT touch the definition region → clean 3-way keeps fork side. TRAP: reflexive upstream-side task.go
#   resolution = duplicate symbols (go build catches). utf8 import: used on both sides (v0.4.12 repeat ruled out).
# Daemon/protocol: ALL additive or capability-gated (DaemonCapabilityRemoteMCPV1; legacy daemon never sees
#   remote fields; GC check normalized server-side explicitly for old daemons). No forced daemon upgrade;
#   optional brew upgrade multica once tap ships v0.4.28 (remote MCP plugin support only).
#
#
# Raw anchors already verified in main session:
# - New upstream migrations = 314-341 (28 stems, up+down pairs); fork max = 313 → zero new numeric collision;
#   6 known collisions (119/158/159/160/213/214) unchanged. MUL-6243 custom-status migrations live in this range.
# - File intersection (both sides changed since v0.4.26) = 57 files (list below).
# - PR state refresh: #6106 archived STILL OPEN (upstream did NOT adopt); #5346/#5309/#4118/#2669/#5160/#2460 still OPEN;
#   #5539/#5199 CLOSED (own forever); #5006/#5393 MERGED (already adopted).
# - packages/core/issues/config/status.ts (fork archived core) NOT in intersection → upstream's
#   MUL-6243 status logic landed elsewhere; location under audit.
#
# Intersection files (fork ∩ upstream changed since v0.4.26):
#   CLAUDE.md
#   packages/core/api/client.ts / schema.test.ts / schemas.ts
#   packages/core/issues/queries.ts
#   packages/core/package.json
#   packages/core/platform/auth-initializer.tsx
#   packages/core/types/index.ts / workspace.ts
#   packages/core/workspace/queries.test.ts
#   packages/views/issues/components/{issue-detail,issues-header,swimlane-view,table-view}.tsx
#   packages/views/locales/{en,ja,ko,zh-Hans}/{editor,issues,settings,skills}.json (16 files)
#   packages/views/settings/components/members-tab.tsx
#   server/cmd/multica/{cmd_issue,cmd_workspace}.go + cmd_issue_test.go
#   server/cmd/server/{main_test.go,router.go}
#   server/internal/handler/{auth,comment,config_test,github,handler,invitation,issue_child_done_stage_test,issue_child_done,issue,workspace}.go (+ tests)
#   server/internal/service/task.go
#   server/pkg/db/generated/{inbox,issue,models,project,workspace}.sql.go
#   server/pkg/db/queries/{inbox,issue,project,workspace}.sql
