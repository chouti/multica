---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.13
base_tag: v0.4.12
merge_commit: (pending)

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true
  audit_strategy_written: true
  db_backed_up: true
  migrated: true
  version_stamped_frontend: true
  version_stamped_backend: true
  services_restarted: true
  health_checked: true

# Negative claims the audit made — each confirmed/refuted by one raw command, not by reading.
negative_claims:
  - claim: "merge-tree 预演 6 个真实文本冲突，全部集中在 drafts/composer 集群；30 个交集文件 auto-merge clean"
    verified_by: "`git merge-tree --write-tree --merge-base=v0.4.12 HEAD v0.4.13` → exit 1, tree `c2cd0357e`; `git grep -l '^<<<<<<<' c2cd0357e` → 6 文件: comment-draft-store.ts/comment-draft-store.test.ts(add/add)/content-editor.tsx/comment-input.tsx/reply-input.tsx/issue-detail.test.tsx; 其余 30 个交集文件 merge-tree stdout 全 `Auto-merging` 无 CONFLICT"
    result: confirmed
  - claim: "duplicate-symbol（语义冲突，merge-tree 抓不到）：fork fallback_comment_truncate.go(cap=200) 与 v0.4.13 task.go(cap=8000) 同时定义 truncateFallbackCommentBody + maxSynthesizedFallbackCommentRunes → 合并后 go build redeclared"
    verified_by: "`git show v0.4.13:server/internal/service/task.go | grep -nE 'func truncateFallbackCommentBody|maxSynthesizedFallbackCommentRunes'` → line 129 const=8000 + line 140 func + line 2885 调用; fork HEAD fallback_comment_truncate.go line 9 const=200 + line 16 func; fork HEAD task.go grep → 空（已移出）; fallback_comment_truncate_test.go 两边行号一致 19/26/27/48/70/71/75/76"
    result: confirmed
    action: "策略 A：删 fork fallback_comment_truncate.go（cap=200 实现），取 upstream task.go（cap=8000 + line 2885 调用）；fallback_comment_truncate_test.go 两边一致自动消解。cap 200 vs 8000 取舍留 Phase 4（fork 200 有防泄漏安全语义，看 upstream 实现后定）"
  - claim: "v0.4.13 新增迁移 231/232（2 个 CONCURRENTLY 索引），fork 最高 230 → 无新撞号；无 FK/REFERENCES/新表/加列"
    verified_by: "`git diff --name-status v0.4.12..v0.4.13 -- server/migrations/` → 仅 A 231_agent_task_queue_terminal_completed_at_index + 232_channel_media_pending_object_due_index; 两 up.sql 均 `CREATE INDEX CONCURRENTLY IF NOT EXISTS`（231 部分 WHERE status IN completed/failed；232 单列 next_attempt_at），单语句文件，grep 无 REFERENCES/FOREIGN KEY"
    result: confirmed
  - claim: "v0.4.13 未 merge/close 任何 fork 定制 PR → 无 MERGED→drop local 策略翻转，所有 carry 定制继续 carry"
    verified_by: "`gh pr list --repo multica-ai/multica --author chouti --state all` → 与 ledger 一致：#5393 MERGED(2026-07-15)；#5346/#5309/#5199/#5160/#4118/#2669/#2460 OPEN；#5539/#5466 CLOSED；v0.4.12..v0.4.13 无新变化"
    result: confirmed
  - claim: "6 个已知 migration duplicate-prefix 碰撞（119/158/159/160/213/214）在 HEAD 仍稳定，v0.4.13 不触及（只加 231/232）"
    verified_by: "`ls server/migrations/ | grep -oE '^[0-9]+' | sort -n | uniq -c | awk '$1>2'` → 119/158/159/160/213/214 计数 >2（真碰撞，其余 ==2 是 up/down 配对非碰撞）；max=230；v0.4.13 仅加 231/232"
    result: confirmed
  - claim: "task.go @ fork HEAD 含 `import \"unicode/utf8\"`（v0.4.12 已加回）→ 本次取 upstream task.go 整文件时 import 由 upstream 决定，无 v0.4.12 的 utf8 import 坑复发风险（除非 upstream 也缺）"
    verified_by: "`git show HEAD:server/internal/service/task.go | grep -n unicode/utf8` → line 13 存在；本次 task.go 取 upstream 版（含 truncateFallbackCommentBody 实现，其依赖 utf8 由 upstream 保证）"
    result: confirmed
    action: "Phase 5 go build 仍须验证（upstream task.go 的 utf8 import 是否齐全）"
---

# Upgrade v0.4.12 → v0.4.13

> Process artifact. Persistent customization list in `docs/customizations.md`.
> 25 upstream commits (v0.4.12..v0.4.13), published 2026-07-28 12:18 UTC. Themes: **unified draft lifecycle + upload ownership inversion (MUL-5181, #5900)**、**paste-long-text-as-file (#6019/#6043)**、**post-send caret policy afterAccepted (#6014)**、**upload placeholder UI 统一 (MUL-5391, #6025)**、**GitHub App repo import (MUL-5088, #5896)**、**agents thinking level + Codex speed (MUL-5390, #6027)**、**usage 错误/失败图表 (MUL-5352/5388/5389)**、**sub-issues agents-working chip (#5834)**、**daemon runtime brief byte-stable (MUL-5377, #6021)**、**fix(migrations) remove historical skill-bundle backfill (MUL-5400, #6045)**。

## Phase 0 — Locate
- base: `v0.4.12` (HEAD `v0.4.12-250-g30c88fe69`)
- target: `v0.4.13` (Latest, published 2026-07-28 12:18 UTC; non-prerelease)
- merge-base(HEAD, v0.4.13) = `v0.4.12` (clean)
- to-merge: **25 commits** (HEAD..v0.4.13), 245 文件 +18744/-2712
- v0.4.13 是 origin/main 的祖先（fetch 已确认 tag 存在）；按惯例 **merge v0.4.13 tag，不 merge origin/main**（origin/main 含 post-tag 未发布 commits）—— Phase 3 前复核 `git rev-list --count v0.4.13..origin/main`
- vs origin/main: 28 behind / 250 ahead
- working tree: clean（git status 开局 clean）

## Phase 1 — Deep audit（进行中）

### 已确认（独立 raw command 验证，见 frontmatter 6 条 negative_claims）
- merge-tree **6 真实文本冲突**，全部 drafts/composer 集群；30 交集文件 auto-merge clean
- **1 语义冲突（merge-tree 抓不到）**:duplicate-symbol `truncateFallbackCommentBody`/`maxSynthesizedFallbackCommentRunes`，根因 = 上游 `24ea38dcb` 当初留 dangling symbol（fork `f3e86c4ad` #5455 补 cap=200 实现）→ v0.4.13 上游自己在 task.go 补 cap=8000 实现 → fork 文件冗余。**策略 A 干净解决**（见 negative_claim #2 action）。
- 新迁移 231/232 无撞号、无 FK、纯 CONCURRENTLY 索引；6 已知碰撞（119/158/159/160/213/214）稳定不动
- PR 无新增 merge/close，无策略翻转
- v0.4.12 的 task.go utf8 import 坑本次取 upstream 整文件规避（Phase 5 go build 仍验）

### Per-file strategy
| 集群 / 文件 | strategy | 文本冲突 | 依据 |
|---|---|---|---|
| **drafts/composer 集群（6 文件）** | 见下 | | 全部 fork `@skill mention` 改造 ∩ upstream MUL-5181 unified lifecycle |
| `comment-draft-store.ts` | **C（careful merge）** | **1 content** | 以 upstream 为底：`CommentDraft` union 两字段（`skillMentionAgents?`+`attachments`）；**保留 upstream `setDraft(key,string)` 签名与 `writeDraft` 体**；给 `writeDraft` 打补丁带出 `existing?.skillMentionAgents`（否则每次敲键盘擦掉 skill 设计ations——核心语义陷阱）；fork `setDraft(key,payload)` 改名 `setDraftPayload`；保留 fork `getDraftPayload` reader（增量，零冲突）。`CommentDraftPayload` 仅 store 内部（grep 验证），改名局部 |
| `comment-draft-store.test.ts` | **B（keep both）** | **add/add** | upstream 为底（3 describe/20 test，主回归网）+ 末尾追加 fork `describe(…skillMentionAgents round-trip)`；fork 测试块按合并后 API 改写 `setDraft(…,{…})`→`setDraftPayload(…)`；reader 断言不动；union imports |
| `content-editor.tsx` | **A（取 upstream + re-apply）** | **1 content** | 单 hunk（getMarkdown 区）：`getMarkdown` **取 upstream 侧**（`stripBlobUrls` 定义已从合并树删除，保留 fork 侧 `stripBlobUrls(...)` 必编译红——grep 验证）；紧接粘贴 fork `getSkillMentionIds`（descendants 收集 `type==='skill'` mention id，不依赖已删符号）；其余 fork 增量（prop/ref 接口/JSX Provider）已 auto-merge |
| `comment-input.tsx` | **A（取 upstream 骨架 + 缝 5 seam）** | **1 content**（5 区） | 取 upstream `useComposerSubmit`+`useCommentUploads` 整骨架；缝回 skillMention seam（state + `useSkillDesignatedPreviewAgents` + `handleSkillMentionChange`/`syncSkillMentionsWithDoc` + onSubmit 第4参 `skillMentionAgentsPayload` + `onAccepted` 清空）。冲突区5 L330-341：取 upstream `setDraft(key,md)` + 补 skill 写入。**隐性 flush-effect 地雷 L123**（对象形 setDraft 无冲突标记 auto-merge → typecheck 红）必改。picker 挂载 L351 已存活。`useCommentUploads` 真实源路径 Phase 4 核实（Agent B 给的 use-comment-uploads.ts 在 v0.4.13 不存在） |
| `reply-input.tsx` | **A（对称）** | **1 content**（5 区） | 与 comment-input 对称；`onAccepted` 两分支均补 `setSkillMentionAgents({})`；隐性 L132 + 显性 L333 同 comment-input 处理 |
| `issue-detail.test.tsx` | **B（keep both）** | **1 content**（2 区） | signature union：`getDraftPayload`(L376/396) + `getAttachments`/`getUploads`(L378-379/398-399) 同 mock 共存（已验证）；fork archived 状态 mock + findAllByText breadcrumb + FZG-343 用例 + upstream agent-working chip/#5997 "Back" 已 auto-merge |
| `fallback_comment_truncate.go`（fork） | **A（drop）** | 0 文本 | upstream task.go 现自含 cap=8000 实现 + oversizedFallbackCommentNotice，**语义同 fork #5455**（over-cap→固定 notice 不嵌入输入）；fork cap=200 过激进，接受 8000。删 fork 文件 = 净简化 |
| `task.go` | A（取 upstream 整文件） | 0 文本 | auto-merged；含 upstream truncateFallbackCommentBody 实现（cap=8000，utf8 import 齐全）；cap 接受 upstream 8000（见上行理由） |
| `fallback_comment_truncate_test.go` | A（upstream，两边一致） | 0 | 行号两边完全一致（19/26/27/48/70/71/75/76），内容相同，自动消解；测试对 cap 值不敏感（用常量本身做边界） |
| `router.go` | C | 0 | auto-merged；fork provenance 1 行 + upstream channel media 不同区 |
| `github.go` | C/B | 0 | auto-merged；fork +2/-1（trivial）vs upstream +297/-22（MUL-5088 import repos） |
| `api/client.ts`+`schemas.ts`+`types/{agent,index}.ts` + 2 .test | C/B | 0 | auto-merged；fork provenance/skill + upstream agent types 正交 |
| `comment-card.tsx` + edit-gate/composers .test | C/B | 0 | auto-merged；fork trigger-chips + upstream 不同区 |
| `issue-detail.tsx` | C | 0 | auto-merged（仅 .test 冲突）；fork pin-comment/identifier-badge + upstream thread rail |
| locale editor/issues/settings × 4 语言 | B | 0 | auto-merged；惯例 take both key sets |
| app-sidebar / use-issue-actions / surface-controller / package.json | C/B | 0 | auto-merged；fork 无重大定制 |
| 迁移 231/232 | A（adopt） | 0 | 新号无撞号，纯 CONCURRENTLY 索引，低风险 |

### Audit prediction（Phase 3 对照）
- **预期真实手动文本冲突 6 个**（merge-tree 已钉死），全部 drafts/composer 集群。
- **1 语义冲突**（duplicate-symbol，go build 抓）→ 删 fork 文件解决。
- drafts 6 文件 re-apply 细节（fork @skill 改造 vs upstream unified lifecycle 共存）待 Agent A/B 回填后定稿。
- Phase 5 语义关注：task.go utf8 import、duplicate-symbol、comment-draft-store add/add 测试符号存活。

## Phase 2 — Irreversible-gate confirmation（待）
- 第一阶段：用户授权 Phase 3-5 可逆流程（建 backup→merge 预览→resolve→代码验证）。
- 第二阶段：Phase 5 全绿后，Phase 6（迁 2 个迁移 + launchd 重启 + 版本重戳 v0.4.13）前再次单独确认 → `gates.user_confirmed: true`。

## Phase 3 — Preview（EXECUTED 2026-07-29）
- safety branch `main-backup-v0.4.13` → `30c88fe69`（merge 前 HEAD）。
- `git merge v0.4.13 --no-commit --no-ff`：**6 内容冲突**，与 Phase 1 merge-tree 预测 100% 吻合：comment-draft-store.ts(content)/.test.ts(add/add)/content-editor.tsx/comment-input.tsx/reply-input.tsx/issue-detail.test.tsx。
- 其余 30 交集文件全 auto-merge clean（router.go/github.go/task.go/locale×12/api client+schema+types/comment-card 等）。
- **rerere enabled=true**，本轮仅 `Recorded preimage`（6 文件），无 `Resolved ... using previous resolution` → 未静默自动解决，策略未被打断。本次 resolve 会被 rerere 记录供未来套用（故本次须 resolve 正确）。
- **vs 预测**：6 冲突 = 6 预测，0 偏差。3 个隐性语义地雷（writeDraft 擦 skillMentionAgents / L123+L132 对象形 setDraft / duplicate-symbol）仍需 Phase 4 手修 + Phase 5 编译验证。
## Phase 4 — Resolve + re-apply（EXECUTED 2026-07-29）
6 文件按策略表 resolve（Write 重写 store/store.test/comment-input/reply-input；Edit content-editor/issue-detail.test）：
- `comment-draft-store.ts`（C）：union 字段（skillMentionAgents?+attachments）+ writeDraft 补丁保留 designations + setDraftPayload（委托 writeDraft + 引用比较 reconcile，保护 stale-submit guard）+ getDraftPayload reader。
- `comment-draft-store.test.ts`（B）：upstream 3 describe（20 test）+ 追加 fork skillMentionAgents round-trip（4 test，setDraft→setDraftPayload 改写）。
- `content-editor.tsx`（A）：getMarkdown 取 upstream 侧（stripBlobUrls 定义已删）+ 粘贴 fork getSkillMentionIds。
- `comment-input.tsx`（A）：取 upstream useComposerSubmit+useCommentUploads 骨架 + 缝 4 skill seam + flush-effect/onUpdate 对象形 setDraft → setDraftPayload。
- `reply-input.tsx`（A）：对称 + 两分支清空 + uploads/removeUpload + afterAccepted refocus + containerRef。
- `issue-detail.test.tsx`（B）：signature union（getDraftPayload+getAttachments/getUploads 同 mock）。

**duplicate-symbol 修正（Phase 4 颠覆 Phase 1 结论）**：Phase 1 据 v0.4.13 tag task.go 断定 duplicate-symbol、`git rm` 了 fallback_comment_truncate.go。Phase 4 grep 工作树发现合并采纳 fork 版 task.go（无 truncate 定义——fork 早删了调用改直接 `redact.Text(body)`）→ **无 duplicate**，fallback 文件是喂 test 的唯一定义。已 `git checkout HEAD` 恢复（撤销误删）。**教训：验证合并后工作树实际状态，非 tag。** fork 既存孤儿：task.go 不调用 truncate（fallback comment 直接发 redact.Text 全文，不截断），fallback_comment_truncate.go+test 仅互相喂养——保持现状（是否恢复 upstream 安全截断是单独产品决策）。

**1 个 merge 引入级联（typecheck 抓到）**：comment-card.tsx inline edit 流 L708/L1053 `edit.setDraft(key,{content:md})` 对象形 → 改 `edit.setDraft(key,md)` 字符串形（edit 流无 skill agents，Agent A 预判）。

## Phase 5 — Verify（EXECUTED 2026-07-29，全绿）
stop-on-first-failure，5 步全过：
- `pnpm install` ✓（4.3s，无新依赖冲突）
- `pnpm typecheck` ✓（6/6：core/ui/docs/views/web/desktop，42s；修 comment-card 级联后一次过）
- `go build -C server ./...` ✓（exit 0）+ `go vet ./internal/service/...` ✓（无 duplicate-symbol、无 task.go import 问题）
- `pnpm test` ✓（views 280 files / **3270 passed**，64s；修 1 处：comment-composers.test.tsx L976 upstream 新 upload-gate test 3 参断言 → 加第 4 undefined 适配 fork onSubmit 4 参签名）
- `pnpm build` ✓（3/3，54s）
- **Skipped**：`make test`/`make start`（Docker）；Go integration tests（独立 DB 缺失）；7 个 residual handler Go test（autopilot authority，v0.4.8/9 即存在，pnpm test 不覆盖 Go）。
- **2 个 auto-merge 语义错被 typecheck/test 抓到**（merge-tree + 4 agent + 对抗验证全漏）：① comment-card setDraft 对象形级联；② comment-composers test 3 参断言。**auto-merge ≠ 语义正确再确认。**
- **→ 推进 `phase: gates`**（解锁 Phase 6）。
## Phase 6 — Irreversible ops（EXECUTED 2026-07-29）
- **Backup**: `~/multica-backups/v0.4.13-pre-migrate-20260729-104808.dump`（38M，pg17 pg_dump）✓
- **Migrate**: `migrate up` 应用 **2 个新迁移**（231_agent_task_queue_terminal_completed_at_index + 232_channel_media_pending_object_due_index，均为 CONCURRENTLY 索引），213×2/214×2 撞号按版本串 skip，224-230 skip，`Done.` ✓
- **Version stamp v0.4.13**：
  - frontend: `.env NEXT_PUBLIC_APP_VERSION v0.4.12→v0.4.13` + `install.sh` rebuild standalone（~/.multica/frontend）✓
  - backend: `install.sh` 的 VERSION 走 `_version-stamp.sh → resolve-official-baseline.sh`（**git-based，不再读 .env**）。**merge 未 commit 时 git describe HEAD 仍 v0.4.12** → install.sh 给 backend stamp 了 v0.4.12（/api/config server_version 验证发现）。**manual 补救**：`CGO_ENABLED=0 go build -C server -ldflags "-s -w -X main.version=v0.4.13" -o ~/.multica/backend/server ./cmd/server` → server_version=v0.4.13 ✓
  - **gotcha（记 memory）**：install.sh version stamp 依赖 merge 已 commit；commit 前 install.sh 会 stamp 前一版本。正确顺序：先 commit 再 install.sh，或 commit 前 install.sh + manual backend stamp。
- **Restart**: `install.sh` reload plists（frontend+backend loaded）+ manual `launchctl kickstart -k ...multica-backend`（backend rebuild 后）✓
- **Health**: backend `/healthz` 200（attempt 1，快于 30-40s 预期），frontend `/` 200，`/api/config` `"server_version":"v0.4.13"`（双注入生效）✓
## Phase 7 — Record（EXECUTED 2026-07-29）
- **Merge commit**: (pending — filled after commit)
- **Predicted vs actual**: Phase 1 merge-tree 预演 6 文本冲突 → 实际 6（100% 吻合）。**2 个 auto-merge 语义错**（typecheck/test 抓到，merge-tree/4 agent/对抗验证全漏）：① comment-card setDraft 对象形级联；② comment-composers test 3 参断言。**+1 个 Phase 4 推翻 Phase 1 的 duplicate-symbol 结论**（tag 验证 ≠ 合并后工作树实际）。
- **vs v0.4.12**：v0.4.12 迁 7 个迁移（含新表+加列），v0.4.13 只迁 2 个 CONCURRENTLY 索引（schema 面更小）。v0.4.12 = 0 文本冲突 + 1 语义坑；v0.4.13 = 6 文本冲突（drafts/composer 集群）+ 2 语义错 + 1 推翻结论——**冲突更集中但可解，全部 5 件套一次过（修 2 处后）**。
- **install.sh gotcha**：version stamp 走 git-based resolver（`_version-stamp.sh → resolve-official-baseline.sh`），**merge commit 前 install.sh stamp 前一版本**（git describe HEAD 不知 v0.4.13）；manual backend `-ldflags v0.4.13` 补救。记 memory（`project_install_sh_version_needs_commit`）。
- **Ledger**: base 推进 v0.4.12 → v0.4.13，Last upgrade 段替换，`fallback_comment_truncate` 条目加 v0.4.13 注释（task.go 采纳 fork 版无 truncate 调用，fallback 文件保留为喂 test 的唯一定义）。
