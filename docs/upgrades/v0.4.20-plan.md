---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.20
base_tag: v0.4.18   # effective merge-base, because v0.4.19 was a fake merge (see banner)

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: gates  # Phase 5 COMPLETE 2026-08-07 — install/typecheck/go build/go vet/sqlc/pnpm test(3754)/pnpm build all green; 14 test failures fixed (see Phase 5). UNLOCKS Phase 6.

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true       # Phase 2 — CONFIRMED 2026-08-07: (1) pg17 backup before migrate 257-260; (2) install.sh full rebuild+restart accepted (30-40s downtime OK)
  audit_strategy_written: true  # Phase 1.3 — per-file strategy written (all 4 clusters integrated 2026-08-07)
  db_backed_up: false         # Phase 6.1 — pg17 dump
  migrated: false             # Phase 6.2 — 253-260 up (8 new migrations, conditional on current DB state)
  version_stamped_frontend: false
  version_stamped_backend: false
  services_restarted: false
  health_checked: false

merge_commit: TBD

# Negative claims the audit will make — each confirmed/refuted by one raw command, not by reading.
# Filled in Phase 1 as each agent returns with verified_by commands.
# Phase 0.5 (merge-tree preview, raw read-only) results below — these are NOT from audit agents
# but are command-verified facts about the diff and conflict shape, ready for Phase 4 use.
negative_claims:
  # --- Phase 0.5 (merge-tree preview, raw 3-way) — direct from `git merge-tree v0.4.18 c95bb7e89 v0.4.20` ---
  - claim: "v0.4.18..v0.4.20 = 61 commits"
    verified_by: "git rev-list --count v0.4.18..v0.4.20"
    result: "confirmed (61)"
  - claim: "v0.4.18..v0.4.20 = 603 files, +51457/-10388"
    verified_by: "git diff --shortstat v0.4.18..v0.4.20"
    result: "confirmed (603 files, +51457/-10388)"
  - claim: "merge-base(HEAD, v0.4.20) = v0.4.18 SHA (b312e1cbb) — the real jump base"
    verified_by: "git merge-base HEAD v0.4.20"
    result: "confirmed (b312e1cbb4613d8e59462e7e7040d8a9f09606f5 = v0.4.18 SHA)"
  - claim: "v0.4.20 not in HEAD history (only tags ≤ v0.4.9 are merged into HEAD)"
    verified_by: "git tag --merged HEAD | grep v0.4.20; echo exit=$?; echo '---'; git tag --merged HEAD | tail -5"
    result: "confirmed (v0.4.20 not in list; merged tags stop at v0.4.9)"
  - claim: "v0.4.19 not in HEAD history (fake-merge skip is correct)"
    verified_by: "git tag --merged HEAD | grep v0.4.19; echo exit=$?"
    result: "confirmed (exit 1 = not found)"
  - claim: "v0.4.19..HEAD diff is 327 files / +29486/-1497 (v0.4.19 content fully missing)"
    verified_by: "git diff --shortstat v0.4.19 HEAD"
    result: "confirmed (327 files, +29486/-1497)"
  - claim: "c03402a94 is a SINGLE-parent linear commit (not a real merge)"
    verified_by: "git log --format='%H %P %s' c03402a94 -1"
    result: "confirmed (1 parent: 276fc551e)"
  - claim: "c95bb7e89 is a SINGLE-parent linear commit (not a real merge)"
    verified_by: "git log --format='%H %P %s' c95bb7e89 -1"
    result: "confirmed (1 parent: c03402a94)"
  # --- Phase 0.5 conflict counts (raw 3-way merge-tree) ---
  - claim: "3-way merge-tree v0.4.18→c95bb7e89→v0.4.20 produces 114 'changed in both' candidates"
    verified_by: "git merge-tree v0.4.18 c95bb7e89 v0.4.20 | grep -c '^changed in both'"
    result: "confirmed (114)"
  - claim: "Of those 114, 37 files / 85 hunks carry real `<<<<<<<` conflict markers (rest auto-merge)"
    verified_by: "python3 -c \"import subprocess; o=subprocess.check_output(['git','merge-tree','v0.4.18','c95bb7e89','v0.4.20'],text=True); c=o.split('changed in both'); conf=[(l[1].split(chr(10))[0].split()[-1] if '<<<<<<<' in l else None) for l in c]; print(len([x for x in conf if x]))\""
    result: "confirmed (37 files, 85 hunks)"
  - claim: "0 'added in local' / 0 'removed in local' — fork doesn't add or delete files in v0.4.18..HEAD"
    verified_by: "git merge-tree v0.4.18 c95bb7e89 v0.4.20 | grep -cE '^(added in local|removed in local)'"
    result: "confirmed (0 each)"
  - claim: "115 'added in remote' = 115 files added by upstream v0.4.20 (auto-add, no conflict)"
    verified_by: "git merge-tree v0.4.18 c95bb7e89 v0.4.20 | grep -c '^added in remote'"
    result: "confirmed (115)"
  # --- Top conflict files (from merge-tree) ---
  - claim: "issue-detail.tsx has 11 conflict hunks (the worst single file)"
    verified_by: "git merge-tree v0.4.18 c95bb7e89 v0.4.20 | python3 -c \"import sys; o=sys.stdin.read(); c=o.split('changed in both'); [print(l[1].split(chr(10))[0].split()[-1], l[1].count('<<<<<<<')) for l in c if '<<<<<<<' in l]\" | sort -k2 -n -r | head -10"
    result: "confirmed (issue-detail.tsx = 11 hunks, locales/zh-Hans/issues.json = 7, server/pkg/agent/qoder.go = 7, locales/{ja,zh-Hans}/onboarding.json = 5 each)"
  # --- Phase 0.6: live production state probes ---
  - claim: "Backend /api/config reports server_version=v0.4.19"
    verified_by: "curl -s -m 5 http://localhost:8081/api/config"
    result: "confirmed (server_version=v0.4.19)"
  - claim: "Backend /healthz = 200, frontend / = 200"
    verified_by: "curl -s -m 5 -o /dev/null -w '%{http_code}' http://localhost:8081/healthz; curl -s -m 5 -o /dev/null -w '%{http_code}' http://localhost:3001/"
    result: "confirmed (both 200)"
  - claim: "Launchd jobs for backend + frontend are loaded and running"
    verified_by: "launchctl list | grep -i multica"
    result: "confirmed (com.fengzhao.multica-backend PID 73883, com.fengzhao.multica-frontend PID 72961)"
  - claim: "Homebrew multica CLI daemon is real v0.4.19 (commit a8d5daac5)"
    verified_by: "multica --version; multica daemon status"
    result: "confirmed (multica 0.4.19 commit a8d5daac5, daemon uptime 19h51m)"
  - claim: "DB schema is at v0.4.19 (MAX(version) = 256), NOT v0.4.18"
    verified_by: "grep DATABASE_URL .env | sed 's|DATABASE_URL=||' | xargs -I {} /opt/homebrew/opt/postgresql@17/bin/psql {} -c 'SELECT MAX(version) FROM schema_migrations;'"
    result: "confirmed (256_drop_agent_task_queue_chat_pending_v2; 307 applied) — v0.4.19 migrate step really did run during the fake-merge deploy, even though source tree was v0.4.18 + fork"
  - claim: ".env NEXT_PUBLIC_APP_VERSION = v0.4.19 (label, not real source)"
    verified_by: "grep NEXT_PUBLIC_APP_VERSION .env"
    result: "confirmed (v0.4.19)"
  - claim: "Backend binary stamped with -X main.version=v0.4.19"
    verified_by: "strings /Users/fengzhao/.multica/backend/server | grep 'main.version='"
    result: "confirmed (-X main.version=v0.4.19)"
  - claim: "Previous v0.4.18 binary kept at ~/.multica/backend/server.bak.v0.4.18"
    verified_by: "ls -la /Users/fengzhao/.multica/backend/"
    result: "confirmed (server.bak.v0.4.18 exists, 52M, 2026-08-06 09:57)"
  - claim: "Last backup: ~/multica-backups/v0.4.19-pre-migrate-20260806-095612.dump (55M) exists"
    verified_by: "ls -la /Users/fengzhao/multica-backups/v0.4.19-pre-migrate-20260806-095612.dump"
    result: "confirmed (55M, 2026-08-06 09:56) — usable for v0.4.19 schema rollback if v0.4.20 goes badly"
  - claim: "pg17 binary present at /opt/homebrew/opt/postgresql@17/bin/pg_dump"
    verified_by: "ls -la /opt/homebrew/opt/postgresql@17/bin/pg_dump"
    result: "confirmed (478k, 2025-06-11)"
  # --- Cluster 1 audit (32 verified claims, 18 ranked risks) ---
  # Full text in §Phase 1 Cluster 1 below. Highlights:
  - claim: "8 new migrations (253-260) all > fork max 252, zero new collision"
    verified_by: "ls server/migrations/ | grep -E '^2(53|54|55|56|57|58|59|60)_'"
    result: "confirmed (8 stems, 16 files, all > 252)"
  - claim: "6 known migration collisions (119/158/159/160/213/214) both-stems still present"
    verified_by: "ls server/migrations/ | grep -E '^(119|158|159|160|213|214)_' | wc -l"
    result: "confirmed (12 files, both fork + upstream stem alive)"
  - claim: "upstream v0.4.20 did NOT redeclare truncateFallbackCommentBody / maxSynthesizedFallbackCommentRunes / oversizedFallbackComment (v0.4.12 教训不复发)"
    verified_by: "3× git diff v0.4.18..v0.4.20 -S <symbol> -- server/"
    result: "confirmed (3 empty diffs)"
  - claim: "upstream v0.4.20 did NOT touch comment.go signature (no Strategy D risk on triggerTasksForComment / bindAndEnqueueSkillMentions / parseSkillMentionAgents / bindDesignatedSkillsForTriggers)"
    verified_by: "4× git diff v0.4.18..v0.4.20 -S <symbol> -- server/internal/handler/comment.go"
    result: "confirmed (4 empty diffs)"
  - claim: "upstream v0.4.20 did NOT touch MUL-5559 path (isAgentRuntimeBound / ReasonAgentRuntimeRequired) — fork 9-param workaround still valid"
    verified_by: "2× git diff v0.4.18..v0.4.20 -S <symbol> -- server/"
    result: "confirmed (2 empty diffs)"
  - claim: "upstream v0.4.20 did NOT touch fork own files (provenance_baseline.go / fallback_comment_truncate.go / skill_mention_trigger.go)"
    verified_by: "3× git show v0.4.20:<path> 2>&1 | head -3 (expect fatal not in v0.4.20)"
    result: "confirmed (3 fatal errors)"
  - claim: "upstream v0.4.20 did NOT touch mention-view.tsx / rich-content.tsx / runtime-local-skill-import-panel.tsx / mobile/{markdown,mention-serialize} / scripts/selfhost/* / CLAUDE.md / docs/customizations.md / multica-mentioning/*"
    verified_by: "git diff --name-only v0.4.18..v0.4.20 over union of 8+ paths"
    result: "confirmed (all empty)"
  - claim: "comment.go upstream v0.4.20 only 1-line comment change (no signature / no Strategy D risk)"
    verified_by: "git diff v0.4.18..v0.4.20 -- server/internal/handler/comment.go | head -40"
    result: "confirmed (1 hunk +1/-1 @ L2335)"
  - claim: "task.go upstream v0.4.20 has 591 lines diff (Strategy D RISK: enqueueIssueTask added pgtype.Timestamptz fireAt param)"
    verified_by: "git diff --shortstat v0.4.18..v0.4.20 -- server/internal/service/task.go"
    result: "confirmed (1 file, +591/-77). Fork HEAD signature still 7-param — auto-merge drops the 8th param, go build will catch it"
  - claim: "merge-base(HEAD, v0.4.20) = b312e1cbb (v0.4.18 SHA) — confirms v0.4.19 is fake-merge"
    verified_by: "git merge-base HEAD v0.4.20"
    result: "confirmed"
  - claim: "v0.4.18..v0.4.20 = 61 commits (not 27 as v0.4.19 plan claimed; union of v0.4.18..v0.4.19 + v0.4.19..v0.4.20)"
    verified_by: "git log v0.4.18..v0.4.20 | wc -l"
    result: "confirmed (61)"
  - claim: "git merge-tree HEAD v0.4.20 produces 42 CONFLICT markers (much more than v0.4.19 plan's 14)"
    verified_by: "git merge-tree HEAD v0.4.20 > /tmp/mt.txt; grep -c 'CONFLICT' /tmp/mt.txt"
    result: "confirmed (42). Cross-checked with my 3-way count 37/85 — different base, both reasonable"
  - claim: "task.go in merge-tree is Auto-merging (not CONFLICT) but has Strategy D risk — go build will catch"
    verified_by: "grep -E 'server/internal/service/task.go' /tmp/mt.txt"
    result: "confirmed (Auto-merging server/internal/service/task.go)"
  - claim: "MUL-5442 files fork pre-implemented (v0.4.19 fake-merge 引入 920 行) — v0.4.19 plan's 'fork 0 改 daemon 文件' prediction was WRONG"
    verified_by: "git diff --shortstat v0.4.18..HEAD -- server/internal/daemon/execenv/{execenv_test,runtime_config_sections,runtime_config_test,runtime_config_kind_test,reply_instructions,reply_instructions_test}.go"
    result: "refuted (v0.4.19 plan prediction). Fork actually changed 920 lines across MUL-5442 files. Real conflict expected"
  - claim: "sqlc regen needed (3 generated files +157/+205/+135)"
    verified_by: "git diff --shortstat v0.4.18..v0.4.20 -- server/pkg/db/generated/"
    result: "confirmed (3 files added significant lines; make sqlc should be run in Phase 5)"
  - claim: "upstream v0.4.20 modifies apps/web/package.json version 0.4.19 → 0.4.20 (content conflict)"
    verified_by: "git diff v0.4.18..v0.4.20 -- apps/web/package.json | head -20"
    result: "confirmed (1 content conflict in version field)"
  - claim: "channel engine router.go has Strategy D 双向签名碰撞 (enqueueMedia 7→9 参)"
    verified_by: "git diff v0.4.18..v0.4.20 -- server/internal/integrations/channel/engine/router.go | grep -E 'func.*enqueueMedia|func.*resolveAndBindMedia|func.*createIssue'"
    result: "confirmed (3 functions had signatures changed by upstream; fork 0 改 these signatures — auto-merge drops upstream's 2 params, go build will catch at the 3 call sites)"
  - claim: "officialBaseline (fork #5539) is the only writer of ServerVersion in router.go HEAD — re-apply 1-line pattern continues"
    verified_by: "git diff v0.4.18..v0.4.20 -S 'officialBaseline' -- server/"
    result: "confirmed (empty — upstream did not add a competing writer; fork HEAD router.go L218 officialBaseline(version) remains the unique stamp)"
  - claim: "4 onboarding/templates/ files deleted by upstream v0.4.20 (modify/delete expected)"
    verified_by: "git diff --name-only --diff-filter=D v0.4.18..v0.4.20 -- packages/views/onboarding/templates/"
    result: "confirmed (4 deletions: create-agent-guide-issue, helper-instructions, helper-starter-prompts, user-context) + 1 content in install-runtime-issue"
  # --- Cluster 4 audit (migrations + sqlc + on-deploy gotchas) ---
  - claim: "8 new migrations (253-260) all compliant: no FK / no cascading delete / all CREATE INDEX CONCURRENTLY / no BEGIN/COMMIT"
    verified_by: "grep -c 'REFERENCES|FOREIGN KEY' + 'BEGIN|COMMIT' + 'CREATE.*INDEX CONCURRENTLY' over all 8 .up.sql files"
    result: "confirmed (0 FK / 0 cascading / 5 CONCURRENTLY / 0 txn markers across all 8)"
  - claim: "Migration lint map covers all 6 collisions (119/158/159/160/213/214), test green"
    verified_by: "cd server && go test -run 'TestMigrationNumericPrefixesStayUniqueAfterLegacySet' -count=1 ./internal/migrations/"
    result: "confirmed (PASS; legacyDuplicateMigrationStems L17-54 lists all 6)"
  - claim: "12 fork+upstream collision pairs still intact in server/migrations/"
    verified_by: "ls server/migrations/ | grep '^119_\|^158_\|^159_\|^160_\|^213_\|^214_'"
    result: "confirmed (12 files)"
  - claim: "fork HEAD does NOT yet have v0.4.20 migrations 257-260"
    verified_by: "ls server/migrations/ | grep '^25[7-9]\|^26[0]'"
    result: "confirmed (empty — only 253-256 present)"
  - claim: "fork skill.sql has UpsertAgentSkillEnabled (TOCTOU fix), upstream untouched — auto-merge clean"
    verified_by: "grep -c 'UpsertAgentSkillEnabled' server/pkg/db/queries/skill.sql server/pkg/db/generated/skill.sql.go"
    result: "confirmed (1 in queries/skill.sql L127, 1 in generated/skill.sql.go L124-141)"
  - claim: "5 sqlc query files upstream heavily modified (+240/-37 agent.sql, +415 chat.sql, +135 dingtalk.sql new, +17 issue.sql, +27 task_usage.sql)"
    verified_by: "git diff --name-only v0.4.18..v0.4.20 -- 'server/pkg/db/queries/*.sql' + corresponding git diff --shortstat"
    result: "confirmed (5 files)"
  - claim: "9 fork sqlc query files overlap upstream (agent, chat, inbox, invitation, issue, project, skill, user, workspace)"
    verified_by: "git log v0.4.18..HEAD --oneline -- 'server/pkg/db/queries/*.sql'"
    result: "confirmed (9 files fork-touched; skill.sql is fork-only safe, other 8 need spot-check during Phase 4)"
  - claim: "v0.4.20 standalone `go build ./...` passes clean (no drift between sqlc generated code and .sql sources)"
    verified_by: "git stash && git checkout v0.4.20 && cd server && go build ./... 2>&1"
    result: "confirmed (exit 0, no output) — if merge brings in upstream generated .sql.go cleanly, build will succeed"
  - claim: "upstream v0.4.20 does NOT touch CLAUDE.md / AGENTS.md / CONCEPTS.md / docs/solutions/ / docs/upgrades/"
    verified_by: "git diff --name-only v0.4.18..v0.4.20 -- <each path>"
    result: "confirmed (all empty)"
  - claim: "8 fork-only TS/Go files upstream-untouched: provenance_baseline.go, fallback_comment_truncate.go, skill-agent-picker.tsx, skill-mention-context.ts, skill-mention-chip.tsx, use-skill-designated-preview-agents.ts, comment-trigger-outcomes.ts, issue-identifier-badge.tsx"
    verified_by: "8× git diff --name-only v0.4.18..v0.4.20 -- <path>"
    result: "confirmed (8 empty diffs)"
  - claim: "rollback path is v0.4.19 dump (restore from v0.4.19-pre-migrate-20260806-095612.dump), NOT v0.4.18 (would delete v0.4.19 migrations 253-256 applied 24h ago)"
    verified_by: "ls -la /Users/fengzhao/multica-backups/ | grep -E 'v0.4.1[89]-pre-migrate'"
    result: "confirmed (v0.4.19 dump 55M exists; v0.4.18 dump 51M exists but would roll back v0.4.19 too)"
  - claim: "Daemon CLI self-upgraded to 0.4.20 during Cluster 4 audit (multica update), but running daemon still 0.4.19 (PID 2695, 20h uptime)"
    verified_by: "multica --version; multica daemon status"
    result: "confirmed (CLI 0.4.20 commit 93342d04a, daemon running 0.4.19) — Phase 6.3 should run `multica daemon restart` to bring daemon to 0.4.20"
  - claim: "DB size is 222 MB"
    verified_by: "psql $DATABASE_URL -c 'SELECT pg_size_pretty(pg_database_size(current_database()))'"
    result: "confirmed (222 MB — Phase 6.1 backup expected ~55-60 MB compressed)"
  - claim: "PR states match ledger (no flips): #5346 OPEN, #5309 OPEN, #4118 OPEN, #2669 OPEN, #5160 OPEN, #6106 OPEN, #5199 CLOSED 2026-07-31, #5539 CLOSED 2026-07-16, #5466 WITHDRAWN"
    verified_by: "gh pr view <N> --repo multica-ai/multica --json state,mergedAt,title,closedAt,number for each"
    result: "confirmed (all states match ledger)"
  # --- Cluster 2 audit (fork ahead-commits cluster + ledger coverage) ---
  - claim: "v0.4.18..HEAD = 277 leading commits, clustered into 24 distinct scopes"
    verified_by: "git log v0.4.18..HEAD --oneline | wc -l + per-cluster grep"
    result: "confirmed (277 commits, 24 clusters in §1 of Cluster 2 report)"
  - claim: "41 commits are 'ghost cherry-picks' — content byte-identical to upstream v0.4.20"
    verified_by: "git log v0.4.18..HEAD --oneline --grep 'access-scope|access scope|AccessPicker' + git diff v0.4.20 HEAD -- packages/views/agents/components/inspector/access-picker.tsx"
    result: "confirmed (access-picker.tsx empty diff; auto-resolves)"
  - claim: "6 known migration collisions (119/158/159/160/213/214) — 25 files (not 12 as v0.4.19 plan claimed; each stem has up+down+notes.md where applicable)"
    verified_by: "ls server/migrations/ | sort | grep -E '^(119|158|159|160|213|214)_' | wc -l"
    result: "confirmed (25 files: 119=4, 158=5 [has notes.md], 159=4, 160=4, 213=4, 214=4)"
  - claim: "fork has 3 ledger coverage gaps: 571b38122 (clearFilters fix), 37d0a1e15 (mobile skill mentions serialize), a55c95c4c (type-scale refactor)"
    verified_by: "git log v0.4.18..HEAD --oneline --grep '<subject>' for each + cross-reference with docs/customizations.md"
    result: "confirmed (3 fork-only commits not in ledger — should add rows post-merge)"
  - claim: "floating-chat.tsx is the highest-probability real conflict (fork FAB-reserve vs upstream #6492 mobile comment rails + full-screen keyboard)"
    verified_by: "git show --name-only --format= 748059058 + git diff v0.4.18..HEAD / v0.4.18..v0.4.20 / v0.4.20..HEAD --shortstat -- packages/views/chat/floating-chat.tsx"
    result: "REFUTED (Cluster 2 错). 748059058 改的是 issue-detail.tsx(给 pin-comment 容器加 pr-14 sm:pr-8),根本没碰 floating-chat.tsx; 三个 pairwise diff 全 0 行; floating-chat.tsx 不在 merge-tree changed_in_both。真正承载 FAB-reserve 的是 issue-detail.tsx(已在 C 策略)。"
  - claim: "fork pin-comment-input #1199a1807 + identifier-badge #ecd309bb0 + archived-dim #7d6d1a361 + mobile chat-FAB #748059058 all present at HEAD"
    verified_by: "git log v0.4.18..HEAD --oneline --grep '<each subject>'"
    result: "confirmed (all 4 fork LOCAL customizations present)"
  - claim: "fork type-scale refactor #a55c95c4c touches 12 fork-only files (mention-chip, skill-agent-picker, identifier-badge, admin user-management, runtime-local-skill-import-panel)"
    verified_by: "git show --stat a55c95c4c"
    result: "confirmed (12 files, all fork-only)"
  - claim: "v0.4.20 upstream does NOT touch apps/mobile/lib/markdown/markdown.tsx + apps/mobile/lib/mention-serialize.ts (fork mobile customizations safe)"
    verified_by: "git diff --name-only v0.4.18..v0.4.20 -- apps/mobile/lib/markdown/markdown.tsx apps/mobile/lib/mention-serialize.ts"
    result: "confirmed (empty)"
  - claim: "fork ledger last_updated=2026-08-05, missing v0.4.19 entry (2026-08-06)"
    verified_by: "head -5 docs/customizations.md"
    result: "confirmed (last_updated: 2026-08-05, ledger out-of-sync with the v0.4.19 fake-merge audit)"
  # --- Cluster 3 audit (B-group frontend + locale depth + mobile timeline-list; focused restart ad7184939) ---
  - claim: "provider-logo.tsx + mobile timeline-list.tsx are byte-identical HEAD↔v0.4.20 (fake-merge staging already brought upstream blobs; the 'fork customizations' are gone)"
    verified_by: "git rev-parse HEAD:<path> v0.4.20:<path> for both files"
    result: "confirmed identical (provider-logo c5a3481c; timeline-list fee16f0a7). d8ae2005f touched 0 lines of timeline-list; only upstream commit in range = MUL-5752 3e1a7e040"
  - claim: "zh-Hans/issues.json + ja/issues.json have ZERO genuine per-key divergence (merge-tree high-hunk flags are both-sides-synced-same-translation artifacts)"
    verified_by: "3-way flatten script: changed(base,upstream) ∩ changed(base,fork), then diverge filter; + git diff v0.4.18..HEAD vs ..v0.4.20 key-value spot-check"
    result: "confirmed (zh-Hans/issues BOTH=99 DIVERGE=0; ja/issues BOTH=114 DIVERGE=0; 'Issues'→'任务' / '还没有 issue'→'还没有任务' identical both sides) → A accept-upstream"
  - claim: "upstream added NO native mention-chip system in v0.4.20"
    verified_by: "git diff --name-status --diff-filter=A v0.4.18..v0.4.20 | grep -i mention"
    result: "confirmed (empty — only 'M mention-suggestion.tsx' in whole tree, no new mention files)"
  - claim: "upstream did NOT touch mention-view.tsx / mention-hover-card.tsx / rich-content.tsx / runtime-local-skill-import-panel.tsx / mobile markdown.tsx / mention-serialize.ts"
    verified_by: "git diff --name-status v0.4.18..v0.4.20 -- <each path>"
    result: "confirmed (all empty) → D keep-fork, auto-merge"
  - claim: "upstream did NOT rename issue-detail.tsx breadcrumb props (leadingAction intact, no leafAction) and ThreadNavPanel NOT removed"
    verified_by: "git show v0.4.20:packages/views/issues/components/issue-detail.tsx | grep -nE 'leadingAction|leafAction|ThreadNavPanel'"
    result: "confirmed (leadingAction L928/936/947/1050; zero leafAction; ThreadNavPanel L77/2429 still present)"
  - claim: "upstream does NOT have fork's pr-14 sm:pr-8 mobile-FAB reserve in issue-detail.tsx (fork-only)"
    verified_by: "git show v0.4.20:.../issue-detail.tsx | grep -nE 'pr-14|sm:pr-8'"
    result: "confirmed (empty — fork-only). Upstream's ImageSequenceProvider is NEW (v0.4.18=0/v0.4.20=4/HEAD=0 refs)"
  - claim: "comment-input.tsx sticky-composer overlap CONVERGED (both sides useStickyComposer); upstream made ZERO change to quickActionMenu/skillMention/follow symbols"
    verified_by: "git show HEAD:.../comment-input.tsx | grep useStickyComposer (L102) + v0.4.20 (L76); git diff -S 'quickActionMenu'/'skillMention'/'follow' all wc -l = 0"
    result: "confirmed (sticky converged; fork skill-mention chain additive+disjoint) → B keep-both, sticky hunk needs no manual resolution"
  - claim: "zh-Hans/skills.json (#5160) fork/upstream DISJOINT (no conflict)"
    verified_by: "3-way flatten: changed(base,up)=1 key, changed(base,fork)=6 keys, intersection=0"
    result: "confirmed (BOTH=0 DIVERGE=0) → B keep-both clean"
  - claim: "genuine locale divergence in ONLY 7 files (not the merge-tree-flagged high-hunk set): {zh-Hans,ja,ko}/onboarding.json rewrite keys, {zh-Hans,ja,ko}/settings.json @mention-clause, zh-Hans/runtimes.json"
    verified_by: "3-way flatten of all 52 both-touched locale files, split upstream-DELETED (accept) vs upstream-REWROTE (careful-merge)"
    result: "confirmed (onboarding: 3/2/1 rewrite keys; settings.json 1 each — semantic @mention-clause divergence; runtimes.json 1 near-trivial quote-style) → C careful-merge on these 7 only; other 45 accept-upstream"
  - claim: "MarkdownImage export signature stable across upstream MUL-5752 change (fork markdown.tsx caller unaffected)"
    verified_by: "git show v0.4.18:.../markdown-image.tsx vs v0.4.20 | grep 'export function MarkdownImage' + Props alt?"
    result: "confirmed (both '({uri,attachments}:Props)', alt? in Props both) → mobile lightbox auto-merge safe"

# Re-verify pattern: see docs/upgrades/v0.4.19-plan.md banner + memory project_v0_4_19_fake_merge.md.
# This upgrade is a v0.4.18 → v0.4.20 jump (61 commits, 603 files, +51457/-10388).
# v0.4.19 fake-merge: c03402a94/c95bb7e89 are linear commits, no upstream content, never merged.
# Real merge-base (merge-base HEAD v0.4.20 = b312e1cbb) is v0.4.18 SHA.
---

# Upgrade v0.4.18 → v0.4.20 (with v0.4.19 fake-merge skipped)

> **Banner.** This is a v0.4.18 → v0.4.20 single jump because the v0.4.19 "upgrade" was a fake merge (see `docs/upgrades/v0.4.19-plan.md` and memory `project_v0_4_19_fake_merge.md`). The merge-base is v0.4.18, the diff is the union of v0.4.18..v0.4.19 and v0.4.19..v0.4.20: 61 upstream commits, 603 files changed, +51457/-10388.

> **Risk profile.** This is the largest single upgrade this fork has ever attempted. The v0.4.20 plan must audit the **full v0.4.18..v0.4.20 diff**, not just v0.4.19..v0.4.20. The v0.4.19 plan's per-file strategy table is *partially* valid (the diff audit was correct for the v0.4.18..v0.4.19 portion) but its application was never real; the v0.4.20 plan supersedes for execution.

> **DB schema state is known (good news).** `SELECT MAX(version) FROM schema_migrations` returns **256_drop_agent_task_queue_chat_pending_v2** (v0.4.19 schema). The v0.4.19 fake-merge deploy *did* run `migrate up` (migrations are applied independently of merge commits). So Phase 6.2 only needs to apply v0.4.20's 4 new migrations (257-260); v0.4.19's 253-256 will skip cleanly.

> **Source state vs DB state is split.** DB is at v0.4.19 schema, but source tree is at v0.4.18 + fork customizations. The v0.4.20 merge brings source tree to v0.4.20 + fork (without losing any customizations). This is the right order: source first, then migrate.

## Phase 0.5 — Pre-audit preview (DONE, raw merge-tree)

`git merge-tree v0.4.18 c95bb7e89 v0.4.20` (read-only) classified **603 files**:
- 147 `merged` (auto-merge OK, no conflict)
- 115 `added in remote` (upstream-only new files, no conflict)
- 14 `removed in remote` (upstream deleted, fork has nothing → OK)
- 0 `added in local` / 0 `removed in local` (fork doesn't add or delete unique files in v0.4.18..HEAD)
- 4 `added in both` (rare: both sides added same stem; need spot-check)
- 114 `changed in both` (both sides modified; subset carries real `<<<<<<<` conflict markers)

**Real conflicts**: **37 files / 85 hunks** carry `<<<<<<<` markers. Top hotspots:

| Rank | File | Hunks | Cluster |
|---|---|---|---|
| 1 | `packages/views/issues/components/issue-detail.tsx` | 11 | TS (fork pin/identifier-badge/chat-FAB/archived-dim + upstream thread-nav/image-nav/unsubscribe) |
| 2 | `packages/views/locales/zh-Hans/issues.json` | 7 | locale i18n (show_closed→show_completed, no_value, no_assignee, etc.) |
| 2 | `server/pkg/agent/qoder.go` | 7 | Server (qoder runtime, fork 0-touch) |
| 4 | `packages/views/locales/ja/onboarding.json` | 5 | locale (mika onboarding rewrites helper flow) |
| 4 | `packages/views/locales/zh-Hans/onboarding.json` | 5 | locale (mika onboarding rewrites helper flow) |
| 6 | `packages/views/locales/ja/issues.json` | 4 | locale (show_closed→show_completed, archived, view_run, identifier) |
| 6 | `server/internal/integrations/channel/engine/router_test.go` | 4 | Server (DingTalk + Lark channel routing) |
| 8 | `server/internal/daemon/execenv/runtime_config_sections.go` | 3 | Server (MUL-5442 daemon brief) |
| 8 | `server/internal/integrations/channel/engine/router.go` | 3 | Server (channel routing) |
| 8 | `packages/views/onboarding/templates/install-runtime-issue.ts` | 3 | TS (fork 7-step mika flow vs upstream mika flow) |
| 8 | `apps/web/features/landing/i18n/{ja,ko,zh}.ts` | 2 each | TS (fork v0.4.19 changelog vs upstream v0.4.20 changelog) |

**Single-hunk files (often additive)** (24 files): `apps/web/package.json` (version bump), `packages/core/api/client.ts` + `client.test.ts` (mika onboarding API), `packages/core/realtime/use-realtime-sync-ws-instance.test.tsx`, `packages/views/issues/components/comment-{composers.test,input}.tsx`, `packages/views/locales/{en,ja,ko,zh-Hans}/{editor,layout,settings,runtimes}.json`, `server/internal/daemon/execenv/execenv_test.go`, `server/internal/handler/chat.go`, `server/pkg/agent/{kimi,models}.go`, `packages/views/workspace/welcome-after-onboarding.test.tsx`, `packages/core/realtime/use-realtime-sync-ws-instance.test.tsx`.

## Phase 0.6 — Live production state (DONE, raw probes)

(Already covered above in banner.)

> **Read-only 3-way merge-tree preview (Phase 0.5).** Ran `git merge-tree v0.4.18 c95bb7e89 v0.4.20` to count conflict markers. Found **37 files with 85 hunks of real `<<<<<<<` conflicts** (out of 114 `changed in both` candidates; the rest auto-merge). Top hotspots: `packages/views/issues/components/issue-detail.tsx` (11 hunks — fork 4 commits vs upstream 5 commits at the bottom region), `packages/views/locales/zh-Hans/issues.json` (7 hunks), `server/pkg/agent/qoder.go` (7 hunks), `packages/views/locales/{ja,zh-Hans}/onboarding.json` (5 hunks each), `ja/issues.json` (4 hunks), `server/internal/integrations/channel/engine/router_test.go` (4 hunks).

> **Phase 0.6 — Live production state (raw probes, 2026-08-07 morning).**
> - Backend `http://localhost:8081/api/config` → `server_version=v0.4.19` (lie; binary is v0.4.18 + fork + ldflags stamp)
> - Backend `/healthz` → 200; Frontend `/` → 200
> - launchd: `com.fengzhao.multica-backend` (PID 73883) + `com.fengzhao.multica-frontend` (PID 72961) running
> - Daemon: `multica 0.4.19 (commit: a8d5daac5)` — **real v0.4.19** because daemon is Homebrew-managed, not repo-built. Uptime 19h51m.
> - `.env` `NEXT_PUBLIC_APP_VERSION=v0.4.19`, `DATABASE_URL=postgres://fengzhao@localhost:5433/multica?sslmode=disable`
> - `strings ~/.multica/backend/server | grep main.version=` shows `-X main.version=v0.4.19` (ldflag stamp was correct, but source tree is v0.4.18 + fork)
> - `~/.multica/backend/server.bak.v0.4.18` exists (the previous v0.4.18 binary, kept for rollback)
> - **DB schema state (real query):** `SELECT MAX(version) FROM schema_migrations;` → **`256_drop_agent_task_queue_chat_pending_v2`** (307 rows applied). **DB is at v0.4.19 schema, NOT v0.4.18.** This means the v0.4.19 `migrate up` step WAS run during the fake-merge deploy (migrations are applied independently of the merge commit; the binary at that time did have v0.4.19 sqlc knowledge from the working tree edits).
> - **Implication for Phase 6.2:** only v0.4.20 migrations 257-260 need to be applied. v0.4.19 migrations 253-256 are already in `schema_migrations` and will skip cleanly.
> - Last backup: `~/multica-backups/v0.4.19-pre-migrate-20260806-095612.dump` (55M) — **does exist**; was created at the time of the v0.4.19 fake-merge deploy. Can be used to roll back to v0.4.19 schema + v0.4.18 source if v0.4.20 goes badly.
> - pg17 binary at `/opt/homebrew/opt/postgresql@17/bin/pg_dump` present (478k).

## Phase 0 — Locate (complete)

- HEAD: `c95bb7e89` (v0.4.18 + 277 fork commits + 2 fake-merge commits)
- target: `v0.4.20` (published 2026-08-06, non-prerelease, non-draft)
- merge-base(HEAD, v0.4.20) = `b312e1cbb` = v0.4.18 SHA → real diff is v0.4.18..v0.4.20
- 61 upstream commits, 603 files changed, +51457/-10388
- 8 new migrations: 253-260 (4 from v0.4.19, 4 from v0.4.20)
- 6 known migration collisions from v0.4.11: 119/158/159/160/213/214 (stems; all expected to be > fork max 252)
- origin/main: 4 commits ahead of v0.4.20 (unreleased, not included)
- working tree: dirty (`M docs/upgrades/v0.4.19-plan.md` — banner correction, will commit as separate doc commit)

## Phase 1 — Deep pre-merge audit (COMPLETE — Cluster 1/2/3/4 all integrated 2026-08-07)

### 61 commits 主题分布 (Cluster 1 partial)

(Filled by parallel audit agents.)

| 主题 | commits | 触 fork hot file? |
|---|---|---|
| chat follow-up queue (#6211) + 维护 | 7 | yes — `client.ts` + `pending.{ts,test.ts}` + 17 chat files + mobile chat |
| chat audience + Mika onboarding (#6378) + starter cards | 3 | yes — `onboarding/templates/*` (5 files) + 9 locales |
| 新 runtime (Reasonix #6370 + QwenPaw #5986) | 4 | yes — `models.go` 真冲突 + `qoder.go` 真冲突 + `provider-logo.tsx` |
| agent / runtime 修复 | 8 | partial — `kimi.go` 真冲突 |
| **MUL-5442 daemon brief 重写** (stage-1 × 4 + stage-2 + retire no-reply) | 6 | **yes — fork pre-implemented** (v0.4.19 fake-merge 引入 920 行) → 真冲突 |
| Mika + DingTalk + chat queue routing (#4829 + #6486) | 5 | yes — `handler.go` + `router.go` + `channel/engine/router.go` 真冲突 |
| per-turn slimming + out-of-band converge (#6382 + #6483) | 3 | yes — `prompt.go` + `reply_instructions.go` + `daemon.go` |
| mobile 修复 (issue spacing + comment rails + keyboard chat) | 4 | yes — `apps/mobile/components/issue/timeline-list.tsx` + `chat/*` |
| desktop 修复 | 2 | 0 (fork 不碰 desktop) |
| channel issue commands terminal + media attach (#6398 + #6388) | 2 | **yes — Strategy D 双向签名碰撞**（enqueueMedia 7→9 参） |
| usage / exec 修复 | 3 | partial — `schemas.ts` 加 `TaskUsageSchema` |
| i18n + docs + release (issue→task MUL-5703 + changelog v0.4.20) | 5 | yes — **11 locale JSON** 真冲突 + 4 landing i18n 真冲突 + `apps/web/package.json` version bump |
| onboarding/templates 重写 (Mika-first) | 4 | **yes — 3 modify/delete + 1 content** (Risk 9) |

### Cluster 1 — Verified negative claims (32 done, more from Cluster 2-4)

(Full table in audit report. Highlights:)
- fork own files (provenance_baseline.go / fallback_comment_truncate.go / skill_mention_trigger.go) all upstream-untouched
- truncate 3 symbols (`truncateFallbackCommentBody` / `maxSynthesizedFallbackCommentRunes` / `oversizedFallbackComment`) all upstream-not-redeclared (v0.4.12 教训不复发)
- comment.go upstream only 1-line comment change; no Strategy D risk on `triggerTasksForComment` / `bindAndEnqueueSkillMentions` / `parseSkillMentionAgents`
- 8 new migrations (253-260) all > fork max 252 → zero new collision; 6 known collisions (119/158/159/160/213/214) still both-present
- 5 fork-only hot files upstream-untouched: mention-view.tsx / rich-content.tsx / runtime-local-skill-import-panel.tsx / mobile markdown.tsx + mention-serialize.ts / scripts/selfhost/*

### Cluster 1 — 18 ranked risks

| # | Risk | Severity | Phase 5 mitigation |
|---|---|---|---|
| 1 | `server/internal/integrations/channel/engine/router.go` 真实 Strategy D 双向签名碰撞 (enqueueMedia 7→9 参) | **HIGHEST** | `go build -C server ./...` 必抓 |
| 2 | `server/internal/service/task.go` Strategy D 静默 auto-merge 丢 fireAt 参 | **HIGH** | `go build` 必抓 |
| 3 | `packages/core/api/client.ts` 大 content conflict (Mika + DingTalk + chat queue + workspaceHeader helper) | **HIGH** | `pnpm typecheck` 抓 + 4 处 inline X-Workspace-Slug 替换为 `workspaceHeader()` |
| 4 | `packages/views/issues/components/issue-detail.tsx` content conflict (10 upstream commits + 4 fork commits) | **HIGH** | 套 v0.4.19 plan Phase 4 流程扩展 |
| 5 | `packages/views/issues/components/comment-input.tsx` content conflict | **HIGH** | 取 upstream follow-up ordering,回植 fork skill_mention 链 |
| 6 | MUL-5442 文件 fork pre-implement (920 行 + 4 真冲突) | **HIGH** | 先核对 fork pin assertion 列表 |
| 7 | `server/pkg/agent/kimi.go` 真冲突 (scanner 行 overlap) | **MED-HIGH** | `go build` 兜底 |
| 8 | `server/pkg/agent/{models,reasonix,qwenpaw}.go` add/add 内容不同 | **HIGH** | C careful-merge;取 upstream 最新 + fork pre-existing |
| 9 | `packages/views/onboarding/templates/{create-agent-guide-issue,helper-instructions,helper-starter-prompts,install-runtime-issue,user-context}.ts` (3 modify/delete + 1 content + 1 add) | **HIGH** | 3 modify/delete 取 fork 版 (MUL-5703 carry 翻译);install-runtime-issue.ts 取 upstream Mika-first + 保留 fork 翻译 |
| 10 | `packages/views/locales/{zh-Hans,ja,ko}/{onboarding,settings,runtimes,layout,editor,issues}.json` (11 真冲突) | **HIGH** | B keep-both additive careful-merge 逐 key 并集 |
| 11 | `server/internal/integrations/channel/engine/router_test.go` (4 hunks, fork 测试 + upstream fakeIssues) | **MED** | C careful-merge |
| 12 | `apps/web/{features/landing/i18n/*.ts, package.json}` (4 landing i18n + version bump) | **MED** | A accept upstream (fork 0 改) |
| 13 | sqlc regen needed (3 generated files +157/+205/+135) | **HIGH must run** | `make sqlc` 验证 |
| 14 | `chat-window.tsx` 上游大改 + `comment-card.tsx` 上游小改 | **MED** | B keep-both auto-merge OK |
| 15 | 8 新迁移 (253-260) zero collision, DB at v0.4.19, only 257-260 to apply | **LOW** | Phase 6.2 `migrate up` 自动 |
| 16 | MUL-3269 out-of-band version converge + halve version-probe freq | **LOW** | auto-merge |
| 17 | install.sh version stamp 倒退 (merge 未 commit) | **MED** | 先 commit merge 再 install.sh;或手动 re-stamp (`memory project_install_sh_version_needs_commit.md`) |
| 18 | launchctl kickstart after bootout 失败 | **MED** | 用 `launchctl bootstrap gui/UID plist-path` (`memory launchctl_bootout_needs_bootstrap.md`) |

### Cluster 1 — Cross-cluster handoff

**→ Cluster 2 (后端 Go server)**: 必检 `bindAndEnqueueSkillMentions` / `parseSkillMentionAgents` / `bindDesignatedSkillsForTriggers` / `triggerTasksForComment` / `isAgentRuntimeBound` / `ReasonAgentRuntimeRequired` 6 个 fork-only 符号上游新增调用点;必检 enqueueMedia/resolveAndBindMedia/createIssue 调用点 L411/L423/L440/L453 是否补 `issue db.Issue, issueTaskID pgtype.UUID` args。

**→ Cluster 3 (前端 TS / i18n / locales / client.ts)**: 必处理 11 个 locale JSON content conflict (v0.4.19 9 个 + v0.4.20 新增 2 onboarding/settings);必处理 4 onboarding 模板 (3 modify/delete + 1 content);必核对 `client.ts` 4 处 inline X-Workspace-Slug 是否替换为 `workspaceHeader()` helper;必核对 `thread-nav-panel.tsx` add/add 冲突 (取 fork HEAD 版);必核对 `welcome-after-onboarding.test.tsx` 与 `comment-composers.test.tsx` (A accept upstream, fork 0 改)。

**→ Cluster 4 (local-only 守护 — fork own file / selfhost / #4118 / #5539 / #5346 / #5199 / #5160 / mobile / CLAUDE.md / docs)**: 全部已 verified upstream 0 改 (Cluster 1 §3 claim 1-3, 17-23) — 保留即可。#5539 provenance 仍 re-apply 1 行模式;`ServerVersion: officialBaseline(version)` 仍为 router.go L218 唯一写入点。

### merge-tree 实测冲突 (TBD — pending Phase 3 preview)

(Run after audit.)

### Per-file strategy (FINAL — Cluster 1/2/3/4 all integrated; issues.json downgraded to A, comment-input to B, 5 fork-only frontend confirmed D, provider-logo/timeline-list confirmed A no-op)

| File | Strategy | Rationale |
|---|---|---|
| `apps/web/package.json` | **A accept upstream** | 1 hunk: version 0.4.19→0.4.20; fork 0 改 |
| `apps/web/features/landing/i18n/{en,ja,ko,zh}.ts` | **A accept upstream** | 4 files, 1-2 hunks: upstream v0.4.20 changelog entries; fork 0 改 (only adds v0.4.19 changelog which upstream rolled into v0.4.20) |
| `packages/core/api/client.ts` | **C careful-merge** | 1 hunk: upstream 211 lines (mika/dingtalk/chat queue/workspaceHeader helper); fork 153 lines (#5539 provenance + archived status + inline X-Workspace-Slug). Take upstream new methods + workspaceHeader helper; replace fork's 4 inline `workspaceSlug ? { ... } : undefined` with `workspaceHeader(workspaceSlug)`; retain fork #5539 provenance 2 lines |
| `packages/core/api/client.test.ts` | **B keep-both** | 1 hunk: upstream new mika onboarding test; fork test tail. Both additive |
| `packages/core/realtime/use-realtime-sync-ws-instance.test.tsx` | **B keep-both** | 1 hunk: upstream new "detached channel media binds" test; fork tail. Both additive |
| `packages/views/issues/components/issue-detail.tsx` | **C careful-merge (HIGH)** | 11 hunks。Cluster 3 关键事实:upstream **未动** `ThreadNavPanel`(v0.4.20 仍 L77/2429)、`leadingAction` 无重命名(无 `leafAction`)、`pr-14 sm:pr-8` FAB-reserve fork-only(upstream 无)。upstream 新增 `ImageSequenceProvider` wrapper(v0.4.18=0/v0.4.20=4/HEAD=0)+ subscribe UI 重写(MUL-5710/5714)。fork 4 个 disjoint 定制全保留:`IssueIdentifierBadge`(L60/2289)、archived-dim(L618/2530/2554)、pin-comment-input、FAB-reserve(L2828)。**⚠️ Phase 4 决策点**:upstream 与 fork 用两套 pin-comment 机制 — upstream `useStickyComposer` sticky-in-column vs fork 独立 `shrink-0 border-t … pr-14` div。Cluster 3 建议**采用 upstream `useStickyComposer` 结构 + 把 fork `pr-14 sm:pr-8` FAB-reserve 重挂上去**(而非保留两套);Cluster 1 倾向 keep fork。**到 Phase 4 看实际 hunk 再定,不在此拍板** |
| `packages/views/issues/components/comment-input.tsx` | **B keep-both (LOW-MED)** | **Cluster 3 精练(推翻 L407 的"imports 1 hunk 冲突"假设)**:upstream 仅 5+/3-,唯一重叠行(sticky-composer)双侧已**逐字收敛**为 `useStickyComposer()`(HEAD L102 / v0.4.20 L76);`-S quickActionMenu/skillMention/follow` 全 0 → upstream 未碰 fork 的 skill-mention 链(`skillMentionAgents`/`useSkillDesignatedPreviewAgents`/`setDraftPayload` 等,additive 且 disjoint)。取 upstream 文件 + 回植 fork additive block;sticky hunk 无需手工解决。**注**:Cluster 3 未测 `useWorkspaceId` import — Phase 4 typecheck 若缺则补(Cluster 1 的 import 关注点保留) |
| `packages/views/issues/components/comment-composers.test.tsx` | **B keep-both** | 1 hunk: fork skill mention test tail; upstream new test. Both additive |
| `packages/views/onboarding/templates/create-agent-guide-issue.ts` | **fork wins (modify/delete)** | upstream v0.4.20 deleted; fork kept it with MUL-5703 translation. **Retain fork** |
| `packages/views/onboarding/templates/helper-instructions.ts` | **fork wins (modify/delete)** | same. **Retain fork** |
| `packages/views/onboarding/templates/helper-starter-prompts.ts` | **fork wins (modify/delete)** | same. **Retain fork** |
| `packages/views/onboarding/templates/user-context.ts` | **fork wins (modify/delete)** | same. **Retain fork** (test file too) |
| `packages/views/onboarding/templates/install-runtime-issue.ts` | **C careful-merge** | 3 hunks: upstream Mika-first 7-step (zh/ja/ko); fork MUL-5703 翻译. **Take upstream Mika-first structure + retain MUL-5703 "任务/タスク/태스크" translation** |
| `packages/views/locales/{zh-Hans,ja}/issues.json` | **A accept upstream** | **Cluster 3 逐键验证推翻 merge-tree 高 hunk 标记**:3-way 逐键比对 DIVERGE=0,fork 的 `"Issues"→"任务"`/`"还没有 issue"→"还没有任务"` 等术语定制与 upstream 官方翻译**逐字收敛**(`git diff v0.4.18..HEAD` 与 `..v0.4.20` 的 key-value 完全一致)。hunk 重叠是双侧同步同一翻译的假象。**纯接受上游** |
| `packages/views/locales/zh-Hans/skills.json` | **B keep-both** | **Cluster 3 逐键验证**:fork #5160 改 6 key、upstream 改 1 key,`changed(base,up)∩changed(base,fork)=0` 完全不重叠 → disjoint,自动合并即可 |
| `packages/views/locales/{zh-Hans,ja,ko}/{editor,layout}.json` | **C careful-merge** | v0.4.19 plan already covered. Reuse v0.4.19 plan's B keep-both additive strategy |
| `packages/views/locales/{ja,ko,zh-Hans}/onboarding.json` (3) | **C careful-merge** | Upstream Mika helper-flow rewrite. Take upstream mika helper text, retain fork MUL-5703 "任务/タスク/태스크" translation in helper-intro paragraphs |
| `packages/views/locales/{ja,ko,zh-Hans}/settings.json` (3) | **C careful-merge** | 1 hunk each: upstream split mentions notification (MUL-5875 #6486). Take upstream new `mentions` notification key, retain fork `comment_mentions` legacy key for back-compat |
| `packages/views/locales/zh-Hans/runtimes.json` | **C careful-merge** | 1 hunk: description "删除「{{name}}」" → "删除\"{{name}}\""; take upstream (Chinese quote convention per conventions.zh.mdx) |
| `packages/views/locales/en/onboarding.json` | **A accept upstream** | 1 hunk: upstream Mika helper-flow rewrite; fork 0 改 |
| `packages/views/workspace/welcome-after-onboarding.test.tsx` | **A accept upstream** | 1 hunk: upstream import cleanup; fork 0 改 |
| `server/internal/daemon/execenv/execenv_test.go` | **C careful-merge** | 1 hunk: MUL-5442 stage-1 file. fork pre-implemented (920 lines total); upstream adds stage-2. Verify pin assertion list in both sides, take union of unique assertions |
| `server/internal/daemon/execenv/runtime_config_sections.go` | **C careful-merge** | 3 hunks: MUL-5442 file. Same as above — pre-implemented, now stage-2 + retire no-reply (#6493). Carefully merge judgment form sections |
| `server/internal/handler/chat.go` | **C careful-merge** | 1 hunk: chat audience #6428 + chat follow-up queue #6211. fork 0 改 on chat.go? — verify |
| `server/internal/integrations/channel/engine/router.go` | **C careful-merge + post-fix go build** | 3 hunks: Strategy D bidirectional signature. Take upstream 9-param/8-param signature, **after merge add `issue db.Issue, issueTaskID pgtype.UUID, assignedRunFireAt time.Time` args to fork's 3 call sites** (post-fixup script) |
| `server/internal/integrations/channel/engine/router_test.go` | **C careful-merge** | 4 hunks: upstream fakeIssues/fakeTasks mock fields + fork test additions. Take union |
| `server/pkg/agent/kimi.go` | **C careful-merge** | 1 hunk: hunk overlap in scanner line (v0.4.18 baseline already uses newAgentStreamScanner). Carefully retain both `scanKimiSessionUsage` and `newAgentStreamScanner` |
| `server/pkg/agent/models.go` | **C careful-merge** | 1 hunk: upstream reasonix + qwenpaw switch cases; fork pre-existing reasonix case. Take union |
| `server/pkg/agent/qoder.go` | **C careful-merge** | 7 hunks: upstream ACP terminal usage (#6503) + fork 0 改. Take upstream new code, retain any fork-internal functions |
| `packages/views/runtimes/components/provider-logo.tsx` | **A accept upstream (no-op)** | **Cluster 3 blob-hash 证实**:HEAD==v0.4.20(`c5a3481c`)。fork 的 qwenLogoSrc IIFE 已被 upstream `staticAssetSrc` refactor 覆盖 — merge 时无操作 |
| `apps/mobile/components/issue/timeline-list.tsx` | **A accept upstream (no-op)** | **Cluster 3 blob-hash 证实**:HEAD==v0.4.20(`fee16f0a7`)。区间内唯一 upstream commit 是 MUL-5752;`d8ae2005f` 对该文件 0 改动 |
| `apps/mobile/lib/markdown/markdown.tsx` + `mention-serialize.ts` | **D keep fork** | upstream `-S` 全 0(未碰)。fork markdown.tsx 仅注释 +2;mention-serialize.ts +2/-2。自动合并保留 fork。upstream 新增 `image-sequence.tsx`/`lightbox-provider.tsx`/`markdown-image.tsx`(MUL-5752 lightbox)自动带入,`MarkdownImage` 导出签名 `({uri,attachments}:Props, alt?)` 跨版本稳定 → fork 调用方不受影响 |
| `packages/views/editor/extensions/mention-view.tsx` / `mention-hover-card.tsx` / `rich-content/rich-content.tsx` / `skills/components/runtime-local-skill-import-panel.tsx` | **D keep fork (upstream 全 0)** | **Cluster 3 `-S` 证实**:upstream 对这些文件全 0 改动、无 rename、**无 native mention-chip 系统**(`--diff-filter=A | grep -i mention` 空)。fork #5346/#5199/#5160 定制原样保留,自动合并 |
| `packages/views/editor/extensions/mention-suggestion.tsx` | **A accept upstream (已收敛)** | upstream 唯一改动(import 迁至 `../../common/picker-keys`)在 HEAD 已 byte-identical → 收敛,fork skill-mention 加法 disjoint。无手工工作 |

### Auto-merging but needs spot-check (TBD — Cluster 2-4 will refine)

- `comment-card.tsx` (changed_in_both, no hunk conflict) — `B keep-both` auto-merge should work (upstream mobile 12px padding same as fork mobile 12px padding)
- `comment-input.tsx` import section: 1 hunk conflict; rest auto-merge
- 24+ single-hunk files: all additive B keep-both
- 147 `merged` files: pure auto-merge
- 115 `added in remote`: pure auto-add
- 14 `removed in remote`: pure auto-remove (fork has nothing to remove)
- 0 `added/removed in local`: nothing to check

### Strategy D post-merge fixup script (Phase 5 prerequisite)

```bash
# After `git merge v0.4.20` and `pnpm typecheck` (which won't catch this)
# MUST run `go build -C server ./...` which will FAIL on:
#   - server/internal/integrations/channel/engine/router.go: 3 functions (enqueueMedia/resolveAndBindMedia/createIssue)
#   - server/internal/service/task.go: 2 functions (enqueueIssueTask/enqueueIssueTaskWithCommentPlan)
# Manually fix the 5-7 call sites by appending new args:
#   - enqueueMedia(ctx, inst, identity, chatMessageID, msg, sessionID, issue, issueTaskID, deadline) — add issue + issueTaskID before deadline
#   - createIssue(ctx, inst, originType, creatorUserID, sessionID, cmd, issuePrefix, assignedRunFireAt) — add assignedRunFireAt at end
#   - enqueueIssueTask(ctx, issue, ..., rerunOfTaskID, pgtype.Timestamptz{}) — add pgtype.Timestamptz{} at end
# After fix, re-run go build → expect green
```

### Verified negative claims (TBD — Cluster 2-3 in progress)

(Filled by parallel audit agents — each "no X" must be verified by a raw command and recorded in `negative_claims:` frontmatter.)

### Cluster 3 status — INTERRUPTED, partially self-audited, restarted focused

**Cluster 3 (aed7cc3ed7920be14) died mid-run** — transcript ended 09:21 at a `tool_result` (no final assistant report, no live process). It only probed a few server `--name-only` diffs before dying; never reached analysis. Server A-group Strategy D was already covered by Cluster 1 cross-verification (§479+), so no server loss. To fill the B/C/D gap without re-flooding context, main session self-audited the cheap groups inline:

**D group (self-host + docs) — ALL CLEAN, fork-owned, strategy D (keep fork)**. Verified by:
- `git diff v0.4.18..v0.4.20 --name-only -- scripts/selfhost/` → **(empty)** — upstream never touches selfhost scripts. `install.sh`/`build-frontend.sh`/`run-backend.sh`/`*.plist` all fork-only.
- `git diff v0.4.18..v0.4.20 --name-only -- CLAUDE.md AGENTS.md CONCEPTS.md` → **(empty)** — fork docs untouched by upstream.
- `git diff v0.4.18..v0.4.20 --name-only -- docs/customizations.md docs/upgrades/` → **(empty)** — ledger + plan artifacts are fork-only.

**B11 chat/ — upstream touches 19 files, fork has ZERO chat customizations → strategy A (accept upstream wholesale)**. Verified by `git diff v0.4.18..v0.4.20 --name-only -- packages/views/chat/` → 19 files (chat-page, chat-input, chat-message-list, chat-queue, chat-window, use-chat-controller, use-visual-viewport-keyboard, task-status-pill, onboarding-starter-cards, + tests). Fork ledger records no chat/ customization. **This is v0.4.19's big chat follow-up queue (#6211) + full-screen keyboard work finally landing — pure accept.**

**C group (mobile) — RESOLVED by Cluster 3**:`timeline-list.tsx` HEAD==v0.4.20 byte-identical(`fee16f0a7`)→ **A accept upstream no-op**(区间内唯一 upstream commit MUL-5752 已并入;`d8ae2005f` 对该文件 0 改动)。`markdown.tsx`(fork 注释 +2)+ `mention-serialize.ts`(fork +2/-2)经 `-S` 证实 upstream 未碰 → **D keep fork**,自动合并。MUL-5752 lightbox 的 `image-sequence.tsx`/`lightbox-provider.tsx`/`markdown-image.tsx` 自动带入,`MarkdownImage` 签名稳定。

**REMAINING → RESOLVED**:重启的聚焦 Cluster 3 agent(ad7184939)已完成 B 组前端热文件 + locale 逐键深度 + mobile timeline-list。核心结论:**绝大多数"冲突"是假象** — issues.json 逐键 DIVERGE=0(双侧同步同一翻译)、provider-logo/timeline-list byte-identical(fake-merge staging 已带入 upstream blob)、5 个 fork-only 前端文件 upstream 全 0。**真冲突只剩**:issue-detail.tsx(HIGH,pin-comment 双机制决策点)、7 个 locale 文件的少数 rewrite key(onboarding 3 语言 + settings.json 的 `@mention`-clause 语义冲突 + runtimes.json 近平凡)。详见下文 §Cluster 3 整合 + 策略表。

### Cluster 4 audit summary (migrations + sqlc + on-deploy gotchas)

**Migration inventory (8 new)**: 253/254 (runtime_profile_add_{qwenpaw,reasonix}, `NOT VALID` CHECK widen), 255 (`agent_task_queue_chat_pending_deferred_v3` partial index), 256 (`DROP INDEX` of v2), 257 (`agent_task_queue_channel_media_pending_unique_v2` widening), 258 (`DROP` of v1), 259/260 (`issue_origin_dingtalk_chat` CHECK widen + `VALIDATE CONSTRAINT`).

**Compliance**: 0 FK / 0 cascading / 5 `CREATE INDEX CONCURRENTLY` / 0 txn markers — **all repo rules pass**. Lint map test green; 12 fork+upstream collision pairs intact.

**Live DB state**: `MAX(version) = 256_drop_agent_task_queue_chat_pending_v2` (v0.4.19 schema). Phase 6.2 must apply **only 4 migrations: 257-260**. The "8 migrations" earlier estimates were for the v0.4.18→v0.4.19 window — those were already applied during the fake-merge deploy.

**sqlc**: 5 upstream query files heavily modified (agent +240/-37, chat +415, dingtalk +135 new, issue +17, task_usage +27). Fork's `skill.sql` is upstream-untouched → auto-merge clean for `UpsertAgentSkillEnabled` (TOCTOU fix). 9 fork sqlc query files overlap upstream — needs Phase 4 spot-check.

**v0.4.20 standalone build**: `git checkout v0.4.20 && cd server && go build ./...` → **clean**. If the merge brings in upstream's generated `.sql.go` files cleanly, post-merge `go build` will succeed.

**Daemon state**: Cluster 4 audit ran `multica update` (CLI went 0.4.19 → 0.4.20 commit 93342d04a); running daemon still 0.4.19 (PID 2695). Phase 6.3 must run `multica daemon restart` to bring daemon to 0.4.20. Per memory `project_daemon_upgrade`, backend is backward-compatible so this is not urgent.

**Backup freshness**: Last backup `v0.4.19-pre-migrate-20260806-095612.dump` (55M) is ~24h old. Phase 6.1 must create a fresh `v0.4.20-pre-migrate-<ts>.dump`.

**Rollback path**: v0.4.19 dump (55M) restores to v0.4.19 schema + v0.4.18 source. **The v0.4.18 dump (51M) would also roll back v0.4.19 migrations 253-256**, which have been live 24h. Use only v0.4.19 dump for rollback.

**PR states**: all 9 PRs match ledger (no flips) — #5346/#5309/#4118/#2669/#5160/#6106 OPEN, #5199/#5539 CLOSED, #5466 WITHDRAWN.

**Daemon drift introduced by Cluster 4 audit**: `multica update` self-upgraded the CLI from 0.4.19 to **0.4.20 (commit 93342d04a)** but the running daemon remains at 0.4.19 (PID 2695, 20h5m uptime). Phase 6.3 must run `multica daemon restart` after the backend + frontend restart to bring daemon to 0.4.20. Per memory `project_daemon_upgrade`, backend backward-compat makes this non-urgent but should be done for consistency. `install.sh` does NOT restart daemon — host-facts.md documents this gap.

### Cluster 2 audit summary (fork ahead-commits + ledger coverage)

**277 fork leading commits → 24 clusters**. Highlights:
- **41 ghost cherry-picks** (content byte-identical to upstream v0.4.20): auto-resolve, but pollute log. Recommend pre-merge cleanup via `git checkout v0.4.20 -- <file>` for affected files.
- **#5346 MentionType + skill-mention** (24 commits): LOW risk — upstream v0.4.20 doesn't touch mention-* paths. comment.go upstream only renamed one string ("QUEUED"→"pre-claim"); handler.go adds DingTalkInstall field — both additive, fork 9-param workaround stays valid.
- **#5199 actor mention** (11 commits, CLOSED 2026-07-31): LOW — fork owns forever.
- **#4118 member display name** (15 commits, OPEN): LOW — upstream doesn't touch cmd_*.go.
- **#2669 skills.sh batch import** (3 commits, OPEN): LOW.
- **#5160 adaptive skill discovery** (12 commits, OPEN): MED — hot locale JSON file (skills.json).
- **#6106 archived issue status** (10 commits, OPEN): MED — issue-detail.tsx hot + migration 213/214 tolerated.
- **#5309 run-comment "View run"** (12 commits, OPEN): LOW — comment.go 1-line rename only.
- **#5539 runtime provenance** (8 commits, CLOSED 2026-07-16): LOW — own file zero conflict.
- **LOCAL customizations** (5): pin-comment-input, identifier-badge, archived-dim, mobile chat-FAB, mobile skill-mentions serialize.

**Ledger coverage gaps** (found by Cluster 2, NOT in docs/customizations.md):
- `571b38122` clearFilters fix (fork-only)
- `37d0a1e15` mobile skill mentions serialize (fork-only, sibling of #5346 but on mobile side)
- `a55c95c4c` type-scale refactor (fork-only, 12 files)

**~~Highest-probability real conflict~~ → REFUTED**: Cluster 2 声称 `floating-chat.tsx` 是最高概率冲突（fork `748059058` FAB-reserve vs upstream `be20af6ed` #6492)。**主会话交叉验证推翻**:`748059058` 改的是 `issue-detail.tsx`(pin-comment 容器 `pr-14 sm:pr-8`),`floating-chat.tsx` 三个 pairwise diff 全 0 行、不在 merge-tree changed_in_both。Cluster 2 把 ledger "mobile chat-FAB" 定制错安到了 `floating-chat.tsx` 头上。真正的高概率冲突仍在 **`issue-detail.tsx`**(11 hunks，已在 C 策略表)。

**Cluster 2 精确度警告**:`41 ghost cherry-picks byte-identical` 也**不完全准** — 抽查 `agent-detail-page.tsx`(3 行 diff)、`agent-overview-pane.tsx`(1 行 diff）均非 byte-identical；只有 `access-picker.tsx` 确认 byte-identical。故 "41 byte-identical" 应读作 "41 个内容等价/近似等价的 ghost cherry-picks",Phase 3 用 `git checkout v0.4.20 -- <file>` 清理时需逐个确认，不能盲目批量。

**PR live state**: All 9 PRs verified — no flips between ledger and `gh`. #5393 MERGED 2026-07-15 (6 fork leading commits are ghost cherry-picks, content-equal to v0.4.20).

**Per-PR strategy table** (24 rows, in Cluster 2 report §5) — most fork hot files are **D (keep fork)** because upstream v0.4.20 doesn't touch them; only **C (careful-merge)** on shared files like client.ts, issue-detail.tsx, comment-input.tsx, comment-card.tsx, floating-chat.tsx.

### Cross-verification of Cluster 1 Risk 1+2 (Strategy D 双向签名碰撞)

**Risk 1 — `server/internal/integrations/channel/engine/router.go`**: cross-verified via `git grep -nE 'func.*enqueueMedia|func.*resolveAndBindMedia|func.*createIssue' server/internal/integrations/channel/engine/router.go` + same on v0.4.18 + v0.4.20.

- fork HEAD `enqueueMedia` L462: **7 参** (set, inst, identity, chatMessageID, msg, sessionID, deadline)
- upstream v0.4.20 L473: **9 参** (insert `issue db.Issue, issueTaskID pgtype.UUID` before deadline)
- `resolveAndBindMedia`: 7→9 参 (same shape)
- `createIssue`: 7→8 参 (add `assignedRunFireAt time.Time` at end)
- fork v0.4.18 baseline: same 7-param as HEAD
- **Phase 5 must run `go build -C server ./...`** — 3 call sites will fail with "not enough arguments" or "too many arguments"

**Risk 2 — `server/internal/service/task.go`**: same cross-verification.

- fork HEAD `enqueueIssueTask` L980: **7 参** (issue, triggerCommentID, forceFreshSession, handoffNote, actorUserID, rerunOfTaskID)
- upstream v0.4.20 L1066: **8 参** (add `fireAt pgtype.Timestamptz` at end)
- `enqueueIssueTaskWithCommentPlan`: 8→9 参 (same shape)
- fork v0.4.18 baseline: same 7-param as HEAD
- **fork HEAD call sites at L922 + L932**: still 7-param — go build will fail
- **Phase 5 must run `go build -C server ./...`** — will fail with "not enough arguments" at L922 / L932; fix is to append `pgtype.Timestamptz{}` to both call sites

**MUL-5442 pre-implement cross-verified**:
- `git diff v0.4.18..HEAD --shortstat -- server/internal/daemon/execenv/` → `15 files changed, 1027 insertions(+), 205 deletions(-)` (Cluster 1 报 920 — undercount; actual 1027)
- The 1027 lines were introduced by `c03402a94` v0.4.19 fake-merge (which claim "merge" but is 1-parent linear commit per §Cluster 1 cross-verify)
- v0.4.19 plan's prediction "fork 0 改 daemon 文件" was **WRONG** — the fake-merge actually pre-implemented MUL-5442 stage 1 from upstream's v0.4.20 (which was the future, so a partial forward-merge)

### Cluster 3 audit summary (B-group frontend + locale depth + mobile — focused restart)

**Headline: 绝大多数 merge-tree "冲突" 是假象。** Cluster 3 用逐键/逐blob 3-way 分析把 merge-tree 的 85-hunk/37-file 恐慌收敛为**极少数真实冲突**。

**两个被推翻的预测(主会话已复核 blob-hash / 逐键 diff,全部成立):**
1. `provider-logo.tsx` + `mobile/timeline-list.tsx` **byte-identical HEAD↔v0.4.20** — fake-merge staging 早已带入 upstream blob,fork 定制已不存在 → **A accept-upstream no-op**。
2. `zh-Hans/ja issues.json` **逐键 DIVERGE=0** — fork 的 `Issues→任务` 术语定制与 upstream 官方翻译逐字收敛,merge-tree 高 hunk 是双侧同步同一翻译的假象 → **A accept-upstream**。

**5 个 fork-only 前端文件 upstream 全 0 改动、无 rename、无 native mention 系统**(`--diff-filter=A | grep -i mention` 空):mention-view / mention-hover-card / rich-content / runtime-local-skill-import-panel / mobile markdown.tsx + mention-serialize.ts → **D keep-fork 自动合并**。`mention-suggestion.tsx` upstream 唯一改动已收敛。

**真实冲突只剩两类:**
- **`issue-detail.tsx`(HIGH)** — 唯一真正的硬文件。upstream 新增 `ImageSequenceProvider` wrapper(0→4 refs)+ subscribe UI 重写(MUL-5710/5714);fork 4 个 disjoint 定制全保留(identifier-badge / archived-dim / pin-comment / FAB-reserve)。**⚠️ Phase 4 架构决策点**:upstream `useStickyComposer` sticky-in-column vs fork 独立 `shrink-0 border-t pr-14` div 是两套 pin-comment 机制。Cluster 3 建议**采用 upstream 结构 + 重挂 fork FAB-reserve**;Cluster 1 倾向 keep-fork。**留待 Phase 4 看实际 hunk 定**。
- **7 个 locale 文件的少数 rewrite key** — onboarding(zh-Hans 3 / ja 2 / ko 1)+ settings.json(3 语言各 1,`@mention`-clause **语义**分歧:upstream 删掉了 "@你" 从句,fork 保留并做了术语化)+ runtimes.json(近平凡引号风格)。onboarding 里 upstream 删除的 key 直接接受删除(fork 对被删步骤的翻译无意义);rewrite key 保留 upstream 新句式 + 保留 fork 的 issue→任务/イシュー→タスク/이슈→태스크 术语。

**Mobile MUL-5752 lightbox 自动带入安全**:`MarkdownImage` 导出签名跨版本稳定,fork `markdown.tsx` 调用方不受影响。

## Phase 2 — Confirm gates (DONE 2026-08-07)

User confirmed both irreversible gates via blocking question:
1. **DB migration** — confirmed: pg17 `pg_dump` fresh backup FIRST (`v0.4.20-pre-migrate-<ts>.dump`, ~55-60MB), then `migrate up` applies only 257-260 (DB already at 256/v0.4.19 schema). Rollback target = this fresh dump.
2. **Service restart** — confirmed: `install.sh` full rebuild + restart (launchd backend :8081 + frontend :3001). Accepted ~30-40s backend warm-up window. Follow-ups after install.sh: manual backend ldflags re-stamp (install.sh stamps prior tag pre-merge-commit), `multica daemon restart` (CLI 0.4.20 vs daemon 0.4.19 drift), `/api/config` health check.

## Phase 3 — Reversible preview (DONE 2026-08-07)

- Safety branch: `main-backup-v0.4.20` @ `c06fd6583` (post Phase 1+2 commits).
- `git merge v0.4.20 --no-commit --no-ff` → **42 files conflicted (diff-filter=U), 39 with real `<<<<<<<` markers** + 3 modify/delete (onboarding templates).
- **Predicted vs actual**: merge-tree said 37 files / 85 hunks; actual merge = 42 files. Delta: onboarding templates (3) surfaced as modify/delete conflicts (merge-tree had classed them "removed in remote"), and `thread-nav-panel.tsx` has **8 hunks** (merge-tree under-reported it — wasn't in its top-conflict list). `issue-detail.tsx` merge-tree said 11, actual = **3**.
- **Confirmed Cluster 3 reversals held**: `provider-logo.tsx` + `timeline-list.tsx` NOT in conflict set (byte-identical auto-merged). But `zh-Hans/ja/ko issues.json` ARE conflicted despite per-key DIVERGE=0 — proves "semantic equivalence ≠ git textual auto-merge"; same-hunk overlap still conflicts.

## Phase 4 — Resolve + re-apply (in progress 2026-08-07)

**Decision point 1 — onboarding templates (USER-CONFIRMED, follow upstream Mika):**
- `create-agent-guide-issue.ts` / `helper-instructions.ts` / `helper-starter-prompts.ts` (3 orphan files): upstream #6378 Mika rewrite deleted them; merged `index.ts` imports only `mika` + `install-runtime-issue` → these 3 became unreferenced orphans. Fork's only change to them was MUL-5703 terminology translation (issue→任务/태스크/タスク) — real work, but its product form (pre-written markdown guide issues) was retired by upstream. **Resolved: `git rm` all 3 (accept upstream deletion).** Corrected my earlier misread of "6+/6- = format noise" — it WAS real translation, but on a retired product surface.
- `install-runtime-issue.ts` (3 hunks): careful-merge. The only true conflicts were each locale's "step 7": fork's old manual "create agent + assign task" vs upstream's new "open Runtimes → Start with Mika → guided first chat". Took **upstream's Mika step 7 for all 3 locales** (file is already Mika-paradigm: title "和 Mika 开始", en step 7, closing Mika paragraph). Fork's MUL-5703 terminology (任务/태스크/タスク) preserved in steps 2/3 and elsewhere outside the conflict hunks.
- (ko locale had a typo during manual edit — fixed via sed line-delete; final verified clean, no markers.)

**Remaining groups**: server Strategy D (router.go, runtime_config_sections.go, router_test.go, reasonix/qwenpaw/models/kimi, execenv_test.go), client.ts + realtime test, landing i18n×4, apps/web/package.json, thread-nav-panel.tsx (8 hunks), issue-detail.tsx (3 hunks — pin-comment dual-mechanism decision), comment-input.tsx (1), comment-composers.test.tsx (1), welcome-after-onboarding.test.tsx (1).

**Decision point 2 — locale JSONs (16 files): CORRECTED mid-flight.**
- Initial user-approved plan was "全部 accept upstream" based on evidence that upstream's zh/ja/ko translations had converged to fork's MUL-5703 terminology (任务/タスク/태스크) AND used full-width punctuation matching fork's own `conventions.zh.mdx:293` rule. ja `lede` was byte-identical to fork.
- **BUT** adversarial verification (systematic `flat(fork)-flat(now)` diff scan) caught that `git checkout --theirs` wiped **324 fork-only keys** — translation keys for fork's ACTIVE features with no upstream counterpart: `mention.skill_*` (#5346 skill-mention), `archived.*` + `status.archived` (#6106), `detail.identifier_*`/`prop_*`/`section_token_usage` (identifier-badge + token-usage), `execution_log.view_run` (#5309), `help.backend_*`/`cli_*` (#5539 version-stamp feature), `members.invite_name_*` (#4118), `comment.trigger_source_*`, `gantt.show_closed`. Deleting these = UI falls back to raw key names for live fork features.
- **Resolution**: restored ALL 324 fork-only keys from `git show HEAD:<file>` via a deep-merge script that ADDS missing keys but preserves upstream values for shared keys. Result = union: upstream's normalized wording for shared keys + fork's feature keys. Some restored keys are dead (e.g. `runtime_aside.*`, `step_header.*`, `detail.section_token_usage`/`prop_*` — upstream retired those surfaces; confirmed 0 code references in Phase 5 and DELETED them there). But `welcome_after_onboarding.*` is NOT dead — fork's own `welcome-after-onboarding.tsx` Mika page is live and references it (a fork path separate from upstream #6378); those keys are required. A missing live key is far worse than an extra dead one, so the restore stayed add-only and the dead-key pruning happened deliberately in Phase 5 with code-reference proof.
- **Verified**: 0 still-missing fork-only keys, 0 invalid JSON, 0 remaining locale conflicts.
- **LESSON (for future upgrades)**: `--theirs` whole-file overwrite is unsafe for locale files because fork adds feature keys upstream lacks. Always pair with a fork-only-key restore + a `flat(fork)-flat(now)` loss scan.

## Phase 5 — Verify code gates (COMPLETE 2026-08-07 — all green)

| Check | Command | Result |
|---|---|---|
| install | `pnpm install` | ✅ |
| typecheck | `pnpm typecheck` | ✅ 6/6 |
| go build | `go build -C server ./...` | ✅ |
| go vet | `go vet -C server ./...` | ✅ |
| sqlc | `~/go/bin/sqlc generate` (not in PATH) | ✅ EXIT=0 |
| TS tests | `pnpm test` | ✅ 3754/3754 (5/5 tasks) |
| build | `pnpm build` | ✅ 3/3 |

**Go integration tests not run** (host has no isolated DB; per skill, risk accepted — `go build`/`go vet` green + audit confirmed backend customizations zero-conflict).

### Phase 5 fallout fixed (2 test-failure classes, 14 failures → 0)

**(A) `issue-detail.test.tsx` — 5 upstream tests failing.** Root cause was NOT the composer/sticky/subscribe structure they assert. All 5 share a `waitFor(getByText("Implement authentication"))` probe that threw `Found multiple elements`: my base-method reconstruction stitched the fork's `IssueIdentifierBadge` + standalone title span into the breadcrumb `leaf`, creating a SECOND bare-title text node that collided with the H1's `{issue.title}`. Fork main had this same duplication but no upstream test exercised uniqueness; upstream v0.4.20's new tests (single `getByText`) depend on the invariant "bare title text is globally unique" (upstream's own leaf is `{identifier} {title}`, prefixed, so only the H1 is bare).
- **Resolution (USER-CONFIRMED "badge + 合并文本")**: breadcrumb `leaf` restored to upstream's merged `{issue.identifier} {issue.title}` span (single text node → uniqueness probe resolves to H1); the fork's tappable `IssueIdentifierBadge` (status icon + copy-to-clipboard) **relocated** to a row above the title in the body — fork interaction value kept, position moved off the uniqueness-constrained node.
- Fork's own test `renders the issue title leaf as a link…` asserted badge-in-leaf; updated it to the new contract (leaf = merged text link; badge present above title via `getByRole("button", { name: /TES-1/ })`).
- **LESSON**: a fork customization is an invariant SET across code + tests + every locale. 3-way merge can't catch fork-only-code × upstream-only-test collisions; only running upstream's new tests surfaces them. And before stitching a "fork customization" during base-method rebuild, verify it exists in merge-base/fork-main — don't trust intermediate rebuild state.

**(B) `locales/parity.test.ts` — 9 failures ("EN covers every ja/ko/zh-Hans key" × issues/onboarding/layout).** Phase 4 restored 324 fork-only keys to ja/ko/zh-Hans but not to EN. Enumerated the exact missing-EN set: **80 keys, all sourced** (78 fork-only with English values in fork-main `en` — token-usage panel, runtime_aside, step_header, welcome_after_onboarding Mika cards; 2 upstream: `gantt.show_completed`, `help.server_version`). Deep-merged them into EN (add-only). parity 166/166.

**(B-follow-up) dead-key cleanup (USER-CONFIRMED "删 dead key,用 upstream execution-log").** Investigation surfaced that fork's "Token usage sidebar panel" (`section_token_usage`/`prop_cache`/`prop_cache_value`/`prop_runs`/`prop_input`/`prop_output`) was actually **upstream's OLD code that upstream v0.4.20 deliberately deleted** (commit ba129b196 MUL-5762: "Removes the standalone Token usage sidebar section… every field it had is in the [execution-log] dialog"). My reconstruction had NOT stitched it — which correctly matches upstream's direction. So instead of keeping those keys: deleted the 6 dead keys from all 4 locales; also deleted dead `runtime_aside` + `step_header` (runtime-aside-panel.tsx deleted upstream) from all 4 onboarding locales. **Kept** `execution_log.view_run` (live in comment-card.tsx) and all 72 `welcome_after_onboarding.*` (fork's own live welcome-after-onboarding.tsx Mika page — a separate fork path from upstream #6378). Re-ran: parity 166/166, onboarding/welcome 75/75, typecheck 6/6, full test 5/5.

## Phase 6 — Irreversible gates

TBD.

## Phase 7 — Record

TBD.
