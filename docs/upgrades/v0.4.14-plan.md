---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.14
base_tag: v0.4.13
merge_commit: 70b39883d

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: done

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true
  audit_strategy_written: true
  db_backed_up: true
  migrated: true
  version_stamped_frontend: true
  version_stamped_backend: true
  services_restarted: true
  health_checked: true

# Negative claims the audit made — each confirmed/refuted by one raw command, not by reading.
negative_claims:
  - claim: "v0.4.14 新增迁移 233/234，fork 最高 232 → 无新撞号；6 已知碰撞（119/158/159/160/213/214）不受影响"
    verified_by: "`git diff --name-only v0.4.13..v0.4.14 -- server/migrations/` → 仅 233/234 各 up/down；fork max=232；233/234 > 232"
    result: confirmed
  - claim: "merge-tree 实测真实文本冲突 = 4 文件（comment.go ×2 块 / table-view.tsx / auth.go / multica-mentioning SKILL.md）；drafts cluster、mention-suggestion、router、task.go 等 auto-merge clean"
    verified_by: "`git merge-tree --write-tree --merge-base=v0.4.13 HEAD v0.4.14` → exit 1, tree `2061bc761`；stdout `<mode> <oid> <stage> <path>` 列出 4 冲突文件 3-stage；stderr `CONFLICT (content)` 4 行 + `Auto-merging`（无冲突）若干"
    result: confirmed
  - claim: "task.go duplicate-symbol 语义冲突（merge-tree 抓不到）：v0.4.14 task.go 含 truncateFallbackCommentBody(cap=8000,L140+调用L2901)，fork HEAD task.go 不含（v0.4.13 移出），fork fallback_comment_truncate.go(cap=200) 为 sole def；三方 ours-delete vs theirs-keep → go build 裁决是否 redeclared"
    verified_by: "`git show v0.4.14:server/internal/service/task.go | grep -nE 'func truncateFallbackCommentBody|maxSynthesizedFallbackCommentRunes'` → L129 const=8000 + L140 func + L2901 调用；`git show HEAD:...task.go | grep …` → 空；`git show HEAD:...fallback_comment_truncate.go | grep …` → L9 const=200 + L16 func"
    result: confirmed
    action: "Phase 5 go build 裁决：若 redeclared → Phase 4 删 fork fallback_comment_truncate.go 取 upstream task.go（沿用 v0.4.13 Phase 4 决策：cap 8000 接受）；若 fork task.go 合并后仍无符号（ours-delete 胜）→ 保持现状"
  - claim: "comment.go 冲突 = fork 与 upstream 独立修复同一 malformed-id panic（非真抢占）：fork ours = `if m.ID==\"all\"{continue}` + ParseUUID + 静默 continue；upstream theirs = ParseUUID + blockTarget(Reason)。采纳 upstream 更完整版"
    verified_by: "`git show 2061bc761:server/internal/handler/comment.go` 冲突块 L2786-2811(squad)/L2872-2888(agent) 对比；ours 侧止于 `if err != nil {`（err 分支=共同 continue，无 blockTarget），theirs 侧含 `blockTarget(\"squad\",m.ID,ReasonTargetUnavailable)` / `blockTarget(\"agent\",m.ID,ReasonInvocationNotAllowed)`；`git show 45e24e356 -- comment.go` = MUL-5411 完整语义"
    result: confirmed
    action: "Strategy A：取 upstream blockTarget 版；fork `if m.ID==\"all\"` 防护视 @all mention type 是否进入 squad/agent 分支（Phase 4 验，大概率删——MUL-5411 重排后 type 检查已排除 @all）；Phase 5 fork @all 测试 + upstream 新 malformed-id 测试验证"
  - claim: "编辑态 mention 面（mention-view/content-editor/comment-input/reply-input/comment-card/issue-detail/skill-agent-picker）未被 v0.4.14 触及"
    verified_by: "`git diff --name-only v0.4.13..v0.4.14` ∩ ledger mention cluster 热表 → 0 交集（仅 mention-suggestion.tsx 被碰，auto-merge clean）"
    result: confirmed
  - claim: "UI 基础样式（globals.css web+desktop / tokens.css）fork 未定制 → type scale revert 后存活的字体改动（Inter italic/Geist Mono）纯 upstream 无冲突"
    verified_by: "`git diff v0.4.13..HEAD -- apps/web/app/globals.css apps/desktop/src/renderer/src/globals.css packages/ui/styles/tokens.css` → 三者均空（fork 未定制）；v0.4.14 对三者有 M 净变更但 ours 无定制 → auto-merge 无冲突面"
    result: confirmed
  - claim: "fork 未在 v0.4.13 之后新增定制化 PR 的 merge/close → 无 MERGED→drop local 策略翻转"
    verified_by: "`gh pr list --repo multica-ai/multica --author chouti --state all` → 与 ledger 一致（#5393 MERGED 2026-07-15 旧；#5346/#5309/#5199/#5160/#4118/#2669/#2460/#6106 OPEN；v0.4.13..v0.4.14 期间无新 merge/close）"
    result: confirmed
---

# Upgrade v0.4.13 → v0.4.14

> Process artifact. Persistent customization list in `docs/customizations.md`.
> 34 upstream commits (v0.4.13..v0.4.14), published 2026-07-29 10:18 UTC.

## Phase 0 — Locate

- base: `v0.4.13` (HEAD `v0.4.13-254-g96634f780`)
- target: `v0.4.14` (Latest, published 2026-07-29 10:18 UTC; non-prerelease, non-draft)
- to-merge: **34 commits** (v0.4.13..v0.4.14), 232 文件
- vs origin/main: 39 behind / 254 ahead（按惯例 merge v0.4.14 tag，不 merge origin/main）
- working tree: clean

## Phase 1 — Deep audit ✅ strategy written

### 主题（34 commits）
daemon/agent 会话修复（retire sessions `30318b79b`、pin session id `b3847b417`、discover CLIs `07a78d910`、scan comment roots `e45a8f6c1`）、editor proxy-mode 图片/附件签名（`06a100d61`/`6d98b5eeb`/`bdae0d2a0`）、UI 字体（type scale `d68d636c9` 被 `545afea82` revert；Inter italic/Geist Mono `4f7048003` 存活）、**comment @all/@agent 归并修复 MUL-5411 `45e24e356`**、issue Table 列交互修复 `e70e71fea`、avatar signed endpoint `607209de7`。

### Per-file strategy

| 文件 | strategy | 文本冲突 | 依据 |
|---|---|---|---|
| **comment.go**（squad+agent 分支，2 块） | **A（采纳 upstream blockTarget）** | 2 content | fork 与 upstream 独立修复同一 malformed-id panic；ours 静默 continue，theirs `blockTarget`+Reason（更完整）。fork `if m.ID=="all"` 防护视 @all type（Phase 4 验，大概率删）|
| **task.go** | **A（go build 裁决）** | 0 text（语义） | v0.4.14 task.go 含 truncate 符号(cap=8000)，fork HEAD 不含，fork `fallback_comment_truncate.go`(cap=200) sole-def；ours-delete vs theirs-keep，go build 裁决 |
| **table-view.tsx** | **C（careful merge）** | 1 content | fork archived status 显示（#6106，3 轻改）∩ upstream `e70e71fea` Table 列大改（18 hunk）；保留 fork archived + 整合 upstream |
| **auth.go** | **B/C（union）** | 1 content | fork UserResponse/issueJWT/GetMe 定制 ∩ upstream 同函数；字段 union；issueJWT fork 独占 |
| **multica-mentioning/SKILL.md** | **B（fork 为底）** | 1 content | fork 大幅重写 @skill gesture 文档 ∩ upstream no-op 小补；fork 为底 + 补 upstream 新 no-op |
| comment-draft-store.ts（auto-merge） | C（typecheck 验） | 0 | v0.4.14 改 import+pruneStaleDrafts，与 fork setDraft/writeDraft 不同区；Phase 5 验语义 |
| mention-suggestion.tsx（auto-merge） | C（typecheck 验） | 0 | v0.4.14 改 createMentionSuggestion，与 fork skill mention 不同区 |
| router.go（auto-merge） | C | 0 | fork provenance 1 行 + upstream avatar 路由不同区 |
| agent_runtime_skills.go（auto-merge） | A | 0 | fork 未定制，v0.4.14 改 1 行 trivial |
| 迁移 233/234 | A（adopt） | 0 | 新号无撞号，纯 CONCURRENTLY 索引，低风险 |

### Audit prediction（Phase 3 对照）
- 预测真实文本冲突 **4 文件**（comment.go/table-view/auth.go/SKILL.md）+ task.go 语义
- 对比 v0.4.13（6 冲突全 drafts cluster）：本次 drafts cluster **auto-merge clean**，主战场转移至 comment.go mention trigger 解析
- 风险等级：**低-中**（comment.go 是唯一语义密集点，且是「重复修复」非「真冲突」；migration 仅纯索引）

## Phase 2 — Confirm irreversible gates ✅
- blocking question：DB migrate 233/234（纯 CONCURRENTLY 索引）+ launchd 重启
- 用户批准全程执行 → `user_confirmed: true`

## Phase 3 — Reversible preview ✅
- safety branch: `main-backup-v0.4.14`
- `git merge v0.4.14 --no-commit --no-ff`（merge tag，非 origin/main）
- 实际 vs 预测：预测 4 content + task.go 语义；**实际 3 手动 + 1 rerere**（table-view.tsx 被 rerere 自动 staged — ledger SOP Step 4 警告的 stale 风险，已验证保留 fork `formatProgressText` + `list-row` re-export 正确）
- 手动冲突：auth.go / comment.go(2 块) / SKILL.md

## Phase 4 — Resolve + re-apply ✅
- **comment.go(2 块)**:union — 保留 fork `if m.ID=="all"`（@all 合法广播静默 skip）+ 采纳 upstream `blockTarget`+Reason（malformed id 记录 blocked）。fork/upstream 独立修复同一 panic 漏洞
- **auth.go**:`h.userToResponse(user)` + 保留 fork `IsSuperAdmin`（upstream 自由函数→方法）
- **SKILL.md**:keep both — upstream malformed-id 句 + fork @skill undesignated 段落
- **table-view.tsx**:接受 rerere（`list-row` re-export `child-progress` 符号，双 import 有效）
- **admin.go（auto-merge 语义修复）**:fork #4118 定制 `userToResponse(updated)` → `h.userToResponse(updated)`（upstream 重构波及 fork 定制，go build 抓到）
- task.go duplicate-symbol:未复发（三方合并采纳 fork 无符号版，`fallback_comment_truncate.go` 仍 sole def）

## Phase 5 — Verify ✅ 五项全绿 → advance phase: gates
| 步骤 | exit | 备注 |
|---|---|---|
| pnpm install | 0 | 7.2s，lockfile 无大变化 |
| pnpm typecheck | 0 | 6/6 tasks，50.8s |
| go build -C server ./... | 0 | 修 admin.go 后通过 |
| pnpm test | 0 | 5/5，views 282 文件/3314 passed（v0.4.13 为 3270）|
| pnpm build | 0 | 3/3，59.6s |

## Phase 6 — Irreversible gates ✅
GATE: phase==gates ✓ / user_confirmed ✓ / audit_strategy_written ✓ / negative_claims 全 verified ✓
- ✅ pg_dump 备份（pg17）：`~/multica-backups/v0.4.14-pre-migrate-20260730-095202.dump`（41M）
- ✅ migrate up：应用 233/234，其余 skip（213/214 duplicate-prefix 正常共存）
- ✅ 版本 re-stamp：**先 commit merge `70b39883d`**（git describe→v0.4.14）→ install.sh rebuild（前端 .env v0.4.14 + 后端 -ldflags v0.4.14），规避 v0.4.13 的 install.sh-stamps-上一版 陷阱
- ✅ launchd 重启：install.sh reload com.fengzhao.multica-{frontend,backend}
- ✅ health check：backend /healthz 200、frontend / 200、/api/config server_version=v0.4.14

## Phase 7 — Record ✅
- merge commit `70b39883d`（含每冲突策略，SOP Step 10 格式）
- ledger `docs/customizations.md`：Last upgrade → v0.4.13→v0.4.14，base 推进 v0.4.14
- 本 plan 关闭（phase: done，全 gates true）
- docs commit（plan + ledger）

## Actuals vs Plan（审计预测对照，校准未来审计）
- 预测：4 content 冲突（comment.go×2/table-view/auth.go/SKILL.md）+ task.go 语义
- 实际：**3 手动**（auth.go/comment.go×2/SKILL.md）+ **1 rerere**（table-view.tsx 被 rerere 自动 staged，已验证非 stale）+ **1 auto-merge 语义错误**（admin.go userToResponse→method，go build 抓）+ task.go 语义**未复发**
- 校准：merge-tree 预测的 table-view 被 rerere 吸收（少 1 手动），但冒出 merge-tree 看不见的 admin.go（upstream API 重构波及 fork 定制）。**文本冲突预测准；语义冲突仍只有 go build 能抓**——与 v0.4.13 结论一致。rerere 介入是新增变量（本次良性，但未来需逐文件验证）。
