---
upgrade_contract: selfhost-upgrade/v1
target_tag: v0.4.16
base_tag: v0.4.15
merge_commit:

# Single-value state machine: audit → preview → resolve → verify → gates → record → done
phase: preview

# Irreversible gates — every one must be true before Phase 6 fires. Flipped as each completes.
gates:
  user_confirmed: true
  audit_strategy_written: true
  db_backed_up: false
  migrated: false
  version_stamped_frontend: false
  version_stamped_backend: false
  services_restarted: false
  health_checked: false

# Negative claims the audit made — each confirmed/refuted by one raw command, not by reading.
# Populated after fan-out audit agents report; every entry must carry verified_by + result.
negative_claims:
  - claim: "v0.4.15..v0.4.16 新增迁移 235-250 全部 > fork max(234) → 无新撞号；6 已知碰撞（119/158/159/160/213/214）不受影响；本次 DB migrate 应用 16 个（非 no-op）"
    verified_by: "`git diff --name-status v0.4.15..v0.4.16 -- server/migrations/` → 全 A(新增) 235-250；`git ls-tree --name-only {HEAD,v0.4.15,v0.4.16} server/migrations/ | awk -F_ '{print $1}' | sort -n | tail` → HEAD/v0.4.15 max=234，v0.4.16 max=250"
    result: confirmed
  - claim: "6 已知撞号 stem（119/158/159/160/213/214）在 v0.4.16 树里无第三个文件新增"
    verified_by: "`git ls-tree --name-only v0.4.16 server/migrations/ | awk -F_ '$1==119||$1==158||$1==159||$1==160||$1==213||$1==214'` → 空输出"
    result: confirmed
  - claim: "merge-tree 实测真实文本冲突 = 6 文件，全部 content 类型，无 modify/delete / add/add / rename"
    verified_by: "`git merge-tree --write-tree --merge-base=v0.4.15 HEAD v0.4.16` → tree `f64936acd`，stdout grep CONFLICT = 6 行全 content；`grep -cE 'CONFLICT \\((modify/delete|add/add|rename)'` → 0"
    result: confirmed
  - claim: "merge-base(HEAD, v0.4.16) == v0.4.15（fork main 是 v0.4.15 的直系后代，merge-base 干净）"
    verified_by: "`git merge-base HEAD v0.4.16` → `a9a4a3d63` == v0.4.15 tag commit（a9a4a3d63 是 ledger 记录的 v0.4.15 顶部上游 commit）"
    result: confirmed
  - claim: "PR #5199（actor mention avatar chips）由 OPEN 翻转为 CLOSED（upstream Bohan-J 2026-07-31 关闭，未 merged）→ 账本分类从 Carry 变 Own-forever；fork 代码仍在 leading commits"
    verified_by: "`gh api repos/multica-ai/multica/issues/5199/events` → closed by Bohan-J；`gh pr view 5199` → state=CLOSED mergedAt=-"
    result: confirmed
  - claim: "comment.go 的 fork skill-bind 区（bindDesignatedSkillsForTriggers/triggerTasksForComment）upstream 完全未触碰 → MT 对 comment.go 判 Auto-merging 无 CONFLICT"
    verified_by: "`git diff v0.4.15..v0.4.16 -- server/internal/handler/comment.go | grep -iE 'bindDesignatedSkills|triggerTasksForComment|designatedSkill'` → 空（主会话独立复核）"
    result: confirmed
  - claim: "task.go duplicate-symbol 不复发：truncateFallbackCommentBody 在 MT 树 task.go 已剔除、唯一存在于 fork 的 fallback_comment_truncate.go（cap=200≠upstream 8000，保 fork 值）"
    verified_by: "`git grep -c func truncateFallbackCommentBody <MT> -- task.go` → 0；`-- fallback_comment_truncate.go` → 1（主会话独立复核）"
    result: confirmed
  - claim: "新增索引迁移 238/241/244/245/246/247/248 全部 CREATE INDEX CONCURRENTLY 且单语句单文件，符合仓库硬规则（migration runner 事务外执行安全）"
    verified_by: "for n in 238..248: `git show v0.4.16:<n>_*.up.sql | grep -oiE 'CREATE (UNIQUE )?INDEX CONCURRENTLY'` → 7 个全命中（主会话独立复核）"
    result: confirmed
  - claim: "auth.go / admin.go upstream 未碰；router.go/handler.go 双方碰但 hunks 不重叠（fork ServerVersion 在 router.go 1039 区）→ MT 干净 auto-merge"
    verified_by: "任务B: `git diff --name-only v0.4.15..v0.4.16 -- auth.go admin.go` → 空；MT 对 router.go/handler.go 无 CONFLICT 行"
    result: confirmed
  - claim: "workspace_delete.sql（MUL-5562）无新增外键/级联——应用层自底向上清理（set_config 关 dirty trigger + advisory lock + 显式 CTE），符合 fork 规则"
    verified_by: "任务B: `git show v0.4.16:server/pkg/db/queries/workspace_delete.sql` 头注释 application-owned；grep FOREIGN KEY/REFERENCES/CASCADE 仅命中注释（指既有 FK 非新增）"
    result: confirmed
  # ---- 任务 A（mention 集群）结论，主会话已独立复核 VA1-VA4 ----
  - claim: "rich-content.tsx 冲突是签名重命名（C）：upstream MUL-5499 把 AutolinkedIssueMentionLink 改名 IdentifierIssueMentionLink+加 fallback 参数，冲突块 ours 仍引用已删除的旧名 → 取 theirs 新组件但保 fork mentionId 变量"
    verified_by: "主会话: `git show <MT>:rich-content.tsx | grep Identifier/Autolinked` → L138 定义为 IdentifierIssueMentionLink、L224 ours 块引用已删的 AutolinkedIssueMentionLink"
    result: confirmed
  - claim: "issues-page.tsx 冲突是空态重构选边（C）+ 语义丢失风险：取 ours 的 IssuesEmptyState 组件后，必须手动 re-apply upstream MUL-5580 的 icon-tone 修复（text-muted-foreground/40→text-faint-foreground）——文本冲突检测不到"
    verified_by: "主会话: `git show <MT>:issues-page.tsx` → L113 IssuesEmptyState 内 icon 仍 text-muted-foreground/40，L88 upstream inline 空态已 text-faint-foreground"
    result: confirmed
  - claim: "comment-input.tsx 冲突是 editor 双 prop 并列（B）：fork skillMentionContext + upstream quickActionMenu 是同 editor 上两正交 prop，content-editor.tsx 在 MT 同时声明两 props（L157/212）无冲突"
    verified_by: "任务A: `git show <MT>:content-editor.tsx | grep -n quickActionMenu/skillMentionContext` → 两 props 并存；`grep -cn <<<<<<<` = 0"
    result: confirmed
  - claim: "client.ts 冲突是 import 字母序并列（B）：fork EMPTY_BATCH_IMPORT_RESPONSE + upstream EMPTY_CHAT_MESSAGE_LIST，方法体零重叠完全正交（fork admin*/skill/archived 13 处 ∩ upstream quick-actions 6 处无方法名重叠）"
    verified_by: "任务A: `git show <MT>:client.ts | sed -n 205,235p`；fork/upstream diff 方法名交集为空"
    result: confirmed
  - claim: "upstream 未碰 fork mention 热文件（mention-hover-card/actor-mention-chip/skill-mention-chip/skill-agent-picker/skill-mention-context）→ 全 fork-only 且在 MT 存活"
    verified_by: "主会话: `git cat-file -e v0.4.16:<4 files>` 全失败（fork-only）；`git cat-file -e <MT>:<4 files>` 全 OK（存活）"
    result: confirmed
  - claim: "upstream Quick Actions 与 fork @skill gesture 正交（不同数据源/UI入口/API），非 upstream 重构 fork 依赖结构；fork skill-agent-picker 调用点在 MT 存活"
    verified_by: "任务A: `git show <MT>:comment-input.tsx | grep skillMentionContext` → L293；content-editor L982 Provider"
    result: confirmed
  - claim: "【纠偏】ServerVersion build-provenance 透传不在 client.ts（任务背景描述有误）——HEAD/MT/v0.4.16 三版 client.ts 均 grep 不到 ServerVersion/provenance"
    verified_by: "任务A: `git show {HEAD,MT,v0.4.16}:client.ts | grep ServerVersion|provenance` → 三处均空"
    result: refuted
    action: "Phase 7 校账本/后续任务背景：provenance 前端透传实际在 schema/config 层（api/schemas.ts 的 ServerVersion 字段），不在 client.ts"
  - claim: "【watch-item】upstream 改了 mention 热文件 comment-trigger-chips.tsx（删 3 处 opacity-60 视觉态），fork 也改但 MT 自动合并（0 冲突）——合并后人工核上游删 suppressed 灰态是否与 fork chip 视觉逻辑运行时交互"
    verified_by: "任务A: `git show <MT>:comment-trigger-chips.tsx | grep -cn <<<<<<<` = 0"
    result: confirmed
  # ---- 任务 C（主题盲区）结论，主会话独立复核 VC/R 系列 ----
  - claim: "MUL-5452 是 design-system 透明度重构（152 处 text-muted-foreground/30-80→实色 token），自带守卫测试 text-contrast.test.ts（替换 muted-foreground-contrast.test.ts）+ 新增 --faint-foreground token，均在 MT 存活兜底"
    verified_by: "主会话 R2: `git ls-tree -r --name-only <MT> | grep contrast` → apps/web/app/text-contrast.test.ts 在 MT；v0.4.15 是 muted-foreground-contrast.test.ts（被替换）；`git show <MT>:tokens.css | grep faint-foreground` → L41/182"
    result: confirmed
  - claim: "fork 独有热文件（9 个 chip/badge/picker/tab）零 guarded-color 透明度违规（text-(muted-foreground|foreground|sidebar-foreground|destructive)/N），不会被 text-contrast.test.ts 抓；runtime-local-skill-import-panel:364 的 text-caption opacity-85 安全（caption 是字号 token 非 guarded 颜色）"
    verified_by: "主会话 VC3: 对 9 文件 `git show HEAD:<f> | grep -cE 'text-(muted-foreground|foreground|sidebar-foreground|destructive)/[0-9]'` → 全 0"
    result: confirmed
  - claim: "ActorAvatar 高 drift 本轮未发生：fork mention chip 用 packages/ui/components/common/actor-avatar.tsx，本轮 upstream v0.4.16 和 fork 都没改它（两边 diff 空）；upstream 改的是另一文件 views/common/actor-avatar.tsx 且 props 签名无变化"
    verified_by: "主会话 R1: `git diff v0.4.15..v0.4.16 -- ui/components/common/actor-avatar.tsx` 空 + `git diff v0.4.15..HEAD -- 同` 空；该文件 upstream 自有（v0.4.14 起，MUL-4016 ebd0248be），非 fork-only——任务C称 fork-only 为表述偏差，但「本轮静态无 drift」结论成立"
    result: confirmed
  - claim: "fork 零触碰 server/pkg/agent/ 与 label 代码 → agent runtime 大改（Qoder CN/codebuddy ACP/Copilot Win/daemon Linux）+ label 系统移除（MUL-5600/5563）对 fork 完全无冲突"
    verified_by: "任务C: `git diff v0.4.15..HEAD --stat -- server/pkg/agent/` 空；`git diff v0.4.15..HEAD --stat | grep -iE label` 空"
    result: confirmed
  - claim: "workspace.go 双方碰但函数级零重叠：fork 改 generateIssuePrefix/CreateWorkspace/ListMembersWithUser（行 20/183/441），upstream MUL-5562 改 DeleteWorkspace（行 794）→ 不同函数 auto-merge 干净"
    verified_by: "任务C: fork/upstream workspace.go diff 行区比对；MT 对 workspace.go 无 CONFLICT"
    result: confirmed
  - claim: "fork 全部 locale key 在 MT 存活（identifier-badge、invite_name_*、整个 admin.json）；upstream 未碰 fork invite 区"
    verified_by: "任务C: `git show <MT>:zh-Hans/issues.json | grep identifier` 存活；`git diff v0.4.15..v0.4.16 -- zh-Hans/settings.json | grep invite` 空"
    result: confirmed
  - claim: "【中风险 watch】issue-detail.tsx 与 comment.go 同文件双改但行区实测不重叠：comment.go fork 改触发器(996-2334) vs upstream 改列表(274-975) 零交叠；issue-detail fork 597-2775 vs upstream 1461/2617。auto-merge 干净但由 typecheck + issue-detail.test.tsx + comment Go 测试兜底语义"
    verified_by: "任务C: fork/upstream 行区比对；MT 对两文件均 Auto-merging 无 CONFLICT"
    result: confirmed
---

# Upgrade v0.4.15 → v0.4.16

> Process artifact. Persistent customization list in `docs/customizations.md`.
> 40 upstream commits (v0.4.15..v0.4.16), published 2026-07-31 10:18 UTC.

## Phase 0 — Locate

- base: `v0.4.15` (HEAD `v0.4.15-260-g6e155c3ba`)
- target: `v0.4.16` (Latest, published 2026-07-31 10:18 UTC; non-prerelease, non-draft)
- to-merge: **40 commits** (v0.4.15..v0.4.16), 476 files changed (+25295/-3119)
- vs origin/main: 43 behind / 260 ahead（按惯例 merge v0.4.16 tag，不 merge origin/main）
- merge-base(HEAD, v0.4.16) = `a9a4a3d63` = v0.4.15 tag commit（干净）
- working tree: **dirty**（M CONCEPTS.md + 12 个定制文件 + 3 untracked docs/solutions — merge 前需 stash，见 Phase 3）
- **本次有 DB 迁移**（235-250，16 个新迁移，非 no-op）——与 v0.4.15 的零迁移不同，Phase 6 migrate 会真实应用。

## Phase 1 — Deep audit（进行中）

### 主题（40 commits）
- **quick actions（本轮最大特性，含 6 个迁移 235-240）**：server-side LLM 生成（MUL-5573 `f13969b99` refactor + `33a2743f9` GPT-5.6 兼容 + `0a54485ab` gpt-5.6-luna 默认）、agent chat quick actions（MUL-5149 `0fdc38704`）、Issue Quick Actions sidebar（MUL-5465 `5e3b7a8c3`）
- **labels 系统移除/收敛**：remove agent labels（MUL-5600 `3cab03cc3`）、always enable resource labels（MUL-5563 `51f44873c`）
- **rich-content / issues**：bare in-app URL→chip（MUL-5499 `d4ae220cc`）、timeline cap 修（MUL-5492 `c3f5df8bf`）、filtered empty-state icon solid tone（MUL-5580 `b530bbad5`）、notify human for sub-issues（MUL-5483 `28b6105ed`）、count agents in surface（MUL-5525 `13b06f038`）、unresolved mention target 不再误报 permission（MUL-5548 `f73250654`）
- **agent runtime 大改**：Qoder CN CLI（MUL-5581 `e4b6f7a31`）、codebuddy ACP discovery（MUL-5549 `75c11db04`/`44ce16d9b`）、drain ACP notifications（`cee985592`）、Copilot Windows native binary（MUL-5586 `cd9b95626`）、daemon 移除 Linux Codex HOME / danger-full-access（MUL-5578 `98766cc06`）、agent avatar emoji（MUL-5534 `d359ce7b9`）、drop runtime badge（MUL-5567 `32ab1e77d`）、show runtime backing agent（`ba43b11ae`）
- **design-system**：replace text-transparency ladder with solid tones（MUL-5452 `9c90327ce`）★ 类 type-scale 重构，fork 独有文件可能需跟随
- **skills**：brief skill list names-only index（MUL-5529 `abdfd3e28` + `519927878`）、trim 8 built-in skill descriptions（MUL-5546 `a4bca74c6`）
- **workspace/deletion**：optimize application-owned workspace deletion（MUL-5562 `f48bd655b`，新增 workspace_delete.sql 539 行）
- **subscribers**：notify delegated（249）+ opt-out scope（250）迁移
- **self-host**：one source of truth for backend host port（MUL-5505 `cac453fe0`）★ 直接关系本 fork
- **license**：rename Multica License + Apache 2.0 verbatim + NOTICE（MUL-5558 多个 commit）
- **realtime**：bound inbound WebSocket message size（MUL-5569 `f4bf8e2c3`）
- **inbox**：mark unread from row context menu（MUL-5496 `f0110da55`）
- **codex**：raise first-turn no-progress ceiling 60s（MUL-5542 `99d8f29bd`）

### merge-tree 实测冲突（6 文件，全 content）
| 文件 | 冲突类型 | 涉及定制 |
|---|---|---|
| `packages/core/api/client.ts` | content | build provenance（#5539 ServerVersion）∩ upstream quick-actions API |
| `packages/views/issues/components/comment-input.tsx` | content | mention 集群 @skill gesture（#5346）∩ upstream Issue Quick Actions（MUL-5465） |
| `packages/views/issues/components/issues-page.tsx` | content | archived status 空态重构（#6106）∩ upstream MUL-5580 empty-state icon |
| `packages/views/rich-content/rich-content.tsx` | content | mention 集群 readonly chips（#5199/#5346）∩ upstream MUL-5499 bare-URL-chip |
| `server/internal/service/builtin_skills/multica-mentioning/SKILL.md` | content | @skill gesture 契约（#5346）∩ upstream skill-brief/desc 重构（MUL-5529/5546） |
| `server/internal/service/builtin_skills/multica-mentioning/references/mentioning-source-map.md` | content | 同上 |

### Per-file strategy

| 文件 | strategy | 冲突块 | 依据 + 动作 |
|---|---|---|---|
| `packages/views/rich-content/rich-content.tsx` | **C** | 1 | upstream MUL-5499 把 `AutolinkedIssueMentionLink` 改名 `IdentifierIssueMentionLink`+加 `fallback` 参数；冲突块 ours 仍引用已删旧名。**取 theirs 新组件签名** `<IdentifierIssueMentionLink identifier={mentionId} fallback={mentionId} />` 但**保 fork 的 `mentionId` 变量**（ours 的 `match[2]` 已被 fork 重构掉）。fork 的 actor/skill chip 分支、4 import、upstream 的 unfurlableEntityLink/ProjectMentionCard 已 auto-merge 存活，原样保留 |
| `packages/views/issues/components/comment-input.tsx` | **B keep-both** | 1 | 冲突块是同 editor JSX 上两正交 prop：fork `skillMentionContext={{...}}` + upstream `quickActionMenu={quickActionMenu}`。**两个都保留**（并列）。content-editor.tsx 已 auto-merge 同时声明两 props（L157/212）走不相交路径 |
| `packages/views/issues/components/issues-page.tsx` | **C（取 ours + 1 行 re-apply）** | 1 | fork archived 空态重构（renderEmpty→IssuesEmptyState）vs upstream 对旧 inline 空态的 MUL-5580 icon-tone 改动。**取 ours 的 IssuesEmptyState 结构**，再**手动 re-apply**：把组件内 `ListTodo` icon 从 `text-muted-foreground/40`（MT L113）改为 `text-faint-foreground`。`workingAgents` prop 链已 auto-merge 存活 |
| `packages/core/api/client.ts` | **B keep-both** | 1 | import 区字母序相邻：fork `EMPTY_BATCH_IMPORT_RESPONSE` + upstream `EMPTY_CHAT_MESSAGE_LIST`。**两个都保留**（BATCH 在 CHAT 前）。方法体零重叠（fork admin*/skill/archived 13 处 ∩ upstream quick-actions 6 处正交） |
| `server/internal/service/builtin_skills/multica-mentioning/SKILL.md` | **B（upstream 骨架 + fork 增量回植）** | 2（frontmatter desc + no-op 段） | upstream MUL-5529/5546 重写 description + "What does NOT happen" 三分法（blocked/coalesced/silent + reason_code + MUL-5548/3963 语义）。**采 upstream 新版骨架**，**回植 fork @skill gesture**：Step 3 整段（upstream 无）、"undesignated @skill = 真 silent no-op" 一条、类型组 `skill|project`（以 fork ValidMentionTypes 实现为准） |
| `server/internal/service/builtin_skills/multica-mentioning/references/mentioning-source-map.md` | **B（upstream 骨架 + fork 段保留 + 重钉行号）** | 1（顶部 regex/type-group 区） | 顶部冲突采 upstream "Guards and outcomes" 3 列新表，但**保 fork 的 `skill|project` regex 行**（fork 已把 project 加进 ValidMentionTypes，与 upstream "project 不解析"矛盾——文档以 fork 实现为准）。fork 的 "@skill mention routing" 整节+"Explicit non-claim" 节保留，**按 CLAUDE.md 硬规则重钉全部 `comment.go:NNNN` 行号**（upstream 插 QuickAction 字段致行号漂移） |
| mention-hover-card / actor-mention-chip / skill-mention-chip / skill-agent-picker / skill-mention-context（auto-merge） | A（保 fork pure-local） | 0 | upstream 全未碰（fork-only），MT 存活 |
| content-editor.tsx / comment-card.tsx / comment-trigger-chips.tsx / mention-suggestion.tsx（auto-merge） | A | 0 | 双方碰但行区不重叠，MT 无冲突。comment-trigger-chips 有 watch-item（upstream 删 opacity-60，人工核视觉交互） |
| issue-detail.tsx / comment.go / task.go / router.go / handler.go / workspace.go（auto-merge） | A | 0 | 同文件双改但行区/函数级不重叠；task.go duplicate-symbol 已确认不复发；typecheck + 测试兜底 |
| ui/components/common/actor-avatar.tsx（auto-merge） | A（upstream 静态） | 0 | fork chip 依赖，本轮双方都没改，无 drift |
| 迁移 235-250（新增） | — | 0 | 16 个新迁移全 > fork max 234，无撞号，索引全 CONCURRENTLY 单语句，lint map 零维护。**DB migrate 应用 16 个（非 no-op）** |

### 后端结论（任务 B，主会话已复核）
- **DB migrate 应用 16 个新迁移（235-250），非 no-op**，但零撞号、索引全并发单语句、lint map 零维护。6 已知撞号（119/158/159/160/213/214）不受影响。
- **后端零文本冲突**：comment.go/task.go/router.go/handler.go/workspace.go 全 auto-merge 干净；后端仅 2 个 builtin_skills 文档冲突。
- **task.go duplicate-symbol 不复发**：truncateFallbackCommentBody 全局唯一于 fork 的 fallback_comment_truncate.go（cap=200）。
- **workspace_delete 无新增外键/级联**（应用层清理，符合 fork 规则）。

### 两个同 PR 强制动作（CLAUDE.md 硬规则，Phase 4 必须做）
1. **mentioning-source-map.md**：合并后按 fork 实现**重钉 fork @skill 段全部 `comment.go:NNNN` 行号**（upstream 插 QuickAction 字段 + isClientAuthorableCommentType 致行号漂移）。
2. **SKILL.md + source-map 顶部**：协调 "upstream 说 project 不解析 vs fork 已把 project 加进 ValidMentionTypes" 的实现差异——文档以 fork 实际 regex 为准（project parses but render-only）。

### Audit prediction（Phase 3 对照）
- 预测真实文本冲突 **6 文件 / 6 块**（merge-tree 已实测，无 modify/delete/add/add）：
  - **4 前端**：rich-content(C 签名重命名)、comment-input(B 双 prop)、issues-page(C 选边+1行 re-apply)、client.ts(B import 并列)
  - **2 后端文档**：SKILL.md(B 骨架+回植)、mentioning-source-map.md(B 骨架+重钉行号)
- **rerere 可能吸收部分**（v0.4.14/v0.4.15 均吸收 2 个）——实际手动数 ≤6。
- **需要"写代码"而非"选边"的**：issues-page 的 1 行 icon-tone re-apply、source-map 的行号重钉、SKILL.md 的 @skill 段回植。其余是选边/并列。
- **语义盲区（auto-merge 干净但需守卫/测试兜底）**：① MUL-5452 透明度重构→text-contrast.test.ts 兜底（实测 fork 零违规）；② issue-detail.tsx/comment.go 同文件双改→typecheck+测试兜底；③ comment-trigger-chips.tsx upstream 删 opacity-60→人工核视觉交互。
- **风险等级：低-中**。冲突数（6）比 v0.4.15（8 手动+2 rerere）少，且无 modify/delete/add/add 这类结构冲突；主战场是 mention 集群的 2 个文档 + 1 个签名重命名，比 v0.4.15 的 type-scale 横扫更聚焦。真正的细心活在 2 个 builtin_skills 文档的「upstream 骨架 + fork 增量回植 + 行号重钉」。
- **与 v0.4.15 对比**：主战场从「type-scale 机械改名 ∩ views 定制」转回「mention 集群语义合并」，且**新增 DB 迁移面**（v0.4.15 零迁移，本轮 16 个）。

## Phase 2 — Confirm irreversible gates（待）
## Phase 3 — Reversible preview（待）
## Phase 4 — Resolve + re-apply（待）
## Phase 5 — Verify（待）
## Phase 6 — Irreversible gates（待）
## Phase 7 — Record（待）
