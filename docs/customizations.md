---
title: "Local customization ledger — tracking divergent commits and upstream PR status"
date: 2026-07-17
last_updated: 2026-08-19
category: "workflow-issues"
module: "git"
problem_type: "workflow_issue"
component: "development_workflow"
severity: "high"
applies_when:
  - "Self-hosted Multica instance tracks upstream and accumulates local customization commits"
  - "One or more customizations were submitted as upstream PRs that are unmerged or rejected"
  - "Before merging a new upstream release, to decide per-customization merge actions"
tags: [local-customizations, upstream-pr, ledger, merge-strategy, self-hosted, drift]
---

# Local customization ledger

> **This is a living document.** Update it whenever you open/close an upstream PR, land a new local customization, or complete an upstream merge. It is the per-customization companion to the merge workflow in `docs/solutions/workflow-issues/safe-upstream-upgrade-with-local-customizations.md` (which covers the *mechanics* of one upgrade; this file tracks *what* you are carrying across all upgrades).

*Audit 2026-08-19:* **✅ v0.4.28 → v0.4.29 upgrade EXECUTED 2026-08-19** (merge commit `895839b9a`, TRUE 2-parent; parents `e8364fb2b` fork HEAD + `c670e0549` v0.4.29 tag commit). Clean merge-base `38c992ad0` (= v0.4.28^{commit}), **18 upstream commits / 196 files (+9198/-814)**。Drivers: **MUL-6243 follow-up（board 按 category 取数 + catalog 状态归档退休）**、MUL-6063 委托失败恢复、MUL-5651 网络分区保任务、desktop Cmd+, 设置快捷键 reland、MiniMax Code ACP（迁移 342）、lark 独立文件/音频入站、NUL-byte 修复。**2 冲突 == merge-tree 预测（第 10 连中）**，均在前端震中。**核心成果：archived compose-and-keep 在 server 端零重放存活**（upstream category 机器 keyed on `IsBuiltIn`/`IsCategory` 且 `IsCategory=IsBuiltIn`，fork 补丁传递得 `status_category:"archived"` 自动下发）；**前端 Option 1（用户拍板：教前端第 8 类 archived）被 auto-merge 吸收大半** —— union 改名 × fork archived 成员干净组合，`BUILT_IN=Set(STATUS_ORDER)` 派生接通三个 category 函数，唯一手工轴 = `fetchFirstPages` 的 `include_archived`（server 端 AND 叠加已证实必须）+ 测试钉 7→8。**Phase 5 首次全零 fork-code × upstream-test 碰撞**（v0.4.23 后最干净：typecheck 6/6 仅 1 处 import 窄化修复、pnpm test 4438 全绿、go build/vet 干净、sqlc 零漂移、go test 失败集与 backup baseline 集合同一 + 2 个时序 flake watch items）。**2 新迁移 342/343** 全 > fork max 341 零撞号；6 已知撞号完好；DB migrated 2（latest=343，archived 267 行 + catalog 35 行完好）；install.sh commit-first 正确（`server_version=v0.4.29` 首探即 200）；daemon 留 0.4.28（tap 未发 0.4.29，agents 5 + workspaces 5 完好，tap 发版后 `brew upgrade multica && multica daemon restart`）。Per-file strategy + verified negative claims: `docs/upgrades/v0.4.29-plan.md` (see "Last upgrade").

## Why this ledger exists

The instance leads upstream `main` by a growing set of commits. Each carries a different merge risk on the next upgrade, and the correct action differs by upstream PR state. Without a ledger, an upgrade resolves every conflict blind — keeping both implementations when upstream adopted a PR differently, or re-porting a fix upstream already shipped. Read the **Action on next merge** column before touching conflicts.

## Status legend

| Status | Meaning | Merge-cost trajectory |
| --- | --- | --- |
| `MERGED` | Upstream shipped the PR. Local commits are now ghosts. | **Eliminate** — take upstream verbatim, drop local version. |
| `OPEN` | Awaiting upstream review. Expect drift; conflicts likely recur. | **Carry** — re-resolve each upgrade; rerere helps. |
| `CLOSED` (by upstream) | Upstream rejected. Code still lives in your commits; will never auto-resolve. | **Own forever** — heaviest burden; minimize file invasiveness. |
| `WITHDRAWN` (by you) | Self-closed, and local code reverted / never merged into `main`. | **None** — no local footprint, no conflict risk. Historical only. |
| `LOCAL` | Never submitted upstream (by choice). | **Own forever** — same as CLOSED. |

> **Verify before classifying.** A `CLOSED` PR is not automatically a burden — check *who* closed it (`gh api repos/multica-ai/multica/issues/<n>/events --jq '.[] | select(.event=="closed") | .actor.login'`) and *whether its code is actually in your leading commits*. Author-closed + reverted = `WITHDRAWN` (no cost); upstream-closed + code present = `CLOSED` (own forever). Misclassifying a withdrawn PR as a live burden inflates your perceived merge cost.

## Active customizations

Ordered by merge risk (highest first). *Files* lists upstream-owned paths most likely to drift — these drive future conflicts. Run the audit in the next section to refresh PR states before each upgrade.

### Mention rendering — the high-conflict cluster

Two open PRs both modify the core mention render path (`packages/views/editor/extensions/mention-view.tsx`, `mention-hover-card.tsx`, `readonly-content.tsx`). Any upstream mention refactor conflicts with **both at once**. Treat this file as the single hottest spot in the repo.

| Feature | PR | Status | Key upstream files | Action on next merge |
| --- | --- | --- | --- | --- |
| MentionType registry + skill mention | [#5346](https://github.com/multica-ai/multica/pull/5346) | OPEN (no review, 3 comments; PR last updated 2026-07-14) [OWNS FORK PATCH PER v0.4.17] | `editor/extensions/mention-suggestion.tsx`, `editor/extensions/mention-view.tsx`, `handler/skill_mention_trigger*.go`, `handler/comment.go`, `handler/handler.go`, `issues/components/comment-trigger-chips.tsx`, `builtin_skills/multica-mentioning/*` | Carry; watch for upstream mention refactors. **Local now leads this PR** — the 7-17 composer-gesture redesign (below) is NOT in the PR. See the @skill gesture note below. **v0.4.17 (2026-08-04) fork 9 参 workaround 决议:** upstream b06af2ae1 (MUL-5559) 收敛了 fork 的 `skillMentionAgents` 路径 — upstream 8 参 `triggerTasksForComment` + `bindAndEnqueueSkillMentions`/`parseSkillMentionAgents`/`skill_mention_trigger.go` 全部消失。用户决定保留 fork 9 参 workaround(Phase 2)。merge 后 fork 9 参路径自动存活,无需 go-build 修复。**含义**: #5346 PR server-side 部分与 upstream 永久分裂,后续每轮升级都需要 fork 9 参 workaround 步骤合并。Upstream MUL-5559 的 `isAgentRuntimeBound` + `ReasonAgentRuntimeRequired` 路径已与 fork skill 路径正交共存(skill chip 用 fork 路径,runtime check 用 upstream 路径)。 |
| Actor mention avatar chips | [#5199](https://github.com/multica-ai/multica/pull/5199) | **CLOSED 2026-07-31** (upstream Bohan-J, not merged) | `editor/extensions/mention-view.tsx`, `editor/mention-hover-card.tsx`, `readonly-content.tsx`→`rich-content/rich-content.tsx`, `ui/components/common/actor-mention-chip.tsx` (fork-only) | **Own forever** (moved to the Rejected/local-only table — upstream closed without merging; fork actor-chip code lives on). Same hot file as #5346 — resolve together. v0.4.16: survives in `rich-content.tsx` post-MUL-5499 (upstream renamed `AutolinkedIssueMentionLink`→`IdentifierIssueMentionLink`; fork chip dispatch untouched). |

> **@skill mention — composer-gesture redesign (EXECUTED 2026-07-17).** Plan: `docs/plans/2026-07-17-001-feat-skill-mention-agent-gesture-plan.md`. The binding-table reverse-lookup was replaced by an **explicit composer gesture**: a `@skill` chip carries a "pick an agent" popover (`packages/views/editor/skill-agent-picker.tsx`, feat `e62cbb40f` U3); on submit the backend durably binds the skill to the designated agent (`AddAgentSkill`, idempotent single-SQL upsert — TOCTOU-closed `fd759a62a`) and enqueues it, so references/scripts ride the normal bound-skill pipeline. `skill_mention_agents` is now the sole skill-routing input, widened to `skill_id → [agent_id]` and capped per-skill + total at the request boundary (`db1fcbf27`). Bind-on-submit is symmetric across CreateComment and UpdateComment edit paths (`a8a799f9e`), draft-persisted across reloads (`65a1ad4d2`), and surfaced in the trigger-preview strip (`3cee62fd2`); `@skill` contract documented in `builtin_skills/multica-mentioning/SKILL.md` (feat `c4ed11891` U4). **No schema migration** — only `server/pkg/db/queries/skill.sql` (sqlc) + generated `skill.sql.go` changed. **Verified hot-file surface (wider than the original plan predicted):** `handler/comment.go`, `handler/handler.go`, `editor/extensions/mention-view.tsx`, `editor/content-editor.tsx`, `issues/components/comment-{input,card,trigger-chips}.tsx`, `reply-input.tsx`, `core/issues/stores/comment-draft-store.ts`, plus new files `editor/skill-agent-picker.tsx`, `editor/skill-mention-context.ts`, `ui/components/common/skill-mention-chip.tsx`, `issues/hooks/use-skill-designated-preview-agents.ts`, `core/issues/comment-trigger-outcomes.ts`. (Plan had listed `use-issue-timeline.ts` + `core/api/client.ts`; neither was touched.) When upstream merges #5346, re-apply only the gesture + bind-on-submit logic on top of upstream's mention render path.

> **@skill mention — bind-run decoupling (EXECUTED 2026-07-24).** Plan: `docs/plans/2026-07-24-001-fix-skill-mention-bind-decouple-plan.md`. Reverses review finding #4 ("bind-without-run weapon"): `bindDesignatedSkillsForTriggers` no longer filters on `source == mention_skill`, so an agent the implicit path already selected (and which the R5 dedup therefore kept in place of the skill duplicate) is STILL bound to the designated skill — bind is decoupled from which trigger source runs the agent (CONCEPTS.md "Skill Mention Gesture"). Triggered by YUP-407: a `@skill` designation on an already-active conversation agent silently failed to bind, because dedup rewrote the trigger's source to implicit and the bind then skipped it. Frontend trigger-preview (`use-comment-trigger-preview.ts`) now surfaces the designation row when an agent is both implicit and skill-designated, so the chip reflects "will run carrying the skill" (R5). Hot files: `handler/comment.go` (`bindDesignatedSkillsForTriggers`, `triggerTasksForComment` comments), `handler/skill_mention_trigger_test.go` (renamed `_NoBindWithoutRun` → `_BindsDespiteDedup`, assertion reversed 0→1; added `_SuppressedDesignatedAgentStillBound`), `issues/hooks/use-comment-trigger-preview.ts`, `issues/hooks/use-skill-designated-preview-agents.ts`. **R6 (forced-skill prompt callout) deferred** — needs a cross-layer handler→task→daemon→prompt signal for the designated skill; tracked in the plan's Scope Boundaries.

> **@skill mention — auto-open + recommended default (EXECUTED 2026-08-18).** Plan: `docs/plans/2026-08-18-1759-feat-skill-auto-bind-popover-plan.md` (branch `feat/skill-auto-bind-popover`). Flips the 7-17 gesture from opt-in to **defaulted**: a `@skill` chip typed through the @-menu auto-opens the agent picker without stealing focus (`6e75b4b9d` U3 — popover `modal=false`/`initialFocus=false`, Tab/↓ enters the list, Esc stops propagation per the MUL-5429 lesson, first editor keystroke closes without intercepting; auto-open gated on the workspace agent list having settled non-empty), and the recommendation is auto-applied as the default designation — **dismiss = accept** (`704870c53` U4). Ranking runs client-side on the UNMERGED backend preview rows (`c1186ebdc` U2, `packages/core/issues/skill-mention-recommendation.ts`): explicit `@agent` mention > reply-parent > conversation-continuation > issue assignee; `use-comment-trigger-preview.ts` now exposes `backendAgents` + `resolved` so the skill-designation merge that erases backend source can't poison the ranking. Guards (KTD5): typed-insertion record (paste/undo/hydrated chips never auto-fill), touched set (explicit select/clear never machine-overridden, survives chip removal), fast-path fills upgrade to the async answer exactly once (null removes the entry), suppress clears non-touched machine entries, and the submit terminal fill reads refs + live doc (cold-start race, OQ3); guards persist with the draft (`CommentDraftPayload.touchedSkillIds`/`filledSkillIds`, value-compared reconcile for entry identity). Also fixes the reply-path drop: `CommentCard.onReply` forwards `skillMentionAgents` as the 5th argument (`7984bf691` U1). Shared engine `issues/hooks/use-skill-auto-bind.ts` wired into both composers; composer scope-reset became mount-skip so draft hydration of designations + guards actually survives virtualization remounts. aria-live announcements in 4 locales. **No server change** — the 9-参 workaround surface is untouched; issue-description editor and comment edit mode deferred in the plan's Scope Boundaries. Manual smoke on self-host is the plan's remaining Verification Contract gate. **Review-fix wave (2026-08-19, `348347c50`, ce-code-review run `20260819-091418-058d4c91` — all 8 findings applied):** KTD5 amended per user decision — the one-shot upgrade window now also covers **async-origin fills**, gated on strict tier outrank (same/lower-tier answers stay sticky, no identity flicker; the recommendation exposes its `tier` via new `recommendSkillMentionAgentWithTier` + `skillMentionSourcePriority` in `packages/core/issues/skill-mention-recommendation.ts`), so an `@agent` mention typed after the chip still wins the skill (R5). Also: `/note` drafts never machine-fill or terminal-fill (manual picker gestures still land; un-noting re-opens the fill — note flag snapshotted in doc-sync); the composer popover slot reconciles with the doc (a slot whose chips all disappear clears — Cmd+Z case; the auto-open in-flight tail drops a held request whose chip was deleted before the agent list settled); picker rows located via `data-agent-row` (not raw `<button>` queries) in both keyboard walkers; dead tri-state `replyParentAgentId` param removed from `useRecommendedSkillAgent`; `multica-mentioning/SKILL.md` Step 3 provenance now covers dismiss=accept (source-map mirror-checked, no change); zh-Hans live-region keys now use the glossary term 智能体. **Shipped 2026-08-19:** merged to main (`43267d383`), self-host redeployed via install.sh (version stamped post-merge), and the manual a–e smoke passed on production — the plan's Verification Contract is fully closed. Go full-suite recheck the same day: 11 failing tests across 5 packages, set-identical to the main baseline (all ledger-documented residuals, zero new; refines the 2026-08-18 audit's "6 residuals" wording, which counted ledger residual entries rather than failing test functions).

> **@skill mention / autopilot authority — known pre-existing test failures (RESIDUAL, 2026-07-24).** `go test ./server/internal/handler/` on self-host reports 7 pre-existing failures unrelated to finding #4: `TestAutopilotDelegationAuthority_LineageBinding`, `TestClaimTask_ManualRetryReusesWorkdir`, `TestCreateComment_AutopilotLeaderMentionEnqueuesPrivateWorker`, `TestCreateComment_AutopilotWorkerResultWakesSquadLeader`, `TestEnqueueSkillMention_NoDesignationIgnoresAssignee`, `TestReconcileCommentsOnCompletion_AutopilotDelegationRestoresAuthority`, `TestUpdateComment_AutopilotAuthorityReStampedToEditingTask`. Verified **not** a v0.4.9 regression (same fail list in v0.4.8 worktree). Most likely cause: tests assume self-host's production-DB-like seed history that the fresh-migrated test DB (`:5432`) doesn't carry — failure path centers on `autopilotDelegationAuthority` lineage lookup via `comment.source_task_id`. **NoDesignationIgnoresAssignee** also reflects the R4 silent-not-suppressive design (see `comment.go:2260-2264`) clashing with a stale test expectation. Documented in plan as **Pre-existing Test Failures (Residual)**. Not in scope for any current plan; treat as separate bug if pursued.

### Open PRs — feature additions

| Feature | PR | Status | Key upstream files | Action on next merge |
| --- | --- | --- | --- | --- |
| Run-comment "View run" affordance | [#5309](https://github.com/multica-ai/multica/pull/5309) | OPEN (no review) | `handler/comment.go`, `migrations/158-160_backfill_comment_source_task_id*`, comments UI | Carry. Upstream `comment.go` is **untouched** in v0.4.3..v0.4.6 — zero conflict this round. The 158/159/160 backfills **collide with upstream's own 158/159/160** — tolerated via the duplicate-prefix lint map, do NOT renumber (see Drift notes). |
| Member display name mgmt (invite + super-admin rename) | [#4118](https://github.com/multica-ai/multica/pull/4118) | OPEN (no review, stale since 06-23) | `cmd_admin.go`, `cmd_workspace.go`, `cmd/multica/main.go`, auth/invitation handlers | Carry; touches CLI + Go handlers — medium conflict rate. |
| skills.sh 2-segment batch import | [#2669](https://github.com/multica-ai/multica/pull/2669) | OPEN (3 reviews — only PR with upstream engagement) | `handler/skill*.go`, `cmd/server/router.go`, `cmd_skill.go`, `core/api/client.ts` | Carry; most likely to merge eventually — re-check first. |
| Adaptive skill discovery (import dialog) | [#5160](https://github.com/multica-ai/multica/pull/5160) | OPEN (no review) | `skills/components/runtime-local-skill-import-panel.tsx`, `locales/*/skills.json` | Carry. **Ledger correction (v0.4.15 plan, refuted in audit):** earlier claim that "fork never implemented `data-branch` so its tests fail pre-existing" is stale — HEAD `runtime-local-skill-import-panel.tsx:1115` renders `data-branch={isSummaryMode ? "summary" : "search"}` and tests are consistent. If tests fail, check the search-input-placeholder-in-summary-mode path instead. |
| Add `archived` issue status | [#6106](https://github.com/multica-ai/multica/pull/6106) | OPEN (supersedes #4931); **upstream landed adjacent system MUL-6243 in v0.4.28 (PR #7065 MERGED) — fork now composes with it, see below** | `migrations/235+236_issue_status_{archived,classifier_functions}` (fork was 213/214, renumbered in PR), `handler/issue*.go`, `issueguard/issue_status.go` (new), `pkg/db/queries/{issue,inbox,project}.sql`, **v0.4.28+ compose surface**: `issuestatus/issuestatus.go` fork patch (Archived const + IsBuiltIn/ActiveKeys), `handler/issue.go` append + filter tail, composed predicates (github.go/issue_child_done.go + SQL guards), ChildIssueProgress archived column, 4 re-pinned tests, `core/types/issue.ts`, `core/issues/config/status.ts`, `views/issues/components/{status-icon,issues-page,issues-header,issue-detail,table-view}.tsx`, `views/editor/extensions/mention-suggestion.tsx`, `locales/{en,zh-Hans,ja,ko}/issues.json`, **mobile** `components/{ui/status-icon,inbox/detail-label}.tsx` + `lib/{issue-status,format-activity}.ts` | Carry + **replay the 5-point compose surface every upgrade** (auto memory `project_archived_status_compose_keep`; full pattern: `workflow-issues/fork-archived-status-semantic-collision-compose-into-upstream-issuestatus.md`). PR branch `pr/upstream-archived-issue-status` is a clean 12-commit replay onto `origin/main` — see `docs/plans/2026-07-24-002-feat-archived-issue-status-plan.md` for the original design. Migration renumbered 213/214→235/236 on the PR branch; **local fork still uses 213/214** (do NOT renumber local — see Drift notes + `project_fork_213_214_migration_collision`). Also carries a `feat(mobile)` parity commit (4 `Record<IssueStatus>` maps). All 9 CI checks green on 2026-07-29. Supersedes upstream #4931 (Jonamora91, 0-review). On MERGED → no longer a simple drop: migration 337 (MUL-6243) dropped the constraint this PR re-creates, so a merge forces semantic reconciliation with the `issuestatus` catalog — the fork-local compose-and-keep is the reference shape; drop local 213/214 only as part of that reconciliation. |

### Adopted upstream — eliminate local version

| Feature | PR | Status | Key upstream files | Action on next merge |
| --- | --- | --- | --- | --- |
| Agents list access-scope column/filter/bulk-edit | [#5393](https://github.com/multica-ai/multica/pull/5393) | **MERGED 2026-07-15** | `agents/components/inspector/access-picker.tsx` (+21 files) | **Drop local commits** (forwardRef→onChange, draft-reset, bulk-dialog fixes are subsumed by upstream's `onChange`+`invocationTargets`). Take upstream verbatim per SOP Step 4.5. Verify no local follow-up still needs to land. |
| Self-hosted Git providers (Forgejo/Gitea/GitLab) alongside GitHub | [#5006](https://github.com/multica-ai/multica/pull/5006) (+ sister PRs) | **MERGED 2026-07-24** (Bohan-J; shipped v0.4.24) | upstream `vcs.go`/`vcs_webhook.go` cluster + `router.go` VCS routes + `settings.json` vcs keys (all identical to fork copies) | **Content converged — zero action needed.** Fork's `vcs*.go`/VCS routes/`vcs.*` locale keys are byte-identical to upstream since v0.4.24 (verified 2026-08-17 during v0.4.26 upgrade: `git diff <base>..HEAD -- vcs*.go` empty). No more carry burden; future merges treat VCS as pure upstream code. |

### Rejected / local-only — own forever

These will never auto-resolve. Keep them as non-invasive as possible; prefer new files over editing upstream files.

| Feature | PR | Status | Key upstream files | Action on next merge |
| --- | --- | --- | --- | --- |
| Runtime build provenance (official baseline in Help menu) | [#5539](https://github.com/multica-ai/multica/pull/5539) | **CLOSED 2026-07-16** | Own file `server/cmd/server/provenance_baseline.go` (zero conflict); `router.go` reduced to a single `ServerVersion:` field line (refactor `8aa05cfb2`); plus `api/client.ts` (2-line) + `help-launcher.tsx` | Own forever. Low-medium invasiveness: logic lives in a dedicated file, only 3 upstream files touched, hottest is `router.go` now down to one line. Recently rejected — confirm intent to keep maintaining locally. **Upgrade gotcha:** `main.version` defaults to `"dev"`; `officialBaseline` maps dev/describe-suffix → `""`, so a backend started without `-ldflags "-X main.version=<clean-tag>"` shows "version unavailable" in Help. Re-stamp on every upgrade (frontend `.env NEXT_PUBLIC_APP_VERSION` too). See memory `project-version-stamp-on-upgrade`. |

### Withdrawn — no local footprint (historical)

These PRs were closed **by the author** and their code was never merged into local `main` (or was fully reverted). They are **not** an ongoing merge burden — no conflict risk. Kept here only as history so a future attempt doesn't repeat a dead end.

| Feature | PR | What happened | Re-entry condition |
| --- | --- | --- | --- |
| @project typed mention + cross-workspace cascade | [#5466](https://github.com/multica-ai/multica/pull/5466) | Implemented end-to-end, then **self-closed by chouti 2026-07-15** as unsatisfactory; local changes fully reverted 2026-07-16 (verified: no `@project` / typed-mention trace in `packages/`, `server/`, `apps/`, or leading commits). | Do not resume without re-confirming the approach first (see project memory). |

### Pure-local customizations (never upstreamed)

| Feature | PR | Status | Key upstream files | Action on next merge |
| --- | --- | --- | --- | --- |
| Pin issue comment input to bottom of detail panel | — | LOCAL | `issues/components/issue-detail.tsx` | Own; single hot file — likely recurring conflict. |
| `ActorAvatar` `showName` prop | — | LOCAL | `ui/ActorAvatar.tsx` | Own; upstream refactored ActorAvatar before (v0.3.42) — high drift risk. |
| Issue prefix fallback to slug for non-latin workspace names | — | LOCAL | workspace/prefix logic | Own; small, low conflict. |
| i18n three-dot ellipsis in backend loading copy | — | LOCAL | backend loading copy | Own; trivial. |
| SkillProfileCard compact redesign + frontmatter | — | LOCAL | skills UI components | Own; overlaps #5160 area — resolve together. |
| Issue identifier badge + click-to-copy on detail page | — | LOCAL | `issues/components/issue-identifier-badge.tsx` (new file), `issues/components/issue-detail.tsx`, `locales/*/issues.json` | Own; **shares `issue-detail.tsx` with the pin-comment-input customization** — resolve together on conflicts. Commits `ecd309bb0` / `13a638be8` / `2e009277d`. |
| `truncateFallbackCommentBody` build-fix (fills symbol left dangling by upstream merge `24ea38dcb`; GH [#5455](https://github.com/multica-ai/multica/issues/5455), closed upstream 2026-07-16) | — | LOCAL | own new file `server/internal/service/fallback_comment_truncate.go` (24 lines, zero conflict) | Own; **duplicate-symbol risk** — if upstream later ships its own impl of `truncateFallbackCommentBody` / `maxSynthesizedFallbackCommentRunes`, this new file redeclares them and breaks the package build. On next merge, `grep` upstream for the symbol first; if defined, drop the local file and take upstream's. **Import-gap risk (materialized v0.4.12, 2026-07-28):** moving the symbol out of `task.go` also stripped `import "unicode/utf8"` from task.go (unused after the move). Upstream MUL-5268 later added a *similar but not same-named* `utf8.RuneCountInString` guard in task.go:4435 → merge broke `go build` (`undefined: utf8`), 0 text conflict — fixed by re-adding the import. **Lesson: when a local move strips an import, grep upstream's new additions in that file for the import's package symbols before assuming clean; `go build` is the only reliable catcher.** |
| Self-host production deploy (standalone frontend + launchd-supervised backend/frontend) | — | LOCAL | new dir `scripts/selfhost/` (5 files: `build-frontend.sh`, `run-backend.sh`, `install.sh`, `com.fengzhao.multica-{frontend,backend}.plist`) | Own forever; **pure addition, zero upstream conflict** (no upstream file touched). After upgrade: re-run `bash scripts/selfhost/install.sh` to rebuild the standalone frontend + `-ldflags`-stamped backend binary and reload the launchd jobs. **Gotcha:** the live plists (`~/Library/LaunchAgents/com.fengzhao.multica-*.plist`) and `/opt/homebrew/etc/Caddyfile` are host-level, outside this repo — back them up separately (dotfiles); `install.sh` re-deploys the plist templates but does not touch the Caddyfile. See `docs/solutions/runtime-errors/caddy-standalone-launchd.md`. Commits `890ac42d8` / `ae4d21bde`. |

## Pre-merge audit (run before every upstream merge)

Refresh this table, then apply the **Action** column. This is the ledger-driven version of SOP Step 4.5.

```bash
# 1. Refresh PR states
gh pr list --repo multica-ai/multica --author chouti --state all --limit 60 \
  --json number,title,state,mergedAt \
  --jq '.[] | "#\(.number)\t\(.state)\t\(.mergedAt // "-")\t\(.title)"'

# 2. For any newly-MERGED PR: take upstream's files verbatim, drop local version
gh pr view <num> --repo multica-ai/multica --json files --jq -r '.files[].path' \
  | xargs git checkout <tag> --

# 3. Confirm the local ghost commits are gone from the diff
git diff <tag>..HEAD -- <path>   # expect only deliberate deltas, not redundant fix-trail
```

## Drift notes & gotchas

- **Migration numbering — duplicate-prefix collisions are tolerated, do NOT renumber.** Local carries **6 known duplicate-prefix collisions** (each number has two unrelated files): `119` (`invitation_invitee_name` [#4118] vs upstream `user_created_at_index`), `158`/`159`/`160` (#5309 `backfill_comment_source_task_id*` vs upstream `agent_task_queue_chat_input_task_id` / `chat_message_message_kind` / `chat_message_input_owner_index`), **`213`/`214`** (fork archived-status `issue_status_archived` / `issue_status_classifier_functions` [FZG-340-343] vs upstream `task_usage_authoritative_cost` / `chat_session_project`). These are **accepted legacy state**, tolerated via the `duplicate-prefix migration map` fixture in `server/internal/migrations/migrations_lint_test.go`. **213/214 added 2026-07-27 (v0.4.11 upgrade, merge `185d446d8`):** fork landed archived-status migrations 213/214 ahead of the release; upstream v0.4.10/11 independently reached 213-223 and self-renumbered (commits `3f2e1c68d`, `be7c3521a`), landing on 213_task_usage/214_chat_session. The fork's `213_task_usage`/`214_chat_session`/`215`-`223` are **byte-identical** to upstream's (fork had cherry-picked those same upstream commits ahead of release — MUL-5228/3772, #5868/#5883), so they merged clean and live `schema_migrations` already matches final numbering. **Earlier fix 2026-07-21 (eval-found bug):** the map previously listed only up to `119`/`128`, so `TestMigrationNumericPrefixesStayUniqueAfterLegacySet` was **failing red** on `158`/`159`/`160` — the three were added to `legacyDuplicateMigrationStems` (their stems), test now passes. **Do not renumber** — the migrations are already applied and recorded in `schema_migrations`; renumbering breaks the version trail on a running instance. Maintain the lint map if upstream changes the migration lint mechanism. No DB foreign keys; concurrent indexes in single-statement files (repo hard rules). **v0.4.12 (2026-07-28):** upstream added **224-230** (lark media ingest MUL-4934 + daemon codex gate MUL-5305), all > fork max 223 → **no new collision**; the 6 known collisions (119/158/159/160/213/214) stand unchanged; DB migrated 7. **v0.4.13 (2026-07-29):** upstream added **231-232** (`agent_task_queue_terminal_completed_at_index` + `channel_media_pending_object_due_index`, both `CREATE INDEX CONCURRENTLY`), all > fork max 230 → **no new collision**; the 6 known stand; DB migrated 2. **v0.4.16 (2026-08-03):** upstream added **235-250** (16 migrations: quick-actions feature 235-240 incl. `quick_action`/`comment_quick_action`/`chat_message_quick_actions` tables + indexes 241/244/245/246/247/248 + workspace-teardown guard 243 + qoderclicn runtime 242 + subscriber delegated/opt-out 249/250), all > fork max 234 → **no new collision**; the 6 known stand; all indexes `CREATE INDEX CONCURRENTLY` single-statement; lint map zero maintenance; DB migrated 16. **v0.4.17 (2026-08-04):** upstream added **251_agent_runtime_unbind** (1 migration, MUL-5559: `agent.runtime_id` + `agent_task_queue.runtime_id` 改 nullable + CHECK NOT VALID invariant "ACTIVE task 必须有 runtime"; application-owned UPDATE, 无新 FK/级联), stem 251 > fork max 250 → **no new collision**; the 6 known collisions (119/158/159/160/213/214) stand unchanged; DB migrated 1 (non-no-op). **v0.4.18 (2026-08-05):** upstream added **252_agent_builder_draft** (1 migration, agent-builder draft 持久化), stem 252 > fork max 251 → **no new collision**; the 6 known stand unchanged; migrations 100/101/103/104 touched by docs commit `c1ec1f464` (MUL-5698 English default) but pure `--` comment cleanup (removed pointers to deleted `docs/timezone-architecture-rfc.md`), **zero SQL semantics**; commit msg explicitly notes runner tracks applied versions by filename not checksum; DB migrated 1 (non-no-op). **v0.4.19 (fake merge, 2026-08-06):** DB applied **253-256** (4: runtime_profile_add_qwenpaw/reasonix, chat_pending_deferred_v3, drop_chat_pending_v2) — but the git "merge" was fake (single-parent), so these migrations were applied against a tree that never landed v0.4.19's code; superseded by v0.4.20 below. **v0.4.20 (2026-08-07, merge `e4b0277ba`):** upstream added **257-260** (`agent_task_queue_channel_media_pending_unique_v2`, `drop_pending_issue_agent_v1`, `issue_origin_dingtalk_chat` + `_validate`), all > fork max 256 → **no new collision**; the 6 known collisions (119/158/159/160/213/214) stand unchanged; DB migrated 4 (final=260). (v0.4.21-25 migration records live in their own Last-upgrade sections below: final 264→272→273→284→309.) **v0.4.26 (2026-08-17, merge `75ec751af`):** upstream added **310-313** (`workspace_private_plugin_identity` ×2 + `plugin_identity_scoped_key_index`/`drop_global_plugin_identity_key_index` + `runtime_profile_add_dsh`), all > fork max 309 → **no new collision**; the 6 known collisions (119/158/159/160/213/214) stand unchanged; DB migrated 4 (final=313). **v0.4.28 (2026-08-18, merge `08c61c3ab`):** upstream added **314-341** (28 stems: workspace/agent MCP 314-317, plugin remote-MCP 318-326, share-link 327-331, **MUL-6243 issue_status 目录 332-340**（337 DROP 了 fork 213 重建的 `issue_status_check` → format-only check，267 条 archived 行过 regex 实证安全；339 seed 5 ws × 7 = 35 行，**不含 archived**——archived 是 Go 层伪内置非 catalog 行）, actor 属性 341), all > fork max 313 → **no new collision**; the 6 known collisions stand unchanged; DB migrated 28 (final=341); dev test DB (:5432) 同轮重建至 341 供集成测试。 **v0.4.29 (2026-08-19, merge `895839b9a`):** upstream added **342-343** (`runtime_profile_add_mcode` + `comment_delegated_failure_pending_index`，343 走 concurrent-index pre-hook), all > fork max 341 → **no new collision**; the 6 known collisions (119/158/159/160/213/214) stand unchanged; DB migrated 2 (final=343); dev test DB (:5432) 原地补迁 342/343（无需 drop/recreate，2 个迁移的小 lag 直接 up 即可）。
- **Locale files.** `packages/views/locales/*/{skills,issues,settings}.json` are touched by several features (#5160, #2460, #4118 invite_name, VCS cluster) and by upstream constantly. Conflicts are frequent but trivial — take both key sets. **`settings.json` specifically:** v0.4.23 upstream reformatted the `shortcuts` block multi-line→compact AND added keys in the same hunk — when upstream reformats + adds, the key-set diff tells you whether theirs is a strict superset (take theirs in the conflict region via surgical `sed '/^<<<<<<< HEAD$/,/^=======$/d; /^>>>>>>> /d'`, NEVER `git checkout --theirs` which nukes fork-only keys like `invite_name`/`vcs` elsewhere in the file). **Ledger-correction 2026-08-12:** this note previously listed only `{skills,issues}.json`; `settings.json` belongs here too (it is a recurring fork touch via #4118 + VCS + upstream shortcut changes).
- **The mention cluster is the recurring cost.** If upstream ever lands a native mention-chip system, expect to drop #5199 and #5346 together and re-apply only the skill-mention gesture + bind-on-submit logic on top of upstream's version. **v0.4.6 status (audited 2026-07-21): that precondition has NOT occurred** — upstream `5a11232c4` is a *renderer move* (hollows `readonly-content.tsx`, relocates read-only render to the new `packages/views/rich-content/rich-content.tsx`), not a native mention system; the `project-mention-a11y.test.tsx` in the new dir is just an a11y fix (`<span onClick>` → `<AppLink>`), not a chip system. So this round: re-apply the readonly chips into `rich-content.tsx` (the editable face — `mention-view.tsx` etc. — is untouched by upstream). The shared hot file is `packages/views/editor/extensions/mention-view.tsx`; the 7-17 @skill redesign widened the cluster's surface further into `content-editor.tsx`, `comment-input.tsx`, `comment-draft-store.ts`, and several new gesture files (see the redesign note under the mention cluster table, and `docs/upgrades/v0.4.6-plan.md`).
- **Rejected features compound silently.** #5539 was closed by upstream (2026-07-16) yet its code still lives in your leading commits — a rejected feature you forget about still drifts and still costs merge time. Track upstream rejections here the moment they happen. (Contrast #5466: self-withdrawn and fully reverted, so it carries **no** local footprint — see the Withdrawn table.)
- **The `'project'` token in `MENTION_MARKUP_SOURCE` is NOT a #5466 revival.** `a75305c27` (skill-mention cluster) added `'project'` to the markup-source enum purely for backend parity/completeness — there is no `@project` UI and no typed-mention code behind it. A future reviewer grepping `'project'` may misread it as #5466 (withdrawn @project typed mention) coming back. It is not; do not "clean it up" by removing it.

## Last upgrade — v0.4.28 → v0.4.29 (EXECUTED 2026-08-19)

✅ **EXECUTED 2026-08-19** — merge commit `895839b9a` (TRUE 2-parent: fork `e8364fb2b` + `c670e0549`=v0.4.29 tag commit), base now `v0.4.29`. Clean merge-base `38c992ad0` (= v0.4.28^{commit}), **18 upstream commits, 196 files (+9198/-814)**. Drivers: **MUL-6243 follow-up（board fetches by category；catalog archive 退休不迁移）+ MUL-6063 委托失败恢复 + MUL-5651 网络分区 + desktop Cmd+, + MiniMax ACP + lark 媒体 + NUL 修复**. **2 手动冲突 == merge-tree 预测（第 10 连中）**，均前端震中（`queries.ts` fetchFirstPages、`hidden-columns-panel.tsx` import 块）。

**Headline — archived compose-and-keep 全线存活，前端接入被 auto-merge 吸收大半。** Server：零代码改动（`IsCategory=IsBuiltIn` 传递，`status_category:"archived"` 自动下发，blob 级验证）。前端（用户拍板 Option 1「教前端第 8 类 archived」）：union 改名 × fork archived 成员由 auto-merge 干净组合成 8 值联合；`BUILT_IN=Set(STATUS_ORDER)` 派生使 `isIssueStatusCategory("archived")=true` → `issueStatusCategory`（不再 null）、`statusCategoryOfKey`（不再误判 todo）、`normalizeStatusPatch` 全链路自动正确。**唯一手工轴**：(1) `fetchFirstPages` 采 upstream per-category 抓取 + 保 fork `include_archived: category === "archived"`（server 端 where 为 `status = ANY(...) AND status <> 'archived'` 的 AND 叠加，不带该参数 archived 桶抓回为空——实证过）；(2) 测试钉 `"accepts exactly the 7"` → 8（镜像 server `TestUnseeded` 7→8）。typecheck 唯一修复：`config/status.ts` 恢复 `IssueStatus` 进 import（upstream 窄化了 import，fork 行仍用该名）。

**Phase 5 全绿且零碰撞（v0.4.23 后首轮）:** typecheck 6/6（1 import 修复）、pnpm test 4438/4438、go build/vet、sqlc regen 零漂移、build 3/3。go test 执行面（dev 库 :5432 先原地补迁 342/343）：失败集与 backup baseline **集合同一**（handler 6 个 autopilot/skill-mention residual + `BuiltinSkills` + `cmd/multica` batch 路由 + `cmd/server` 2 个 source_task_id seed——全部 backup 复现一致）；**2 个新时序 flake watch items**（`TestChannelMediaReconciler_SettlesThreeStates` 1/9、`TestExpireStaleQueuedTasks` 1/5，隔离均过、backup 全过——若复现再查）。`truncateFallbackCommentBody` 本轮虚惊一场（@@ 上下文行误读教训：+8 实为旁置常量 `RuntimeClaimFreshnessSeconds`，函数体 upstream 零改动，合并后唯一定义在 fork 文件 cap=200，调用点 :3748 存活）。`comment.go` 9 参 workaround 安全（upstream +14 落在不相交区域，merge-file exit 0）。**2 新迁移 342/343** 全 > fork max 341 → **零新撞号**；6 已知撞号（119/158/159/160/213/214）完好；DB migrated 2（final=343；archived 267 行 + catalog 35 行完好）。**Phase 6:** pg17 备份 `~/multica-backups/v0.4.29-pre-migrate-20260819T142949Z.dump`(62M,762 TOC)；install.sh commit-first 正确（`git describe --tags` 解析 v0.4.29，backend strings `main.version=v0.4.29`，前端 layout chunk 含 v0.4.29）；health `/healthz` **首探即 200**、`:3001` 200、`/api/config server_version=v0.4.29`；daemon 留 0.4.28（tap 未发，CLI=daemon 自洽，agents 5 + workspaces 5 完好——tap 发版后 `brew upgrade multica && multica daemon restart` 对齐）。行为级 follow-up（非阻塞）：fork 现有 cancel 路径隐式 `CancelTaskOptions.UserInitiated=false`（MUL-6063 语义，按需显式 true）；fork skill 导入继承 upstream `sanitizeNullBytes` 的 U+FFFD 语义（良性）。

→ **`docs/upgrades/v0.4.29-plan.md`** (frontmatter `upgrade_contract: selfhost-upgrade/v1`, `phase: done`, merge_commit `895839b9a`, 15 verified negative claims, actuals-vs-plan in Phase 7 section)

## Last upgrade — v0.4.26 → v0.4.28 (EXECUTED 2026-08-18)

*Audit 2026-08-18:* **✅ v0.4.26 → v0.4.28 upgrade EXECUTED 2026-08-18** (merge commit `08c61c3ab`, TRUE 2-parent; parents `dcb8d70d8` fork HEAD + `38c992ad0` v0.4.28^{commit}). 49 upstream commits / 612 files。Drivers：MUL-6243 自定义状态、share-link invites、workspace/agent MCP、Redis channel leases（postgres 默认零动作）、actor 属性、skills bulk update、MUL-6254 auth 重写。**23 冲突 == merge-tree 预测（第 9 连中）**；**核心决策：archived compose-and-keep**（用户拍板）—— fork 携带面扩大为 issuestatus 补丁 + SQL guards + 测试钉（详见顶部 audit 行与 plan Phase 1 策略表）。**handler 集成测试环境复活**：dev 库 :5432 stale（停在 212）→ drop/recreate + migrate 1-341 → 365 失败降为恰好 6 个 ledger 存量 residual（`TestClaimTask_ManualRetryReusesWorkdir` 还被治好）；此法已固化为升级流程可用工具。新增 3 个存量 residual 现身（此前被坏库掩盖，均 backup 复现一致非回归）：`TestBuiltinSkillsConformToTemplate`（fork multica-mentioning 描述 892 字超 300 帽）、`TestRunSkillImportJsonTreatsDuplicateAsConflictResult`（#2669 batch 路由）、cmd/server 两个 source_task_id seed 测试（190 期约束）。daemon 留 0.4.26（tap 未发，协议增量兼容；tap 发 0.4.28 后 `brew upgrade multica && multica daemon restart`）。

→ **`docs/upgrades/v0.4.28-plan.md`**

## Last upgrade — v0.4.24 → v0.4.25 (EXECUTED 2026-08-14)

*Audit 2026-08-14:* **✅ v0.4.24 → v0.4.25 upgrade EXECUTED 2026-08-14** (merge commit `2ca50e006`, TRUE 2-parent; parents `3033ef5b0` fork HEAD + `ea1ace776` v0.4.25^{commit}). Clean merge-base `ad23d1da3` (= v0.4.24^{commit}), **26 upstream commits / 349 files (+29638/-1139)** —— 体积大但含大量机械新增。Drivers: **全新 plugin 子系统**（migrations 285-299，plugin_identity/release/contribution/installation/grant/binding + snapshot/health 等表）、**dingtalk group-route**（304-307）、**drop-redundant-indexes**（300-303）、**local-worktree capability**（MUL-5707，capability 取代 version floor）、agent-task branch/runtime indexes（308-309）。**3 手动冲突 == merge-tree 预测完全吻合**（client.ts / types/index.ts / cli-version.test.ts，全部 3-way keep-both：fork 加的成员与 upstream 加的成员名字不相交）。**2 处 auto-merge 语义碰撞（merge-tree 看不见，Phase 5 gate 抓到）**：(1) **schemas.ts 重复 SkillSchema**（fork batch-import + upstream refreshSkill 各自在不同区域 ADD 了同名 SkillSchema/SkillFileSchema/EMPTY_SKILL → 3-way 各保一份 → TS2300 重复标识；删 upstream 重复块、留 fork 的，去重 Skill import）—— **新型碰撞：same-symbol different-region fork-add∩upstream-add**；(2) **task_source_task_id_test.go arity**（fork-ADDED 测试文件调旧 7/8 参 CompleteTask/FailTask，upstream 改 8/9 参并更新了自己的测试但触不到 fork 专属文件；go vet 抓到，go build 抓不到；加 branchName="" 修复）。**CompleteTask/FailTask 地雷**：非测试代码 Scenario B 干净（upstream 单边改签名+调用点）；仅 fork 测试需 arity bump。**sqlc generate 0-diff**（auto-merge 已一致，因双方改的 query 区域不相交）。loss scan 全 ∅（client.ts 11 方法、types、8 locale 文件 jq flat-key 零丢失、router.go 13 路由 + VCS/admin/import/officialBaseline、gc.go issueguard import）。**25 新迁移 285-309** 全 > fork max 284 → 零新撞号；6 已知撞号（119/158/159/160/213/214）完好。Phase 5 全绿（typecheck 6/6、test 4079/4079、go build/vet、build 3/3）。install.sh commit-first 正确（merge commit 2ca50e006 → git describe 解析 v0.4.25）；**daemon brew upgrade 0.4.24→0.4.25 + restart**（pid 50085→22569，CLI/daemon/backend/frontend 全 v0.4.25，5 workspace + 5 agent 完好）。**审计盲区教训**：签名变更的调用点扫描必须含 `*_test.go`（fork 新增测试对 upstream 的更新盲区）。Per-file strategy, verified negative claims, actuals-vs-plan live in `docs/upgrades/v0.4.25-plan.md` (see "Last upgrade").

→ **`docs/upgrades/v0.4.25-plan.md`**

## Last upgrade — v0.4.23 → v0.4.24 (EXECUTED 2026-08-13)

✅ **EXECUTED 2026-08-13** — merge commit `cf41b2434` (TRUE 2-parent: fork `026e45e89` + `ad23d1da3`=v0.4.24 tag commit), base now `v0.4.24`. Clean merge-base `e0d0b3815` (= v0.4.23^{commit}), **31 upstream commits, 251 files (+16394/-3234)**. Drivers: **workspace-teardown 重写 + agent-template 全量移除 + slug 罗马化 (MUL-6050 issue-prefix 改 4-char) + 聊天历史 nav + daemon GC/repocache**. **6 手动冲突 == merge-tree 预测完全吻合**（client.ts / schema.test.ts / readonly-content.test.tsx / step-workspace.tsx / handler.go / workspace.go），18 auto-merge。

**Headline — Phase 5 连续第二轮零 fork-code × upstream-test 碰撞（继 v0.4.23 后）。** agent-template 是 fork 里的 **dead base-inherited code**（server `agenttmpl/` pkg + client queries/schemas/types/tests），上游原子删除 → 干净 accept-upstream 全收，零活行为损失（grep 验过零 consumer）。**一个产品决策（Phase 2 用户批准）：放弃 fork 的 slug-fallback issue-prefix 定制**（`generateIssuePrefix(name,slug)` = 3-char + "ws" tail）**取上游 MUL-6050**（`defaultIssuePrefixFromSlug` = 4-char）—— 这是 *deliberately abandoned* fork invariant，不再 carry。冲突要点：**handler.go (C keep-both)** 保 fork `parseSkillMentionAgents` (#5346, 上限 8/16) + 插 upstream `DeclareChannelFileDelivery`/`channelDeliversFiles`；**workspace.go + step-workspace.tsx (A)** 取上游 MUL-6050；**client.ts** 保 fork `BatchImportResponseSchema` 删 upstream-removed `AgentTemplateSchema`；**schema.test.ts** 保 fork `importSkillsBatch` (2 tests) 删 `createAgentFromTemplate` dead block；**readonly-content.test.tsx** 删 3 autolink tests（fork 移除了 mock 定义）取 upstream CJK emphasis parametric。loss scan 全 ∅。

**Phase 5 全绿:** pnpm install、typecheck、go build/vet、sqlc generate 无 drift、pnpm test 零碰撞、build 3/3。**Phase 6 deploy:** pg17 备份 `~/multica-backups/v0.4.24-pre-migrate-20260813T035243Z.dump`(58M);migrate up 应用 **11 新迁移 (273-284 skip 280 — 上游本身跳号,非 fork 撞号)**,latest=`284_task_owner_row_fence`,零新撞号(6 已知 119/158/159/160/213/214 完好);install.sh commit-first 正确（cf41b2434 在 HEAD → `git describe` 解析 v0.4.24,非 v0.4.23 fallback 陷阱）,`_version-stamp.sh` export NEXT_PUBLIC_APP_VERSION 覆盖 .env,backend `-ldflags -X main.version=v0.4.24` 验过（strings 二进制 main.version=v0.4.24）,前端 0.4.24 in 4 files（0.4.23 仅 changelog 历史,非 stale）,`.env` line 27 bump v0.4.24;health `/healthz` 200、`/api/config server_version=v0.4.24`、`:3001` 200;**brew upgrade multica 0.4.23→0.4.24 + `multica daemon restart`**(pid 43145→39135,CLI commit `ad23d1da3` = v0.4.24 tag commit) —— CLI/daemon/backend/frontend 全 v0.4.24,5 workspace + 5 agents (claude/codex/opencode/openclaw/hermes) 完好。

→ **`docs/upgrades/v0.4.24-plan.md`** (frontmatter `upgrade_contract: selfhost-upgrade/v1`, `phase: done`, merge_commit `cf41b2434`, 18 verified negative claims, actuals-vs-plan in Phase 7 section)

## Last upgrade — v0.4.22 → v0.4.23 (EXECUTED 2026-08-12)

✅ **EXECUTED 2026-08-12** — merge commit `76f9164a3` (TRUE 2-parent: fork `f1731c0f9` + `e0d0b3815`=v0.4.23 tag), base now `v0.4.23`. Clean merge-base `8bd49bba8` (= v0.4.22 tag commit), **28 upstream commits, 145 files (+13464/-641)**. Drivers: **WeCom 媒体入站 (MUL-5906) + Reasonix thinking_level (MUL-5991) + browser-style Cmd+[/+] history nav + sub-issue 继承 parent project/assignee + codex skill symlink (MUL-6000) + hermes agent memory (MUL-5932) + workspace-delete advisory-lock 修复 (MUL-5983)**. **3 手动冲突（全是 CJK settings.json）/ 11 auto-merge** — 预测完全吻合（lane a "EN 干净、CJK brace trap" → 实际正是 EN 自动干净 + ja/ko/zh-Hans 撞）。

**Headline — 近期最干净的一轮 Phase 5，零 fork-code × upstream-test 碰撞（对比 v0.4.20 的 14 个）。** 唯一真实 Go 签名 drift（`BroadcastCancelledTasks` 加 `workspaceID`）落在 fork 零 footprint 的 caller 文件上 → auto-merge 干净 + `go build` 非静默兜底（非 Strategy D 静默丢参）；def + 4 caller 全新签名一致（验过）。`truncateFallbackCommentBody` v0.4.22 调用点失活教训**本轮不复现**（四维都验：定义位置、调用点存活 task.go:3606、与上游字节一致、上游没碰该区域）。冲突要点：**3 个 CJK settings.json (B)** —— 上游把 `shortcuts` 块从多行 pretty-print 重排成紧凑单行 + 加 `goBack`/`goForward`，fork 在该块**无自定义键**（键集 diff 证 theirs 严格超集），**外科式 sed 取 theirs**（仅冲突标记区，绝非 `--theirs` 整文件——会抹掉 fork 的 `invite_name` + `vcs` 键）。loss scan 全 ∅（4 locale JSON valid，invite_name+vcs 存活，goBack/goForward 到位）。

**最大发现（lane b）—— VCS 自托管 Git provider 簇（MUL-3772/#5006/#5888/#5868/#5883）是 ledger 未记录的盲点。** ~50 文件含新建 `server/internal/handler/vcs.go`、`packages/views/settings/components/vcs-tab.tsx`、`vcs_webhook.go`，触碰 2 个交集文件（router.go + settings.json×4）。本轮 router.go auto-merge 后**全部 5 个 VCS 路由 + secretbox 存活**（验过非假设），未造成冲突；但若 auto-merge 丢了路由会静默失能。**已补入下方 Open PRs 表**。另修 5 个 stale 热文件引用（`rich-content.tsx`/`use-issue-surface-data.ts`/`cmd_skill.go`/`skill_mention_trigger.go` 不存在；`actor-avatar.tsx` 无 showName）+ locale 漂移注释扩到 `settings.json`。

**Phase 5 全绿:** pnpm install、typecheck 6/6、go build/vet exit 0、sqlc generate 无 drift、**pnpm test 3890 passed 零碰撞**、build 3/3。**Phase 6 deploy:** pg17 备份 `~/multica-backups/v0.4.23-pre-migrate-20260812T043203Z.dump`(58M);migrate up 仅应用 **272_rollup_task_usage_hourly_xact_lock**(271→272 零撞号);install.sh commit-first 正确（先 merge commit `76f9164a3` 再 install.sh,`git describe` 解析 v0.4.23,`.env NEXT_PUBLIC_APP_VERSION=v0.4.23`,backend ldflags `-X main.version=v0.4.23` 验过);health `/healthz` 200、`/api/config server_version=v0.4.23`、frontend `:3001` 200;**brew upgrade multica 0.4.22→0.4.23 + `multica daemon restart`** —— CLI/daemon/backend/frontend 全 v0.4.23,5 workspace 完好,agents claude/codex/opencode/openclaw/hermes 全在。

→ **`docs/upgrades/v0.4.23-plan.md`** (frontmatter `upgrade_contract: selfhost-upgrade/v1`, `phase: done`, merge_commit `76f9164a3`, 20+ verified negative claims)

## Last upgrade — v0.4.21 → v0.4.22 (EXECUTED 2026-08-11)

✅ **EXECUTED 2026-08-11** — merge commit `7206599e2` (TRUE 2-parent: fork `c78d65637` + `8bd49bba8`=v0.4.22 tag), base now `v0.4.22`. Clean merge-base `0dfaac266` (= v0.4.21 tag), **40 upstream commits, 405 files (+26222/-4302)**. Driver: **saved issue views V1 (MUL-4796) + 16 提交 WeCom hardening**。**7 手动冲突 / 10 hunks** — 预测完全吻合（mention-view.tsx 4 hunks 一致；issues-header.tsx 2 hunks 被 git 合并成 1；locale 3 个 CJK + EN 自动干净）。

**Headline — 第一次 fork 决策中途切换 D(b)→D(a)。** C2 报告标记 `use-issue-surface-data.ts` 为 D 签名漂移（auto-merge 静默丢 fork-BASE assignee-grouped 子系统 100+ 行 body），C4 又确认上游 `groupBranches.loadMoreGroups + pagination: Record<string, IssueGroupPageState>` 是**严格更强的 server-cursor 替代**——覆盖 status + assignee + project 全维度、cursor 共享、server 一致。原计划选 D(b) 保留 fork load-more hooks（~500 行手工恢复）；**实际选了 D(a)**：上游 `groupBranches` 已经把 KTD4/R7 看板的"按 assignee 分列 + 每列 load more"行为用更强的方式实现了，0 行手工恢复。Phase 1 plan 里写的"D(b) 保留 fork 特性（推荐）"是错的——必须读完上游替换实现才能下结论，这是本轮 negative claim 教学的活样本。

**最大陷阱（C4 发现）—— `task.go:3565` 静默语义丢失。** GH #5455 防护（raw-stream Output 190-264KB 落到 issue 线程的截断）被 auto-merge 抹掉：fork 在 task.go:143-160 删了内联 `truncateFallbackCommentBody` 定义（搬到 fork-only `fallback_comment_truncate.go` cap=200），但没碰 3565 那个调用点；上游把 3565 调用点改了。auto-merge 把 fork 的 `source_task_id` 注释 + 上游的（已变）调用区段拼起来，得到一个**编译过、test 过、但运行时无界**的混合体——Phase 1 的 negative claim 只验证了"函数定义不重复"，没验证"调用点还活着"，正是这种"verified what I asked, missed what I should have asked"的失败模式。**Phase 4 必修**:在 task.go:3565 前插入 `content := truncateFallbackCommentBody(redact.Text(body), maxSynthesizedFallbackCommentRunes)`，调用处传 `content`。保留 fork cap=200（5 workspace / 965 issue 体量用不到上游 8000）。fork-only 文件的常量定义不动。

**冲突分布（4 code + 3 locale）：** `mention-view.tsx` (B+C,4 hunks) — 删 fork 5 个 import(`useWorkspacePaths/useIssueLinkStore/useNavigation/IssueChip/ProjectChip`)，inline anchor 全替换为 `<IssueMentionCard>`/`<ProjectMentionCard>`，保留 `SkillMention` 函数 verbatim，删 inline `IssueMention`/`ProjectMention` 函数定义（不可达），union docblock；`issues-header.tsx` (B,1 hunk) — lucide-react 加 `Archive`(archived chip)、去 `X`(clear-filters 按钮已被上游重写删除)；`swimlane-view.tsx` (C→D(a),1 hunk) — 初始保 `myIssuesOpts` useMemo + KTD4/R7 注释，D(a) 切换后清掉 `myIssuesOpts` 块（consumer 已被上游 load-more 移除，悬空引用 undeclared 变量）；`use-issue-surface-data.ts` (D(a),1 hunk) — 冲突只决定 imports，取上游 imports + 补 `BOARD_STATUSES`（保留 fork 的 KTD4/R7 `visibleStatuses` 默认值 at line 329 仍引用），删 fork 死符号 `QueryKey`/`IssueAssigneeGroup`。locale ×3 JSON (B + 大括号/逗号陷阱 fix per `docs/solutions/workflow-issues/merge-keep-both-shared-context-brace-trap.md`) — fork block.rstrip() + `'\n  },'` + upstream block + shared tail；3 个 660 keys、0 lost、+75 upstream；EN 自动干净（fork 在 EN 第 213 行 mid-file 插 `archived`，CJK 第 679 行 end-of-file 插——只有 CJK 与上游 end-of-file 插入位置撞）；parity normalize(_one/_other→_count) 双向 0 missing。

**Phase 5 全绿:** typecheck 6/6、go build/vet exit 0、sqlc generate 干净（fork 的 `Archived int64` 与上游新生成的 `IssueView`/`ChannelChatSessionBinding.PendingFresh` 已共存）、**pnpm test 326 files / 3867 tests passed**、web/desktop build 成功（docs 因 sandbox 拿不到 Google Fonts 失败，环境问题非代码）。Phase 5 修复：mention-view.test.tsx 一处 `renderMention(args, adapter)` → `renderMentionWithAdapter(args, adapter)`；issues-header.tsx 加 `const counts = useIssueCounts(facetCountsExact ? scopedIssues : NO_COUNT_ISSUES, tableFacetCounts)`（auto-merge 保了 `archivedCount = counts.status.get(...)` 引用但丢 `counts` 本地定义）；issues-header.tsx 删 unused `X` import。

**Phase 6 deploy executed:** pg17 备份 `~/multica-backups/v0.4.22-pre-migrate-20260811T025400Z.dump`(58M);migrate up 7 应用 **265-271**(issue_view + 2 索引 + issue_view_preference + issue_view_workspace_variant + pinned_item 加 'view' enum + channel_chat_session_binding.pending_fresh 列,DB 264→271 零撞号);**install.sh commit-first 正确** — 先 merge commit `7206599e2` 再 install.sh,`git describe` 解析 v0.4.22,`.env NEXT_PUBLIC_APP_VERSION=v0.4.21→v0.4.22`,backend `-ldflags "-X main.version=v0.4.22"` 正确 stamp;health `/api/config server_version=v0.4.22`（sandbox 拿不到 `/healthz` 真响应但 backend 进程 exit 0 + lsof LISTEN :8081 健康);**brew upgrade multica 0.4.21→0.4.22 + `multica daemon restart`** —— CLI/daemon/backend/frontend 全 v0.4.22,5 workspace 完好，agents codex/opencode/openclaw/hermes/claude 全在。

→ **`docs/upgrades/v0.4.22-plan.md`** (frontmatter `upgrade_contract: selfhost-upgrade/v1`, `phase: done`, merge_commit `7206599e2`, 30+ verified negative claims)

## Last upgrade — v0.4.20 → v0.4.21 (EXECUTED 2026-08-07)

✅ **EXECUTED 2026-08-07** — merge commit `55bf3432f` (TRUE 2-parent: fork `ec3dea072` + `0dfaac266`=v0.4.21 tag), base now `v0.4.21`. Clean merge-base `93342d04a` (= v0.4.20 tag), **29 upstream commits, 315 files (+19892/-2761)**. Drivers: **WeCom 渠道 (MUL-5226)** + **Analytics 重命名 (MUL-5759, Usage→Analytics)**. **6 手动冲突 == merge-tree 预测完全吻合**（连续第 7 次文本预测命中 v0.4.14/15/16/17/18/19/20/21 系列中最准的一次）。

**Headline — 后端最干净的一轮，前端一个真 C+D。** **后端零 Strategy D scenario A**:dreaded funcs(enqueueIssueTask/CompleteTask/FailTask/StartTask 等）签名跨 3 ref 全 byte-identical;`enqueueMedia`(9→11 参）+ `generateIssuePrefix`(1→2 参）都是 **scenario B 单边干净**（前者 fork 零碰 engine/router.go，后者 fork 2 参 upstream 未动）。冲突集中前端：**mention-suggestion.tsx (C+D, 3 hunks)** — 保 fork typeGroups 注册表分组（getMentionGroupLabel 产 Users/Issues/Skills)+ 补 upstream cancelled 分层（status 轴，永远最后）,**删 upstream 冗余 users/issues 数组**(typeGroups 已涵盖），采 groupItems(items, query) 新签名。**editor.json×4** — manual union + 逗号修复：fork skill_* 块末行 `skill_agent_row_aria` 无尾逗号，union 后补 `,` 再接 upstream group_cancelled。**README** 取 upstream MUL-5812 重写（fork 旧 self-host 块被 upstream 等价内容取代，SELF_HOSTING.md 未冲突保留）。

**语义陷阱（auto-merge 文本过，人工 grep 确认 keep-both）:** issue.go buildSearchQuery — fork `WHEN 'archived' THEN 7` ⊕ upstream `cancelledRank` ⊕ 3-arg ORDER BY(cancelledRank, rankExpr, statusRank) ⊕ fork `issue_status_is_closed` 全存活；task.go `truncateFallbackCommentBody` 0 内联定义（只在 fork-only fallback_comment_truncate.go)→ 无 redeclared;cmd_issue_test.go fork archived 测试 ⊕ upstream compact 测试共存。**sqlc regen 必须**:issue.sql.go 两侧都 stale(fork 缺 upstream 2 查询、upstream 缺 fork Archived)→ `make sqlc` 统一。

**Phase 5 全绿零碰撞**（与 C4 预测一致）:typecheck 6/6、**pnpm test 3824/3824**（含 mention-suggestion.test.tsx 50/50 — fork skill/typeGroups ⊕ upstream cancelled-demotion 在 merged groupItems 上正确组合）、go build/vet、build 3/3（新 /wecom/bind 路由）。**无 v0.4.20 那种 title-uniqueness 测试碰撞**(fork IssueIdentifierBadge 只渲染 identifier 不渲染 title)。loss scan 代码符号 ∅ + locale ∅（仅 upstream 删的 usage.json metric_errors/subtitle dead key，接受非恢复）。

**Phase 6 deploy executed:** pg17 备份 `~/multica-backups/v0.4.21-pre-migrate-20260807-192339.dump`(649 TOC);migrate up 4 应用 **261-264**(DB 已在 260）零撞号；**install.sh commit-first 正确** — 先 merge commit `55bf3432f` 再 install.sh,`git describe` 解析 v0.4.21;`.env NEXT_PUBLIC_APP_VERSION=v0.4.20→v0.4.21`;health backend `/healthz` 200（立即，预热快）、frontend `/` 200、`/api/config server_version=v0.4.21`（前后端）;**daemon brew upgrade 0.4.20→0.4.21 + restart**(CLI/daemon/backend/frontend 全 0.4.21,5 workspace 完好，agents claude/codex/opencode/openclaw/hermes 全在）。

→ **`docs/upgrades/v0.4.21-plan.md`** (frontmatter `upgrade_contract: selfhost-upgrade/v1`, `phase: done`, merge_commit `55bf3432f`)

## Last upgrade — v0.4.18 → v0.4.20 (EXECUTED 2026-08-07)

✅ **EXECUTED 2026-08-07** — merge commit `e4b0277ba` (TRUE 2-parent merge), base now `v0.4.20`. **Real merge-base `v0.4.18`** — the v0.4.19 "upgrade" below was a fake merge (single-parent linear commits `c03402a94`/`c95bb7e89`, never merged v0.4.19's tree), so this jump was v0.4.18→v0.4.20: **61 upstream commits, 603 files (+51457/-10388)**. **42 手动冲突** (Phase 1 predicted 37; locale JSON hunk-level conflicts exceeded file-level estimate), all resolved, 390 files in the merge.

**Headline — 多个真实产品语义岔路，全部经用户拍板。** Resolved: **onboarding Mika 重写 (#6378)** — upstream 把 Mika name/prompt/permissions 移到 server constants (`POST /api/agents/mika`),删 pre-written markdown 模板 → `git rm` 3 个 orphan 模板 (create-agent-guide-issue/helper-instructions/helper-starter-prompts),install-runtime-issue.ts 取 upstream Mika step 7 (3 locale),保 fork MUL-5703 术语;fork 自己的 `welcome-after-onboarding.tsx` Mika 页（独立路径）未动。**locale JSONs (16 files)** — 取 upstream 共享 key 措辞（upstream 已采纳 fork 的 issue→任务 + 全角标点）+ deep-merge 恢复全部 324 fork-only 功能 key。**MUL-5442 reply 策略** — 跟 upstream（删 fork 的 agent-to-agent 沉默规则），取 upstream reply-mandatory + careful-@mention。**server Strategy D 签名碰撞** — router.go createIssue/enqueueMedia 7→9 参、task.go enqueueIssueTask/CompleteTask/FailTask 取 upstream 新签名 + 修测试签名 + 删 auto-merge 重复块。**issue-detail pin-comment 双机制** — 用户选 upstream sticky composer,base-method 重建 + 缝 fork archived-dim/childProgress/formatProgressText/FAB 留位/IssueIdentifierBadge。

**Phase 5 抓住 14 个测试失败（3-way merge 不可见的 fork-code × upstream-test 碰撞）:**(A) issue-detail 5 个 — fork 把 `IssueIdentifierBadge`+独立 title span 缝进 breadcrumb leaf → 出现第二份纯 title 文本，撞 upstream 新测试的单数 `getByText` 唯一性探针；**用户选"badge+合并文本"**:leaf 恢复 upstream 合并 `{identifier} {title}`,badge 迁到标题上方（fork 交互保留、位置迁移），更新 fork 自己的 leaf-link 测试。(B) locale parity 9 个 — Phase 4 把 324 fork-only key 恢复到 ja/ko/zh-Hans 但漏 EN；枚举 80 个 missing-EN key 全有来源（78 fork-only 有英文值 + 2 upstream)deep-merge 补 EN。**死 key 清理（用户确认）**:fork 的"Token usage 侧边栏面板"其实是 upstream 旧代码，upstream v0.4.20 已用 execution-log 对话框取代并删除（commit ba129b196 MUL-5762)→ 删 6 个 dead token-usage key + dead `runtime_aside`/`step_header`(4 locale 全删）,保留活的 `execution_log.view_run` + 72 个 `welcome_after_onboarding.*`。最终 parity 166/166、pnpm test 3754/3754、typecheck 6/6、build 3/3。

**Phase 6 deploy executed:** pg17 备份 `~/multica-backups/v0.4.20-pre-migrate-20260807-124441.dump`(55M);migrate up 4 应用 257-260（DB 已在 256，非最坏 252);**install.sh commit-first 顺序正确** — 先建 merge commit `e4b0277ba` 再跑 install.sh,`resolve-official-baseline.sh` 的 `git describe --tags --abbrev=0` 正确解析 v0.4.20,**无需手动 re-stamp 兜底**（前一轮 v0.4.19 的教训已修复）;`.env NEXT_PUBLIC_APP_VERSION=v0.4.19→v0.4.20`;health check backend `/healthz` 200、frontend `/` 200、`/api/config server_version=v0.4.20`;**daemon 漂移清除** `multica daemon restart` 0.4.19→0.4.20(CLI/daemon/backend 全 0.4.20,5 workspace 完好）。

→ **`docs/upgrades/v0.4.20-plan.md`** (frontmatter `upgrade_contract: selfhost-upgrade/v1`, `phase: done`, merge_commit `e4b0277ba`)

## Last upgrade — v0.4.18 → v0.4.19 (SUPERSEDED — was a FAKE MERGE, redone as v0.4.18→v0.4.20 above)

⚠️ **这条目记录的"v0.4.19 upgrade"是 fake merge**:`c03402a94`/`c95bb7e89` 是单 parent 线性 commit，从未真正合入 v0.4.19 的 tree(327 files 未合入）。真实升级是上面的 v0.4.18→v0.4.20。保留此条目仅为历史留档。

✅ **EXECUTED 2026-08-06** — merge in progress, base now `v0.4.19`. Clean merge-base `v0.4.18`, **34 upstream commits, 322 files (+21114/-4567, 净增 +16547)** — 纯增量为主（vs v0.4.18 是 refactor/搬迁净增≈0）。**14 手动冲突（远超 v0.4.18 的 4 个；连续第 6 次文本预测命中 v0.4.14/15/16/17/18/19）+ 2 build 兜底修复 + 1 typecheck 兜底清理**。

Resolved: **comment-input.tsx**（C）upstream b3fa17f0e #6419 改 follow-up ordering ⊕ fork skill-mention 链（5+ commits）— 删除 fork 不再使用的 `useCommentComposerStore` import（upstream 已重构），保留 `useWorkspaceId` import；**issue-detail.tsx**（C 3 hunk）— 取 upstream 整体 `BreadcrumbHeader leading={leadingAction}` + `ThreadNavPanel` (#6426) 结构 + 替换 leaf 为 fork `IssueIdentifierBadge`（identifier badge 正交特性）；**task.go**（C 6 case union）— fork 4 case ⊕ upstream 2 case（`agent_error.context_overflow` + `codex_resume_oversized`）；**provider-logo.tsx**（C）— 取 upstream `staticAssetSrc(qwenLogo)` helper（比 fork 的 `as unknown` IIFE 干净）；**main_test.go**（C hunk）— 删 fork 死 `TestOfficialBaseline` 签名 + 取 upstream `mainSourceFile` const + `backgroundServiceConstructors` var + `TestMainUsesRouterOwnedBackgroundServices`（background-services守护）；**9 个 locale JSON**（C zh-Hans/ja/ko × editor/issues/layout）— fork 译法 ⊕ upstream "タスク/任务" 译法（MUL-5703 本地化） + 保留 fork 独有 key（`group_skills` / `admin_user_management` / `identifier_*` / `view_run` / `workdir_path_unavailable` 等）。

**Headline — 后端冲突面扩大，2 个 build 兜底**。Typecheck 报 `stickyComposer` TS6133（upstream 加回但 fork 不用）→ 删变量声明 + 删 useStickyComposer import（L121）。Build 报 inbox-page.tsx 传 `leadingAction` 但 IssueDetailProps 缺字段（auto-merge 丢字段）→ re-apply `leadingAction?: ReactNode` 到 IssueDetailProps interface + destructure + 加 `import { type ReactNode }`。**Pre-existing 4 failed**：issue-detail.test.tsx 的 `findByText("Implement authentication")` 测试在 fork `main-backup-v0.4.19` baseline 上也 fail — SOP 7.5 确认 pre-existing，不计入本轮回归。

**Migrations:** upstream 新增 **253-256**（4 个：QwenPaw runtime profile + Reasonix runtime profile + chat_pending_deferred_v3 + drop chat_pending_v2）→ **零新撞号**（全部 > fork max 252）；6 已知撞号（119/158/159/160/213/214）完好；**DB migrated 4**（含非 no-op 的 chat_pending v3 数据迁移）。

**Phase 6 deploy executed:** backup pg17 `~/multica-backups/v0.4.19-pre-migrate-20260806-095612.dump`（55M）；migrate up 4 应用 253-256 全 skip 252-；**version re-stamp 异常**：`install.sh` 在 merge 未 commit 时跑 `_version-stamp.sh` 走 `git describe --tags --abbrev=0` → HEAD 没 v0.4.19 tag → 取 v0.4.18（binary strings 显示 `-X main.version=v0.4.18`），**手动 `go build -ldflags "-X main.version=v0.4.19"` 兜底**；**launchctl bootout 后需用 `launchctl bootstrap gui/UID plist-path` 重新加载**（kickstart 要求 service 已注册 — bootout 后 "Could not find service"）；health check backend `/healthz` 200、frontend `/` 200、`/api/config server_version=v0.4.19`。

**Calibration (actuals vs audit prediction):** merge-tree 预测 14 content 冲突 → **实际 14**（连续第 6 次文本预测命中）。Strategy D 签名盲区预测"不触发" → **实际不触发**（comment.go 整文件 0 改动）。4 新迁移预测"零撞号" → **实际零撞号**。install.sh version stamp 异常：先前所有升级都依赖 install.sh 自动 stamp v0.4.x，本轮**首次出现 merge 未 commit 导致 baseline 倒退** — 后续升级若想在 install.sh 之前 commit merge，或在 install.sh 之后手动 re-stamp backend binary。

The full per-file strategy, verified negative claims (4 agents, 49 条), and Phase-by-Phase record:

→ **`docs/upgrades/v0.4.19-plan.md`** (frontmatter `upgrade_contract: selfhost-upgrade/v1`, `phase: done`)

## Last upgrade — v0.4.17 → v0.4.18 (EXECUTED 2026-08-05)

✅ **EXECUTED 2026-08-05** — merge `0d1994332`, base now `v0.4.18`. Clean merge-base `v0.4.17`, 36 upstream commits, 222 files (+10907/-11042，删除≈新增提示含重构/搬迁)。**4 手动冲突全 additive/orthogonal + 0 rerere 自动吸收 + 0 go-build 兜底修复**。

Resolved: **CLAUDE.md**（C 3 行加法并集）fork `docs/solutions/`+`CONCEPTS.md` bullet ⊕ upstream `packages/eslint-config/` bullet — `## 用户偏好` 治理节 upstream 零触碰自动合并；**issues/config/index.ts**（B）fork status 多行 export ⊕ upstream `PRIORITY_DISPLAY_ORDER`（MUL-5666）；**reply-input.tsx**（B）fork `skillMentionContext`（#5346）⊕ upstream `quickActionMenu`（MUL-5588）双 prop 并列；**cmd_issue_test.go**（B keep-both）fork `TestValidateIssueStatusArchived` + upstream `TestIssueCommentListHelpCarriesReadContract`（git 把两函数尾部 `}` 当共同 context，python slice + gofmt 重建两完整函数）。

**Headline — 后端零风险，全冲突在前端 TS + docs（近几轮最干净）。** 4 audit agent 实测：`triggerTasksForComment` upstream 整个 `comment.go` 0-line diff（Strategy D 不触发）、task.go `truncateFallbackCommentBody` 无 redeclaration（v0.4.12 教训不复现，fork own file 唯一定义）、#5539 provenance `ServerVersion` L218 + own file 完好、multica-mentioning SKILL.md upstream 未碰目录（daemon brief A∩B=∅）。task.go 新引入 `lockChatSessionForTaskWrite`（MUL-5686）sqlc 依赖预警"可能需 make sqlc" → 实际不需要（merged tree 已带 upstream 生成代码，go build 直接过）。

**Migrations:** upstream 新增 **252_agent_builder_draft**（1 个，agent-builder draft 持久化）→ **零新撞号**（stem 252 > fork max 251）；6 已知撞号（119/158/159/160/213/214）完好；migration 100/101/103/104 被 docs commit（`c1ec1f464`）扫到但纯 `--` 注释清理（删 `docs/timezone-architecture-rfc.md` 指针）零 SQL 语义改动；**DB migrated 1**（非 no-op）。

**docs/ 裁剪零受损：** upstream 删 17 个 docs 文件（`design.md` 折进 CLAUDE.md + `product-overview.md` 等），fork 现有 14 个被删副本全 blob-equal v0.4.17 baseline → modify/delete 不触发，干净删除生效；fork 本地目录（solutions/upgrades/customizations.md/plans/ideation/brainstorms）100% 未受影响。

**Phase 6 deploy executed:** merge commit `0d1994332` 先落（`git describe --tags --abbrev=0`→v0.4.18，**轻量 tag 需 `--tags`**）；pg_dump 备份 pg17 `~/multica-backups/v0.4.18-pre-migrate-20260805-093744.dump`（51M）；migrate up 1 应用 252 全 skip 既有；版本 re-stamp 前端 `.env NEXT_PUBLIC_APP_VERSION=v0.4.18` + `install.sh` 全量 rebuild + ldflag backend + reload plists；health check backend `/healthz` 200、frontend `/` 200、`/api/config server_version=v0.4.18`（`curl --retry-connrefused` 消化 backend ~40s 启动窗口）。

**Calibration (actuals vs audit prediction):** merge-tree 预测 4 冲突 → **实际 4**（连续第 5 次文本预测命中：v0.4.14/15/16/17/18）。rerere 记录 4 resolution。adversarial verify 推翻 5 个预判（"2 commit 碰 CLAUDE.md"实际 3、"用户偏好节在冲突区"、"design.md 折进会冲突"、"AGENTS.md 会冲突"、"migration 100-104 有 SQL 改动"全 refuted）。本轮**无新变量**：Strategy D 不触发 + 无 redeclaration + 无 source-map 重钉 — 后端免疫，纯前端 + docs 加法冲突。

The full per-file strategy, verified negative claims（4 agent，~35 条），and Phase-by-Phase record:

→ **`docs/upgrades/v0.4.18-plan.md`** (frontmatter `upgrade_contract: selfhost-upgrade/v1`, `phase: done`)

## Last upgrade — v0.4.15 → v0.4.16 (EXECUTED 2026-08-03)

✅ **EXECUTED 2026-08-03** — merge `2912c6a32`, base now `v0.4.16`. Clean merge-base `v0.4.15`, 40 upstream commits, 476 files. **6 手动冲突 + 0 rerere 自动吸收 + 1 go-build 语义修复（Strategy D 双向改签名）+ source-map 全量重钉**。

Resolved: **rich-content.tsx**（C）取 upstream `IdentifierIssueMentionLink`+`fallback`（MUL-5499 bare-URL-chip），保 fork `mentionId` 变量 + actor/skill chip dispatch；**comment-input.tsx**（B）editor 双 prop 并列 `skillMentionContext`（#5346）+ `quickActionMenu`（MUL-5465）；**issues-page.tsx**（C）取 fork `IssuesEmptyState` archived 空态重构（#6106）+ re-apply upstream MUL-5580 icon tone（`text-faint-foreground`，语义丢失点）；**client.ts**（B）import 并列 `EMPTY_BATCH_IMPORT_RESPONSE`+`EMPTY_CHAT_MESSAGE_LIST`；**builtin_skills/multica-mentioning/SKILL.md**（B）upstream 新骨架（MUL-5548 reason_code + coalesced/deferred + MUL-3963 canInvokeAgent）+ 回植 fork @skill 契约（Step 3 + undesignated-@skill silent no-op）；**mentioning-source-map.md**（B）fork `skill|project` ValidMentionTypes regex + **全量重钉 comment.go 行号** + 修符号名。

**Headline — Strategy D 双向改签名（本轮最大教训）。** typecheck 6/6 全绿但 `go build` 报 `quick_action.go:917` 参数错位：fork 和 upstream **都改了 `triggerTasksForComment` 签名**（fork +`skillMentionAgents`，upstream +`delegationAuthorityUserID` MUL-4857），auto-merge **静默选 fork 签名、丢 upstream 参数**，文本零冲突、typecheck 不涉及（Go）、**只有 go build 能抓**。修复：签名合并两轴 + port 回被覆盖的 MUL-4857 delegation 逻辑（CreateComment `FromRequest` / UpdateComment `FromComment`）+ quick_action.go 补 `nil` + 8 测试调用点插 `""`。**教训：fork 定制了某函数签名时，审计必须额外 grep upstream 是否也改了同一签名或新增该函数调用点。**

**Migrations:** upstream 新增 **235-250**（16 个：quick-actions 235-240 + 索引 241/244/245/246/247/248 + workspace-teardown 243 + qoderclicn 242 + subscriber 249/250），全 > fork max 234 → **零新撞号**；6 已知撞号（119/158/159/160/213/214）完好；索引全 `CREATE INDEX CONCURRENTLY` 单语句；lint map 零维护；**DB migrated 16**（非 no-op）。

**PR #5199 状态翻转：** OPEN→CLOSED（upstream Bohan-J 2026-07-31 关闭，未 merged）→ 从 Carry 改记 **Own forever**（mention 集群表 + 此处同步）；fork actor-chip 在 `rich-content.tsx` post-MUL-5499 存活（upstream 改名 `AutolinkedIssueMentionLink`→`IdentifierIssueMentionLink`，fork chip dispatch 未碰；非原生 mention-chip 系统，#5199/#5346 仍 carry/own 不 drop）。

**Design-system 盲区零返工：** MUL-5452 透明度重构（152 处 `text-muted-foreground/N`→实色 token + 新 `--faint-foreground` + 新守卫 `text-contrast.test.ts`）实测 fork 独有文件**零违规**；用户工作区已先发迁移 type-scale（commit `a55c95c4c`）→ 守卫首跑即绿，无 v0.4.15 式返工。

**Deploy (Phase 6) executed:** merge commit `2912c6a32` 先落（`git describe --abbrev=0`→v0.4.16）；pg_dump 备份 pg17 `~/multica-backups/v0.4.16-pre-migrate-20260803-094826.dump`（44M）；migrate up 16 应用全 skip 既有；版本 re-stamp 前端 `.env NEXT_PUBLIC_APP_VERSION=v0.4.16` + `install.sh` 全量 rebuild + ldflag backend + reload plists；health check backend `/healthz` 200、frontend `/` 200、`/api/config server_version=v0.4.16`。

**Calibration (actuals vs audit prediction):** merge-tree 预测 6 冲突 → **实际 6**（连续第三次文本预测命中）；rerere 本轮未吸收（全新模式）但记录 6 resolution。新变量：**Strategy D 双向改签名**（go build 兜底）+ source-map 全量重钉（清偿多轮符号名/行号陈旧债务）。

The full per-file strategy, verified negative claims（25 条：24 confirmed + 1 refuted — ServerVersion provenance 不在 client.ts 的纠偏）, and Phase-by-Phase record:

→ **`docs/upgrades/v0.4.16-plan.md`** (frontmatter `upgrade_contract: selfhost-upgrade/v1`, `phase: done`)

## Related

- `docs/solutions/workflow-issues/safe-upstream-upgrade-with-local-customizations.md` — the per-upgrade merge mechanics this ledger feeds into.
- `docs/solutions/workflow-issues/upstream-api-divergence-cherry-pick-port.md` — why an upstream API change turns a trivial carry into a re-port.
- `docs/solutions/workflow-issues/unapplied-migrations-after-upstream-upgrade.md` — post-merge migration renumber/apply step.
